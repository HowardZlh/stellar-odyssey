# Changelog

本项目所有值得注意的变更都记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- 实现全站中英文切换覆盖（i18n 全站覆盖，B3 UI 壳层双语的后续批次）：语言切换（左上角 zh/EN 钮 / `?lang=`）现覆盖 3D 场景天体标签与全部说明文案。**A) 3D 场景标签**——行星/卫星/彗星/星系/特殊天体标签经取名收口函数 `displayBodyName` 双语（地球→Earth、天宫空间站→Tiangong Space Station 等，数据层既有英文 `name` 字段渲染接线）；说明性标签（天狼星A·主序星/你在这里/奥尔特云/终端激波·日鞘·日球层顶/旅行者穿越标记/类星体·触须·透镜弧·GRB 后缀/本星系群本动/可观测宇宙边界/拉尼亚凯亚边界/巨引源/银河年刻度/合并倒计时与阶段）入字典新键组 `sceneLabel.*`；性能纪律登记：3D 组件不直接订阅 locale，新叶组件 `Scene/LocalizedLabelText.tsx`（BodyNameText/LabelText）把重渲染限制在 Html 标签 DOM 层、语言切换不重建 3D 场景，Universe 合并标签帧循环经 getState().locale 逐帧写入。**B) 数据层与信息面板值行**——`getBodyInfoById(id, locale)` 双目录（zh 目录模块加载即建、en 懒加载缓存；默认 zh 既有测试零改动），值行单位与括注（年/天/分钟/小时/万光年/逆向/逆行/示意尺寸等）按 locale 生成；数据文件增补英文字段（中文逐字节不变）：specialBodies 17 天体 66 条 fact `valueEn` + `dynamicsEn`、galaxies 8 星系 `descriptionEn` + 运动登记 `GALAXY_MOTION_NOTE_EN` + 银河系旋臂/人马座A* 英文、moons 17 卫星 `noteEn`、sunStructure 六层剖面三英文字段 + 六条科普常量 `*_NOTE_EN`、heliopause 旅行者发射日期/备注英文 + 形态登记英文、supernova 频率登记英文、m87Environment 三行英文、galaxyNearView 四形态结构说明英文、lmcStructures 标志结构英文；黑子群/日珥点选科普卡片（titleEn/descEn）、太阳剖面分层卡片、合并演化卡片（阶段名 `mergerStageLabel(locale)`/太阳系命运/来源登记）、太阳活动状态行（`sunActivityStatusLines`/`solarCycleStatusLine` 增 locale 参数）随语言切换；HUD 数值格式化 locale 化（尺度标尺"光年/ly"、超远期模拟时间"百万年/Myr"、银河年刻度单位词经 `gradationPercentText` 拆分）。**C) 页面级**——场景加载占位与 404 页（标题/正文/倒计时/按钮）双语（`useLocaleInit` 按 `?lang=` > localStorage > zh 解析）；HelpHint 语言说明行更新为"界面、天体标签与说明均切换"。豁免登记沿用：dataSource 数据来源署名保持原文、SEO metadata 保持 zh-CN、开发者错误消息与 dev 预览组件不迁。测试：新增 i18nFullCoverage 16 例（pickLocalized 收口/en 目录抽查 + **全量条目值行无中文残留扫描**/merger·solarCycle·solarActivity·scale·time 双语/标签叶组件双语切换/404 页 en 态），既有中文断言零改动，全量 3,155 用例/168 套件、type-check/lint/build 全绿
- 新增展馆模式 MVP（UI 显隐 + 自动巡游 + 空闲检测 + 全屏）：面向展馆大屏/长期无人值守场景的自动演示模式。A) UI 显隐基建——store 新增 `uiVisible`（默认可见）+ `setUiVisible`/`toggleUiVisible`，快捷键 `H` 一键隐藏/显示界面（展馆模式外亦独立可用）；受控方式登记 = SolarSystemApp 顶层包裹 `<div hidden>`（单点覆盖 ControlPanel/HudInfo/PerformanceMonitor/BodyCycleSwitcher/HelpHint/ContactBadge 六组件并保留组件内部状态，ContactBadge B1 预留登记就此收口）；LoadingProgress（加载期必须可见）与启动参数 logo 不受控。B) 状态机与驱动——纯逻辑状态机 `src/utils/kiosk.ts`（`inactive`/`touring`/`paused` 三态，事件+时钟纯函数 `kioskTick`，dwell 取 `?dwell=` 启动参数默认 30 秒含 2.5s 运镜、空闲恢复 90 秒默认）+ store `kioskEvent` 事件入口 + 驱动 hook `src/hooks/useKiosk.ts`（激活态 0.5s 定时 tick + 全局 pointerdown/wheel/keydown 捕获监听，未激活零挂载、卸载全清理防泄漏）；巡游推进全部复用既有 store action（域内下一站 `cycleScopeBody(1)` / 域全景锚点 `setViewLevel` / 域起点天体 `requestFlyTo`，不新建运镜逻辑）。`tour=solar|galaxy|universe` 单域循环、`tour=all` 四域轮转（system → solar → galaxy → universe 由内向外，域末先切下一域全景锚点站再飞域默认天体——两步分离踩坑登记：同 tick 连发会在低层级尺度错误解析高层级目标位置，详见 kiosk.ts 文件头）。C) 交互语义——巡游中任意输入（点击/滚轮/按键）即暂停并显示 UI，顶部中央角标"展馆模式（暂停中，N 秒后恢复）· 退出"实时倒计时；持续无操作到时自动隐 UI 恢复巡游并立即推进一站；退出恢复 UI 并退出全屏。D) 进入出口——ControlPanel 新增"展馆模式"分区启动按钮（用户手势内 `requestFullscreen`，被拒/不支持静默降级照常巡游登记）；`?mode=kiosk` 链接启动即巡游（URL 启动不尝试全屏——无手势必拒直接降级登记）；kiosk 全部新文案入 i18n 字典双语（zh/en）；HelpHint 补 `H` 键与展馆模式说明行。测试：新增 45 例（kiosk.ts 全分支 23 例模块覆盖率 100% + store uiVisible/kioskEvent 16 例 + useKiosk 定时/监听/清理 6 例），全量 3,132 用例/166 套件、覆盖率 gate ≥90% 保持（语句 98.47%）、type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 b5 系列）：`?mode=kiosk&tour=solar&dwell=10` UI 隐藏自动逐站（mars→encke→ceres 序列三站截图）、模拟输入暂停 + UI 显示 + 89 秒倒计时角标、缩短计时自动恢复隐 UI 续巡、角标退出回常规态；`tour=all` 跨域两步实测（银河系末站 → L4 全景锚点 → 飞抵 M31 与手动基准 dist 逐位一致、粒子云/M110/合并标注可辨，续推 m33）；`H` 键双向独立切换；ControlPanel 按钮启动 + 无头全屏实测生效；长跑 235s 约 48 站 >3 圈 FPS 57.4–60.1 无回退、JS 堆 46–69 MB 震荡稳定无泄漏、全程控制台零错误
- 新增 URL 启动参数框架：主应用支持通过 URL 查询参数定制启动行为（纯逻辑解析模块 `src/utils/launchParams.ts`，挂载后读取 `window.location.search` 一次性解析写入 store `launch` 字段，静态导出兼容不用 `useSearchParams`）。参数表登记：`mode`（合法值 `kiosk`，展馆模式，仅解析存储——消费待后续展馆模式交付，当前无行为）、`tour`（`solar|galaxy|universe|all` 巡游域，默认 `solar`，同为后续消费）、`dwell`（每站停留秒数，整数 5–600，默认 30，同为后续消费）、`body`（天体 id，场景就绪后一次性自动飞往并跟随——就绪信号取纹理加载进度完成 + 3s 兜底定时器，非法 id 经 `requestFlyTo` 自含校验静默忽略）、`logo`（客户 logo 图片 URL，仅 https 且长度 ≤2048，屏幕右侧小尺寸展示，加载失败 onerror 即静默隐藏；占位避让登记：偏离建议位"右下信息面板上方"改锚定 `right-4 top-64`——信息面板高度随条目数可变无法可靠上方避让，现位置全 UI 状态无碰撞；预留语义登记：后续展馆模式隐藏 UI 时 logo 保持显示）、`lang`（`zh|en` 语言，统一迁移收口——原 B2 独立轻量解析 `parseLangParam` 删除并入本模块单一入口，优先级链 `?lang` > localStorage > 默认 zh 语义零变更）；全部参数大小写不敏感（mode/tour/lang）、非法值静默回退默认（不抛错、控制台零错误）、无参数启动与现状行为逐项等价；新增单测 64 例（launchParams 全分支 62 例含 B2 lang 用例语义等价迁移 + store launch 字段 2 例，原 i18nB2 中 9 例 parseLangParam 用例迁出），launchParams.ts 覆盖率 100%、全量 3,087 用例/163 套件、覆盖率 gate ≥90% 保持、type-check/lint/build 全绿；无头 Chrome 四组目验（Metal 1280×800，截图 b4 系列）：`?body=jupiter` 加载完成自动飞往并跟随、`?logo=` 有效 https 显示/404 与 DNS 不可达 onerror 静默隐藏/非 https 解析层过滤不渲染、`?lang=en` 经统一入口全界面英文生效、非法全参数组合逐项回退默认且控制台零 JS 错误、全参数组合（kiosk 仅存储 + body 飞往 + logo + lang）互不干扰

- 新增界面英文支持（UI 壳层双语）：控制面板顶部新增 zh/EN 语言切换钮，切换即时生效无需刷新并持久化；六大 UI 组件（控制面板/HUD 信息与底部信息面板/操作引导/巡游切换器/性能监控/加载进度）与全部动态事件通知（太阳耀斑/CME/CME 抵达地球/超新星/合并演化卡片）壳层文案接入中英字典；天体显示名双语（英文态取各天体既有英文名 `name` 字段，经取名收口函数 `displayBodyName` 统一），信息面板标签列（质量/半径/轨道周期等约 90 项）与天体类型行接入英文映射；操作引导补语言切换说明。迁移边界登记：科学说明段/数据来源署名/信息面板值行与描述行暂保持中文（英文态混排为登记预期），默认语言仍为中文、既有中文界面文案逐字不变；新增单测 44 例（插值/标签映射/取名函数/六组件双语切换），既有测试零改动全绿（全量 3,032 用例/161 套件，覆盖率 gate ≥90% 保持，语句 98.45%）、type-check/lint/build 全绿；无头 Chrome 目验（截图 b3 系列）：中英双态全组件截图、语言切换不重建 3D 场景（canvas 引用不变）、双态 60 FPS、控制台零错误

- 新增界面语言切换基建与英文 README：i18n 客户端 locale 机制（`src/i18n/` 字典模块 zh/en + 查找纯函数 `t(locale, key)`，en/zh 键集合由 TypeScript 类型强制一致——缺键/多键编译报错；Zustand store 增 `locale`（默认 zh）+ `setLocale`，localStorage 持久化键 `stellar-odyssey:locale`，启动优先级 `?lang=` > localStorage > 默认 zh，locale 变更客户端同步 `<html lang>`（zh → zh-CN / en → en，SEO metadata 保持 zh-CN 不动）；「商业合作」角标作为打样件接入字典双语，界面其余组件的批量双语迁移随后续批次交付）；新增 `README.en.md` 英文版（简介/亮点特性/快速开始/Attribution 摘要链接中文详表/参与贡献/商业合作/License），两 README 顶部互链；新增单测 49 例（字典键一致性/查找回退/`?lang` 解析/启动优先级/持久化与 `<html lang>` 同步/ContactBadge 双语切换），i18n 新模块覆盖率 100%、既有测试零改动全绿（全量 3,000 用例/159 套件，覆盖率 gate ≥90% 保持，语句 98.45%）、type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 b2 系列）：默认启动 zh 零副作用与现状等价、`?lang=en` 启动角标英文、切换持久化刷新保持、全程 60 FPS 控制台零错误

- 新增商业合作联系入口：README 增补「商业合作」小节（面向教育机构/科技馆/展陈集成商，合作方向示例：展馆大屏部署、定制开发、课程内容）并在「开源协议」节补充商业授权联系方式（邮箱 mailto + GitHub Issues 链接）；站内新增左下角常驻「商业合作」角标（`src/components/UI/ContactBadge.tsx`）——点击展开联系卡片（邮箱 + GitHub Issues + 一句话说明），再次点击或点击外部收起，样式与既有控制面板深色半透明风格一致；左下角冲突避让取「临时隐藏」方案（任一事件通知可见或太阳剖面分层卡片占位时角标隐藏，解除即恢复），角标外区域不拦截 pointer 事件、canvas 拖拽/滚轮交互零影响（无头 Chrome 实测 60 FPS、控制台零错误）；kiosk 模式 `uiVisible` 联动登记待后续展馆模式接入；新增组件单测 13 例（渲染/展开收起/点击外部/避让分支/监听清理），全量 2,964 用例/155 套件通过，覆盖率 gate ≥90% 保持（语句 98.44%），type-check/lint/build 全绿

- 完成 R5 迭代集成回归验收（R5-7，IMPROVEMENT_REQUIREMENTS_5 §R5-7 收尾）：R5-1～R5-6/R5-8 全量交付后的端到端回归 + 资产与性能总账 + 文档回写。A) 测试四件套全绿——`npm test` 2,938 用例/153 套件、覆盖率语句 98.44%/分支 97.80%/函数 99.58%/行 99.59%（gate ≥90% 保持）、type-check/lint/build 零告警；`npm run bake:data` 幂等复核（两次运行 + 提交产物三方 shasum 26 文件逐字节一致，覆盖影像 galaxy-maps 20 件 + 目录 galaxy-catalog 2 件 + R4 既有 4 件，总量 2,519.9 KB ≤ 15 MB 修订上限）。B) 无头 Chrome 集成回归（Metal 1280×800，截图/日志 r57 系列归档，控制台全程零错误）——L4 8 站巡游整圈（官方序列 m31→m33→m87→quasar-3c273→milky-way→lmc→smc→sagittarius-dwarf）逐站截图 + FPS + GC 后 JS 堆：全站 60 FPS、堆 51→53 MB；**对照 R4-24 基线（60 FPS/堆 46→52 MB）：FPS 持平不回退、堆 +1～3 MB 为 R5 新层（影像粒子/体积尘埃盘/2MRS 目录/M87 环境层）合计开销**，逐站持有者核对 m31/m33/lmc 站 particles+volume 双激活（R5-1+R5-2）、m87 站 particles+starCatalog（R5-4）；smc 站采样瞬时 73–75 MB（影像解码期字节数组，两次运行同现）下一站回落 53 MB 无累积登记。真实点云背景多角度（锚点/偏航 ×2/俯视/拉远 625 Mpc 总览）FPS 全 60；M31 完整序列（飞抵影像驱动近观 → 斜视绕行 ×3 → Esc 拉远 seq0–3 远景影像贴图）结构连续、退出后 volume 池释放；M87 推近 EHT 联动序列（星系团语境 → 滚轮推近 4 档 lensing 池 [m87] 激活光子环近正视大环可辨 → 拉远滞回释放池清空）。C) 连续切换 10 个细节层目标（四类交叉含 L3↔L4 跨域与 R5-5 新增 grb-221009a）×3 轮——全程各池 holder ≤ 容量 1、GPU 估算峰值 4.1 MB ≤ 64 MB 且随释放出账；dev 轮末堆 51→54→56 MB（≈0.17 MB/次切换，R4-24 已登记 dev-only React/drei churn 同量级）；堆稳定性以生产构建为准（R4-24 同方法论）：`out/` 静态产物 L4 键盘巡游 8 站 ×3 轮轮末 GC 堆 32.6→33.1→33.4 MB 平台稳定、60 FPS 零错误。D) R4 关键点回归抽查全 PASS——L3 体积星云站（orion-nebula volume 池激活 60 FPS）、黑洞透镜站（sgr-a-star lensing 池激活 60 FPS）、detailLayer LRU 交叉（ring 逐出 orion/cygnus-x1 逐出 sgr-a/antennae 逐出 pleiades，×3 轮语义一致）；R2/R3 语义经全量单测零修改回归覆盖。E) 资产与性能总账（附录 A 修订预算逐项达标）——`public/data/` 26 文件 2,519.9 KB ≤ 15 MB（galaxy-catalog.bin 679.5 KB/galaxy-maps ≈1.85 MB 单星系 213–298 KB ≤600 KB/R4 既有 ~710 KB）；显存：细节层 GPU 峰值 4.1 MB ≤ 64 MB，R5 新增体积纹理逐项（尘埃盘 128×32×128 R8 512 KB volume 池、30 Dor 48³ ≈110 KB 并入 lmc 预算、费米气泡 64³ ≈262 KB 常驻不占池）、远景影像贴图 4×512px 计入纹理预算体系；L4 粒子峰值 M31 9,850/M87 全量 8,100 ≤ 单目标 12,000。F) 授权与署名汇总登记（呈现位置）——DSS2 影像（STScI DSS/AAO/ROE/Caltech + hips2fits CDS）：meta 逐星系 source/license/credit + 星系信息面板数据来源行呈现、源图不进 public/；2MRS（Huchra et al. 2012 公开科学数据）：目录 meta + 显示开关说明文案（三项失真）+ HelpHint；文献参数类（SIMBAD/Biretta 1999/Tamura 2006/EHT 2019/Su et al. 2010/Levine et al. 2006/Piran 2004）：各天体信息面板 dataSource 行。G) 文档回写——IMPROVEMENT_REQUIREMENTS_5 §R5-7 全 ✅（版本 1.9；遗留登记：R5-3 人工检查点仍待用户目检 🔶 保持）、REQUIREMENTS.md §4.1/§4.2 R5 总结同步（版本 3.6）
- 实现银河系 L4 增补（R5-6，IMPROVEMENT_REQUIREMENTS_5 §R5-6 / §0.3 方案 H）：费米气泡（银心上下双极体积辉光）+ HI 翘曲盘（外盘 m=1 S 形垂直位移），补齐银河系两大标志性真实结构。**A) 费米气泡**——新纯逻辑 `utils/fermiBubbles.ts`（形态参数 Su, Slatyer & Finkbeiner 2010（Fermi-LAT）登记：单泡高 ~25,000 ly/宽 ~20,000 ly 量级，椭球近似半轴 (9,500, 11,000, 9,500) ly、中心 ±14,500 ly、顶端 25,500 ly；密度 = ellipsoidSdf ×2 → smoothUnionSdf 银心收腰沙漏 → sdfDensityFalloff 软衰减 × fbm3 轻度斑驳，全部复用 utils/volume 既有基元勿新造塑形函数登记）+ 组件 `Scene/FermiBubbles.tsx`（R5-5 LmcTarantula 直绘小体积同款先例：64³ R8 单纹理双泡 ≈262 KB 经 R4-3 VolumeMaterial 直绘 raymarch，屏占比小免 R4-4 半分辨率 RT；非立方包围盒 24,000×54,000×24,000 ly 各向异性光程经 uWorldStepScale 修正 R5-2 先例）挂 Galaxy.tsx 根组银心（组本地原点，倾斜/平移自动继承），不占 volume 池（随银河系组常驻、仅 visible/uniform 门控登记）；淡紫 #8f6bdc → 品红 #ff66cc 双色档极低密度弥散辉光（**伽马射线可见光艺术化登记**，非真实可见光形态——呈现于显示选项说明文案 FERMI_BUBBLES_SOURCE_ZH；初值 densityScale 1.5/intensity 0.85 无头目验双泡过于醒目盖过银盘，下调 1.2/0.5 弥散淡雅档登记）；可见性 = 银河系层级权重（trapezoidWeight 闭包注入）× 开关淡入淡出（0.8s 平滑，uDensityScale×opacity 同步趋零无暗盒残影）；显示开关"费米气泡"接入显示选项区（store showFermiBubbles 默认开 + panelScopes 'fermiBubbles' 作用域 **['L3','L4'] 登记**——银河系可见的两个视角域均可切换）+ HelpHint 登记说明。**B) HI 翘曲盘**——`utils/galaxy.ts` 新纯函数 `warpYLy(rLy, φ)`：外盘（r > 35,000 ly）m=1 S 形垂直位移 A(r)·sin(φ−φ₀)（Levine, Blitz & Heiles 2006 近似参数登记：①盘可视截断 50,000 ly ≈ 15.3 kpc 处真实振幅仅 ~0.3 kpc，按"侧视可辨"验收将盘缘振幅**艺术化放大至 3,000 ly 登记**（径向增长趋势仍取 Levine m=1）；②振幅二次增长起点 C¹ 连续；③交点线取 +x 太阳–银心方向近似；④CPU 生成期一次性烘焙 warpsLy 通道，粒子较差自转后图案缓慢缠绕登记，t=0 与文献相位对应）；`generateGalaxyDiskParticles` 增 warpsLy 输出（确定性派生自 r/φ 不消耗随机数——heightsLy 等既有通道逐字节不变、粒子总量不变；核球/棒恒 0；近观星系复用生成器不消费该通道登记）。**C) morph 组合（实现差异登记）**——需求"shader 零改动"字面路径（warp 烘进 heightsLy）与"组合无形变异常"验收冲突：盘 shader 椭球 morph 目标 hTargetLy 由 aHeightLy 派生，烘入将使翘曲在 uEll/uExpand 终态被放大 ~r/1000 倍（外盘 50×，3,000 ly → 150,000 ly 形变异常）——改为**最小 shader 增量**（新 attribute aWarpLy 一处加法 pos.y = aHeightLy + aWarpLy）：warp 为基线位移、morph 目标仍由未翘曲高度派生 → uEll/uExpand→1 时翘曲随 morph 平滑淡出（Milkomeda 终态椭球/展开态无翘曲，语义 = "从翘曲基线 mix 到椭球目标"）；组合顺序 CPU 镜像 `diskWarpMorphYLy` 单测锚定（uEll=uExpand=0 完整呈现/任一权重 1 无翘曲/闭式等价 combinedMorphWeight/任意权重 |y| 有界不放大）；R2-9 尘埃带中平面判据保持未翘曲 aHeightLy（尘埃随翘曲中平面同步抬升语义登记）。**验收登记**（截图 r56 系列）：侧视 S 形有/无 warp 同机位对照（银心固定 G 模式）——盘亮度质心逐列量化差沿盘符号翻转（左侧 −35～−67 px 抬升、右端 +36 px 下压，S 形签名实证）；银心上下弥散双泡正/侧两视角可辨 + 开关对照生效；R3-7 展开态/R2-11 合并预览回归无形变异常（各截图登记）；R2-9 银晕/星团/棒/尘埃带零回退（galaxy.test/galaxyR29 全绿 + 粒子总量不变）；全程 60 FPS、控制台零错误；新增单测 galaxyR56（16 例：warpYLy 内盘零位移/振幅径向增长/m=1 相位反号/C¹ 连续/RangeError + 生成器通道等价与既有通道零回退 + 组合顺序镜像）+ fermiBubblesR56（12 例：双泡形态 8 字侧向轮廓/银道面远端零密度/确定性/几何换算/光程缩放/64³ 单纹理双泡数据/来源文案）+ panelScopes 17 选项矩阵更新，全量 2,938 用例/153 套件、覆盖率 gate ≥90% 保持（语句 98.44%，新模块 100%）、type-check/lint/build 全绿
- 实现本星系群与河外长尾细节（R5-5，IMPROVEMENT_REQUIREMENTS_5 §R5-5 / §0.3 方案 H）：LMC 近观补齐两大标志结构（30 Doradus + 中央棒色彩分层），GRB 补齐近观细节层（相对论双喷流 + 余辉膨胀壳）。**A) LMC 30 Doradus（蜘蛛星云）**——LMC 近观激活且 R5-1 影像驱动就绪时，在真实相对位置叠加小型体积发射区（Hα 粉红，48³ R8 直绘 raymarch，复用 `makeSphericalFbmCloudSampler` 球壳+fBm 基元勿新造塑形函数登记）+ 中心 R136 超星团蓝白亮核 glow sprite：位置换算（新纯逻辑 `utils/lmcStructures.ts`）取 SIMBAD（NGC 2070，RA 84.6767°/Dec −69.1009°）经 gnomonic 切平面镜像烘焙侧 `galaxyMapsCore.skyToPixel` 投至影像盘面（东=−x/北=−z 约定；影像帧常量与 `scripts/bake-data/galaxyMaps.ts` LMC 配置单点同源单测跨断言）——落点 (−3837, −1746) ly 在产物密度图上为 255 饱和亮区（**位置正确性以产物 PNG 解码单测实证锚定**）；尺度放大系数 **5× 登记**（真实直径 ~600 ly ≈ 盘 2% 原比例不可辨 → 视觉直径 ~3,000 ly ≈ 盘 10%；初值 3.5 无头目验粉红区仅数像素被加性星光淹没，上调登记）+ 体积亮度/密度目验调参登记（intensity 3.0/densityScale 3.2）；渲染路径登记：直绘小体积（R4-3 预览先例，屏占比小免 R4-4 半分辨率 RT），随 lmc particles 池细节层挂卸不占 volume 池——与 R5-2 体积尘埃盘（volume 池）共存实测正常；淡入淡出经 uDensityScale×opacity（发射/自吸收同步趋零无暗盒残影登记）；GPU/粒子预算并入 `galaxyDetailLayerSpec('lmc')`（R136 sprite ×1 + 48³ 纹理 ≈110 KB，R42/R410 单测例外登记）；影像降级参数化时不挂载零回退（登记：参数化团块分布无棒/亮区几何对应）。**B) LMC 中央棒色彩分层**——R5-1 密度图已含棒亮区（几何零改动），新纯函数 `applyLmcBarTint`（副本语义与 applyBulgeTint 一致）对影像基础层按棒椭圆权重 `lmcBarWeight01` 向老年星族偏黄 tint 混合（blend 0.45 椭圆中心满档平滑衰减）；棒椭圆几何为产物密度图高亮区（≥170、排除 30 Dor 邻域）流量加权二阶矩拟合档登记：中心 (−266,−421) ly、主轴 −0.503 rad、半长/短轴 4,300/2,000 ly（van der Marel 2001 图景近似登记）；影像组合确定性/棒外零改动/与采样+tint 逐字节等价单测锚定。**C) 信息面板与预览**——LMC 卡片补"标志结构"行（`LMC_LANDMARK_NOTE_ZH`）+ dataSource 追加位置换算/棒拟合来源登记；预览页 `?body=lmc` 增滑杆两件（30 Dor 亮度 0=关闭对照/尺度放大 1–8，帧读 getter 直达 uniform 零重建）恰达 8 上限。**D) GRB 近观细节层**——`GammaRayBurst` 经 `useDetailLayer({kind:'particles'}, 'lru-retain')` 挂载（与星系近观/类星体共池容量 1，阈值与类星体同式同源）`Scene/GrbNearView.GrbNearCore` 两层结构（新纯逻辑 `utils/grbNearView.ts`，Piran 2004, Rev. Mod. Phys. 76, 1143 火球模型图景登记）：相对论双喷流复用 `RelativisticJet`（**参数化登记**：新增可选 radiusFactor prop = tan(全开角/2)，缺省 0.035 现状锚定零回退——GRB 档全开角 5°、蓝白更亮 #e8f4ff/baseOpacity 1.0、长度 7× 基准半径，轴与静态双锥同姿态常量单点同源）+ 余辉膨胀壳（壳层 mesh 临边增亮渐变 shader，logdepthbuf 三件 + tonemapping/colorspace；观测系半径 R ∝ t^(1/4) 绝热减速近似 + 光度幂律衰减 α=1.2 + 周期末淡出归零防重放跳变、颜色随龄蓝白→暗橙为频段演化艺术化登记；真实数天–数月压缩至 45s 演示周期登记）。**现状核对结论登记（§R5-5 B 第 2 条）**：GRB 为常驻演示物（Universe 无条件挂载 + grbFlashState 45s 周期重放 FRED 闪光，非事件触发物）——近观层随该周期时钟出现/衰减（喷流爆发同步增亮 → 指数衰减至 0.15 地板档演示可见性登记 → 周期末归零；静态双锥按 GRB_STATIC_NEAR_DIM=0.6×near01 减淡让位细节喷流退出恢复）；信息面板 GRB dataSource 追加 Piran 2004 登记；预览页注册 `?body=grb`（componentKey grb-near-view，滑杆 4 件：时间流速/喷流开角/喷流亮度/余辉强度）。**验收登记**（截图 r55 系列）：预览页 lmc 默认档 30 Dor 粉红区可辨（像素探针 53 粉红 px @(583,323) vs 关闭 0）+ 棒偏黄分层可辨 + 参数化对照/恢复正常；grb 两帧对比（8.7s 蓝白致密壳 → 27.9s 膨胀减暗橙壳）演化可辨 + 开角 12° 对照 + 余辉关闭对照；主场景飞抵 LMC（30 Dor/棒/体积尘埃盘共存）与 GRB（近观壳 15s 两帧演化、双喷流细节）正常、L4 巡游 8 站（m31/m33/lmc/触须/类星体/透镜团/grb/m87）逐站 60 FPS、堆 46→51 MB 平稳、控制台零错误；新增单测 lmcStructuresR55（22 例，lmcStructures.ts 覆盖 100%：换算镜像/产物 PNG 亮区实证/常量同源/放大系数与包围盒/棒权重各向异性与单调/tint 副本语义与逐字节等价/预算/面板与预览注册）+ grbNearViewR55（16 例，grbNearView.ts 覆盖 100%：规格同源/开角 tan 关系/膨胀单调与 t^(1/4) 锚定/幂律 α 比值锚定/重放连续性/喷流权重曲线/两帧演化可辨锚定/预览与登记），全量 2,906 用例/151 套件、覆盖率 gate ≥90% 保持（语句 98.44%）、type-check/lint/build 全绿
- 实现 M87 纵深与星系团环境（R5-4，IMPROVEMENT_REQUIREMENTS_5 §R5-4 / §0.3 方案 H）：飞往/跟随 M87 时近观呈"星系团中心"完整语境——椭球星光 + 锐利球状星团点 + 节点喷流 + 室女座成员点缀 + ICM 弥散辉光，继续推近核心平滑过渡到 M87* EHT 光子环。A) 纯逻辑 `src/utils/m87Environment.ts`（实现选择/艺术化逐条登记于文件头）——喷流节点：既有 M87 喷流轴向叠加 4 个 HST-1 类亮 knot（**sprite 方案登记**，非小体积；Biretta et al. 1999），亮度沿轴衰减（首节点最亮）+ 缓慢循环外移（全轴 ≈2.8 分钟，**视觉化登记**非真实视超光速运动；循环端点淡入淡出防回绕闪现）；球状星团：真实 ~12,000 个（Tamura et al. 2006 量级）**按预算缩减为 2,000 稀疏 Points 登记**，径向取 Sérsic 外包络的幂律近似档 n(r) ∝ r^−2.5（**差异登记**：Sérsic n=4 精确逆变换过度向心集中不呈外包络观感；半数以内半径 = 20,000 ly > 恒星 Rₑ 12,000、截断 4×Rₑ 与椭球云同口径）、轴比沿用近观椭球、红黄老年色（金属丰度双峰可见光合色近似登记）、确定性种子两次采样逐字节一致；室女座成员：**取 R5-3 2MRS 目录室女座方向子集登记**（R5-3 已交付，内嵌 VCC 表替代路径不启用）——6° 锥（与 R5-3 自校验同源）+ 径向壳 [11,25] Mpc 团本体窗口按亮度取前 100，椭圆偏黄/旋涡蓝白形态档色调（R5-3 同源色板）、相对 M87 位移方向真实（超星系→场景与目录同一旋转链）、幅度**线性压缩 1,200 units/Mpc 艺术化登记**；ICM 辉光：**径向渐变 sprite 方案登记**（≈2 Mpc 半径，X 射线热气体可见光淡蓝紫艺术化登记）。B) 渲染接入（`Scene/M87Environment.tsx` + `M87Jet` 增补）——环境层经 `useDetailLayer({kind:'starCatalog'})` 挂 M87 近观（与昴星团/触须星系共池容量 1，lru-retain L4 语义；阈值与星系近观同源，粒子预算 2,100，M87 近观全量 8,100 ≤ 单目标 12,000）；点云复用 GalaxyNearView 软圆点 shader（导出复用禁止两套，星团 maxSizePx 3 高 alphaBase 锐利小点与椭球云 6px 软点区分）；成员目录经 R5-3 `useGalaxyCatalog` 懒加载，未就绪/失败仅无成员点缀其余照常（降级登记）；**成员不可点选登记**（全部子层禁用 raycast 防干扰 M87 交互热区，面板行列名说明）。C) M87* EHT 联动——跟随 M87 推近至核心阈值 900 units（滞回 ×1.4 同源；跟随判据仅 followBodyId，R4-13 Esc 释放修复同款）激活 R4-13 `BlackHoleLensedLayer`，**M87* 参数档登记**：盘更暗（0.35 < Sgr A* 0.55，ADAF 低吸积率）、环更大（r_s 世界长度 9 ≈ Sgr A* 4.8 的 1.9×，真实质量比 ~1,500× 可视化压缩登记）、盘倾角 17°（EHT 2019 近正视环形态，与两黑洞侧视 69.23° 档区分）、独立星场种子；与 Sgr A*/Cyg X-1 共用 lensing 池容量 1 LRU；退出跟随/拉远即淡出卸载 dispose。D) 信息面板 M87 卡片补 M87*（65 亿 M☉/首张黑洞照片/推近可见光子环）/球状星团（12,000 计数与 2,000 缩减登记）/室女座团（成员点缀不可点选登记 + ICM 艺术化）三行 + dataSource 来源登记。E) 预览页注册 `?body=m87`（componentKey `m87-environment`：椭球近观 + 环境层 + 节点喷流 + 核心透镜组装，透镜门控以相机距离对主场景阈值同式滞回判定）+ **预设视角按钮基建**（`PreviewEntry.viewPresets`/`minCameraDistance` + harness 按钮与相机距离应用器）：「全景语境」/「核心推近（EHT 光子环）」一键推至阈值内；滑杆 3 件（球状星团数/成员点缀开关/ICM 强度）。测试：新增 `m87EnvironmentR54` 32 例（knot 参数与端点淡出/GC 分布中位半径与截断与确定性/成员锥壳过滤与亮度截断与旋转保长与色调区分/真实产物集成断言——满配 100 成员且位移有界落于近观语境内、无 M87 重影点/阈值次序链与参数档对比/规格预算/面板行/预览条目与预设校验 RangeError 分支），全量 2,868 用例/149 套件、覆盖率 98.49%（gate ≥90% 保持）、type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r54 系列 01–23 登记）：M87 飞抵完整语境多角度可辨（椭球云 + 锐利星团点 + 喷流亮节点 + 成员点缀 + 弥散辉光，对照 R4 交付纯椭球云）；滚轮推近序列 04→07 平滑过渡到 EHT 光子环（近正视大环 + 暗弱盘，与 Sgr A* 侧视暖环参数档区分明显）、拉远滞回释放恢复；lensing 池交叉切换 Sgr A* → M87 核心 → Sgr A* LRU 正常；进出 ×3 强制 GC 堆 52→52→53 MB 平台无泄漏；预览页预设按钮推近/回全景 + 成员开关对照生效；全程 60 FPS（≥55 达标）、控制台零错误
- 实现真实巡天目录背景（R5-3，IMPROVEMENT_REQUIREMENTS_5 §R5-3 / §0.3 方案 G）：L4 宇宙视角大尺度背景由程序化宇宙网升级为 2MASS 红移巡天（2MRS，Huchra et al. 2012, ApJS 199, 26）**43,488 个真实星系**点云——室女座团聚集、银道空带、纤维走向均为真实数据结构。A) 烘焙（`scripts/bake-data/galaxyCatalog.ts` 接入 `npm run bake:data`，`--fetch-2mrs` 重拉快照）——获取沿用 R4-5"公开接口 + 提交快照"策略：VizieR TAP（J/ApJS/199/26/table3）全量快照 gzip 入库 `snapshots/2mrs-vizier.csv.gz`（网络受限按亮度截断至 ~2 万的降级路径本次未启用登记）；红移 → 距离取哈勃流近似 cz/H₀（**H₀ = 70 km/s/Mpc 登记**，cz < 100 km/s 剔除 9 条）+ 赤道 → 银道 → 超星系笛卡尔坐标（坐标链与运行时同源 `src/utils/galaxyCatalogCore.ts` 纯函数，禁止两套公式）；产物 `public/data/galaxy-catalog.bin`（Float32 小端 [magic 21175, version, N] + N×[sgx,sgy,sgz,w]，w = 形态档·1000 + 亮度档·999 整数打包 ≈16 B/星系，**679.5 KB ≤ 1 MB**）+ `galaxy-catalog-meta.json`（来源/授权/H₀/去重/自检数值）；**三项失真登记**（meta + 显示开关说明文案常量 `GALAXY_CATALOG_DISTORTIONS_ZH`）：本动速度污染/指状效应、近距（cz ≲ 1,000 km/s）距离误差可达数十%、银道遮挡带（|b| < 5° 为尘埃消光观测限制非真实空洞）；9 个实体星系 + M87 目录内剔除去重（**匹配半径 0.5° 登记**，实际命中 10 条：LMC×4/M33×1/M87×5）；自校验（失败退出非零）：条目数域/坐标域/w 域/**室女座团方向 6° 锥超密度比 32.6× ≥ 3 断言**/银道带占比 <2%/M87 方向近距清零/≤1 MB；幂等实测两次运行产物 shasum 逐字节一致。B) 运行时加载（`bakedData.ts`）——`validateGalaxyCatalog`（魔数/版本/字节长度精确匹配/坐标有限且 ≤800 Mpc/w 整数域）+ `loadGalaxyCatalog`（fetch + URL 缓存成功不缓存失败可重试）+ `useGalaxyCatalog` hook（进入 L4 淡入窗口才懒加载，~680 KB 不占首屏）；失败返回 null → 降级现状程序化宇宙网（fixture 单测 + CDP 拦截 bin 实测零报错）。C) 渲染（新组件 `Scene/GalaxyCatalog.tsx` + 纯逻辑 `utils/galaxyCatalog.ts`）——两级 Points 各一次 draw call（**拉尼亚凯亚近域 ≤80 Mpc 11,110 粒软圆点适度增大 + 远景 32,378 粒单像素**）：亮度档→顶点尺寸与颜色强度（加性混合等效 alpha）、形态档→色调（椭圆偏黄/旋涡偏蓝白/未知中性）；透明度档目验调参登记（远景 0.3 实测仅 371 可辨像素 → 0.75、近域 0.95）；对数距离压缩与 L4 实体星系同源（`cosmicDistanceToSceneUnits` 登记）；**场景对齐两级锚定登记**（utils/galaxyCatalog 文件头）：主锚银道面 ↔ 场景银盘面（银道空带精确落在渲染银盘平面内自洽）+ 次锚绕 y 方位对齐 M87（残余仰角偏差 ≈15.2° 登记，各实体偏差 `catalogAnchorDeviationDeg` 单测断言；det=+1 真旋转手性登记）；**与程序化宇宙网关系取推荐方案登记**：真实目录为主层、`generateCosmicWeb` 纤维降为低透明度氛围底层（0.75→0.2，开关关闭/降级即恢复主层）；**拉尼亚凯亚示意边界对齐核对**：边界环由场景 XZ 平面改置真实超星系平面（SGZ=0，室女座团 SGB≈−2.3° 落在面内 → 环穿过目录室女座超密度处；相对银盘面倾角 ≈84.5° 为真实几何登记）；L4 窗口淡入与 Universe.tsx 收敛同源公式（`universeFadeWeight`/`UNIVERSE_FADE` 3.05–3.6，禁止两套参数）；哈勃膨胀联动同式（`hubbleScaleFactor` 组缩放）；shader 含 logdepthbuf 三件 + tonemapping/colorspace 输出（附录 A §5），geometry/material 卸载即 dispose。D) UI——显示开关"真实巡天背景（2MRS）"接入 ControlPanel 显示选项区（L4 域，panelScopes 登记）附来源 + 三项失真说明文案；HelpHint 补真实巡天背景段落。E) **P0 竞态修复（无头目验发现）**：`useGalaxyCatalog` 在 L4 飞入途中 enabled 抖动 true→false→true 时，fetch 在 false 窗口落地被 alive 守卫丢弃且模块缓存已写入 → 后续 effect 早退，目录永远不挂载（降级假象、宇宙网不降氛围层）；修复为重新 enabled 时同步模块缓存到状态 + 回归测试（renderHook 复现竞态时序）。测试：新增 `bakedDataGalaxyCatalog`（22 例：校验全分支/加载缓存降级/实际产物集成断言——运行时校验通过 + ≤1 MB + 室女座超密度复核 + M87 方向 0.3° 锥清零无重影 + 两级 LOD 拆分非空）+ `galaxyCatalogCore`/`galaxyCatalogR53`（坐标换算/去重判据/两级 LOD 属性/锚定偏差/超星系平面）+ `storeR53` + `useGalaxyCatalog` hook 竞态回归 4 例，全量 2,836 用例/148 套件、覆盖率 gate ≥90% 保持（语句 98.55%，bakedData.ts 100%）、type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r53 系列 41–49 登记）：L4 锚点/拉远/偏航 ×2/俯视/侧视多角度真实点云结构可辨，WebGL 层地面真值核对目录两级 draw call（11,110 + 32,378）随开关挂载/卸载；定量复核（最大拉远 ON/OFF 像素差分）：目录独占像素 71,469，**室女座团方向局部密度 5.19× 全场平均**（反方向 3.26×，方向性成立）；跟随 M87 与目录无重影；开关切换/CDP 拦截降级（回落程序化宇宙网主层）正常；FPS 前后对比登记：目录开 60 / 关 60 / 绕行 60 / M87 60 / 降级 60，控制台零错误。**人工检查点：待用户目检确认真实结构观感。**
- 实现星系体积尘埃盘视线消光（R5-2，IMPROVEMENT_REQUIREMENTS_5 §R5-2 / §0.3 方案 F）：M31/M33/LMC 近观（跟随/飞往）叠加薄盘包围盒体积层——斜视/侧视时近侧尘埃暗带**真实遮挡**后方星光与核球辉光（R4-10 暗粒子只能"画暗点"不能"挡光"的登记短板补齐；M31 打样自检达标后套用 M33/LMC，SMC/椭圆不套用登记）。A) 纯逻辑 `utils/galaxyDustVolume.ts`——密度 = R5-1 尘埃通道 2D 图（双线性采样 + 对比度整形 dust²，与 R4-10 尘埃粒子权重同口径：抑制弥散地板防斜视长光程全域压暗，打样目验修正登记）× z 向归一化指数薄层（伪 3D 登记：盒边缘衰减为 0 无硬截断缝）；**纯吸收模式**（VolumeMaterial uIntensity=0，输出 rgb=0 + alpha=1−T，预乘合成 C_out = T·C_bg 仅衰减后方颜色）；消光方案 a 登记（体积层置于星光粒子后按透过率调制帧缓冲——盒足印内近侧粒子同被压暗为登记近似；方案 b 逐粒子采样差异登记于文件头）；逐星系强度登记：M31 σ=30/盒厚 3,000 ly/标高 500 ly、M33 σ=22、LMC σ=10 弱档（=M31 的 1/3，尘埃弱登记）；极侧视（~90°）暗线为真实尺度（数像素）且受星光加性 HDR 堆积 + Bloom 泛光冲淡呈细弱暗线登记（主验收姿态 = M31 真实 77° 斜视）。B) `VolumeMaterial` 增 uWorldStepScale 各向异性光程修正（非均匀薄盒局部步长 → 相对世界光程，最长轴归一化；默认 (1,1,1) 既有星云体积层零行为变化）——侧视光程 ≈ 正视的 宽/厚 倍，消光随倾角增强的物理特征成立。C) 组件 `Scene/GalaxyDustVolumeLayer.tsx`——渲染路径沿用 R4-4/R4-8（独立子场景 + 半分辨率 RT 动态视口 + 全屏三角形合成 renderOrder 晚于星光粒子；自适应质量 R4-4 状态机逐帧写步数/RT 比例）；密度纹理非立方 128×32×128 R8（512 KB，各维 ≤128 附录 A；可分离乘积同步构建 ≪100ms 登记免分帧）；`useDetailLayer({kind:'volume'})` 容量 1 与星云体积层同池（星系↔星云巡游 LRU 互逐实测 m31↔orion-nebula 单持有者无双挂）、release-on-exit 卸载全量 dispose；影像产物缺失/加载失败不挂载零回退（登记）。D) 互斥（§0.3 方案 F）：体积淡入权重经 fadeRef 输出，`GalaxyNearView` dust 暗粒子 uOpacity 乘 (1−fade) 互补淡出，体积卸载即恢复 R4-10 现状。E) 预览页 m31/m33/lmc 滑杆增两件：体积消光强度 σ（0 = R4-10 暗粒子对照档，A/B 目检）/尘埃盘厚；HUD 体积质量档行接入；`&spin=0` 关闭预览自转（dev 专用，无头 A/B 截图固定姿态）。验收登记（截图 r52 系列）：M31 77° 斜视近侧暗带遮挡核球辉光 vs σ=0 对照、正视暗纹与影像对应、M33/LMC 套用同验；主场景飞抵 M31 消光激活、volume 池星系↔星云切换 LRU 正常、m31↔m33 进出 ×5 强制 GC 堆 48→49→49 MB 平台无泄漏、全程 60 FPS（自适应 high 档未降档）、控制台零错误；新增单测 galaxyDustVolumeR52（41 例，galaxyDustVolume.ts 覆盖率 100%：双线性采样/垂直衰减/体素布局与确定性/透过率与 R4-3 离散格式交叉校验/各向异性光程侧正比 = 宽/厚/包围盒口径/volume 池规格阈值同源/预览注册），全量 2,776 用例/143 套件、覆盖率 gate ≥90% 保持（语句 98.54%）、type-check/lint/build 全绿
- 实现影像驱动星系升级（R5-1，IMPROVEMENT_REQUIREMENTS_5 §R5-1 / §0.3 方案 E）：M31/M33/LMC/SMC 近观粒子层与远景贴图由"参数化数学合理"升级为"公版天文影像驱动"——旋臂/尘埃环/恒星形成区与真实照片结构可对应（M31 尘埃带走向、M33 NGC 604 类蓝亮结、LMC 棒与 30 Dor 亮区、SMC 延展形状）。A) 烘焙管线扩展（`scripts/bake-data/galaxyMaps.ts` + 纯逻辑核 `galaxyMapsCore.ts` + 最小 PNG 编解码器 `pngCodec.ts`——零新依赖，node:zlib 压缩，省 token 约定落实）：DSS2 彩色合成公版影像（源图入 `scripts/bake-data/assets/galaxy-images/` 不进 public/，下载 URL/授权登记于同目录 README；hips2fits 切取 TAN 1024²）→ 前景星去除（局部中值对比钳制，延展亮结保留）→ 污染源遮罩（M31 画面的伴系 M32/M110 与亮星 ν And、SMC 画面的前景球状星团 47 Tuc/NGC 362，按天球坐标圆形遮罩 + 归一化卷积 inpainting 平滑填充）→ 背景扣除 → `public/data/galaxy-maps/` 每星系五件产物：密度/颜色/尘埃遮罩 256² + 远景贴图 512px（alpha 噪声地板 + 径向羽化 + 4bit 量化压缩）+ meta（source/license/credit/pixelScaleLyPerPx/inclinationDeg/positionAngleDeg/失真登记）；M31 做 77° 倾角反投影到盘面坐标（薄盘短轴 1/cos i 拉伸 + 核球径向缓和防雪茄状拉伸，方法登记于 meta；残差 = 盘环带流量加权轴比 0.836 登记）；单星系产物 213–298 KB ≤600 KB，public/data/ 总量 1.85 MB ≤15 MB（附录 A §1 修订上限，index.ts 同步 5→15 MB 登记）；`npm run bake:data` 幂等（两次运行全产物 shasum 逐字节一致实测）。B) 运行时采样纯逻辑（`utils/galaxyNearView.ts` §R5-1 B）——`sampleParticlesFromMap`（密度图 2D 逆变换采样：CDF 前缀和 + 二分 + 像素内抖动；颜色图查色 + 密度调制亮度防加性混合过曝目验调参登记；z 向厚度沿用 R4-9 参数模型——旋涡 = 盘厚高斯薄层、不规则 = 半径×flattenY×0.55，"盘面分布真实、垂直分布参数化"口径登记）；HII/年轻星团分量改为密度图高亮蓝区加权布点（权重 = 密度²×蓝色超出，替代 R4-9 泊松盘随机；权重全零返回空分量）；尘埃分量改为尘埃遮罩加权布点（真实暗带走向，权重×密度约束防盘外缘无背景漂浮暗粒目验修正登记）；M31 参数化 M31_DUST_RING/applyBulgeTint 在影像路径不套用（10 kpc 尘埃环与偏黄核球由影像自带，登记）；分量配额与参数化路径同源 `galaxyComponentQuota`（M31 9,850 ≤ 单星系 12,000 粒预算/GPU 估算零变化）；确定性：FNV-1a 种子派生（`:map` 后缀与参数化域分离）+ 每粒子固定 7 随机数消耗，两次进入逐字节一致（单测）；统一入口 `generateGalaxyNearViewCompositeAuto`——产物缺失/加载失败/未覆盖星系降级 R4-9 参数化输出逐字节一致（逐星系单测）。C/D) 近观接入（M31 打样自检达标后批量）——`GalaxyNearView.tsx` 增 maps 可选 prop（就绪即影像驱动、参考半径 = meta 图半径与贴图平面对应）；新 hook `useGalaxyImageMaps`（meta 经 `bakedData.loadGalaxyMapMeta` 校验 + 像素经浏览器 Image/canvas 解码为字节数组，模块级缓存，失败返回 null 降级；主场景近观激活才懒加载）；椭圆类（M87/M32/M110/人马座矮）不套用登记（Sérsic 解析分布已足够，M87 近观回归截图确认零回退）；近观姿态修订登记：M33/LMC/SMC（影像未反投影、已含天空投影）由 id 哈希改为盘面正对视线（防双重投影致照片结构不可对应；M31 = 反投影 + 真实倾角 77° 再倾转，自银河系视线看回投影 ≈ 公版影像；其余星系哈希零变化）；预览页 `?body=m31|m33|lmc|smc`（新增 m33/smc 条目）增加"参数化/影像驱动"对比切换滑杆（目检对照用；非反投影星系预览默认倾角 0 登记）。E) 远景贴图接入——`Universe.tsx` `GalaxyObject` 贴图源影像优先（`galaxySpriteImageUrl` 覆盖清单纯函数：四星系返回产物 URL、其余 null 沿用程序化 canvas 现状登记），经既有 `useBitmapTexture`/textureManager 懒加载计入纹理预算与加载进度体系（L4 淡入域内才请求，加载完成前 canvas 降级）；billboard/尺寸/淡入淡出逻辑零改动；M31 远近景过渡结构对应（同源影像，近观 → Esc 拉远序列截图 seq0–3 登记连续）。授权署名（附录 A §2）：应用内呈现于星系信息面板数据来源行（`GALAXY_STRUCTURE_SOURCE_ZH` 追加 STScI DSS/AAO/ROE/Caltech + hips2fits 署名，位置登记）。新增单测三套 75 例：`galaxyNearViewR51`（31 例：覆盖清单/贴图源选择/CDF 逆变换/采样确定性与 delta 密度落点/颜色查图/蓝区加权/尘埃密度约束/降级逐字节一致/配额与预算/z 厚度口径/入参校验）+ `bakedDataR51`（18 例：meta 校验全分支/加载缓存降级/实际产物完整性集成——PNG 签名/单星系 ≤600 KB/M31 反投影登记断言）+ `scripts/bake-data/__tests__/galaxyMapsCore`（26 例：PNG 编解码往返与确定性/TAN 投影北上东左/前景星去除点源-延展区分/遮罩修补边界连续/合成 60° 倾斜环反投影恢复圆形轴比 >0.93/核球缓和端点/归一化与尘埃遮罩/贴图羽化量化/残差度量锚点；scripts 不计入 src 覆盖率 gate 登记）；R4-10 姿态/预览断言等价迁移登记；全量 2,735 用例/142 套件通过，覆盖率 gate ≥90% 保持（语句 98.53%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r51 系列登记）：预览页四星系影像/参数化对比切换 + 正视/绕行两角度（M31 打样自检 ≥3 处结构对应：尘埃暗带走向/暖核球-蓝盘色分层/蓝区星团串聚集/倾斜椭圆轴比）；主场景 L4 远景四星系影像贴图可辨（M31 倾斜椭圆 + 尘埃带真实形态）、飞往四星系近观影像驱动逐站 60 FPS、M31 近观 → 拉远过渡序列结构连续、M87 椭圆星系不回退、m31↔m33 LRU 进出 ×3 强制 GC 堆 48→48 MB 无泄漏、控制台零错误

### 改进

- README 与 docs/ 教程文档全面更新至 R4/R5 交付状态（提升项目吸引力与准确性）：README 重写亮点特性——新增「体积渲染星云与引力透镜黑洞」（raymarch 星云五件套/黑洞光子环与 EHT M87* 档/恒星表面物理化/帧率自适应质量档）与「真实天文数据驱动的星系」（DSS2 影像驱动近观/体积尘埃盘消光/2MRS 43,488 真实星系点云/Gaia DR3 昴星团/M87 星系团环境等）两大区块，效果演示区补新看点导览，技术栈增数据管线行、徽章与测试数更新至 2,951 用例/154 套件，项目结构补 volumetric/dev/scripts/bake-data/public/data，素材许可增天文星表与巡天数据授权块（Gaia DR3/2MRS/DSS2+hips2fits/SIMBAD 文献/Harris）；view-guide L3 重写为 14 站深空导览（体积星云/透镜黑洞/物理化恒星/真实星表星团四类看点 + "跟随后继续推近"玩法提示，修正原 15 站笔误）、L4 增影像驱动近观/2MRS 背景/M87 EHT 推近/GRB/费米气泡与 HI 翘曲、跨视角机制表补近观细节层与自适应质量两行；controls 显示开关补费米气泡（L3/L4）与真实巡天背景（L4）两行、巡游序列站数修正（L3 14 站）并补"跟随+推近解锁近观细节层"说明；getting-started 推荐路线扩至 7 步（新增"推近一颗黑洞"与 M31/M87 看点）、FAQ 补自适应降档说明；science-notes 真实数据表增恒星黑体色/昴星团 Gaia DR3/M13 King/黑洞透镜原型/星系影像/2MRS/费米气泡与翘曲文献等八行，艺术化登记表增体积星云程序化/M87* 尺寸压缩/2MRS 三项失真/费米气泡可见光化/翘曲振幅放大/30 Dor 放大 5×/GRB 周期压缩等八行；events-guide 补离散视角判定说明（R5-8）与 GRB 常驻周期演示物注记；development 技术栈/命令（dev:3100、bake:data）/目录结构/核心约定（新增统一细节层机制、透明层 renderOrder 注册表、离线烘焙纪律三节）/预览工位（/dev/preview 条目与调参说明）/性能预算（细节层显存 ≤64 MB、产物 ≤15 MB）/需求文档索引（R1–R5）全面同步；全部数字与序列经代码事实抽查（cycleScopes/panelScopes/devPreview 注册表），lint 通过

### 修复

- 修复人造卫星 glTF 共享场景所有权缺陷（P0 用户可见缺陷：太阳系/行星视角切到地球或直飞 hubble / geo-satellite 时两颗卫星不可见；iss 因启发式重着色、tiangong 因程序化主路径幸存）：根因为 `modelManager` 每 URL 只缓存一个 `gltf.scene` 而每次挂载对该共享对象就地修改，四类问题同根——①归一化非幂等（`prepareGltfSatellite` 第二次调用量到已归一化的 2.4 跨度 → scale 被重置回原始尺寸：hubble 原始跨度 ≈525 相机没入模型内部、geo-satellite ≈20.7 且材质近黑均不可见；R3F v9 起 StrictMode 桥接进 Canvas 使 dev 下 `useMemo` 工厂双调用必现，生产下近观门控进出复用缓存 scene 时奇偶次交替损坏——非幂等本身是缺陷，StrictMode 只是放大器）②reparent 抢夺（`container.add(scene)` 把共享 scene 从旧容器偷进新容器，废弃容器被掏空）③材质污染跨挂载泄漏（淡入 opacity/transparent 与地影 color×dim + userData.baseColor 写入共享材质）④ISS 着色重复替换泄漏（旧材质无人 dispose）。修复采用**实例化所有权模型**（源场景永远只读、每次挂载克隆独立实例、谁创建谁释放）：`prepareGltfSatellite` 改实例化语义——`source.clone(true)` 克隆层级（geometry/texture 按引用共享，GPU 零增量）+ 逐 mesh 克隆材质为实例私有，ISS 着色仅作用实例且被替换的克隆材质就地 dispose，归一化在全新克隆上计算（构造性幂等，数学与原"正确的第一次调用"完全一致、视觉零变化），不再 reparent 共享对象；`SatelliteModel` 卸载 effect 补 glTF 分支——只 dispose 实例材质（共享 geometry/texture 归 modelManager `release()` 统一释放），程序化分支行为不变；`modelManager` 文件头登记"源场景不可变"契约。新增单测 `satelliteGeometry.test.ts`（7 例：两次调用两独立实例缩放均 = 2.4/原始跨度且逐值一致的幂等锚定/最长维 Z 源模型对齐 X 轴 + 居中 + 源零污染/源场景无父级、变换恒 identity、材质未替换/geometry 引用共享与材质引用独立/实例材质 opacity+color 污染不泄漏源与其他实例/ISS 着色仅作用实例、源材质未替换未 dispose、被替换克隆材质恰好 3 次 dispose/卸载释放只 dispose 实例材质不影响源且源可再次实例化），全量 3,139 用例/167 套件通过、覆盖率 gate ≥90% 保持、type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，dev:3100，截图 sat-fix 系列登记）：`?body=earth`（ISS/哈勃/TDRS/天宫全部可见）、`?body=hubble`、`?body=geo-satellite` 三组卫星模型均可见；门控进出复验——同一会话飞 hubble → 切 L2 拉远（卸载）→ 再飞回 ×2 轮 + geo-satellite 二次挂载，第 2/3 次挂载（缓存复用路径）模型均正常呈现，奇偶两相全覆盖；全程 60 FPS、控制台零错误
- 修复 L4 跟随星系时近观盘面频闪（P0 用户反馈缺陷；根因两项已定位——宇宙域透明层深度排序键反复交叉致 draw 顺序逐帧翻转 + R4-4 自适应质量档极限环振荡，均经 Bloom mip 链放大）。**A) 排序稳定化**——新纯常量模块 `utils/universeRenderOrder.ts`（L4 宇宙域透明层 renderOrder 注册表，单一事实来源禁散落魔数，登记序列单测锚定递增/唯一）：星场 −1（对象原点 (0,0,0) 深度键具欺骗性，实测曾与银晕加性粒子交叉翻转，负值锁定透明队列最先）→ 2MRS 目录点云 1 → 宇宙网 2 → 加性流层 3（麦哲伦/人马座流/M31 流光点/合并辉光/银河年刻度）→ 静态引导线 4（卫星轨道/接近虚线/边界环/可观测环）→ 运动线 5（尾迹/预测/高度线——顶点逐帧增长深度键漂移，实测与静态线环组内交叉，独立分档）→ 银河系尘埃暗带 6（原魔数 2 迁入，"晚于全部加性宇宙层吸光"语义保持）→ 近观加性粒子 7 → 近观尘埃暗纹 8（原 DUST_LAYER_RENDER_ORDER=2 迁入，R4-10"晚于加性星光层"语义保持）→ 直绘发射体积 9（费米气泡/30 Dor——**renderOrder=10 并列组核对结论：需错开**，原与体积合成同值深度歧义；置合成之前被尘埃盘按透射率压暗与消光方案 a 物理一致；组内共档登记：两者原点锚定银心/LMC 盘面相距 ~16 万 ly 深度键稳定）→ 体积合成 10（VOLUME_RENDER_ORDER 改由注册表取值、值 10 不变零回退）；配套七处透明线材质 depthWrite=false（显式排序后写深度会按次序裁剪后绘透明层）。**B) 档位抖动阻尼**（`utils/adaptiveQuality.ts` 纯函数改动，二措施组合）——降档阈值 55→52（迟滞带 3→6 FPS，帧耗时骑 55 的场景落入迟滞带稳定持档）+ 新增 3s 换档驻留 `QUALITY_CHANGE_DWELL_MS`（任何换档后 3s 内不再换档；驻留期升档达标累计照常推进届满即结算，驻留 3s < 升档判据 5s 故"恢复后 ~5s 升档"时序零回退；创建时刻视作换档起点 = 挂载后 3s 观察期）；`decideTier` 增可选 lastChangeMs 入参（缺省无驻留，旧签名零回退）；判据修订登记于 IMPROVEMENT_REQUIREMENTS_4 §R4-4 修订块（参照 R5-8 在 R2-4 的登记先例，文档版本 1.4）；预览页强制档位滑杆（R4-24）语义不回退（实测 auto high → 强制 low 32 步/RT 50% → 强制 high → auto 往返生效）。测试：新增 universeRenderOrder 5 例（登记序穷举/严格递增/整数域/锚定值/既有相对序不变式——近观尘埃晚于加性星光、暗带晚于加性宇宙层、合成晚于一切直绘与粒子）+ adaptiveQualityR44 增驻留时序 7 例（驻留内不降档届满即降/驻留内达标累计届满结算/旧签名零回退/**骑阈值不振荡回归**——mid 档 54–56 摆动 60s 恒零换档、51/60 交替窗口均值落带内不振荡/深跌两次降档间隔 ≥3s/升档 ~5s 时序零回退），既有 55 锚定例同步迁移 52；全量 2,951 用例/154 套件、覆盖率语句 98.44%/分支 97.81%（gate ≥90%，两改动模块 100%）、type-check/lint/build 全绿。**实测**（无头 Chrome Metal 1280×800，dev:3100，跟随 m31/m33 未暂停采样）：draw 顺序翻转修复前 7 次/378 帧（≈1.1 次/s，与用户"每 0.5–1.5s 频闪"吻合；实证交叉对 = 银晕粒子↔星场、尾迹线↔边界环）→ 修复后 **0 次/329 帧（m31）与 0 次/328 帧（m33），各 ≥7.6s**；raymarch uSteps 全程恒 48 无振荡（③）；60 FPS 不跌破、控制台零错误（④）；回归——L4 8 站巡游整圈逐站 60 FPS、堆 51→52 MB 平稳、逐站持有者与 R5-7 基线一致，L3 orion-nebula 体积站/sgr-a-star 透镜站抽查 60 FPS 持有者正常。**残留登记（独立于本缺陷的既有现象，非本次引入）**：区域亮度极差指标经逐帧 readPixels 探针表征为 60Hz 严格帧间交替的 Bloom 晕微调制（区域均值幅度 ~0.8/255 ≈0.9%，围绕亮核平滑径向场；修复前后等幅 1.04 vs 0.92–1.02），逐项排除法定位——与体积模块无关（GL 钩子屏蔽合成三角形/RT 内体积盒/全部 raymarch 后等幅存在）、与 MSAA（强制 0 复现）/渲染后端（ANGLE Metal 与 GL 均复现）/模拟动画（暂停 + 屏蔽流光点复现）/相机与 continuousLevel（逐帧恒定）无关，仅 EffectComposer 挂载时出现且随亮内容放大（Bloom 关闭逐字节静止、L4 自由视角仅 0.03）；30Hz ±1% 感知上趋于融合，非用户报告的秒级大面积明暗频闪；建议后续独立排查 postprocessing 管线帧间态
- 抑制浏览器扩展导致的 React 水合警告（用户反馈）：Grammarly 等扩展会在 React 水合前向 `<body>` 注入自有属性（`data-gr-ext-installed`/`data-new-gr-c-s-check-loaded`），造成 SSR 与客户端属性不一致的 hydration mismatch 告警（dev 模式控制台红字，功能无实际影响；无扩展的干净环境不复现）——`layout.tsx` `<body>` 加 `suppressHydrationWarning`（React 官方针对该场景的标准做法：仅抑制该元素自身的属性差异告警，子树水合校验不受影响），type-check/lint/build 全绿
- 修复银河系视角下太阳事件仍触发/弹通知（R5-8，IMPROVEMENT_REQUIREMENTS_5 §R5-8，P0 用户反馈缺陷）：银河系巡游跟随造父一/天狼星等 L3 特殊天体时，HUD 显示"银河系视角"却弹出太阳耀斑/CME/CME 抵达通知；镜像缺陷——同场景活跃超新星被误丢弃、超新星演示按钮被误置灰。根因：事件门控四处消费方（HudInfo 通知可见/ControlPanel 演示按钮/SunActivity+Supernova 自动触发/store R3-3 离域丢弃计时）均读 `continuousLevel`（相机-原点距离换算连续层级），而 R3 层级锁定使跟随/飞往巡游天体期间离散 `viewLevel` 锁定为域主层级、`continuousLevel` 仍随相机距离同步——跟随造父一（距原点 ~190 单位）时 continuousLevel≈2.2 落入原太阳事件窗口 [1, 2.4]。修复（用户确认方案）：`utils/eventScopes.ts` 判定基准统一由连续层级窗口改为**离散 `viewLevel` 视角集合**（耀斑/CME/CME 抵达 → {L1, L2}、超新星 → {L3}（R3-5 收窄语义保持）、合并预览 → {L4}），`eventInScope` 及三层门控（自动触发/通知可见/演示按钮）签名改收 `ViewLevel`、不再判断相机距离，四处消费方层级来源全部改 `viewLevel`（单一语义杜绝再分叉）；原连续窗口常量（SOLAR_EVENT_MAX_LEVEL/SUPERNOVA_EVENT_MIN_LEVEL/SUPERNOVA_EVENT_MAX_LEVEL/MERGER_EVENT_MIN_LEVEL）废弃删除（文件头登记语义变迁 R2-4 连续窗口 → R5-8 离散视角集合）。**行为变更登记（边界微调两处，用户已确认）**：自由缩放（无跟随）时 viewLevel 本就随距离同步离散层级（边界 1.5/2.5/3.5），等效边界较原窗口微调——太阳事件上界 2.4 → **2.5**（消除原 2.4–2.5"事件已域外但特效仍满值"空窗）、合并预览下界 3.6 → **3.5**。R3-3 丢弃宽限（1 秒）/锚点切换与飞往运镜豁免语义不变（按 1-4 显式切换时 viewLevel 即时变更而相机仍在运镜，豁免继续防误丢弃）；特效 LOD 淡入淡出仍由连续层级驱动（SunActivity trapezoid(0.5, 0.9, 2.4, 3.0)/Supernova snFadeWeight(2.5, 2.9, 3.5, 4.0) 不动，缩放平滑零回退；已知边界差异登记于需求文档 §8.2-A）。测试：eventScopes 单测重写为四视角 × 五事件 ViewLevel 矩阵（三层门控逐格一致 + 视角互斥覆盖）+ 跟随造父一用户场景门控回归；storeR33 丢弃套件层级设置等价迁移为 viewLevel 双写（迁移登记于文件头）+ 新增用户场景回归套件（锁定 L3 + continuousLevel 2.2：太阳整链 ~1 秒丢弃、超新星保留不误杀）；全量 2,659 用例/139 套件通过、覆盖率 gate ≥90% 保持（语句 98.55%，eventScopes.ts 100%）、type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r58 系列登记）：跟随造父一（viewLevel L3 锁定/continuousLevel 2.25）种入太阳事件整链 1.8s 后全链丢弃、DOM 无太阳通知、HUD"银河系视角"，超新星可触发/通知可见/演示按钮域内可用；四视角锚点 × 事件矩阵逐格复验（L1/L2 仅太阳事件、L3 仅超新星、L4 仅合并预览，触发/通知双层一致）；合并预览启动 viewLevel 即时 L4 恒域内不误杀；跟随造父一 + 活跃超新星 60 FPS、控制台零错误
- 修复信息面板"距离"行重复与串卡（R4-10 目验发现，属既有问题）：3C 273/触须星系等特殊天体卡片因 catalog 自动距离行与 factsZh 自带"距离"行同 label 出现重复行，且 React 同 key 复用会把旧卡行串到下一张卡（如人马座矮星系卡出现类星体距离）——catalog 特殊天体分支改为 factsZh 已含"距离"时以其富文本为准不再叠加自动行；HudInfo 信息行 key 增加序号前缀防同 label 复用串卡；L4 巡游经停类星体站复验控制台零告警
- 修复体积预览拖拽绕行出现残影（用户反馈，R4-7 复验发现同样影响 R4-4 volume-test）：根因——`postprocessing` 的 `EffectComposer.setRenderer` 会把 `renderer.autoClear` 永久置 `false`（@react-three/postprocessing 包装层仅在 composer 自身渲染前后保存/恢复该值，恢复的仍是 false），体积 RT pass 手动 `gl.render` 依赖的隐式自动清屏因此失效 → RT 帧间累积：旋转相机时旧帧云影/暗湾按帧叠成点状拖尾与拉长团块，且整屏残留 alpha 会盖住参考网格、低通糊化云体细节；修复——RT pass 内 `setRenderTarget` 后显式 `gl.clear(true, false, false)`（遵循当前视口/剪裁子区域，半分辨率动态视口路径不受影响），`OrionNebulaPreview` 与 `VolumeTestPreview` 两处同源修复；无头 Chrome 复验（Metal 1280×800）：注入 WebGL 钩子实测 RT 绑定态 clear 1,140 次/秒（每帧显式清屏生效）、两次快速大幅拖拽后画面无拖尾、参考网格恢复可见、云体 fBm 碎散边缘较修复前明显变锐，8 方向验收截图以修复后渲染重新登记（r47-01～18，烘焙打点墙钟 582ms/最大单块 26.8ms 不变），60 FPS 零控制台错误（用户反馈：曝光和时间流速滑杆滑动没有效果）：A) 曝光滑杆完全无效——根因：预览主体 `StellarSurface` 为裸 ShaderMaterial（不含 three tone mapping 着色片段），`renderer.toneMappingExposure` 对其无效，且 Bloom 开启时 EffectComposer 会强制 `NoToneMapping` 令内建材质也不响应；修复：Canvas 置 `flat` + EffectComposer 常驻挂载并在管线末端追加 `ToneMapping(ACES_FILMIC)` 效果，曝光在帧缓冲级统一生效（对裸 ShaderMaterial 及后续 R4 体积/透镜等自定义 shader 预览组件同样有效），Bloom 仍按开关条件渲染并置于 ToneMapping 之前（作用于线性 HDR）。B) 时间流速滑杆逻辑生效但肉眼难辨（对流噪声漂移与自转过缓，数秒内几乎无视觉变化）——预览自转基准角速度 0.05→0.15 rad/s（仅预览页，主场景不动），HUD 新增「虚拟时钟」读数（拖动滑杆立刻可见走速变化，=0 时冻结可校验）
- 预览场景健壮性加固（同批重构）：滑杆调参由"props 触发 useMemo 高频重建 ShaderMaterial"改为挂载时缓存材质引用 + 每帧 uniform 直写（零重建、渲染循环零遍历/零闭包分配，附录 A §2）；uTime 覆写由依赖 React effect 挂载顺序的隐式行为改为显式 useFrame 优先级（0.5，介于默认订阅者与 EffectComposer 渲染之间）；OrbitControls `minDistance` 按条目相机距离推导（0.1→cameraDistance×0.5，防止推进到天体内部黑屏）
- 登记（不在本轮修复范围）：`StellarSurface` 现状 shader 未含 `logdepthbuf` 兼容片段（附录 A §5，P6 遗留），归入 R4-6 恒星表面物理化改造时补齐

### 新增

- 完成 R4 迭代集成回归验收（R4-24，IMPROVEMENT_REQUIREMENTS_4 §R4-24 收尾）：R4-1～R4-23 全量交付后的端到端回归 + 性能总账 + 文档回写。A) 测试四件套全绿——`npm test` 2,660 用例/139 套件、语句覆盖率 98.55%（gate ≥90% 保持）、type-check/lint/build 零告警；`npm run bake:data` 幂等复核（两次运行 `public/data/` 产物 shasum 逐字节一致，总量 706.5 KB ≤ 5 MB 上限）。B) 无头 Chrome 集成回归（Metal 1280×800，截图/日志 r424 系列归档）——L3 银河系巡游 14 站（登记：需求文本"15 站"含 sun，R4 期间 sun 因与 heliopause 语境重复移出序列，实际 14 站为既有登记行为）+ L4 宇宙巡游 8 站整圈：逐站近观截图 + FPS + GC 后 JS 堆登记，全部 22 站 60 FPS（≥55 无一跌破、未触发自适应降档），堆 46→52 MB 平稳，控制台零错误；逐站细节层持有者核对（恒星/星云 volume/黑洞 lensing/昴星团 starCatalog/星系 particles 按站激活）。C) 连续切换 15 个细节层目标（particles/volume/lensing/starCatalog 四类交叉，含 L3↔L4 跨域）×3 轮——四池 LRU 交叉逐出全部符合容量 1 语义（volume：orion→ring→crab 依次逐出；lensing：sgr-a-star↔cygnus-x1；starCatalog：pleiades↔antennae-galaxies；lru-retain 保留层不被跨类切换误逐）、GPU 估算峰值 4.2 MB ≤ 64 MB 预算且随释放出账归零；堆稳定性以生产构建为准登记——`out/` 静态产物 L3 巡游 ×3 轮轮末 GC 堆 30.9→31.4→31.1 MB 平台稳定无泄漏；dev 模式下切换伴随 ~0.1–0.7 MB/次堆增长，分配采样取证为 React dev/drei Html root churn 保留（jsxDEV 元素树 + createRoot 监听器），细节层自身资源无保留（快照 Float32Array 不在保留栈、空转 300 s 堆零漂移、GPU 出账归零），登记为 dev-only 现象不作产品缺陷处理。D) 预览页 19 个注册对象逐一截图归档全部正常渲染（亮像素/FPS 抽查 60）；自适应降级强制触发验证——orion-nebula 质量档滑杆 0 自动（high 64 步/RT 100%）→ 1 强制 low（32 步/RT 50%）→ 3 强制 high → 0 自动恢复，HUD 档位/步数/RT 比例与实际渲染一致。E) R2/R3 关键点回归抽查全 PASS——R3-1 巡游域切换（L3 巡游落银河系序列、切 L4 后落宇宙序列不串台）；事件域隔离（L3 触发超新星激活 → 切 L1 离域按宽限丢弃 activeSupernova=null）；合并演化预览（激活/演化推进 0.250→0.585/恢复回退 3,168 Myr 快进、残差 30 Myr 为 L4 自然时钟推进）；卫星角尺寸钳制以截图人工判定（ISS 跟随抵达/极近两档均 ≈2–3% 屏高 ≤ 10% 钳制上限，`satelliteScreenClampFactor`/`satelliteProximityFade01` 纯函数单测 100% 覆盖；登记：滚轮无法压过跟随相机最小距离下限，极近淡出档由单测覆盖）。F) 性能总账（附录 A 逐项对照全部达标）——单目标近观粒子峰值：L3 = M13 近观 +1,200（全局峰值 ≤16,200 ≤ 20,000 预算）/ L4 = M31 9,850 ≤ 单目标 12,000 上限；体积纹理最大 128³×2ch = 4 MB（≤128³ 约束，volume 池容量 1 → 常驻至多一份）；四池共存最坏 GPU 估算 ≈4.9 MB ≪ 64 MB（`DETAIL_GPU_BUDGET_BYTES`）；`public/data/` 706.5 KB ≤ 5 MB。G) dev 调试钩子（验收基础设施）——dev 环境暴露 `window.__simStore`/`window.__detailLayerDebug` 供无头 Chrome CDP 验收脚本读写状态与核对细节层注册表（`NODE_ENV production` 条件恒假摇树剔除，生产 bundle 与运行时行为零影响）
- 实现透镜星系团屏幕空间引力透镜弧（R4-23，IMPROVEMENT_REQUIREMENTS_4 §R4-23）：L4 飞往/跟随星系团引力透镜（原型 Abell 370）时，以 postprocessing 自定义 Effect（**方案 a** 登记：与 EffectComposer/Bloom 集成实测无风险，未降级方案 b）对屏幕 UV 施加 SIS（奇异等温球）模型偏转——背景（星场/宇宙网/同方向 M87 光晕/新增背景源）在爱因斯坦半径附近被拉伸成切向弧/部分爱因斯坦环，非跟随时透镜 pass 不挂载零渲染开销。A) 新建纯逻辑 `src/utils/clusterLensing.ts`（附录 A §3 纯函数先行，物理近似逐条登记于文件头）——SIS 透镜方程 β = θ − θ_E·θ̂（Narayan & Bartelmann 1996 §3.1；Schneider, Ehlers & Falco 1992）：`sisDeflectionMagnitude`（偏转恒 θ_E、θ=0 连续性取 0）/`sisSourceOffset`（环上 β=0 完整环、环内翻转像）/`sisTangentialMagnification`（μ_t = (1−θ_E/r)⁻¹，r→θ_E 发散单测锚定爱因斯坦环）；屏幕空间近似登记：帧缓冲 UV 重采样 uv_src = center + β（仅对团块之后背景严格成立，前景/透镜体自身同被偏移——同方向 M87 光晕被拉成环状属该近似的登记差异）；有效爱因斯坦半径取场景单位定值 300（θ_E = atan(R_E/d) 随相机距离缩放远观自然消隐；真实 Abell 370 θ_E ≈ 30″–40″ 压缩至近观 ~9.5° 可视化档登记；无头目验调优登记：初值 420 时 M87 光晕放大过甚淹没弧细节）；影响域窗 `lensDomainWindow`（θ_E×2.5 外沿 smoothstep 归零，实现性裁剪登记——真实 SIS 偏转全域恒 θ_E）+ θ_E（UV）安全上限 0.28 防近距全屏翻转；`clusterLensingUniforms`（NDC→UV 中心 + 角半径换算，out 参数复用零分配）；帧间源持有者（场景组件写入团块世界坐标/可见权重/θ_E 覆写 → 后期 Effect 读取，galacticFrame 注册表范式）+ 确定性背景源布局 `lensedBackgroundSources`（mulberry32 定种 6 源：方位均布 + 0.35–1.1×R_E 横向散布 + 纵深压在团块之后）。B) `ClusterLensingEffect.tsx`——postprocessing `Effect` 子类最小实现（单 mainUv fragment，域窗与 TS 纯函数同式；uniform 由 `ClusterLensingPass` 每帧直写：投影 NDC/视空间距离/fov/aspect，相机后方判据归零，临时向量复用零分配）；与既有管线集成登记（附录 A §5）：非卷积 Effect 自成 EffectPass 排在 Bloom（卷积）之前——Bloom 采样已透镜化帧缓冲、泛光与透镜像几何一致无错位；纯颜色域不读深度，与 log depth buffer/体积层（RT 在 composer 前合成）天然兼容，既有 Bloom 配置零改动。C) `PostEffects.tsx` 集成——域判据复用 LensingArcs 现状（跟随/飞往 cluster-lensing，:409-416）：命中时挂载透镜 pass 并 0.5s 淡入（`CLUSTER_LENSING_FADE_SECONDS` 与统一细节层同值同源），焦点离开淡出完成即卸载（Bloom 关闭且透镜未激活时整个 composer 维持卸载零开销现状）；实际强度回写持有者供场景层减淡消费；修复登记：透镜挂载 setState 触发 PostEffects 重渲染时，库的 Bloom 便捷包装 `JSON.stringify` 全部 props 会命中已填充的 bloomRef（BloomEffect 场景图循环引用）崩溃——Bloom 元素 useMemo 固定引用令 React 跳过该子树重渲染规避。D) `LensingArcs` 升级（`ExtragalacticObjects.tsx`）——每帧写入团块质心世界坐标 + 层级权重至持有者；静态示意 ring 弧按效果强度减淡 75%（`CLUSTER_LENSING_STATIC_ARC_DIM`，R4-22 静态减淡同范式，保留残影登记）让位真折射弧；新增团块弥散光晕 sprite（透镜体可见锚点）+ 6 个确定性背景源 sprite（世界系固定于团块之后（+direction 纵深），非 billboard 组——绕行视差下被拉伸弧位置随视角一致）；组件卸载重置持有者。E) 预览页注册 `?body=cluster-lensing`（新组件 `dev/ClusterLensingPreview` 场景 + harness composer 挂入同一 `ClusterLensingPass`，Bloom/ToneMapping 之前）：滑杆 3 件——爱因斯坦半径（场景单位，持有者帧写直达 uniform）/透镜强度（0 关闭对照）/背景源亮度；参考网格开启时同被 UV 偏转可直观检查折射域。新增单测 `clusterLensing`（24 例，clusterLensing.ts 覆盖率 100%：SIS 偏转恒 θ_E 与 θ=0 连续性/透镜方程径向内拉与环上归零与环内翻转/切向放大率发散-收敛-翻转三态/爱因斯坦角半径随距离消隐/角度→UV 换算 fovY 半高锚定/域窗内沿全强度外沿归零中点 0.5 单调平滑/uniform 组合与安全上限钳制与 out 复用/淡入淡出时长与细节层同源/持有者写读钳制重置/背景源确定性双次一致与径向-纵深-参数域/非法入参 RangeError/预览注册），全量 2660 用例/139 套件通过，覆盖率 gate ≥90% 保持（语句 98.55%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r423 系列登记）：预览页背景源拉伸切向弧/部分环清晰（强度 0 对照亮像素 4,854 → 开启 25,114、环带 22,519，帧差 24.6）、爱因斯坦半径滑杆环径实时响应（θ_E 3.5 时中心暗域扩至 minR 80px）、绕行偏航/俯仰弧位置随视角一致、Bloom 开/关组合均无伪影；主场景 L4 飞抵 cluster-lensing 透镜激活（M87 光晕成部分爱因斯坦环 + 星点切向smear + 静态弧减淡残影）、绕行 2 角度弧构型一致、同位置切焦点 M87 透镜关闭对照（帧差 77.2）、Bloom 关闭 + 透镜组合正常；性能对比登记：L4 非跟随基线 60 FPS（透镜 pass 未挂载零开销）vs 跟随激活 60 FPS，Esc/重进 ×3 强制 GC 后 JS 堆 46→46 MB 无泄漏（Effect dispose）、控制台零错误
- 实现触须星系 N-body 烘焙潮汐尾（R4-22，IMPROVEMENT_REQUIREMENTS_4 §R4-22 / §0.3 方案 D）：L4 飞往/跟随触须星系（NGC 4038/4039）达近观阈值时，静态示意层（双扭曲盘贴图 + 参数化尾弧线）之上挂载烘焙 N-body 快照粒子层——两核辉光（NGC 4038 暖橙 / NGC 4039 冷蓝）+ 双潮汐尾测试粒子随 simDays 缓慢演化，绕行立体可辨。A) 离线烘焙（`scripts/bake-data/antennae.ts` 接入 `npm run bake:data`，模拟参数逐条登记于文件头）——受限三体/测试粒子模拟（Toomre & Toomre 1972, ApJ 178, 623 图景）：两 Plummer 软化点质心（G=1、M1=M2=1、ε=0.15 r_p）抛物线相对轨道交会（e=1 按需求指定登记：T&T 原文 Antennae 用 e≈0.5 椭圆，尾形态图景一致、两核末段分离偏快）+ 各 2,796 粒/盘 ≤3,000（12 同心环 ∝ 半径配数、Plummer 软化圆速度、双盘顺行倾角 60°/节点角相差 π——T&T 强潮汐尾条件、两尾反对称展开）；RK4 定步长 dt=0.01 自 t=−12（r≈10 r_p 入轨，Barker 方程反解）积至 t=+7，10 快照均布 t∈[−2,+7]（快照数域 8–12）；mulberry32 确定性（seed=40384039）；自校验（§R4-22 第 3 条）：粒子数上限/无 NaN/坐标界 |r|<48/两核间距近心点前单调减后单调增/数值近心距偏离 r_p <10%/两核两体能量 RK4 漂移 <2%·μ（"能量单调性宽松断言"落实为守恒断言登记），失败退出非零；产物 `public/data/antennae.bin`（Float32 小端自述头 magic/version/S/N/nA，655.6 KB，public/data 总量 706.5 KB ≤5 MB）；幂等实测两次运行 SHA-1 逐字节一致。B) 运行时二进制加载（`bakedData.ts` 首个二进制产物路径）——`validateAntennae`（魔数 fround 比较/版本/快照数 8–12/粒子数 16–6,000/盘 A 计数域/字节长度精确匹配/坐标有限且 |r|≤64）+ `loadAntennae`（fetch + 按 URL 缓存成功不缓存失败可重试）+ `useAntennaeSnapshots` hook（模块级共享一次加载）；加载中/失败返回 null → 消费方降级现状静态渲染（CDP 拦截 antennae.bin 实测：预览页降级占位/主场景静态层保持，零报错登记）。C) 纯逻辑 `src/utils/antennaeNearView.ts`（附录 A §3 纯函数先行）——时间映射登记（§R4-22 第 2 条）：快照全程 ↔ 600 Myr（T&T 尾发育时标量级），simDays 经 simDaysToMyr 换算后按**三角波 ping-pong** 在快照序列上往返（登记：真实交会为单向事件，取往返循环保证任意 simDays 演化持续可见且插值连续——环绕 wrap 会产生末帧→首帧跳变故弃用；L4 时压 1 s ≈ 20 Myr → 单程 30 s 缓慢演化）；`antennaeSnapshotPhase`（seg/mix 相位，折返两侧连续性单测锚定）+ 快照位置缩放写入（1 r_p = 0.75× 基准半径，尾端伸展 ≈6.7× 与现状静态尾长同量级登记）+ 两核插值位置 + 粒子属性构建（FNV-1a 种子确定性；双盘暖橙/冷蓝配色为区分尾源盘的艺术化强调档登记，线性工作空间值）；`antennaeDetailLayerSpec`（starCatalog 池容量 1 与 R4-17 昴星团共池，进入/退出阈值与 resolveFocusTarget/R2-7 同源；粒子 6,000+2 ≤ 单目标 12,000，GPU 预算按双快照插值实际布局 40 B/粒登记——高于 estimateGpuBytes 默认 28 B/粒）。D) 渲染组件 `Scene/AntennaeNearView.tsx`（主场景与预览页共用观感同源）——快照插值取**顶点属性双快照布局**（position = 快照 seg、aPosB = 快照 seg+1，shader 内 mix(position, aPosB, uMix)）：每帧仅写 uniform、跨段才重传两侧属性缓冲（预展开数组 set 拷贝，渲染循环零对象分配附录 A §2）；软边圆点 shader 含 logdepthbuf 三件 + tonemapping/colorspace 输出（附录 A §5 Starfield/Pleiades 先例）；两核辉光 sprite 随相位插值移动；geometry/material/CanvasTexture 卸载即 dispose。`ExtragalacticObjects.AntennaeGalaxies` 挂接 `useDetailLayer({kind:'starCatalog'}, 'lru-retain')`（L4 语义快速切回免重建；近观激活时才挂载内层 hook → 首次进入才 fetch 产物）；既有静态示意层近观减淡 85% 让位粒子结构（保留残影登记，近观层 sprite 经 userData.nearLayer 标记跳过减淡遍历）。E) 预览页注册 `?body=antennae`（新组件 `dev/AntennaeNearViewPreview`）：滑杆 2 件——时间流速（虚拟时钟 1 s = 30 Myr 基准，加速目验演化与插值连续性）/粒径增益（帧读 getter 直达 uniform 零重建）；数据未就绪/失败显示线框占位。新增单测 `antennaeNearView`（19 例：spec 阈值同源与预算登记/相位起点单调与折返两侧连续 <1e-3/seg-mix 钳制域/非法输入回落/负时间对称/缩放写入与越界 RangeError/核插值槽位与防御钳制/属性确定性双次逐字节一致与双盘配色粒径域/静态减淡域/预览注册）+ `bakedDataAntennae`（11 例：合法解构/魔数版本快照数粒子数盘计数字节长度坐标域逐项拒绝/加载缓存与失败重试/自定义 baseUrl/实际产物完整性集成断言——通过运行时校验、每盘 ≤3,000、末帧最大粒子半径 >2× 首帧潮汐尾已甩出），全量 2636 用例/138 套件通过，覆盖率 gate ≥90% 保持（语句 98.56%，antennaeNearView.ts/bakedData.ts 均 100%），type-check/lint/build 全绿（登记：tsconfig 全局新增 `allowImportingTsExtensions`——Node 原生 TS 执行烘焙脚本 ESM 需显式 .ts 扩展名，noEmit 下合法、build 复验无影响）；无头 Chrome 目验（Metal 1280×800，截图 r422 系列登记）：预览页双核 + 两条反对称弯曲潮汐尾构型与公版图像可对应（暖/冷亮像素 721/804 双盘配色可辨）、绕行偏航/俯仰视差立体、时间流速 8× 两帧演化显著（帧差 6.34/2.49、亮区包围盒收缩对应折返回盘）、慢速连续 10 帧帧间差序列 2.42–4.82 无跳变尖峰、粒径滑杆生效，60 FPS；主场景 L4 飞抵触须星系（fiber 提取 selectBody+requestFlyTo 与 HudInfo"飞往"按钮同路径登记——两盘面倾斜视角点选命中面积过小属测试 harness 局限非缺陷）近观粒子层激活（暖/冷 1,595/2,921）、12 s（≈240 Myr）帧差 5.83 演化可测、绕行 60 FPS、Esc 进出 ×3 强制 GC 后 JS 堆恒 47 MB 无泄漏、控制台零错误
- 实现类星体 3C 273 近观升级（R4-21，IMPROVEMENT_REQUIREMENTS_4 §R4-21）：L4 巡游飞往/跟随类星体 3C 273 达近观阈值时，在既有极亮核心 + 双向相对论喷流 + 光变闪烁之上挂载近观细节层——内→外呈现"吸积盘（亮蓝白）→ BLR 弥散辉光 → 尘埃环面（暗红棕）→ 相对论喷流"四层结构。A) 新建纯逻辑 `src/utils/quasarNearView.ts`（附录 A §3 纯函数先行，科学近似逐条登记于文件头）——吸积盘为 R4-12 盘着色逻辑的**非透镜简化版**（平面环形 mesh：温度剖面黑体色 + 多普勒束流 δ³ + 引力红移 g³ + 差速条纹，公式/常数自 `utils/blackHoleLensing` 导入复用勿复制，GLSL 镜像单点同源；**透镜 raymarch 不启用**登记——近观取盘远观尺度弯折翻折像不可辨，平面盘即达验收观感）；盘峰值色温压标 12,000 K（3C 273 真实为 UV/光学"大蓝包"~10⁴–10⁵ K，R4-12 压标先例登记）；盘径向参数化 `quasarDiskRadiusRs`（内缘 ISCO 3 r_s → 外缘 12 r_s，R4-12 常数同源）；尘埃环面（AGN 统一模型，Urry & Padovani 1995）取**粒子环**（§R4-21"小型体积或粒子环二选一"登记：volume 池容量 1 已被星云类 L3 巡游站高频占用、插入将致 LRU 反复逐出重烘焙，粒子环成本更低）——`generateQuasarTorusParticles` 确定性生成 2,400 粒（FNV-1a 种子 + 管截面面积均匀采样 + y 向压扁 0.72，暗红棕内暖外暗配色为线性工作空间值登记：直取 sRGB 数值会被输出编码提亮成米色，目验调参）；BLR 辉光过渡层 sprite（宽线区弥散近似登记）随光变闪烁联动呼吸（`quasarBlrOpacity`）；核心辉光近观减淡 `quasarCoreNearFactor`（×0.45 让出盘视野，**光变闪烁因子保留不回退**）；几何次序单测锚定（盘外缘 0.85 < BLR 1.15 < 环面主半径 1.45 基准半径倍数，尺度比例可视化档登记——真实跨 3–5 量级）。B) 新组件 `Scene/QuasarNearView.tsx`（`QuasarNearCore`，主场景与预览页共用观感同源）——盘 shader/环面 points shader 均含 logdepthbuf 三件 + tonemapping/colorspace 输出（附录 A §5 Starfield/PleiadesCluster 先例）；环面 points 取 NormalBlending（尘埃为吸收介质：粒子堆叠维持暗红棕而非 additive 堆亮成米白，目验调参登记）；黑体 LUT 复用 `buildBlackbodyLutTexture`（R4-12）；`ExtragalacticObjects.Quasar` 经 `useDetailLayer({kind:'particles'}, 'lru-retain')` 挂载（L4 语义与 R2-8 星系近观共池 LRU 容量 1；进入阈值 = 河外飞往观察距离 ×1.5 与 resolveFocusTarget 同源——`cameraFocus` 新导出 `EXTRAGALACTIC_VIEW_RADIUS_UNITS` 单点收敛原地字面量）；盘/环面平面 ⊥ 既有喷流轴（AGN 统一模型几何）；卸载即 dispose（geometry/material/LUT/辉光纹理）。粒子预算登记：环面 2,400 + BLR sprite 1 = 2,401 ≤ 单目标 12,000 且低于星系近观峰值（共池容量 1 → 全局峰值不变，单测断言）。C) 预览页注册 `?body=quasar-3c273`（新组件 `dev/QuasarNearViewPreview`：近观三层 + 核心光变 sprite + `RelativisticJet` 主场景同比例复用——四层结构预览页整体目验），滑杆 4 件：束流强度（0 关闭/1 物理档 δ³/2 夸大）/盘亮度/尘埃环面亮度/时间流速（帧读 getter 直达 uniform 零重建）。新增单测 `quasarNearViewR421`（26 例，quasarNearView.ts 覆盖率 100%：规格阈值同源与现值 2700 锚定/particles 池与 GPU 预算 28 B/粒/四层几何次序与不重叠/环面粒子确定性双次逐字节一致与压扁管内落点与暗红棕 r>g>b 与粒径域与尺度线性/核心减淡单调有界与光变保留/BLR 光变联动/盘径向映射端点单调/峰值色温 LUT 域内/预览注册与滑杆），全量 2606 用例/136 套件通过，覆盖率 gate ≥90% 保持（语句 98.53%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r421 系列登记）：预览页四层结构俯仰/侧视立体可辨（盘差速条纹 + 暗红棕环面 + 喷流亮节点）、束流/环面/时间滑杆实时生效、光变呼吸亮度序列可辨（73→179 luma）；主场景 L4 巡游至 8/8 类星体站近观四层可辨、光变不回退（核心亮度序列 201→236 luma 持续波动）、绕行 2 角度 60 FPS；L4 巡游回归——M31（5/8）↔ 类星体（8/8）particles 池共池 LRU 交叉逐出往返 ×2，JS 堆 46→48 MB 稳定无泄漏、全程 60 FPS、控制台零错误
- 实现 WR 124 星风抛射壳 + 脉冲星射束体积化（R4-20，IMPROVEMENT_REQUIREMENTS_4 §R4-20 / §0.3 方案 B）：L3 飞往/跟随 WR 124 达近观阈值时，抛射壳 mesh 平滑过渡为团块泡沫状真体积壳层（M1-67 观感：壳上离散气体结 + 径向立体膨胀）；蟹状脉冲星双极射束由静态 uv 条纹锥升级为轻量体积锥（轴向密度衰减 + 视线弦长边缘软化，柔和无硬边）。A) `utils/nebulaVolume.ts` 新增 WR 124 密度场——`makeWr124Sampler(seed)`（NASA/ESA Hubble 与 JWST 2023 M1-67 公版图像观感参考，程序化近似登记：非逐结贴合照片）：球壳骨架（中面 0.6、fBm 扰动 ±0.12 去正球感）× 高频 fBm 阈值化团块泡沫（sstep 0.48→0.72 离散气体结 + 谷底 0.12 弱连接不撕裂壳层；瑞利-泰勒不稳定性成因 knot 以阈值化噪声近似登记）；壳内近空（中心为 StellarSurface 恒星本体）+ 吸收通道恒零（Hα/[NII] 发射主导登记）+ 内/外双早退门零噪声调用；纹理 64³（§R4-20 指定小型体积层，RG 双通道 512 KB）。B) 径向膨胀（§R4-20"径向速度场 uTime 驱动"）——`NebulaVolumeMaterial` 新增 uExpandAmp/uExpandPeriodSec uniform：raymarch 采样域按 1 + amp·sin²(π·fract(t/period)) 齐次缩放（= v∝r 均匀膨胀流近似，Hubble 型抛射流场登记；膨胀不烘焙进纹理），幅度 0.14/周期 80s 与既有壳 mesh 动画 nebulaExpansionScale(t,80,0.14) 同参（体积接管后节奏一致、交叉淡出无跳变），CPU 镜像 `wr124VolumeExpansionScale` 单测锚定同式；uExpandAmp 缺省 0——M42/M57/马头/蟹状行为零回退。C) `WolfRayet` 接入同 R4-8 模式——挂接 `useDetailLayer({kind:'volume'})`（release-on-exit，volume 池容量 1 与 M42/M57/马头/蟹状共池 LRU）+ 泛化 `NebulaVolumeLayer` 复用（无内嵌星点登记：中心为真实 shader 球非 sprite 点源）；包围盒边长 = 视觉尺寸 ×6.0（壳中面折算 1.8× 视觉半径与既有壳 mesh 1.9× 衔接、膨胀满幅时壳外缘仍在密度域内无切边登记）；交叉淡出：抛射壳 mesh（`wr124ShellMeshFactor` = 1−vol01，vol01=0 行为零回退）随体积淡入 0.5s 隐去、退出反向恢复；恒星光晕按 `wr124CoreBoostFactor`（×(1+0.35·vol01)，壳内腔近空压暗弱于蟹状 → 低于其 0.6 档登记）透射补偿。D) 脉冲星射束体积锥（`PulsarRemnant` beamMaterial shader 升级）——轴向密度衰减：smoothstep 改幂律软衰减（`pulsarBeamAxial01` = (1−y)^1.6 CPU/GLSL 镜像）；边缘软化：视线穿越圆锥截面弦长近似（圆截面几何弦长 ∝ |n·v| 精确关系 + 指数 1.3 艺术化软化档登记，`pulsarBeamChord01` 镜像）替代原静态 uv.x 条纹（视角无关假径向渐变）——轮廓边缘弦长→0 平滑消失，侧视/斜视均无硬边；DoubleSide 前后两面各按半弦长贡献（×0.5，相机入锥内连续）；扫描旋转/脉冲节奏零改动（pulsarBeamAngle/pulsarPulseIntensity 不动，密集采样复验灯塔峰间隔 ≈2.4s 自转周期）；补齐 log depth buffer 兼容（附录 A §5 P6 遗留，logdepthbuf 三件）；与 R4-16 蟹状体积层深度共存不变（射束绘制先于体积合成、crabCoreBoostFactor 透射补偿路径零改动）。E) 预览页 `?body=wr-124` 升级为恒星 + 抛射壳组合条目（componentKey 维持 stellar-surface，PreviewScene 特例叠挂 `NebulaVolumePreview`——boxEdge 覆写 14 与恒星尺度对齐、相机 3.2→9 外推覆盖完整壳层登记）：R4-6 三滑杆保留 + 新增抛射壳密度倍率/径向膨胀幅度两件（默认与主场景同源）；`NebulaVolumePreview` 时间流速滑杆调制虚拟时钟（其余体积条目无此滑杆恒 1 零回退）+ uExpandAmp 直写；HUD 体积质量档行经新纯函数 `previewHasVolumeLayer` 特例显示。新增单测 `nebulaVolumeR420`（24 例：吸收恒零/壳带定位与内外早退归零/团块峰值与结-隙离散比例/[0,1] 值域/确定性双次逐字节一致/64³ 预算与形态常量域/spec 阈值同源/包围盒尺度与壳中面折算/交叉淡出与透射补偿零回退边界/膨胀 CPU 镜像与壳 mesh 同参同式/配置字段与采样器接线/预览条目滑杆与 HUD 特例/射束轴向幂律端点单调与钳制/弦长对称端点单调与钳制）+ devPreview 单测更新，全量 2580 用例/135 套件通过，覆盖率 gate ≥90% 保持（语句 98.51%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r420 系列登记）：预览页 64³ 烘焙 89ms、两帧膨胀对比量化（timeScale×4 半周期：外缘半径 449→473px、外环带亮度 +125%）、密度滑杆归零壳层全隐、绕行 60 FPS；主场景 L3 巡游飞抵 WR 124 站团块泡沫壳包裹恒星 + 25s 两帧膨胀可辨 + 绕行两角度立体视差，连续巡游 WR 124→天鹅座 X-1→蟹状（volume 池 LRU 交叉逐出，往返 ×2 交替烘焙打点 64³ 51–61ms/128³ 291–313ms 一致）强制 GC 后 JS 堆 52→52 MB 无泄漏、射束侧视软边无硬边锥、脉冲节奏保留、全程 60 FPS、控制台零错误
- 实现 M13 球状星团 King 分布 + HR 图颜色（R4-19，IMPROVEMENT_REQUIREMENTS_4 §R4-19 / §0.3 方案 D）：`GlobularCluster` 位置分布由 rand² 均匀采样升级为 King (1966) profile 逆变换采样（Harris 目录 NGC 6205 真实结构参数驱动），颜色由固定色板升级为 HR 图两档 Teff → blackbodyRGB——近观呈现中心致密核 + 外围稀疏晕的真实球状星团密度梯度，红黄主色 + 少量蓝星点缀。A) 新建纯逻辑 `src/utils/m13Cluster.ts`（科学近似逐条登记于文件头）——三维密度取 King (1962, AJ 67, 471) 经验轮廓解析去投影式（King 1966 单质量模型标准近似）：ρ(r) ∝ (1/z²)[arccos(z)/z − √(1−z²)]，r ≥ r_t 截断为 0；结构参数自 R4-5 烘焙产物 `m13-profile.json`（核半径 1.28 pc / 潮汐半径 43.4 pc / 浓度 c=1.53）；逆变换采样按 §R4-19 省 token 约定取数值反查表（累积质量梯形积分 512 步 → 预计算 64 点逆 CDF 插值表，每星采样 O(1)，非解析求逆）——M13 参数下半质量半径 ≈0.121 r_t ≈ 5.2 pc（≈1.5× 投影半光度半径 3.49 pc，King 模型典型比值，单测锚定）；潮汐半径映射满视觉半径（核半径仅占 ~3%，密度梯度登记）；HR 颜色两档比例登记：老年红黄星族 90%（红巨星支/主序拐点以下，Teff 3,900–5,800 K，u² 偏斜取样偏冷端）+ 蓝离散星/水平支蓝端 10%（`M13_BLUE_FRACTION`，Teff 7,500–10,500 K 均匀）→ `blackbodyRGB`（R4-6 复用）+ sRGB→线性（vertexColors 工作空间）；确定性种子（每星固定 6 次抽取，两次构建逐字节一致）。B) `GlobularCluster` 接入——远观基础星场 420 粒 + R2-7 近观增量 +1,200 粒两级均接新分布（总预算不变、种子沿用现状 20260722/20260723 登记；近观层新组件 `M13KingStarField` 消费预构建属性，R2-7 门控/粒径/透明度参数零变化）；降级路径：新增 `useM13Profile` hook（`bakedData.loadM13Profile` 同源 fetch + 校验 + 模块级缓存），加载中/失败返回 null 时整体回落现状程序化分布（rand² + P6 色板 + rand^2.4 近观，降级登记；CDP 阻断产物实测回落零报错）；R2-9 银晕程序化 29 星团不在范围（登记）。C) 预览页注册 `?body=m13`（新组件 `M13ClusterPreview`，与主场景共用 m13Cluster 纯函数 + M13KingStarField 观感同源，远观/近观两级同挂），滑杆 2 件：粒径增益/亮度增益（亮度经帧读 getter 直达材质，粒径重建两份小几何 ≤1,620 粒登记）。新增单测 `m13ClusterR419`（17 例，m13Cluster.ts 语句覆盖 100%：径向密度严格单调递减/潮汐截断连续归零/核平台特征/反查表 64 点单调与端点/半质量半径 0.121±0.015 锚定与 pc 域/采样插值与域外钳制/HR 两档 Teff 端点与偏斜/属性构建确定性与数值域/径向壳层体密度单调递减与核内过半/蓝星比例 10%±3%/暖冷色观感比例/非法入参 RangeError），全量 2556 用例/134 套件通过，覆盖率 gate ≥90% 保持（语句 98.49%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r419 系列登记）：预览页 King 分布致密亮核 + 稀疏晕梯度、红黄主色 + 蓝星点缀可辨，粒径/亮度滑杆实时生效；主场景 L3 巡游飞抵 M13 站近观新旧对照（CDP 阻断 m13-profile.json 降级 rand² 弥散分布 vs 正常加载 King 致密核，截图 r419-scene-00/01 对照登记）、绕行 2 角度核/晕纵深立体可辨；昴星团↔M13↔邻站连续切换 ×3 强制 GC 后 JS 堆 50→50 MB 无泄漏、全程 60 FPS、控制台零错误
- 实现参宿四非对称巨对流胞 + 恒星近观日冕/衍射星芒（R4-18，IMPROVEMENT_REQUIREMENTS_4 §R4-18 / §0.3 方案 A）：参宿四表面叠加低阶球谐（l ≤ 3）大尺度不对称亮/暗斑缓慢演化（VLTI/SPHERE Montargès et al. 2021 观感参考），并为 L3 全部 6 类恒星（参宿四/参宿七/天狼星 A/B/造父一/WR 124）新增近观点缀——色球边缘辉光环 + 相机中距衍射星芒 sprite。A) 新建纯逻辑 `src/utils/stellarNearView.ts`（附录 A §3 纯函数先行，科学近似逐条登记于文件头）——球谐扰动 `betelgeuseShPerturbation`（shader CPU 镜像；省 token 约定不做通用球谐库：4 项峰值归一低阶实球谐基 l=1,2,2,3 固定系数展开，系数缓变余弦驱动，视觉周期 46/61/74/88 s 全部落在需求 40–90 s 区间登记、周期互质防拍频锁相；30 s 间隔全球面扰动 RMS 变化 >0.1 单测锚定验收演化可辨；登记：可选"尘埃抛射暗斑事件"不实现——非硬性，球谐负半周期本身呈现大暗斑已覆盖 2019-20 大变暗观感基调）+ 亮度调制 `applyShBrightness`（幅度 [0,1] 校验、零幅度零回退）；衍射星芒距离窗口 `starSpikeWindow01`（smoothstep(2.2r, 4.0r, d)：飞往驻留视角 ≈6r 全显、推近表面 ≤2.2r 全隐防遮挡，单调/边界/中点 0.5 单测）；色球环色温联动 `chromosphereRGB`（黑体基色向 Hα 红端混合，权重 (8000−Teff)/8000 钳 [0, 0.55] 随 Teff 单调递减——参宿四 3,600 K 显著偏红、≥8,000 K 热星纯黑体色；物理近似登记：真实色球为发射线叠加，此处线性混合近似）+ 环几何 `chromosphereRingSpriteScale`（贴图环峰值半径 = limb 外 1.04× 恒星半径）。B) `StellarSurface` shader 增量——新 uniform uShAmp/uShSpeed + `shPerturb`（GLSL 与 CPU 镜像逐式一致），球谐调制乘于对流亮度（spun 采样域——斑块随自转共转；uShAmp=0 时乘 1，其余 5 类恒星行为零回退）；RedGiant 注入 `BETELGEUSE_SH_AMPLITUDE_DEFAULT`（0.55）。C) 新组件 `StarNearDress`（SpecialBodies.tsx）——色球环 sprite（新 `proceduralTextures.createChromosphereRingCanvas`：中心透明、峰值 62% 半边长薄发射环，内缘陡外缘缓）+ 衍射星芒 sprite（复用 `createDiffractionSpikeCanvas`，颜色与环同源色温联动；边长 6.5× 半径——长于蓝巨星/WR 常驻辉光 sprite 半程避免被辉光淹没，目验调参登记）；每帧透明度 = 层级权重 × 近观权重 ×（星芒另乘距离窗口），世界坐标模块级暂存零分配；RedGiant/BlueGiant/WolfRayet/Cepheid 新增 `useNearViewGate` 门控（远景零开销、release-on-exit 卸载即 dispose），SiriusBinary 复用既有门控——A 星环+芒随互绕轨道运动、B 星仅环（常驻衍射芒线已有不叠加第二张，登记）；近观增量登记 `NEAR_VIEW_PARTICLE_INCREMENTS` 恒星站 0→2（sirius 0→3），与 `STAR_NEAR_DRESS_SPRITE_COUNTS` 单测断言同步防漂移（全局峰值预算内不变）。D) 预览页参宿四专属滑杆两件（§R4-18 指定）：球谐斑块幅度（0–1 默认 0.55）/球谐演化速度（0–6 默认 1），`STELLAR_PREVIEW_CONFIGS` 增 shAmplitude 字段（仅参宿四非零，其余恒星均匀颗粒观感区分单测断言），PreviewScene 每帧 uniform 直写路径复用。新增单测 `stellarNearViewR418`（28 例：周期区间/系数逐项与常量表一致/确定性/速度冻结与等价/扰动有界与归一化不变性/30s 演化可辨/亮暗两极不对称/亮度调制线性与零回退/距离窗口边界单调平滑/色温联动单调与热星收敛/环几何峰值落点/增量登记同步）+ devPreview 单测更新，全量 2539 用例/133 套件通过，覆盖率 gate ≥90% 保持（语句 98.48%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r418/r418b/r418d 系列登记）：预览页参宿四真实速度 30 s 两帧对比大尺度不对称斑块可辨且演化（亮斑重分布、暗区迁移），球谐幅度滑杆 0.55→0 斑块消失、演化速度 6× 下 6 s 图案重分布，与参宿七均匀细颗粒观感区分明显；主场景 L3 巡游 5 个恒星站——色球环（参宿四暖红环/热星蓝白环色温联动可辨）与衍射星芒全部呈现、逐站 60 FPS，参宿四站滚轮推近星芒平滑淡出（≤2.2r 全隐防遮挡表面）、拉远平滑恢复，Esc 取消跟随点缀随 0.5s 淡出，控制台零错误
- 实现昴星团 Gaia 真实成员星 + 反射星云（R4-17，IMPROVEMENT_REQUIREMENTS_4 §R4-17 / §0.3 方案 D）：`OpenCluster` 昴星团分支由程序化点簇升级为 `public/data/pleiades.json`（R4-5 烘焙产物，Gaia DR3 成员星 600 颗）真实数据驱动——真实 3D 位置 + B−V 转色 + 视星等分级粒径，近观呈现命名亮星星芒构型与蓝色反射星云包裹。A) 新建纯逻辑 `src/utils/pleiadesCatalog.ts`（科学近似逐条登记于文件头）——pc→场景单位比例（模型半径 6 pc 映射视觉半径：成员星 |r| p90≈5.7 pc 落入视觉半径、最远 8.5 pc 少量外溢保留真实分布不截断，登记）；B−V→Teff 取 Ballesteros (2012, EPL 97) 黑体近似公式 + R4-6 `blackbodyRGB` 查表转色（复用登记）；视星等→亮度（星等线性归一 [3.8,14] 域钳制）→粒径（超线性 b^1.5，域 0.022–0.117× 视觉半径与 R2-7 现状粒径档相当）；星表属性构建 `buildPleiadesStarAttributes`（positions/colors 线性空间/sizes 逐星，确定性纯函数）。B) 9 颗命名亮星（七姊妹 + Atlas/Pleione）真实相对位置——簇质心 ICRS 常量 (56.6866°, +24.1888°, 135.76 pc) 以产物内高置信匹配 3 星（Maia/Celaeno/Asterope，位置残差 <0.02′、|ΔV|≤0.02）最小二乘拟合（bake 质心 = 成员均值非锥形检索中心，拟合方法登记 + 实际产物单测断言防漂移）；产物内存在的 4 颗（Maia/Pleione/Celaeno/Asterope，角距 <1′ 且 |ΔV|<0.35 判据）吸附目录真实 3D 位置，Gaia DR3 缺失的最亮 5 颗（Alcyone/Atlas/Electra/Merope/Taygeta，G<5 亮星天测缺失）按 SIMBAD 天球坐标 + 簇质心距离合成（Hipparcos 视差系统差 ~10 pc 大于簇深度不采用，登记：天球面构型真实、径向近似）；衍射星芒 sprite 复用 `proceduralTextures.createDiffractionSpikeCanvas`（复用登记），星芒尺寸按相对通量平方根压缩；星名交互二选一登记取"悬停显示星名"（小热区 + ClampedHtmlLabel 悬停标签，含昴宿中文星名——Pleione/Celaeno 无昴宿定名用神话译名登记）；"地球天空视图"姿态纯函数 `pleiadesSkyViewRows`（北在上/东在左纯旋转 det=+1，主场景与预览页共用——自地球方向看亮星构型与公版图像可对照，构型截图登记）。C) 反射星云二选一登记取"分层 sprite"（volume 池容量 1 已被 M42/M57/马头/蟹状 L3 巡游站高频占用，昴星团插入将致连续巡游反复逐出重烘焙，性能优先登记）——围绕 Merope（NGC 1435）/Maia/Alcyone/Electra 四亮星各 3 层嵌套 sprite（共 12 张，内小而亮外大而淡 + 确定性抖动偏移），蓝色反射色调（尘埃散射星光 #c4d6ff–#5a78c8 档）区别于发射星云红（Hα）/青（OIII）；远景整体反射星云 sprite 现状保留。D) 细节层挂接（R4-2）——近观层经 `useDetailLayer({kind:'starCatalog'})` 挂载（starCatalog 池首个消费方；阈值与 R2-7 同源，release-on-exit）：基础星场（远景常驻）= 目录最亮 160 颗，近观增量 = 目录暗星 points +440 + 星芒 sprite ×9 + 反射星云 sprite ×12 = 461（`NEAR_VIEW_PARTICLE_INCREMENTS.pleiades` 327→461 登记，R2-7 的 +320 程序化增量粒子与"七姊妹"辉光 ×7 被真实星表替代登记；全局峰值预算内单测保持）；成员星 points 为自定义 shader（每星独立粒径距离衰减 + 软边圆点 + logdepthbuf include，Starfield 先例附录 A §5）。E) 降级路径——新增 `usePleiadesCatalog` hook（`bakedData.loadPleiades` 同源 fetch + 校验 + 模块级缓存），加载中/失败返回 null 时基础星场与近观层整体回落现状程序化分布（120 粒基础 + R2-7 320 粒近观 + 辉光 ×7，降级登记；产物临时改名实测回落不报错、恢复后正常）。F) 预览页注册 `?body=pleiades`（新组件 `PleiadesCatalogPreview` 与主场景共用 PleiadesCluster 三件套观感同源），滑杆 3 件：粒径增益/星芒尺寸/反射星云强度（帧读 getter 直达 uniform 无材质重建，R4-1 修复登记路径）。新增单测 `pleiadesCatalogR417`（32 例，pleiadesCatalog.ts 覆盖率 100%：换算/色温单调/星等映射域与单调/sRGB 逆变换/属性构建确定性与分级/实际产物命名星匹配 4+5 与吸附位置/天空视图正交归一 det=+1 与北上东左语义/反射星云布局嵌套与确定性/spec 阈值同源与 461 预算登记），全量 2508 用例/132 套件通过，覆盖率 gate ≥90% 保持（语句 98.52%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r417 系列登记）：预览页天空视图构型 + 星芒/反射星云滑杆响应 + 悬停星名标签（昴宿四 Maia · V 3.87）可辨，主场景 L3 巡游飞抵昴星团站近观拉近星芒亮星 + 蓝色反射星云包裹成立、绕行立体可辨，降级路径复验（改名产物 → 程序化回落零报错），马头↔昴星团↔M13 连续切换 ×3 强制 GC 后 JS 堆 49→50 MB 无泄漏、全程 60 FPS、控制台零错误
- 实现蟹状星云丝状结构 + PWN 环面/喷流（R4-16，IMPROVEMENT_REQUIREMENTS_4 §R4-16 / §0.3 方案 B）：L3 飞往/跟随蟹状星云脉冲星达近观阈值时，遗迹壳 billboard + R2-7 丝状云团平滑过渡为真体积丝网 raymarch，并叠加 Chandra 形态参考的 PWN 内核——近观呈现"外围红橙丝网 + 内部 OIII 青弥散 + 中心蓝白环面/双喷流"三层结构，绕行立体可辨。A) `utils/nebulaVolume.ts` 新增蟹状密度场——`makeCrabSampler(seed)`（NASA/ESA Hubble 公版图像丝状网络形态参考，程序化近似登记：非逐丝贴合照片）：外围丝状网络（§R4-16 省 token 约定取 12 条参数化曲线骨架 `crabFilamentPolylines`——单位球面切向随机游走长弧形成笼壳丝网形态，任意视角投影均呈网状长丝而非径向辐条投影团块；体素到最近骨架线段距离软衰减成密度脊 + 采样域经湍流值方向场扭曲 ±0.06 + 沿线束状调制亮结；骨架距离经包围球平方距离早退 + 扁平段数组零分配，128³ 烘焙实测墙钟 ~300ms 与 M42 同量级）+ 内部 OIII 青色弥散（椭球归一化半径平滑充盈，幅度 ≈丝峰值 1/6——丝状脊 ≥2.5× 弥散区单测锚定）+ 整体三轴椭球包络（fBm 轮廓扰动去正椭球感）；吸收通道恒零登记（蟹状无显著前景尘埃，单测断言）；双色权重 `crabColorWeight01` 按椭球归一化半径分层（内 OIII 青弥散 → 外 Hα 红橙丝，纯径向近似登记：真实丝网红/青逐丝交织按内外分区近似）；纹理 128³（§R4-16 指定，RG 双通道 4 MB）。B) PWN 内核（`CrabPwnCore` 近观组件）——赤道环面 shader 发射体（蓝白同步辐射色 #bfe0ff）：径向剖面 = 纯函数 `crabTorusIntensity` 的 GLSL 镜像（主环 0.62 + 内环 0.28 双环 Chandra 形态 + 中心弱辉 × 环面平面高斯增强——平面增强项由平面几何承载 height01=0、CPU 纯函数含高度衰减供验收断言登记）+ wisp 细纹缓慢外流；极向双喷流**复用** `ExtragalacticObjects.RelativisticJet` 锥体 shader（复用登记勿新造：组件导出 + 新增可选 `getWeight` 覆写注入 L3 近观权重、参数化缩小尺度，类星体/M87 既有消费零回退）——方向 ±y 自转轴与射束进动轴一致；尺度夸大登记：真实环面 ~0.5 ly ≪ 星云 ~11 ly，可视化按环面半径 0.30× 视觉半径（~6×）保近观可辨。C) `PulsarRemnant` 整合——射束扫描/脉冲节奏保留（`pulsarPulseIntensity`/`pulsarBeamAngle` 零改动）；挂接 `useDetailLayer({kind:'volume'})`（release-on-exit，volume 池容量 1 与 M42/M57/马头共池 LRU）+ 泛化 `NebulaVolumeLayer` 复用（内嵌中心脉冲星蓝白核 sprite）；交叉淡出：遗迹壳 billboard（`crabBaseLayerFactor`，vol01=0 时 = R2-7 近观减淡 45% 行为零回退）与 +16 丝状云团（`crabNearLayerFactor` = near01×(1−vol01)）随体积淡入 0.5s 交叉淡出、退出反向恢复；深度关系登记（§R4-16 第 3 条）：体积合成三角形维持 depthTest=false（R4-8 结论沿用），脉冲星本体/射束/环面/喷流绘制于体积合成之前按整盒透射率压暗（R4-7/R4-8 星点 sprite 先例登记），射束/脉冲闪烁按 `crabCoreBoostFactor`（×(1+0.6·vol01)）透射补偿保节奏观感不回退；包围盒边长 = 视觉尺寸 ×2.6（包络长半轴折算 ≈1.0× 视觉半径与遗迹壳 billboard 衔接登记；billboard 膨胀动画不随动体积——体积激活时已交叉淡出登记）。D) 预览页注册 `?body=crab-pulsar`——`NebulaVolumePreview` 配置注入复用，滑杆 6 件（步数默认 64/密度/双色权重/亮度/质量档/抖动；无尘埃滑杆——吸收通道恒零登记）；预览页仅体积层登记（PWN 环面/喷流为主场景近观组件，经主场景截图验收）。新增单测 `nebulaVolumeR416`（33 例：骨架条数 8–12 与域内与确定性/丝状脊 ≥2.5× 弥散区/内弥散为正/包络外归零/吸收恒零全域/环面平面增强——同径向平面内 > 离面 ×2 与主环 > 中心与内环隆起/双色权重锚点/确定性双次逐字节一致/spec 阈值同源与 128³ 预算/包围盒尺度/交叉淡出零回退边界与透射补偿/配置与 PWN 尺度常数/预览注册），全量 2478 用例/131 套件通过，覆盖率 gate ≥90% 保持（语句 98.55%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r416a/r416c 系列登记）：预览页 3 角度绕行丝网长弧缠绕 + 青弥散内域可辨（128³ 烘焙墙钟 ~300ms、最大单块 26.2ms <100ms 卡顿约束）；主场景 L3 巡游飞抵蟹状站近观 3 角度红橙丝网/青弥散/蓝白环面（双环 + wisp）/双喷流三层立体连续；脉冲节奏复验——连续快拍 24 帧中心亮度呈 ~1.2s 周期峰值（2.4s 自转周期 × 每转两次射束扫过，节奏不回退）；体积+环面+射束+Bloom 组合全程 60 FPS（≥55 达标，未触发降档）；蟹状↔M42 连续切换 ×3 volume 池 LRU 逐出正常（烘焙打点交替 蟹状 ~300ms/M42 ~490ms）、强制 GC 后 JS 堆 51→52 MB 无增长趋势（无泄漏）、控制台零错误
- 实现马头星云吸收体积（R4-15，IMPROVEMENT_REQUIREMENTS_4 §R4-15 / §0.3 方案 B）：L3 飞往/跟随马头星云达近观阈值时，剪影 billboard + R2-7 近观层平滑过渡为真体积吸收 raymarch——马头剪影随视角变化呈现真实体积轮廓（正面剪影清晰、侧向可见云柱在红色发射幕前的纵深，billboard 不可能呈现的核心升级点）。A) `utils/nebulaVolume.ts` 新增马头密度场——`makeHorseheadSampler(seed)`（NASA/ESA Hubble 公版图像轮廓形态参考，程序化近似登记：不逐像素贴合照片）：**吸收为主**的暗分子云柱——马头轮廓 = 5 个椭球 SDF 平滑并（颈柱/头部/吻部/鬃丘 + 底部暗云堤，B33 从 Lynds 1630 暗云堤伸出的形态近似；§R4-15 省 token 约定"3–4 个椭球"为补底部云堤取 5 个登记）+ 3 八度 fBm 边缘侵蚀（轮廓半径扰动 ±0.09 + 表层湍流阈值化碎散云缘，深处不受侵蚀）+ 盒边缘软窗防切边；马头轮廓内发射通道近零（冷分子云不发光，≤0.02 单测断言）；纹理 96³ 预算登记（RG 双通道 ≈1.69 MB）。B) 背景发射幕（§R4-15 第 2 条二选一登记，取"低密度大尺度发射层"）——IC 434 红色发射幕烘焙进体积后半域（z 向软窗剖面 + 横向软窗防盒边切边 + fBm 云絮起伏），剪影 = raymarch 内吸收柱按透射率**物理遮挡**背景幕（非贴图叠加）；主场景既有背景 billboard 保留并在体积激活时部分减淡（`horseheadCurtainFactor` ×(1−0.35·vol01)）作幕布远景延伸——体积盒外围无幕布断边；单色登记：IC 434 为 Hα 主导发射，材质双色权重经 weightBias=+1 恒取 colorHa 红棕档（#c9503a）。C) 主场景接入同 R4-8 模式——`DarkNebula` 挂接 `useDetailLayer({kind:'volume'})`（release-on-exit）+ 泛化 `NebulaVolumeLayer` 复用（分帧烘焙 22ms/帧预算实测 ~190ms/8 块/最大单块 24.6ms + 半分辨率 RT + 自适应质量，无内嵌星点登记：B33 为冷分子云无点源）；volume 池容量 1——M42/M57/马头巡游切换 LRU 逐出；包围盒边长 = 视觉尺寸 ×2.0（马头全高折算 ≈1.7× 视觉半径与剪影 billboard 组衔接登记）；交叉淡出（§R4-15 第 3 条登记）：R2-7 交付的 2 视差发射层 + 3 前景暗云团（`horseheadNearLayerFactor` = near01×(1−vol01)，vol01=0 行为零回退）与 3 块前景暗云柱剪影 billboard（volDim ×(1−vol01)，体积吸收柱接管剪影）随体积淡入 0.5s 交叉淡出、退出反向恢复。D) 预览页注册 `?body=horsehead`（§R4-15 指定短名，与天体 id `horsehead-nebula` 差异登记）——`NebulaVolumePreview` 配置注入复用，滑杆 6 件（步数默认 48/发射幕密度/尘埃吸收/亮度/质量档/抖动；无双色权重滑杆——Hα 单色登记）。新增单测 `nebulaVolumeR415`（26 例：轮廓 SDF 部件内负/外正与平滑连接/密度场关键点——轮廓内吸收 ≥0.5 且发射 ≤0.02、轮廓外吸收 ≤0.05、发射幕后方 ≥0.04 且前方近零、角落双通道零、[0,1] 值域/确定性双次逐字节一致/spec 阈值同源与 96³ 预算/包围盒尺度/交叉淡出零回退边界/配置无星点与 weightBias=+1/预览注册），全量 2445 用例/130 套件通过，覆盖率 gate ≥90% 保持（语句 98.58%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r415a/r415c 系列登记）：预览页正面马头剪影清晰可辨（颈柱/头部/吻部/鬃丘轮廓衬红色发射幕）、水平绕行 45°/侧视云柱纵深真实、俯仰回绕视差连续；主场景 L3 巡游飞抵马头站 billboard→体积交叉过渡位置连续、近观绕行 3 角度剪影/纵深/云柱立体可辨；M57↔马头连续切换 ×3（volume 池 LRU 逐出，每次返回重新烘焙打点一致）强制 GC 后 JS 堆 50→50 MB 无泄漏、全程 60 FPS（未触发降档）、控制台零错误
- 实现环状星云 M57 壳层体积模型（R4-14，IMPROVEMENT_REQUIREMENTS_4 §R4-14 / §0.3 方案 B）：L3 飞往/跟随环状星云达近观阈值时，环面 billboard + R2-7 环体粒子平滑过渡为真体积三轴椭球壳 raymarch——正视呈"环"、侧视呈"桶状/椭球壳"的视差变化真实可辨（billboard 不可能呈现的核心升级点）。A) `utils/nebulaVolume.ts` 新增 M57 密度场——`makeM57Sampler(seed)`（O'Dell et al. 2013, ApJ 780, 26 三轴椭球壳模型形状参考，程序化近似登记）：三轴椭球壳骨架（半轴 0.58/0.48/0.38 归一化域，a:b:c ≈ 1:0.83:0.66，极轴 +z 朝观察者——真实极轴倾斜 ~30° 由主场景环面姿态组承担登记）+ 赤道增密环/极向暗瓣（椭球坐标极角余弦平滑压暗至 0.22 档）+ 内腔近空（仅 0.05 弱 OIII 充盈）+ 外晕弱壳（壳中面 ×1.35、幅度 0.06，真实双层晕合并单层登记，晕外缘恰落归一化域边界标定）；壳层为解析式塑形（§R4-14 省 token 约定），3 八度 fBm 仅做扰动（壳半径 ±0.1 + 密度束状起伏 0.55–1）；吸收通道恒零登记（M57 无显著前景尘埃，单测断言）；双色权重 `m57ColorWeight01` 按椭球归一化半径 qLen 分层（壳内缘 OIII 青绿 → 外缘 Hα/NII 红橙，NII 与 Hα 合并单档登记）；纹理 96³ 预算登记（结构较简单，RG 双通道 ≈1.69 MB ≪128³ 的 4 MB）。B) `NebulaVolumeMaterial` 双色权重泛化——新增可选参数 core/weightInnerR/weightOuterR/weightInvRadii（椭球归一化逐轴倒数 uniform `uWeightInvRadii`），M42 默认 (1,1,1) 欧氏距离行为零回退（单测断言参数等价）；M57 以 (1/a,1/b,1/c) 令混色沿壳法向分层。C) 主场景接入同 R4-8 模式——`OrionVolumeLayer` 泛化为通用 `NebulaVolumeLayer`（M42/M57 共用同一接线：分帧烘焙 22ms/帧预算 + 半分辨率 RT + 自适应质量 + release-on-exit dispose，密度场/材质参数/内嵌星点经 `NebulaVolumeLayerConfig` 注入，`utils/nebulaVolumeScene.ts` 新增 `m57VolumeDetailLayerSpec`/`m57VolumeLayerConfig`/`orionVolumeLayerConfig`）；volume 池容量 1——M42↔M57 巡游切换 LRU 逐出（无双体积同挂）；M57 位姿参考 = 环壳缩放组（倾斜姿态 + 膨胀动画随动）、包围盒边长 = 视觉尺寸 ×2.9（壳中面折算 ≈0.84× 视觉半径与 R2-7 环径 0.85× 衔接登记）；交叉淡出（§R4-14 第 3 条登记）：环面 billboard/中心白矮星网格（`m57BillboardFactor`，vol01=0 恒 1 行为零回退）与 R2-7 交付的 +200 环向粒子/外晕壳（`m57NearLayerFactor`）随体积淡入 0.5s 交叉淡出、退出反向恢复；中心白矮星亮点复用 R4-6 色档——Teff≈125 kK 经 `blackbodyRGB` 表上限 50 kK 档偏蓝白 sprite 内嵌体积子场景（远紫外主导真实色感以表上限近似登记），星点 sprite 工具泛化支持色调注入（`TrapeziumSprites.buildStarSpriteTexture(tint)`/`addVolumeStarSprites`，M42 蓝白默认零回退）。D) 预览页注册 `?body=ring-nebula`——`OrionNebulaPreview` 泛化为 `NebulaVolumePreview`（M42/M57 共用，配置注入），滑杆 6 件（步数默认 48/密度/双色权重/亮度/质量档/抖动；无尘埃滑杆——吸收通道恒零登记）。新增单测 `nebulaVolumeR414`（30 例：双色权重锚点/密度场关键点——赤道环 ≥2.5× 极向、内腔 ≈0.05 充盈档、外晕弱正值、吸收恒零、早退路径、[0,1] 值域/确定性双次逐字节一致/spec 阈值同源与 96³ 预算/包围盒尺度/交叉淡出零回退边界/M42 泛化配置等价/白矮星色档/预览注册），R4-7 31 例 + R4-8 22 例零修改回归全绿，全量 2419 用例/129 套件通过，覆盖率 gate ≥90% 保持（语句 98.56%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r414 系列登记）：预览页正视"环"（暗腔 + 青绿内缘 + 红橙外缘 + 中心白矮星）→ 斜视 45° → 侧视"桶状壳"三角度视差变化真实可辨（96³ 烘焙墙钟 97ms、最大单块 23.1ms <100ms 卡顿约束）；主场景 L3 巡游飞抵环状星云近观绕行 3 角度同样可辨；M42↔M57 连续切换 ×3——烘焙打点交替出现（M42 ~490ms/M57 ~80ms，单挂载互斥、LRU 逐出正常）、强制 GC 后 JS 堆 49→50 MB 无增长趋势（无泄漏）、全程 60 FPS、控制台零错误
- 实现黑洞引力透镜 ③（R4-13，IMPROVEMENT_REQUIREMENTS_4 §R4-13 / §0.3 方案 C）：场景接入——L3 飞往/跟随人马座 A* / 天鹅座 X-1 达近观阈值时，廉价 shader（黑球 + 吸积盘 + 光环公告板）平滑交叉淡出为 R4-11/R4-12 透镜 raymarch（光子环 + 背景星光弯曲 + 吸积盘上下缘翻折像 + 近亮远暗束流不对称），Esc/离开跟随反向恢复、资源随卸载 dispose。A) 新建纯逻辑 `src/utils/blackHoleScene.ts`——两黑洞参数配置对象（§R4-13 第 2 条区分登记：Sgr A* 盘暗弱偏橙红——峰值色温压标 7200 K×0.64≈4,600 K + 亮度 0.55 + 暖橙光子环，实际银心吸积流为射电/亚毫米波段亮度、黑体色档为艺术化映射登记；天鹅座 X-1 盘亮偏蓝白——×1.36≈9,800 K + 亮度 1.15 + 冷蓝白环，真实盘内区 ~10⁷ K X 射线域不可光学表现承 R4-12 压标登记；伴星联动登记：现状 BlackHole 组件无伴星渲染，"如有则保留"条件不成立；质量差异经 visualRadiusLy 尺度体现——视界/光子环/盘世界尺寸 Sgr A* ≈2.3× Cyg X-1）；尺度登记（§R4-13 第 3 条）：透镜 r_s 世界长度 = 廉价 shader 黑球半径（视觉尺寸 ×0.32 单点同源，真实 r_s 角尺寸不可辨的可视化压缩登记），包围球（14 r_s）小于两黑洞飞抵观察距离（67.2<90 / 29.1<39 场景单位，相机恒在球外，单测断言）；背景弯曲采样二选一登记：取**程序化星场近似**（确定性 cubemap，非场景快照——快照需每黑洞 6 面离屏渲染主场景成本高且跟随中需重拍；差异：球内背景星点与球外真实场景背景不逐星对应，球缘弯曲趋零 + 随机星点下不连续性目验不可辨）；盘倾角两黑洞同取 69.23°（= 廉价盘 rotation.x=−π/2.6 的法线姿态，交叉淡出盘面对齐无跳变，矩阵元素纯函数 `blackHoleDiskRotElements` 与 R4-12 预览页同式、单测断言廉价盘法线映射至盘空间 +y）；`blackHoleLensingDetailLayerSpec`（lensing 池容量 1、进入/退出阈值 R2-7 同源、GPU 预算 = 星场 cubemap 384 KB + 黑体 LUT 256 B）。B) 新建 `src/components/Scene/BlackHoleLensedLayer.tsx` + `BlackHole` 组件挂接——`useDetailLayer({kind:'lensing'})`（release-on-exit）门控挂载透镜包围球，材质置 transparent + 新增 uFade uniform（= 门控 opacity01 × 层级权重，0.5s 交叉淡出）+ renderOrder=2 晚于廉价盘绘制，廉价盘/光环 uOpacity 同步 ×(1−门控) 反向淡出；透镜接管（门控 >0.5）后事件视界黑球停写颜色/深度（colorWrite/depthWrite 关闭——raymarch 阴影 b_crit≈2.6 r_s 大于黑球 1 r_s，保留黑球深度会切断盘前景翻折像；黑球 mesh 保留为点选 raycast 命中体登记）；shader 增量 uniform `uDiskTempScale`（两黑洞温标缩放，R4-12 常数/同式断言单测零修改保持）；自适应降级接入（§R4-13 第 4 条）：复用 R4-4 档位状态机，档位映射步数 64/48/32（基准 64×stepScale，clampLensingSteps 钳制、≤0.5s 平滑插值；透镜为不透明全分辨率 raymarch，RT 半分辨率通道不适用登记）。C) 门控判据修复登记（目验发现）：`useDetailLayer` 默认 focused 判据含 flyToBodyId（抵达后不清零）导致 Esc 退出跟随后透镜层无法释放——透镜层经 `getFocused` 注入改为仅按 followBodyId（requestFlyTo 同步置 followBodyId，运镜期门控不变；其余细节层默认判据零改动）。D) 信息面板 dataSource 登记两黑洞透镜近似/压缩/色档差异。新增单测 `blackHoleSceneR413`（24 例，blackHoleScene.ts 覆盖率 100%：常数登记/两黑洞配置区分与自洽校验/尺度换算与包围球-观察距离关系/spec 阈值同源与预算/盘姿态矩阵端点与廉价盘对齐与正交性/档位步数映射/shader 增量同源断言），全量 2389 用例/128 套件通过，覆盖率 gate ≥90% 保持（语句 98.58%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r413 系列登记）：L3 巡游飞抵两黑洞站廉价 → 透镜交叉过渡平滑（时序截图 1200/2500/3000/3600ms 对照现状登记升级幅度），Sgr A* 暖橙光子环 + 盘翻折 + 左右束流不对称可辨、天鹅座 X-1 蓝白明亮盘对照区分清晰，绕行 4 方向均 60 FPS（≥55 达标，未触发降档），进出 5 次 JS 堆恒稳 47 MB（出入零净增、无泄漏），Esc 退出 0.5s 恢复廉价远景、重入重建正常，L3 全序列回归其余站点无影响、控制台零错误
- 实现黑洞引力透镜 ②（R4-12，IMPROVEMENT_REQUIREMENTS_4 §R4-12 / §0.3 方案 C）：吸积盘物理——R4-11 raymarch（用户已目检确认光子环）内加入几何薄盘求交与相对论着色，预览页 `?body=blackhole-test` 呈现盘上下缘翻折像 + 近亮远暗束流不对称（EHT/Interstellar 观感），不接主场景（R4-13 范围）。A) `utils/blackHoleLensing.ts` 纯逻辑扩展（物理近似逐条登记于文件头）——温度剖面 `diskTemperatureFactor01`：T(r) ∝ r^(−3/4) × [1−√(r_in/r)]^(1/4)（Novikov-Thorne/Shakura-Sunyaev 薄盘近似 + 内缘应力零边界截断，§R4-12；峰值恰落 r = (49/36)·r_in，归一化系数 NORM = (49/36)^(3/4)·7^(1/4) 闭式求得，单测断言峰位/单调性/r^(−3/4) 渐近）；峰值温度 7,200 K 为可视化压标档（真实恒星级黑洞盘内区 ~10⁷ K X 射线域，光学黑体色板不可表现，登记）；开普勒速度 `diskKeplerianBeta`：β = √[(r_s/2)/(r−r_s)]（Schwarzschild 圆轨道静止观察者局域速度精确式，ISCO 3 r_s 处恰 0.5c 单测锚定，上限钳 0.72）；多普勒束流 `dopplerFactor`（δ = √(1−β²)/(1−β·cosθ)）+ `diskBeamedBrightness`（亮度 ∝ δ_eff³ × g³，δ³ 为 §R4-12 指定近似——频率积分真值 δ⁴ 登记；δ_eff = δ^束流强度，滑杆 0 关闭/1 物理档/2 夸大，引力项不随滑杆关闭）；引力红移 `gravitationalRedshiftFactor`：g = √(1−r_s/r)（静止发射体→无穷远近似登记，下限钳 0.05）；观测色温 `diskObservedTemperatureK` = T × δ_eff × g（近侧蓝移升温/远侧红移降温）；黑体着色复用 R4-6 `starPhysics.blackbodyRGB`——`buildBlackbodyLutData` 预采样 [1,000, 16,000] K → 64 texel RGBA LUT（确定性双次逐字节一致 + 逐 texel 与 blackbodyRGB 同式单测断言；<3,000 K 段承 blackbodyRGB 域钳制登记）；辅助纯函数 `planeCrossingLerp`（盘面跨越线性插值，shader 同式）与 `clampDiskRadii`（内外缘滑杆防交叉：最小间隔 1 r_s、外缘上限 = 包围球半径 −1、内缘下限 1.5）。B) `BlackHoleLensed.tsx` shader 增量——弯折步进循环内薄盘平面跨越检测（前后两步盘空间 y 异号 → 线性插值求交，**非解析求交**；弯曲光线可多次交盘 = 上下缘翻折像来源），盘空间经 mat3 uniform `uDiskRot`（物体→盘空间旋转，mesh 不旋转保 cubemap 采样约定）；命中处按上述同式公式着色（常数经模板插值单点同源，单测逐条断言），前向透射合成：盘近不透明（每次交盘透射率 ×(1−a·0.92)，a 含内外缘 smoothstep 软化与条纹微透明），交盘后背景星场/阴影按剩余透射率显示、光子环发光改乘透射率（盘后环光被遮蔽，盘前不受影响）；差速旋转流动条纹：ω ∝ r^(−3/2)（开普勒律形状、速率为可视化节奏登记）驱动值噪声两八度（cos/sin 嵌入消 φ 接缝；径向频率高/角向低呈细密环状纹理），uTime 推进流动；黑体 LUT 恒由工厂自建并登记 userData 随 dispose 一并释放；盘亮度基准 0.85 目验调参登记（2.6 档实测全盘 tonemap 洗白失去黑体色阶与束流对比）。C) 预览页滑杆 3→7 件（≤8 上限）：§R4-12 新增盘倾角（0°=正视/90°=侧视，仅值变化时重算旋转矩阵复用 Matrix3 实例）/盘内缘（2–6 r_s 默认 3 = ISCO）/盘外缘（6–13 r_s 默认 12）/束流强度（0–2 默认 1），内外缘经 clampDiskRadii 钳制且仅值变化时重求值（渲染循环零分配）；dataSource 补 NT 薄盘/δ³/引力红移/峰值温度压标登记。新增单测 `blackHoleLensingR412`（36 例：常数登记/温度剖面峰位与单调与渐近/ISCO β=0.5 精确锚定/多普勒近亮远暗与横向二阶红移/引力红移关键值与单调/束流组合近远比 = (δ⁺/δ⁻)³ 与 strength=0 只剩 g³/跨越插值/内外缘钳制/LUT 确定性与 blackbodyRGB 逐点一致与色板方向/shader 同式系数与公式片段与 uniforms 断言），R4-11 33 例零修改回归全绿，全量 2365 用例/127 套件通过，覆盖率 gate ≥90% 保持（语句 98.58%，blackHoleLensing.ts 99.6%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r412 系列登记）：正视 5°/斜视 60°/侧视 85° 三角度——侧视呈经典翻折像（远侧盘面折上阴影上方成拱 + 下缘二次像）、斜视近侧（右）亮远侧（左）不对称清晰、正视细密环状条纹与 ISCO 暗隙可辨；束流 A/B（0/1/2）不对称随滑杆增强、内外缘滑杆（2–13/5–8）实时生效、条纹 2.5s 两帧流动可辨、质量尺度 2×+距离 6 推近无 NaN 黑块/白闪；全部视角/操作 60 FPS（占比满足 ≤1/2 验收线）、JS 堆 21–24 MB、控制台零错误。**人工目检检查点：请确认盘上下缘翻折观感后方可启动 R4-13**
- 实现黑洞引力透镜 ①（R4-11，IMPROVEMENT_REQUIREMENTS_4 §R4-11 / §0.3 方案 C）：raymarch 原型（光子环 + 背景弯曲），仅预览页 `?body=blackhole-test` 可见（人工目检检查点：确认光子环观感后方可启动 R4-12），不接主场景（R4-13 范围）。A) 新建纯逻辑 `src/utils/blackHoleLensing.ts`（r_s = 1 单位制，shader 常数单点同源）——光线弯曲取"弱场积分核空间分布 + 二阶闭式总量预算"（**不做全数值测地线积分**，登记）：总偏转角 α(b) = 2·r_s/b + (15π/16)·(r_s/b)²（一阶即弱场式 4GM/(c²b)，二阶系数 Keeton & Petters 2005 即近光子球增强项；适用域 b ≳ 2 r_s，b→b_crit 真解对数发散域以近似有限值 + 驻留发光高斯核补足，艺术化登记），沿视线逐步弯折用弱场核 dα/ds = r_s·b/r³ 且以 α(b) 为硬预算——出射方向与二阶闭式严格一致（单测断言），并避免沿弯折路径评估增强项的正反馈过弯（实测登记：含增强项分布核会把撞击边界推至 b≈3.9 r_s 远超真捕获截面）；撞击/阴影双判据：r ≤ 1.05 r_s 撞击终止为黑（§R4-11 指定阈值）+ b < b_crit = 3√3/2·r_s ≈ 2.598 且起步方向朝向中心判为阴影（Schwarzschild 捕获截面精确解析结果 MTW §25.6；方向判据防近距观察者视野整片误黑，目验发现修复登记）；光子环发光 = 撞击参数选通核（b ≈ 2.6 r_s 高斯，§R4-11 指定位置）× 光子球邻域驻留核（r = 1.5 r_s 高斯）沿程积累，CPU 数值验证环发光峰值恰落 b=2.6（两侧 ≥2× 衰减）；CPU 参考追踪 `traceLensedRay` 与 shader 逐步同式（撞击参数入口一次性求得守恒、单步偏转钳 0.35 rad、方向重归一化、出包围球提前终止）；程序化星场 cubemap 面数据 `buildStarfieldFaceData`（128px/面 RGBA，FNV-1a 确定性种子，高斯点扩散星点 + O/B 蓝→M 红色板与 Starfield 同源观感，勿引入贴图资产；面间接缝随机星点下不可辨登记）。B) 新建 `src/components/Scene/volumetric/BlackHoleLensed.tsx`——包围球（半径 14 r_s，物体空间即 r_s 单位、世界尺寸经 mesh.scale）raymarch ShaderMaterial 工厂 + 组件：常数经模板插值与纯逻辑模块单点同源（单测断言系数一致）、log depth 兼容（logdepthbuf include，Starfield 先例）、64 步默认/循环编译期上界 128、不透明输出 + 亮度硬钳 8.0 防 Bloom 溢出、BackSide 相机推入球内连续、出射方向经 varying 物体→世界旋转采样 cubemap（fragment 无内建 modelMatrix，目验发现修复登记）；包围球轮廓处切向短弦弯曲趋零→与 scene.background 直射星场连续无接缝；材质/工厂自建 cubemap 卸载即 dispose。C) 预览页注册 `?body=blackhole-test`——滑杆三件（§R4-11 指定）：质量尺度（rsWorld = 0.5×massScale）/相机距离（值变化时相机径向重置，不干预 OrbitControls 拖拽）/步数（16–128）；星场 cubemap 同时设为 scene.background（卸载恢复黑底）。新增单测 `blackHoleLensingR411`（33 例：常数登记/弱场与二阶闭式/撞击与阴影判定边界/环权重核峰位/CPU 样例光线 5 条（弱场一致/中场一致/近临界环发光/深入射撞击/近距外行光线不误黑）/域截断偏转不超闭式/全 b 扫掠无 NaN/确定性/shader 系数同源断言/星场面数据确定性与格式），全量 2329 用例/126 套件通过，覆盖率 gate ≥90% 保持（语句 98.56%，blackHoleLensing.ts 99.42%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r411 系列登记）：4 距离档（40/20/8/4）光子环清晰成形、背景星弧状拖曳可辨、绕行两段拖拽连续无闪烁，质量尺度 4× 推入至 ~2 r_s 近光子球无 NaN 黑块/白闪（2 r_s 处全视野入影为物理正确：真解阴影半张角 ~67° 大于半视场 53°，3 r_s 档巨影 + 暖色边缘辉光可辨），全部距离档与 128 步档 60 FPS，控制台零错误
- 实现星系近观多分量 ②（R4-10，IMPROVEMENT_REQUIREMENTS_4 §R4-10）：R4-9 新分量渲染接入 + M31 专属细节——L4 飞往/跟随星系近观时呈现旋臂间尘埃暗纹、粉色 HII 区点缀、蓝白年轻星团串与色调分层的多分量粒子结构，M31 以真实倾角 77° 姿态 + 10 kpc 尘埃环 + 偏黄核球呈现。A) 渲染接入（`GalaxyNearView.tsx` 消费 `generateGalaxyNearViewComposite`）——软圆点 shader 管线复用改三层 Points：基础层（加性混合，R2-8 现状参数不变，颜色升级为老年盘底色梯度）；HII + 年轻星团合入第二加性 Points（不同 size/color 通道：HII 大颗粒点径上限 6→16px 呈发射团辉斑、星团小颗粒串沿脊线）；尘埃带第三 Points 用 normal 混合暗色 + renderOrder=2 置于加性星光层之后（方案登记：加性混合无法画暗，对先绘制亮层普通混合变暗实现"吸光"暗纹，Galaxy.tsx 银河系尘埃带同款先例）；层间差异经 uniform 注入（uMaxSize/uAlphaBase/uAlphaScale），shader 源共享；几何仍只建一次、渲染循环仅写 uOpacity，卸载 dispose 全部几何/材质。B) M31 专属（差异登记）——近观朝向统一入口 `galaxyNearViewOrientation`：M31 由 id 哈希改为真实倾角 77° + PA 38° 登记值构造（新纯函数 `inclinedOrientationRad`：视线 = 原点→M31 方向，法线-视线夹角 = 倾角，欧拉角提取与 three.js 'XYZ' 同约定，单测复核夹角），其余 8 星系沿用哈希公式零变化；10 kpc 尘埃环 `M31_DUST_RING`（半径 32,600 光年，dust 配额 45% 划归环粒子：方位均匀 + 径向高斯 ±3σ，环宽/占比为观感示意档登记；`generateDustLaneParticles` 新增可选 ring 参数，缺省时与 R4-9 行为逐字节一致零回退单测锚定）；核球偏黄 `applyBulgeTint`（核球内 3D 半径线性衰减混合暖黄 #ffd18c 档，纯函数副本语义，仅 M31 套用）。C) billboard ↔ 粒子层过渡回归（观感登记）——远观 billboard 恒面向相机，M31 新姿态差异在 0.5s 交叉淡入中呈现、位置零跳变（属预期观感，目验无异常）。D) `galaxyDetailLayerSpec` GPU 估算兑现 R4-9 登记：按多分量配额合计计（M31 = 9,850 粒；HII 泊松盘实际接受数 ≤ 配额取上界登记；detailLayerR42 断言等价迁移）。E) 预览页注册 `?body=m31` + 不规则对照 `?body=lmc`（新组件 `GalaxyNearViewPreview` 复用主场景层组件 + 缩放组适配预览尺度）：滑杆三件 dust 强度/HII 密度（[0,1] 域 `GalaxyCompositeOverrides` 重新生成分量，越界抛 RangeError，域内任意组合总量 ≤12,000 单测断言；LMC 新分量配额恒 0 属 R4-9 登记，滑杆标注）/倾角覆写（0–90°，预览视线 +z 重构姿态）。F) 信息面板——旋涡/棒旋"结构"行扩展尘埃带暗纹与 HII/年轻星团描述、椭圆标注无 dust/HII；`GALAXY_STRUCTURE_SOURCE_ZH` 追加 RC3（de Vaucouleurs et al. 1991）/S4G（Sheth et al. 2010）形态参数来源。新增单测 `galaxyNearViewR410`（29 例：倾角姿态构造正交性与万向锁分支/M31 姿态夹角 77° 与其余星系哈希零变化/尘埃环带内聚集与无环零回退与确定性/核球偏黄副本语义与混合域/覆写配额缩放与校验/spec 迁移/面板扩展/预览注册）+ `galaxyNearViewR28` 29 例零修改回归全绿 + detailLayerR42 spec 断言等价迁移，全量 2296 用例/125 套件通过，覆盖率 gate ≥90% 保持（语句 98.53%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r410 系列登记）：飞往 M31 近观倾斜盘面姿态/旋臂间尘埃暗纹/粉色 HII 点缀/偏黄核球可辨（M33 站 HII 密度 0.9 粉色发射团最为醒目），绕行 4 方向 + 推近立体连续（对照 r28-L4-05 升级幅度：单色调粒子 → 多分量色彩分层 + 暗纹 + M31 真实姿态）；L4 全序列 8 站巡游逐站截图形态与类型一致（M87/人马座矮椭球云无 dust/HII；M32/M110 不在巡游序列属 R2-5 登记，类型一致性由单测断言覆盖）、全程 60 FPS、站间堆 44–48 MB 平稳；m31↔m33 LRU 进出 5 次（容量 1 互相挤出）强制 GC 后堆 46–47 MB 无增长趋势；预览页 m31/lmc 滑杆实时生效（倾角覆写姿态变化、dust 0.8→0 暗纹消失可辨）60 FPS
- 实现星系近观多分量 ①（R4-9，IMPROVEMENT_REQUIREMENTS_4 §R4-9）：R2-8 星系近观粒子生成器（`src/utils/galaxyNearView.ts`）纯逻辑扩展——为 R4-10 渲染接入准备尘埃带/HII 区/年轻星团/老年盘底色四类新分量，本阶段渲染零改动（`GalaxyNearView.tsx` 不消费新导出，主场景行为零变化，回归确认；纯逻辑无需无头 Chrome 目验，登记）。A) 新分量生成器（确定性纯函数，FNV-1a 分量派生种子 `galaxyNearViewSeed('<id>:<component>')`，坐标约定与基础层一致）——尘埃带 `generateDustLaneParticles`（旋臂内缘暗吸收粒子：相位 = 对数螺旋脊线 + 0.6×臂宽内缘偏移 + 0.35×臂宽窄散布，深棕全通道 <0.3，输出 `component:'dust'` 标记供 R4-10 normal 混合暗纹渲染）；HII 区 `generateHiiRegionParticles`（旋臂离散粉红发射团，大颗粒少量 3.0–4.5：确定性 dart-throwing 泊松盘采样防重叠，最小间距 = 盘半径×0.055，尝试上限 60×配额）；年轻星团 `generateYoungClusterParticles`（旋臂脊线蓝白小颗粒串：散布 0.15×臂宽 ±3σ 截断、b ≥ r 通道）；老年盘底色 `oldDiskColorAtRadius`（内红黄 #ffce8a → 外偏蓝 #aabfff 线性参数化，红通道单调不增/蓝通道单调不减）+ `applyOldDiskColorGradient`（旋涡基础层盘区粒子向梯度色混合 0.35，纯函数不改入参）；组合入口 `generateGalaxyNearViewComposite`（基础层 + 新分量，双次生成逐字节一致，总量防御性断言）。B) 9 星系形态参数表 `GALAXY_MORPHOLOGY_PARAMS`（§0.4 数据源：RC3/S4G/NED 近似档逐星系登记）——{倾角/臂数/螺距角/B/D 比/尘埃带强度/HII 密度}：M31 真实倾角 77°（R4-10 姿态消费）、椭圆/矮椭圆（M32/M110/人马座矮/M87）dust/HII 为 0 且 B/D 无盘登记 Infinity（M110 少量尘埃云差异登记）、不规则（LMC/SMC）dust/HII 非零但新分量配额为 0（HII 粉与蓝白年轻星已由 R2-8 团块分量承载，登记）、银河系近观复用既有 Galaxy.tsx 渲染（参数仅登记，无近观配置）；螺距角为登记值不驱动几何——新分量沿基础层 spiralTightness 对数螺旋对齐旋臂（差异登记）。C) 粒子预算——单星系总量上限 `GALAXY_NEAR_VIEW_MAX_PARTICLES` 8,000 → 12,000（上调登记，附录 A 单目标 ≤12,000 内；LRU 容量 1 不变 → 全局峰值增量 +4,000），基础层上限 8,000 不变（新常量 `GALAXY_NEAR_VIEW_BASE_MAX_PARTICLES`，R2-8 预算断言等价迁移登记）；分量配额纯函数 `galaxyComponentQuota`（旋涡按 dustStrength/hiiDensity 线性缩放：dust 1600×、HII 140×、年轻星团 1000×；峰值 M31 合计 9,850 ≤12,000 单测断言）；`galaxyDetailLayerSpec` GPU 估算仍按基础层粒子计（随 R4-10 渲染接入一并更新，登记）。新增单测 `galaxyNearViewR49`（34 例：9 星系参数表逐一与形态类型一致/配额与总量预算/尘埃带颜色与内缘对齐与分布范围/HII 泊松盘最小间距与配额/年轻星团脊线残差与蓝白色/色梯度单调性与钳制/组合确定性/非旋涡分量为空且 base 与 R2-8 逐字节一致）+ 现有 `galaxyNearViewR28` 29 例回归全绿，全量 2267 用例/124 套件通过，覆盖率 gate ≥90% 保持（语句 98.54%），type-check/lint/build 全绿
- 实现猎户座星云 M42 体积化 ②（R4-8，IMPROVEMENT_REQUIREMENTS_4 §R4-8）：R4-7 体积密度场（用户已目检确认）主场景接入 + 色彩调参——L3 飞往/跟随猎户座星云达近观阈值时，billboard + R2-7 团絮层平滑过渡为真体积 raymarch 星云（绕行/穿越可辨电离腔、Trapezium 星点、东南尘埃湾，无薄片跳变）。A) 新建纯逻辑 `src/utils/nebulaVolumeScene.ts`——`orionVolumeDetailLayerSpec()`（detailLayer volume 池规格：进入/退出阈值与 R2-7 近观层同源同值——两层同时机激活、交叉淡出无空档；GPU 预算 128³ RG 双通道 = 4 MB ≤64 MB 总预算，volume 池容量 1、后续 R4-14/15/16 同池 LRU 逐出）、`orionVolumeBoxEdgeUnits`（包围盒边长 = visualRadiusLy 场景尺寸 × 2.6，发射包络折算 ≈1.0× 视觉半径与 billboard/PuffCloud 尺度衔接）、交叉淡出权重三件（`orionBaseLayerFactor`：billboard 在 R2-7 近观减淡基础上 × (1−vol01)，vol01=0 时与现状逐点一致行为零回退；`orionPuffFactor`：团絮 near01×(1−vol01)；`orionVolumeFadeTarget`：烘焙未就绪时淡入目标恒 0——首进期间 billboard 保持原样，实现差异登记：detailLayer 0.5s 门控淡入之上叠加构建就绪二次平滑，烘焙晚于门控就绪时有效过渡 0.5–1s）。B) 新建 `src/components/Scene/OrionVolumeLayer.tsx`——`EmissionNebula` 经 `useDetailLayer({kind:'volume'})`（R4-2，release-on-exit）门控挂载：分帧烘焙（22ms/帧预算，主场景实测墙钟 480–506ms、20 块、最大单块 26.2ms <100ms 卡顿约束，console.info 打点）→ 半分辨率 RT + 全屏三角形合成（R4-4 路径复用，合成材质新增 uOpacity 淡入淡出 uniform——预乘 alpha 链路颜色/alpha 同乘，淡出为变透明而非变黑；合成三角形 raycast 置空不拦截点击）；自适应质量状态机主场景生效（high 64 步/full → mid 48 步/half → low 32 步/half）；位姿对齐：体积容器逐帧复制星云组世界矩阵（useGalacticPlacement 银河系组变换 + sun-relative 偏移），远近景过渡无位置跳变；Trapezium 四亮星 sprite 内嵌体积子场景（新共享模块 `volumetric/TrapeziumSprites.ts`，R4-7 预览页重构复用；主场景原 youngStars sprite 经 volDim 标记交叉淡出移交——位置与空腔对齐）；退出反向恢复，卸载即 dispose（纹理/RT/材质/几何 + fadeRef 复位）。深度合成差异登记（R4-4 遗留归本阶段）：合成三角形维持 depthTest=false——M42 体积仅近观跟随时激活、视野内无前景实体穿插，实测无遮挡穿帮，避免深度附件额外带宽。C) 色彩调参（附录 A §4）——主场景默认自然色近似：Hα 红棕 #cc5a3c + OIII 青灰 #8fb3a8（相对预览页窄带饱和默认降饱和，接近真彩观感），亮度 1.3→1.15 随主场景 Bloom 联调（目验核心不过曝、外缘不糊黑）；与"哈勃调色板"（SII/Hα/OIII→RGB 假彩色）映射差异登记入信息面板 dataSource。D) 信息面板 M42 卡片补"结构"行（电离腔/四边形星团空腔/东南前景尘埃湾）+ dataSource 追加 Hubble 公版图像形态参考与色彩差异登记。新增单测 `nebulaVolumeSceneR48`（22 例，nebulaVolumeScene.ts 覆盖率 100%：阈值同源/预算字节/池容量/盒尺度线性与校验/交叉淡出零回退边界与单调性/构建就绪门控/默认参数域），全量 2233 用例/123 套件通过，覆盖率 gate ≥90% 保持（语句 98.56%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r48/r48c/r48d 系列登记）：L3 巡游飞抵猎户座站 billboard→体积 0.5s 交叉过渡位置连续（t=1.8s billboard 平面淡出中 → t=2.7s 体积接管，对照 R2-7 sprite 团絮升级为可绕行真体积——尘埃湾暗斑/青灰内腔/星点随视角连续视差）、6 方向绕行 + 渐进穿越星云无薄片或跳变观感、绕行/穿越/全程 60 FPS（未触发降档，≥55 达标）；连续进出 5 次（→环状星云→返回）JS 堆强制 GC 后采样 out 45–47 MB / in 50–51 MB 无增长趋势（体积纹理随卸载 dispose 登记）、每次返回重新烘焙 ~490ms 打点一致；L3 全序列巡游整圈（14 站 + 循环闭合）回归逐站截图无其他站点观感/性能回退（60 FPS、末 10 站堆净差 0 MB）、控制台零错误
- 实现猎户座星云 M42 体积化 ①（R4-7，IMPROVEMENT_REQUIREMENTS_4 §R4-7 / §0.3 方案 B）：M42 双通道密度场构建 + 预览页验证，人工目检检查点（确认形态后方可启动 R4-8 场景接入）。A) 新建纯逻辑 `src/utils/nebulaVolume.ts`——`makeM42Sampler(seed)` 组合 R4-3 塑形基元（椭球/球/壳层 SDF + 软衰减 + 平滑并 + `valueNoise3D`）生成 M42 双通道密度场：主体不对称扇贝状发射腔（椭球包络 − 前向炮膛碗腔开口朝观察侧 +z，碗壁经湍流扰动成扇贝缘）、Trapezium 空腔（乘法软挖孔，星点处密度近零）+ 电离前沿增密壳（空腔外侧 1.8× 增密带）、西北亮弓（碗壁增密壳 × 西北象限角向加权 + 全域角向增益，西北发射柱积分 ≥1.25× 东南镜像柱单测锚定）、3 八度 fBm 湍流（轮廓扰动去正椭球感 + 表层阈值化侵蚀出碎散云缘）+ 丝状密度脊（ridged 噪声，采样域经湍流值方向场扭曲）；双通道输出 R=发射密度 / G=吸收密度（东南前景尘埃湾：双椭球平滑并 z 向压薄 + 湍流束状调制——薄板设计登记：避免高消光柱在云体内投出过长阴影隧道，目验调参）；Hα/OIII 混色权重不烘焙、由 `m42ColorWeight01(r)` 随到 Trapezium 距离径向计算（内区 OIII 青 → 外区 Hα 红，纯径向近似登记）；形态登记：NASA/ESA Hubble 公版图像仅作形态参考（§0.4），复现四特征相对方位与量级、云体细节程序化生成与真实天区方位存在艺术化差异；发射总量 ×0.32 标定（防 8-bit 钳制饱和抹平亮弓角向梯度）。B) 分帧烘焙（实现方式登记：取"分帧"而非 Worker——Jest/Next 集成简单且已满足卡顿约束）——`createRgVolumeBuild`/`advanceRgVolumeBuild` 以 z 切片为粒度按每帧 22ms 预算推进 128³ RG 纹理构建，数据与分块方式无关（任意预算逐字节一致单测断言）、打点字段（chunkCount/maxChunkMs/computeMs）随状态累计；实测（无头 Chrome Metal）墙钟 513ms（<1s）、计算 473ms、20 块、最大单块 26.2ms（<100ms 卡顿约束），console.info 打点登记；FNV-1a 种子确定性（同种子双次逐字节一致）；性能登记：每体素 ≤4 次噪声调用、包络外早退（本机基准 57ns/噪声调用）。C) 新建 `NebulaVolumeMaterial.ts`——RG 双通道 raymarch 变体（尘埃只消光不发射：T' = T·exp(−(e·σe + a·σd)Δt)），离散格式与 CPU 参考实现 `integrateEmissionAbsorptionDual` 同式（吸收通道全零时与 R4-3 单通道参考一致 + 恒定密度解析解 `constantDualEmissionAnalytic` 收敛校验，单测断言）；管线兼容全部沿用 VolumeMaterial 先例（log depth/盒内外入射/预乘 alpha/蓝噪声抖动/输出硬钳）。D) 预览页注册 `?body=orion-nebula`——半分辨率 RT + 自适应质量（R4-4 路径复用）、Trapezium 四亮星 sprite 内嵌体积子场景（位置 `trapeziumStarBoxPositions()` 与空腔一致，程序化双高斯 glow 纹理；近似登记：体积按全程透射率压暗星点、未按星点深度截断积分，空腔内密度低偏差可忽略）、滑杆按 §R4-7 指定三件（密度倍率/双色权重/步数）+ 尘埃吸收/亮度/质量档/抖动共 7 个（≤8 上限）、HUD 构建进度 + 质量档行；本阶段不接主场景（R4-8 范围）。新增单测 `nebulaVolumeR47`（31 例：确定性/关键采样点密度断言——腔内 0 与壳层 ≥0.25 且 ≥10× 腔内/暗湾吸收 >0.5 与西北镜像近零/亮弓不对称/星点在腔内/分帧预算语义与逐字节一致/积分一致性与解析收敛/纹理参数/输入校验，nebulaVolume.ts 覆盖率 99.5%）+ devPreview 单测 5 例，全量 2211 用例/122 套件通过，覆盖率 gate ≥90% 保持（语句 98.56%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r47-01～18 登记）：8 方向绕行 + 俯仰 + 推近——扇贝开口/暗湾/内腔三向可辨、视差真实无 billboard 感（暗湾随视角绕行跨越腔前）、双色权重滑杆 ±0.6 全域 OIII 青/Hα 红实时切换、尘埃吸收 0↔2.5 暗湾消隐/加深、强制低档 HUD 与 RT 50% 一致，全程 60 FPS（未触发自适应降档）、JS 堆 29–31 MB、控制台零错误
- 实现恒星表面物理化增强（R4-6，IMPROVEMENT_REQUIREMENTS_4 §R4-6 / §0.3 方案 A）：`StellarSurface`（P6 交付的 fBm 对流 shader 球）增量物理化改造——黑体色温基色 + 光谱型临边昏暗 + 时变对流 + 自转流动，覆盖 L3 全部 6 类恒星（参宿四/参宿七/天狼星 A/B/造父一/WR 124）。A) 新建纯逻辑 `src/utils/starPhysics.ts`——`blackbodyRGB(teffK)`：Planck→CIE→sRGB 预计算查表插值（25 采样点，3,000–50,000 K 域外钳制，表取 Mitchell Charity CIE 10°/sRGB 黑体色数据，登记：不做运行时完整 CIE 管线；关键点单测断言 3,500 K 橙红 / 5,800 K 白黄 / 9,900 K 蓝白 / 25,000 K 蓝 + 蓝红比随温度单调）；`limbDarkeningU(spectralType)`：M/K/G/F/A/B/O + 白矮星 D 档线性临边昏暗系数（Claret 2000 V 波段近似档登记，M 0.85 → O 0.30、WD 0.25 最弱档，Wolf-Rayet W 型归 O 档高温近似、未识别型回落 G 档均登记）；`granulationCellScale(radiusRsun)`：恒星半径 → 对流颗粒噪声频率（巨星颗粒大而少/矮星细密，近似关系 clamp(12−3.4·log10(R/R☉), 2, 12) 登记，锚点参宿四 764 R☉→2.2 与 P6 现状一致，单调性单测）。B) `StellarSurface` shader 增量改造——uniform 注入黑体基色（替换硬编码颜色，sRGB→线性工作色彩空间）、u 系数临边昏暗、噪声频率按 granulationCellScale 调制、对流时变（fBm 噪声域随 uTime 漂移，首层视觉周期 ≈20 s 落在需求 20–60 s 区间登记）+ 自转流动（uSpin 绕 y 轴旋转采样域；各恒星无既有自转参数，默认 0.02 rad/s 缓慢流动为可视化选择登记）；补齐 log depth buffer 兼容（附录 A §5 P6 遗留，logdepthbuf include 三件，Starfield 先例）。C) 参数接入——新增 `useStarParams` hook 经 `bakedData.loadStarParams` 读取 `public/data/star-params.json`（R4-5 烘焙产物），失败降级 `FALLBACK_STAR_PARAMS` 硬编码表（登记；降级表与烘焙产物逐字段一致由单测断言同步防漂移）；消费组件 RedGiant/BlueGiant/WolfRayet/Cepheid 全部由物理参数驱动，天狼星双星 A/B 由 meshBasicMaterial 纯色球升级为 StellarSurface（A 星 9,940 K 蓝白 A 档；B 星白矮星单独档 25,200 K 蓝 + u 最弱 + 颗粒频率上限，与 R2-7 既有 ~25,000 K 调蓝一致化）。D) 预览页注册全部 6 类恒星（betelgeuse/rigel/sirius/sirius-b/delta-cephei/wr-124），滑杆按 §R4-6 指定三件：Teff 覆写（黑体基色实时重算，仅值变化时求值零逐帧分配）/噪声频率/时间流速；切换恒星强制重挂载（材质引用缓存与虚拟时钟重置）。登记：太阳（Sun.tsx 独立管线）不在本阶段范围。新增单测 `starPhysics`（26 例，starPhysics.ts 覆盖率 100%）+ devPreview 单测更新（6 星条目/滑杆三件/配置映射/默认值同源断言），全量 2175 用例/121 套件通过，覆盖率 gate ≥90% 保持（语句 98.52%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r46-* 登记）：预览页逐星截图 6 张颜色与光谱型一致（参宿四橙红强临边昏暗巨对流胞/参宿七蓝白细颗粒/天狼星 A 蓝白/天狼星 B 蓝近平盘弱昏暗/造父一白黄/WR 124 深蓝）、对流时变可辨（4× 流速虚拟时钟 6.9 s vs 31.4 s 两帧对流图案明显重分布）、Teff 覆写 3,600→25,000 K 实时变蓝、巨星/矮星临边观感区分可辨；L3 巡游 5 个恒星站回归逐站 60 FPS、远景无突兀变化、无过曝、JS 堆 21–28 MB 稳定、控制台零错误
- 实现离线数据烘焙管线（R4-5，IMPROVEMENT_REQUIREMENTS_4 §R4-5）：`scripts/bake-data/` + `npm run bake:data` 构建期烘焙真实天文数据到 `public/data/`（产物随仓库提交，运行时零外部网络请求），为真实星表昴星团（R4-17）/恒星物理参数（R4-6/R4-18）/M13 King profile（R4-19）提供数据基座。A) 数据获取策略（双路径）——默认从内嵌 Gaia DR3 查询快照（`scripts/bake-data/snapshots/`，ADQL 语句与选星判据登记于 meta 文件）离线烘焙，`npm run bake:data -- --fetch` 可选联网重拉快照（Gaia TAP sync 接口），网络不可用时默认路径不受影响。B) 首批产物三件——`pleiades.json`（昴星团成员星 600 颗：Gaia DR3 锥形检索 2.5° + 视差 7.0–7.7 mas + 自行共动 |Δμ|<5 mas/yr、按 G 取最亮 600 颗；每星 {x,y,z} pc 簇质心系（ICRS 轴向）、B−V、V 视星等，光度转换用 Gaia DR3 文档 §5.5.1 表 5.9 官方 Johnson-Cousins 关系：V = G − f(BP−RP)（σ=0.030）、B−V 由 GBP−GRP = f(B−V)（σ=0.066）单调区间二分反解，抽检 Maia 计算 V=3.89 vs 目录 3.87 在 σ 内）、`star-params.json`（参宿四/参宿七/天狼星 A/B/造父一/WR 124 六星 Teff/半径/光度/光谱型：SIMBAD sp_type + 逐星文献 ref 登记——Joyce 2020/Przybilla 2010/Kervella 2003/Barstow 2005/Mérand 2005/Hamann 2019）、`m13-profile.json`（Harris 1996/2010 目录 NGC 6205 行：核半径 0.62′/半光度半径 1.69′/浓度 c=1.53/距离 7.1 kpc/积分星等 5.78，潮汐半径 21.01′ 由 r_t = r_c·10^c 导出）；每产物含 `meta {source, retrievedAt, license, count}`，总量 50.9 KB（上限 5 MB）。C) 脚本自校验——星数范围/质心距离 125–145 pc/坐标模长 ≤30 pc/B−V、V 数值域/无 NaN/产物总量，失败退出非零；幂等验证：两次运行产物逐字节一致（retrievedAt 取自快照 meta，默认模式为快照的纯函数）。D) 运行时加载器 `src/utils/bakedData.ts`——fetch 同源静态资产 + 结构/数值域校验（纯函数 validate* 三件）+ 按 URL 内存缓存（成功缓存、失败不缓存可重试），任何失败返回 null（消费方可降级到现状程序化分布；本阶段无消费方，主应用行为零影响）。新增单测 `bakedData`（21 例，fixture 驱动加载/校验/降级 + 实际产物完整性集成断言，bakedData.ts 覆盖率 100%），全量 2146 用例/120 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿
- 实现体积渲染框架 ②（R4-4，IMPROVEMENT_REQUIREMENTS_4 §R4-4）：半分辨率管线 + 蓝噪声抖动 + 帧率自适应降级，补齐体积渲染的性能弹性基座（R4-7 M42 体积化的前置）。A) 半分辨率路径（方案登记：选「独立 RT + 合成 pass」而非降步数等效近似）——新建 `src/components/Scene/volumetric/VolumeHalfRes.ts`：体积 mesh 置独立子场景渲染到 HalfFloat 线性 HDR RT（无深度附件），RT 常驻满分辨率、渲染经**动态视口/剪裁子区域**落到 scale² 像素（比例可连续取值、切档零重分配），主场景全屏三角形合成（uv 子区域采样 + 边缘半像素钳制防残留渗色、预乘 alpha 与直绘链路等效），与 EffectComposer（Bloom/ToneMapping）零改动共存；差异登记：合成三角形 depthTest=false（预览页仅参考网格受影响，主场景深度合成归 R4-8）。B) 蓝噪声抖动——`utils/volume.buildBlueNoiseData/Texture` 程序化生成 64×64 排序掩码（void-and-cluster 简化：逐秩最小能量填充 + 环绕高斯斥力核，Repeat 平铺无缝，确定性种子，零新依赖；直方图严格均匀 + 邻差高于白噪声期望的频谱特征单测断言），shader 步进起点逐像素偏移 [0,1) 步长（texelFetch + gl_FragCoord mod 64），滑杆可关做 A/B 对比；截图对比登记：20 步下关闭时"洋葱环"步进条带清晰、开启后完全打散无肉眼可见条带。C) 新建纯逻辑 `src/utils/adaptiveQuality.ts`——3s 滑动窗 FPS → 档位状态机（high 64 步/full → mid 48 步/half → low 32 步/half），滞回防抖（降档 <55 即时、升档 ≥58 连续 5s，55–58 迟滞带，换档清窗，样本不足不决策），映射 uQuality/步数/RT 比例，`advanceQualityBlend` ≤0.5s 平滑插值；实现差异登记：升档达标起点回溯到窗口起点（否则窗口积累期把"5 秒内升档"验收拖长至 ~6.5s）。D) 预览页 volume-test 接线——质量档强制切换滑杆（0 自动/1 低/2 中/3 高）+ 抖动开关滑杆（容量登记：R4-3 混色阈值滑杆移除以守住条目 8 滑杆上限，uThreshold 保持默认 0.45），HUD 增设「体积质量档」行（档位/步数/RT 比例/窗口 FPS 每帧直写）；RT/合成材质/几何/蓝噪声纹理卸载即 dispose（`disposeVolumeMaterial` 托管工厂自建掩码）。新增单测 `adaptiveQualityR44`（44 例，覆盖率 100%：滞回边界恰值/迟滞带/窗口不足/换档清窗/时间回退/插值收敛/强制档映射）+ `volumeR44`（9 例：蓝噪声确定性/直方图/频谱/平铺），全量 2125 用例/119 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800 截图 r44-01～08 + 时序 2560×1600 r44-timing-01/02 + SwiftShader r44-sw-01～03 登记）：全屏体积（占比 >2/3、128 步负载）~1s 自动降至 mid、帧率恢复 59–60（≥55），拉远小占比 4.5s 内升档（HUD 捕捉 RT 60%–88% 插值中间态、无跳变），强制档逐档生效（HUD 与观感一致），负载恰处两档间时按滞回规则周期性探测上档（~6.5s 一轮，动态分辨率控制器标准行为登记），SwiftShader 极端环境加载期即降至 low 封底稳定，控制台零错误
- 实现体积渲染框架 ①（R4-3，IMPROVEMENT_REQUIREMENTS_4 §R4-3 / §0.3 方案 B）：raymarch 体积材质 + 3D 密度纹理工具 + 预览页测试体，为星云体积化（R4-7/R4-8）提供渲染基座。A) 新建纯逻辑 `src/utils/volume.ts`——`buildDensityTexture(size, sampler)` 程序化密度场 → `THREE.Data3DTexture`（R8 单通道，边长 2–128 附录 A 约束校验，确定性：FNV-1a 种子 `volumeSeed(id)`，同一 sampler 双次构建逐字节一致）；密度场塑形基元（`fbm3` 复用现有 `valueNoise3D` 噪声基元 + 球/椭球/壳层 SDF + 软衰减 + IQ 多项式平滑并/差）；发射-吸收积分 CPU 参考实现 `integrateEmissionAbsorption`（与 shader 循环同式，恒定密度解析解 `constantDensityEmissionAnalytic` 对比校验积分格式一致）；`intersectRayBox`（shader hitBox 的 CPU 镜像，盒内/盒外两种入射参数化）。B) 新建 `src/components/Scene/volumetric/VolumeMaterial.ts` raymarch ShaderMaterial 工厂——单位盒内固定步进（默认 64、uniform 可调 16–128），发射-吸收模型 + 密度阈值双色平滑混色（uColorA/uColorB，Hα/OIII 窄带映射载体），相机盒内/盒外入射均连续（slab 求交 t0 钳 0 + BackSide），透明排序（depthWrite=false + `VOLUME_RENDER_ORDER`）、log depth buffer 兼容（logdepthbuf include，Starfield 先例）、uIntensity 控亮 + 输出硬钳上限防 Bloom 溢出、方向零分量下限防除零无 NaN，预留 uTime（R4-7 流动）/uQuality（R4-4 降级）；实现差异登记：不设 `glslVersion: GLSL3`（three r169 WebGL2 下 ShaderMaterial 默认路径即编译为 GLSL ES 3.0 且保留 gl_FragColor 兼容 define，显式 GLSL3 反而令 tonemapping/colorspace include 编译失败，实测登记）。C) 预览页注册测试体 `?body=volume-test`（框架检查点）——球形 fBm 密度云（96³ 纹理，确定性种子），滑杆调步数/密度倍率/吸收系数/混色阈值/双色（色相 A/B）/亮度，密度纹理与材质卸载即 dispose；本阶段不接主场景、不做半分辨率（R4-4 范围）。新增单测 `volumeR43`（37 例，volume.ts 覆盖率 100%），全量 2074 用例/117 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r43-01～09 登记）：多角度绕行密度云体积感真实（侧视/俯仰内部结构视差正确、无 billboard 感）、穿入盒内画面连续无翻转、Bloom 开/关组合无发光异常、双色与密度滑杆实时生效，64 步默认视角 / 穿盒 / 128 步全部 60 FPS，控制台零错误
- 实现细节层管理泛化（R4-2，IMPROVEMENT_REQUIREMENTS_4 §R4-2）：统一近观细节层注册/门控/LRU/显存预算机制，泛化 R2-7（L3 近观滞回门控）与 R2-8（L4 星系近观 LRU）两套并存机制为单一基础设施，为后续体积星云（R4-8）/引力透镜（R4-13）/真实星表（R4-17）细节层提供挂接点。A) 新建纯逻辑 `src/utils/detailLayer.ts`——`DetailLayerKind`（particles/volume/lensing/starCatalog 可扩展）+ `DetailLayerSpec {bodyId, kind, enterDistanceUnits, exitDistanceUnits, budget}`；泛化滞回门控 `detailGateUpdate`（进入 = 飞往观察距离 ×1.5、退出 ×1.4，与 `resolveFocusTarget` 同源，语义与现状逐项一致）；LRU 按 kind 分池（particles 沿用容量 1，volume/lensing/starCatalog 各 1）；GPU 显存估算 `estimateGpuBytes`（粒子按 float32 属性布局 28 B/粒、体积纹理按分辨率³×通道×字节）+ 总预算 `DETAIL_GPU_BUDGET_BYTES = 64 MB`（超限先按池容量逐出、再跨池按最旧优先逐出，新层豁免）+ 持有者注册表（claim/release/reset，GPU 在用量出账）。B) React 薄封装 `src/hooks/useDetailLayer.ts`——返回 `{active, opacity01}`（0.5s 交叉淡入淡出；opacity01 以帧读 getter 交付遵守零逐帧重渲染纪律），保留策略两档（`release-on-exit` = R2-7 退出即释放 / `lru-retain` = R2-8 LRU 保留），卸载即 dispose。C) 迁移改造（行为零回退）——`nearView.nearViewGateUpdate` 与 `galaxyNearView.nearViewLruUpdate`/持有者注册表委托统一机制（兼容包装保持 API 不变），三处调用方（`SpecialBodies.useNearViewGate`/`Heliopause`/`Universe.GalaxyObject`）切换到 `useDetailLayer`，进入/退出阈值、LRU 语义、0.5s 时长逐项一致，现有 `nearViewR27`（43 例）/`galaxyNearViewR28`（29 例）单测零修改全绿。新增单测 `detailLayerR42`（42 例，detailLayer.ts 覆盖率 100%），全量 2037 用例/116 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r42-L3-01～15 + r42-L4-01～08 登记）：L3 15 站 + L4 8 站巡游整圈近观激活/释放正常，L3/L4 实测均 60 FPS，连续切换 10 站 JS 堆稳定（29–53 MB 波动、末 10 站净差 +4 MB，无泄漏趋势），观感与 R2-7/R2-8 交付一致无回退
- 实现开发预览工位（R4-1，IMPROVEMENT_REQUIREMENTS_4 §R4-1）：新增独立路由 `/dev/preview?body=<id>`，隔离渲染指定天体细节模型用于人工目检提效——独立 Canvas（黑背景 + 可选参考网格 + OrbitControls），不挂载主场景任何组件（无 Galaxy/Universe/SolarSystem/音频/store 主循环）。A) 纯逻辑注册表 `src/utils/devPreview.ts`——`previewEntryForBody(id)` 查找预览挂载配置（未注册返回 null，页面显示占位提示并列出可用对象链接）、`PreviewParam { key, label, min, max, default, step? }`（每条目 ≤8 个调试滑杆）、`validatePreviewEntry`（注册期防错：数量超限/键重复/min>max/默认值越界/相机距离非正即抛 RangeError）、`defaultParamValues`/`clampParamValue`（滑杆越界钳制、非有限数回落默认值）；后续 R4 各阶段把细节组件经 `componentKey` 注册进来，渲染依赖不污染纯逻辑层。B) 预览页面板——Bloom 开关 + 曝光滑杆（tone mapping exposure）+ 参考网格开关 + 组件声明的调试滑杆 + 时间流速；左上 HUD 实时显示 FPS 与 JS 堆（复用 `utils/performance.ts`，组件自持 rAF 不依赖主循环）与数据来源登记。C) 生产安全（用户登记方案）——`NODE_ENV === 'production'` 下渲染空页且预览专用 harness（含 three/drei/postprocessing）走 `next/dynamic` 动态 import 仅 dev 加载，实测生产构建将 harness 打入独立异步 chunk、不被生产页 HTML 引用，主应用 bundle 零增大、生产路由不可用。D) 首个可预览对象 `betelgeuse`——复用现有 `StellarSurface`（红巨星档 limbU/cellScale/convection/rednessStrength 参数），时间流速经虚拟时钟覆写 uTime 实现可调，验证管线闭环。新增单测 `devPreview`（24 例，覆盖率 100%），全量 1995 用例/115 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r41-01～05 + r41-metal 登记）：betelgeuse 恒星球体可见、绕行 60 FPS、cellScale 滑杆实时改变对流颗粒尺度、未注册 id 与缺参均显示占位不报错

### 改进

- 银河系巡游（L3）序列由 15 站精简为 14 站（用户反馈：太阳与日球层顶示意语境重复）：移除 `sun` 站，保留 `heliopause`（日球层顶示意）作为太阳系出发/告别站，巡游"上一个/下一个"不再经停太阳，位置标签变为 x/14；太阳仍可经点选/耀斑通知飞往（域归类保持当前域、近观激活距离公式保留不变），银河系巡游中跟随太阳时点击切换回落到域记忆天体。`nearView.ts` 粒子增量登记表同步移除 sun 条目（保持"登记表 = 序列成员"不变式）；单测同步更新（序列断言/循环闭合 14 步/位置标签/回落语义），全量 2072 例/117 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）：L3 巡游整圈 15 次切换逐站核对——日球层顶 1/14 起步、武仙座 M13 14/14 收尾、循环闭合回日球层顶，全程无太阳站、无异常
- 支持同一份代码并行运行多个开发服务器（此前 `next dev -p <port>` 换端口无效：Next.js 在 `<distDir>/dev/lock` 放互斥锁，第二个实例会以 "Another next dev server is already running." 直接退出）。新增 `npm run dev:3100` / `npm run dev:3200` 两个脚本，各自通过 `NEXT_DIST_DIR` 指定独立构建目录（`.next-3100` / `.next-3200`）绕开该锁，可与占用 3000 端口的 `npm run dev` 同时运行、互不干扰。该覆盖仅在 dev 阶段生效——`output: 'export'` 下自定义 distDir 会让静态导出产物改落到 distDir 而非 `out/`（Next 内部 `hasCustomExportOutput` 特殊处理），限定阶段可确保 `npm run build` 与 GitHub Pages 部署产物路径不受影响；`.next-*` 已加入 .gitignore、ESLint `ignores` 与 Jest `testPathIgnorePatterns`（否则并行实例的产物会让 `npm run lint` 冒出数百条误报），`tsconfig.json` 预置对应 `include` 条目以避免 Next 启动时改写该文件

## [0.1.2] - 2026-07-28

### 新增

- 设计站点品牌 icon（深空蓝紫背景 + 恒星与轨道遨游意象，SVG 矢量源 `public/icon.svg`）并生成完整 favicon 套件（favicon.ico / apple-touch-icon / 192 / 512 PNG），浏览器标签页与 iOS 主屏不再显示默认图标；同步登记 Open Graph / Twitter Card 元信息与 1200×630 分享卡片图（`public/og-image.png`），分享到社交平台时显示标题、描述与预览图
- 新增自定义 404 页面（`src/app/not-found.tsx`，静态导出为 `404.html`）：深空星野 + 科幻文案「你已漂流到已知宇宙之外」，10 秒倒计时自动返回首页（location.replace 不留浏览历史）+「立即返回星图」按钮，替代 GitHub Pages 默认 404 页面；含渲染/倒计时/跳转单测
- README 新增在线体验入口（stellar.guushu.com badge 与快速开始引导）与三段效果演示 GIF（四层级连续缩放遨游 / 太阳耀斑与 CME 事件链 / 银河系—仙女座碰撞合并预览，`docs/media/`，Playwright 无头录制 + ffmpeg 调色板压缩生成）

### 改进

- 事件通知最短展示时长（用户反馈：通知显示太短来不及点击）：耀斑/CME/CME 抵达通知的收起与事件生命周期解耦——事件先于 15 真实秒完成时，通知驻留满 15 秒再自动收起（此前高时间压缩比下耀斑 1.6 模拟天可能不足 2 真实秒，通知随事件完成瞬间消失）；事件持续更久则仍随事件收起，手动关闭与离域丢弃不受下限约束；通知卡片渲染改用触发时快照（`solarFlareNoticeInfo`/`cmeNoticeInfo`），事件置空后级别/速度等信息仍完整展示；CME 抵达通知新增自动收起（极光增强结束且展示满 15 秒，此前仅能手动关闭）
- 设定开源协议为 GNU AGPL-3.0（新增 LICENSE 全文与 package.json license 字段）：个人/教育/科研用途自由使用，商业闭源集成需联系作者获取商业授权（双许可模式）；README 新增「开源协议」一节说明代码协议与素材许可的边界
- 建立贡献者许可协议（CLA）体系保障双许可模式：新增 CLA.md（许可授予型 ICLA，参考 Project Harmony——贡献者保留版权，授予维护者含商业条款的再许可权，维护者对等承诺贡献始终保持开源可用）、CONTRIBUTING.md（贡献流程/门禁要求/代码规范/CLA 签署说明）与 CLA 签署机器人 workflow（contributor-assistant/github-action，PR 评论签署、记录存于 cla-signatures 分支、owner 与 bot 白名单豁免），cla 检查纳入分支保护 required status checks——未签署 CLA 的外部 PR 无法合并；README 新增「参与贡献」一节

## [0.1.1] - 2026-07-27

### 改进

- 集成 Cloudflare Web Analytics（RUM beacon 手动嵌码于全局布局）：统计真实访客的访问量/来源/地域/设备与 Core Web Vitals，无 cookie 隐私友好，仅作用于 stellar.guushu.com 本站

## [0.1.0] - 2026-07-27

首个公开发布：开源上线 https://stellar.guushu.com （GitHub Pages 静态托管 + Cloudflare 代理）。

### 新增

- 实现银河系整体垂直展开（R3-7，IMPROVEMENT_REQUIREMENTS_3 §R3-7，用户反馈：R3-6 只展开 13 个特殊天体未达预期，期望整个银河系从圆盘变成椭球体；用户确认项 5 条全采纳）：V 开关/增益滑块下**整个银盘 40,000 粒子 morph 为扁旋转椭球体**（轴比 0.5，正面/俯视轮廓仍为圆形、旋臂俯视可辨，最厚侧面为椭圆）。A) 银盘粒子 GPU morph——盘粒子顶点着色器新增 `uExpand` uniform，复用 R2-11 Milkomeda 已验证椭球公式 `hTargetLy = (aHeightLy/500)·max(aRadiusLy, 6000)·0.5` 做同目标第二次 mix（与合并 uEll 组合权重 = 1−(1−uEll)(1−uExpand)，`combinedMorphWeight` 镜像单测断言，终态 Milkomeda 不受 V 开关破坏）；每颗粒子按自身真实高度等比例抬升、x/z 不动（垂直夸大手法，观察辅助登记于 utils/galacticLatitude.ts 文件头），零新增粒子、uniform-only。B) morph 权重与 R3-6 同源——纯函数 `diskMorphWeight(expandGain) = clamp((gain−1)/5, 0, 1)`（×1→0、默认 ×3→0.4 中等椭球、×6→1.0 完整椭球）由 `renderedGalacticFrame().expandGain`（已含 1 秒开关过渡 + 滑块平滑）派生，零新增注册表字段、渲染/解析天然同源，特殊天体 R3-6 银纬增益机制零改动（同一滑块驱动）。C) 超新星事件/遗迹随盘抬升（**行为变更**，推翻 R3-6"超新星不参与展开"登记）——CPU 镜像纯函数 `morphGalacticYLy(yLy, √(x²+z²), morph01)` 与 shader 公式逐字镜像，应用 Supernova.tsx 活跃事件/遗迹两处定位 y 通道（每帧标量运算零分配）+ `cameraFocus.supernovaFocusTarget` 解析同源施加（展开态飞往/跟随超新星落点正确；`galacticPointToSceneUnits` 本身不改，sgr-a-star 银心原点 y=0 morph 恒等）。D) 展开态联动——银晕增亮 +30%（`haloExpandBoost`，强化椭球轮廓）+ 尘埃带侧视暗带渐隐（`dustLaneExpandFade`，morph 后盘中平面语义消失；单一应用点联动 shader vDust/暗带 mesh/核球辉光压低）；不参与 morph 登记：银晕粒子/球状星团（本已球状分布）、太阳系标记/尾迹/预测线/银河年刻度（太阳振荡 ×10 机制不变）；层级语义登记：V 开启时 L4 同样呈椭球（银河系组 L4 仍可见，预期行为）、L1/L2 组不可见零影响；权衡登记（HelpHint/文件头）：morph 后侧视旋臂图案被垂直弥散（俯视仍清晰）。新增单测 `galacticLatitudeR37`（23 例：权重映射与钳制/效果图确认值 ×5.25→0.85/公式镜像与常量同源/恒等·符号·单调·核球下限/增亮渐隐因子/组合权重顺序 mix 镜像/超新星渲染-解析同源含银心固定 w=1 与 ×1 恒等零回退），R3-6 既有单测零改动（口径注释更新），全量 1885 用例/111 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r37v-01～13 登记）：侧视薄盘/×3 中等椭球/×6 完整椭球对比、×1 与关闭 V 均恢复薄盘、近正俯视默认 vs ×6 圆形轮廓与旋臂核对、×6 侧视尘埃带暗带消失 + 银晕增亮、展开 ×6 下飞往超新星落点正确（遗迹居中跟随）、全场景 FPS 60 不跌破

- 实现银河系视角天体垂直展开（R3-6，IMPROVEMENT_REQUIREMENTS_3 §R3-6，用户反馈：L3 天体几乎都在一个平面上，希望在符合科学事实前提下展开得更立体）。A) 数据修正（默认生效，**行为变更**）——12 个 sun-relative 特殊天体 `offsetLy.y` 按真实银纬重定 `y = round(√(x²+z²) × tan(b))`（b 值逐天体登记于 data/specialBodies.ts 注释，来源 SIMBAD；"从太阳看的方向按真实银纬、水平距离示意"口径）：参宿四 −510 / 参宿七 −1,902 / 天狼星 −498 / 蟹状星云脉冲星 −538 / 猎户座星云 −1,420 / 环状星云 +1,005 / M13 +4,858（原 6,200，"高悬银晕"事实不变）/ 天鹅座 X-1 +179 / WR 124 +288 / 造父一 +40 / 昴星团 −1,616 / 马头星云 −1,403，默认观感即立体（猎户座/昴星团/参宿七低于盘面、M13 高悬）；`utils/galaxy.m13GalactocentricT0Ly` 同源更新。B) 展开开关（观察辅助视觉夸大，登记于 utils/galacticLatitude.ts 文件头）——store 新增 `galaxyVerticalExpand`（默认关）+ `galaxyExpandGain`（滑块 [1,6]、默认 ×3、步进 0.5，setter 钳制），快捷键 `V` + 控制面板显示选项区复选框与增益滑块；开启后特殊天体 `offsetLy.y` 乘生效增益，开/关约 1 秒平滑过渡（advanceFrameTransition 进度 × easeInOutCubic 缓动）+ 滑块拖动全量程约 1 秒平滑跟随（`advanceExpandGainValue`），太阳垂直振荡 ×10 增益机制不变、互不相乘；范围登记——仅 13 个 L3 特殊天体（sgr-a-star 银心原点不参与），银盘粒子/旋臂/超新星/太阳系标记不展开，L1/L2/L4 零视觉影响（可见窗口 2.5–3.9 天然限定）。C) 高度指示线——展开开启且标签开启时每个天体显示"天体 → 银盘面（y=0）投影点"虚线（LineDashedMaterial 预分配属性零分配更新）+ ±ly 高度标注（ClampedHtmlLabel 近距钳制；标注为未乘增益的银纬推算真实高度，登记）；关闭展开/标签即隐藏。D) 渲染/解析同源——`renderedGalacticFrame` 注册表扩展 `expandGain` 字段（Galaxy.tsx 每帧缓动后写入），`SpecialBodies.useGalacticPlacement` 与 `cameraFocus.specialBodyFocusTarget` 同源消费（展开状态下飞往/跟随落点正确，单测断言渲染/解析两路径逐分量一致；超新星与银心解析不受影响）。新增纯逻辑 `utils/galacticLatitude.ts`（`offsetYFromLatitude` 非法输入 RangeError / 增益钳制与平滑 / 指示线端点 / 标注文案，覆盖率 100%）+ 单测 `galacticLatitude`（29 例，含 12 天体数据表交叉断言与同源断言）/`storeR36`（4 例），既有受影响断言更新（galaxyR29 M13 / galacticFrameP6 注册表），全量 1862 用例/110 套件通过，覆盖率 gate ≥90% 保持（语句 98.18%/分支 97.88%/函数 98.99%/行 99.49%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）：默认/展开 ×3/×6/×1 四态对比（M13 高悬渐次分离，截图 r36-01～04、08）、12 个 ±ly 标注 DOM 逐一核对（+4,858 ly 等）、M13 指示线特写（r36-09）、展开状态下飞往/跟随 M13 与猎户座星云落点正确（r36-05/06）、关闭恢复（r36-07）、全场景 FPS 60 不跌破

- 搭建 Next.js 14 + React 18 + TypeScript + Three.js/React Three Fiber + Zustand 项目骨架
- 实现尺度与精度管理方案：分层场景尺度归一化（1 AU = 10 场景单位）+ 对数深度缓冲，天体半径对数压缩并登记为视觉夸大处理
- 实现开普勒轨道物理计算：求解开普勒方程（牛顿迭代，支持高离心率）得到位置，满足匀面速度（第二定律）；初始相位基于 J2000 历元真实数据，打开应用时行星位置与真实当前日期一致
- 实现八大行星椭圆轨道公转（完整轨道六要素，NASA JPL 数据）与真实自转（金星逆向自转、天王星 97.8° 侧躺、木星最快约 9.9 小时）
- 实现太阳（发光球体 + 多层光晕 + 点光源）与确定性星场背景（恒星温度着色、距离衰减）
- 实现轨道线渲染：与天体分离的预计算准确椭圆（512 分段），三维姿态正确（倾角与升交点），可显示/隐藏
- 实现四视角锚点（行星/太阳系/银河系/宇宙）一键切换，相机位置、朝向、FOV 与背景色平滑过渡；支持手动旋转/平移/缩放
- 实现全局时间系统：各层级差异化时间压缩比、速度倍率滑块与暂停、跨层级压缩比对数插值
- 实现基础分级音效系统：四层级音景映射、切换时等功率交叉淡入淡出（2 秒）、音量调节与静音、初始化失败静默降级（Web Audio 程序化合成环境音）
- 实现控制面板（视角/速度/音效/轨道线/标签）、HUD 信息（当前视角与模拟时间）、行星点选信息面板（含数据来源）、键盘快捷键（1-4 视角、空格暂停、M 音效、O 轨道线、L 标签）与首次进入操作引导
- 建立单元测试体系（Jest + Testing Library）：核心业务逻辑（物理计算、尺度管理、时间系统、动画插值、音效混合、状态管理、天体数据）覆盖率超过 90%（语句 97.75%、分支 100%）
- 新增卫星数据集：月球（黄道面倾角 5.145°、潮汐锁定）、ISS 与哈勃（真实轨道高度/倾角/周期）、伽利略四卫星（1:2:4 轨道共振）、土卫六与土卫二（NASA Planetary Satellite Fact Sheet）
- 新增小天体数据集：哈雷彗星（逆行高离心率轨道）与恩克彗星、矮行星冥王星（17° 倾角、与海王星 2:3 共振、逆向自转）、小行星带与柯伊伯带粒子带配置（JPL SBDB / JPL Keplerian Elements）
- 新增星系数据集：银河系结构（棒旋、四条主旋臂、人马座A*）、本星系群成员（M31/M33/大小麦哲伦云/M87）、室女座星系团、拉尼亚凯亚超星系团与巨引源方向（NED / Tully et al. 2014）
- 新增统一天体信息目录：聚合行星/太阳/矮行星/卫星/彗星/星系，输出中文信息行（逆向自转、逆行轨道、接近/退行速度等标注）供信息面板使用
- 土星增加真实主环参数（C 环内缘至 F 环，含卡西尼缝）；地球增加云层与大气辉光配置；金星、火星增加大气辉光配置
- 实现行星表面细节（P1）：程序化纹理呈现各行星可辨识特征（地球大陆/海洋/极地冰盖、火星极冠与水手号峡谷、木星云带与大红斑、海王星大暗斑、水星陨石坑等），纹理 LOD 两级切换（远距 256px / 行星视角 1024px，同一生成器保证切换无突变）；地球独立旋转云层与蓝色大气边缘辉光；土星环渲染（径向环纹 + 卡西尼缝）
- 实现卫星系统（P1）：月球（27.3 天周期、5.1° 黄道倾角、潮汐锁定始终同面朝向地球、环形山纹理）、国际空间站与哈勃望远镜（真实高度/倾角/周期）、伽利略四卫星（1:2:4 共振）、土卫六与土卫二绕各自行星运动；人造卫星与自然卫星分层缩放策略已登记；卫星轨道线可开关（行星视角默认显示）
- 实现快周期天体速率钳制（需求 3.3）：ISS 等视觉转速超过 0.5 圈/秒时平滑降速显示，UI 提示"运动已减速显示"；外层视角下行星/彗星运动冻结退化，返回内层按共享模拟时间轴重新求值保证时间连续性
- 实现彗星系统（P1）：哈雷彗星（e≈0.967 高离心率、162° 逆行轨道，匀面速度近日点疾驰）与恩克彗星；近日点附近出现彗发与彗尾，彗尾始终背向太阳（蓝色离子尾/黄白尘埃尾可区分），亮度长度随日心距离变化
- 实现小行星带（2.2–3.2 AU）与柯伊伯带（30–50 AU，冷色低密度，含冥王星）：每个粒子沿各自开普勒轨道公转（顶点着色器推进），内圈角速度大于外圈（开普勒剪切），无静态环或刚性旋转
- 实现银河系场景（P1）：3D 棒旋结构（核球 + 4 万粒子银盘 + 4 条主旋臂 + 中心辉光与银晕光层 + 人马座A* 位置标记），银盘较差自转（线速度平坦 ~220 km/s，角速度内圈大于外圈）
- 实现太阳系绕银心运动（P1）：距银心约 2.6 万光年、银河年约 2.3 亿年、垂直振荡（周期 7000 万年）呈波浪形轨迹；黄道面与银道面 60.2° 倾斜；"You are here" 高亮标记（可开关）+ 运动方向箭头；轨迹以"历史尾迹（环形缓冲、尾端渐隐）+ 未来预测线（虚线）"呈现；HUD 显示银河年进度
- 实现宇宙级场景（P1）：本星系群成员按真实相对距离/大小分布（距离对数压缩已登记），四类星系形态差异化可辨识（旋涡/棒旋/椭圆/不规则）；银河系—仙女座沿连线相互接近（~110 km/s，含接近轨迹线与碰撞倒计时提示）；大小麦哲伦云绕银河系运动；本星系群本动以速度矢量箭头 + 数值标签表达（可开关，不移动场景）；宇宙网大尺度结构（星系团节点—纤维—空洞，确定性算法）；拉尼亚凯亚超星系团边界示意与巨引源标记
- 实现连续维度缩放遨游模式（P1）：滚轮从行星表面一路拉远到宇宙宏观无需点击切换；相机距离实时换算连续层级（1.0–4.0），驱动跨层级内容 LOD 淡入淡出、时间压缩比对数插值与背景色实时混合；HUD 实时显示当前尺度标尺（AU/光年/Mpc 按层级自动切换）与参考系说明
- 音景随连续缩放实时混合（需求 3.4.2）：各层级音景增益按连续层级等功率混合，跟随维度变化而非仅跟随切换事件；锚点切换经由相机过渡自然形成 2 秒交叉淡入淡出
- 天体信息面板扩展：卫星、彗星、矮行星、星系均可点选查看（统一目录，含类型、关键参数与数据来源）；控制面板新增卫星轨道线 / You are here / 速度矢量开关
- 实现天体跟随/飞往模式（P2，需求 3.2.3）：点选任意天体后可"飞往"（2.5 秒平滑运镜，目标随天体运动每帧追踪）并自动进入跟随模式（相机锁定天体保持相对偏移，仍可旋转缩放）；信息面板新增"飞往/跟随"按钮，HUD 显示跟随状态；快捷键 F 飞往选中天体、Esc 取消跟随；视角锚点切换自动取消跟随
- 实现特殊天体系统（P2，需求 3.1.5，均基于真实原型并附数据来源，可点选查看信息）：
  - 人马座A* 黑洞（银心）：事件视界纯黑球体 + 吸积盘 shader（开普勒较差旋转 ω∝r^-3/2 内圈快外圈慢、内圈白热外圈橙红、多普勒集束接近侧增亮）+ 引力透镜 shader（爱因斯坦环 + 背景星光弧状扭曲，公告板面向相机）
  - 蟹状星云脉冲星：双极射束旋转扫描（灯塔效应），射束扫过视线产生周期性脉冲闪烁（33 ms 自转降频表现，已登记）；外围超新星遗迹星云缓慢膨胀（与脉冲星联动为同一对象）
  - 红巨星参宿四（双周期叠加的半规则脉动 + 弥散气体壳）、蓝巨星参宿七（高频微闪烁 + 强光晕）
  - 天狼星A/B 双星系统：白矮星与主星绕共同质心互绕（质量比约 2:1，50 年周期按速率钳制降速呈现）
  - 星云类：猎户座发射星云（氢α粉红多层雾状 + 内部年轻恒星点亮）、环状星云 M57（环壳 + 中心白矮星 + 缓慢膨胀动画）、武仙座球状星团 M13（银晕中致密老年恒星，420 粒子确定性分布）
  - 特殊天体随层级合理退化（L4 淡出不可见），动态效果仅可见时演算
- 实现超新星爆炸动态事件系统（P2，需求 3.1.5）：完整四阶段动画（前身星数秒骤增至峰值 → 球形冲击波按 Sedov-Taylor r∝t^0.4 减速扩张（半透明壳体+外缘增亮）→ 亮度指数衰减 → 永久遗迹：膨胀星云 + 中心致密天体（前身星 ≥20 M☉ 为黑洞否则中子星）），总时长 10–30 秒可配置；触发方式：模拟时间内泊松过程低概率自动触发（位置在旋臂内随机）+ 控制面板手动演示按钮；爆发时 UI 通知（含"飞往观看"按钮与科学性说明：真实频率约每 50–100 年一次，模拟已加速）；音效联动低频冲击（程序化合成正弦下扫 + 噪声余响）；遗迹 FIFO 上限防内存增长
- 实现河外特殊对象（P2，需求 3.1.5）：类星体 3C 273（约 24 亿光年，极亮核心 + 双向相对论喷流含流动动画 + 不规则光变闪烁）；M87 单侧可见喷流（与室女座星系团 M87 条目联动为同一对象）
- 实现真实比例模式开关（P2，需求 4.1）：控制面板/快捷键 R 一键切换，天体半径、卫星轨道、行星环均按真实线性比例映射（1 AU = 10 场景单位），如实呈现"真实比例下行星接近不可见"的尺度事实；与对数压缩视觉模式无缝互切
- 实现全部可选项需求（约 20 项）：
  - 太阳系扩展（需求 3.1.1）：更多卫星（火卫一/火卫二、土卫一/土卫五、海卫一逆行轨道 156.9°、地球静止轨道卫星示意）；更多矮行星（阋神星 44° 高倾角、鸟神星、妊神星 3.9 小时最快自转）沿各自开普勒轨道公转并纳入信息目录；奥尔特云外边界球壳粒子示意（真实 2,000–100,000 AU，压缩至 L2/L3 过渡位置已登记）；地球夜半球城市灯光层（与大陆噪声同 seed 对齐、随地表同步旋转）
  - 银河系细节（需求 3.1.2）：旋臂密度波效果——旋臂图案以恒定角速度 Ω_p≈0.02 rad/Myr 刚性旋转（Lin & Shu 密度波理论），与恒星较差自转不同步，恒星周期性穿越旋臂（银盘 shader 顶点亮度调制）
  - 宇宙细节（需求 3.1.3）：银河系—仙女座碰撞合并快进预览（12 秒缓动快进至 45 亿年后合并时刻，切宇宙视角观看，合并辉光随距离增强，可一键恢复预览前时间）；M31 伴星系 M32/M110（随 M31 一同移动）与人马座矮星系；麦哲伦星流（沿 LMC 轨道回溯拖尾的气体流示意）；哈勃膨胀示意（宇宙网随时间整体缩放 a(t)=1+H₀t，遥远星系红移变红示意已登记）；可观测宇宙边界圈层（半径约 465 亿光年）与标注
  - 特殊天体扩展（需求 3.1.5）：天鹅座X-1 恒星级黑洞（21 M☉ + 吸积盘）、沃尔夫-拉叶星 WR 124（强星风粒子外流 + M1-67 星云壳）、造父一（不对称锯齿光变：快升缓降，周光关系"量天尺"说明）、昴星团（疏散星团 + 蓝色反射星云）、马头星云（暗分子云剪影遮挡背景发射星云）；河外新增触须星系（碰撞并合潮汐尾）、星系团引力透镜弧（原型 Abell 370，示意位置已登记）、伽马射线暴 GRB 221009A（FRED 光变曲线，周期性重放为演示示意已登记）
  - 3D 空间音效（需求 3.4.2）：太阳低频轰鸣与人马座A* 深沉嗡鸣经 PannerNode 定位，靠近对应天体时音量自然增强（每帧相机局部坐标归一化 + 层级窗口门控）；UI 操作音效（选择天体双音提示、视角切换"嗖"声、开关翻转点击音）；音效引擎改为共享单例
  - 性能监控（需求 3.5.2）：FPS（500ms 窗口平均 + 健康度指示）与内存占用（Chrome performance.memory，其他浏览器降级显示）面板，控制面板复选框开关
  - 信息面板补充质量字段（需求 3.5.2）：行星/矮行星/卫星/彗星/太阳质量以科学计数法上标显示（如 5.97×10²⁴ kg）；卫星逆行轨道（海卫一）标注"（逆行）"
- 实现真实天体纹理（P3，需求 4.2）：八大行星、太阳与月球接入 Solar System Scope 2K 实拍级贴图（CC BY 4.0，许可已在 README 登记），加载失败自动降级为原程序化纹理
- 实现纹理懒加载与加载进度（P3）：贴图按当前视角优先级异步加载（ImageBitmap 解码不阻塞渲染），页面顶部显示加载进度条，完成后自动淡出
- 实现 Bloom 泛光后处理（P3，需求 4.2）：太阳/恒星/黑洞吸积盘等发光体获得真实辉光，强度随视角层级自适应，控制面板可开关；实测性能成本约 1.2 ms/帧（无头 Chrome Metal 非垂直同步 Bloom 关 ~728 FPS / 开 ~394 FPS，垂直同步下均稳定 60 FPS）
- 实现行星光影增强（P3，需求 4.2）：自定义 shader 呈现清晰昼夜界线（terminator）、土星环与星球双向投影、云层与地表统一光照，并兼容对数深度缓冲与色调映射
- 实现恒星闪烁效果（P3，需求 4.2）：星场恒星以确定性双正弦微幅闪烁（仅行星视角启用，科学性说明已在帮助提示登记：大气闪烁效果，太空中实际不闪烁）
- 实现行星差异化音景（P3，需求 3.4.2）：八大行星各自专属环境音参数（水星近真空以极微弱音景表达并登记说明），行星视角跟随/飞往目标行星时 2 秒平滑过渡切换
- 实现行星视角天体切换（P4，需求 3.2.4）：L1 下 HUD 显示「← 上一个 | 当前天体名 序列位置 | 下一个 →」控件，沿固定序列（水星→金星→地球→月球→火星→木星→土星→天王星→海王星→哈雷彗星→恩克彗星）循环切换，复用 2.5 秒飞往运镜并自动跟随；快捷键 `[` / `]`；按 1 键/点击"行星视角"改为飞往并跟随序列当前天体（默认地球，会话内记忆上次锚定天体）
- 全行星 4K 纹理升级（P4，需求 4.7）：水星/金星/地球（含夜灯与云层）/火星/木星/土星/月球表面贴图升级至 4096×2048（Solar System Scope 8K 源图本地降采样，CC BY 4.0）；天王星/海王星/土星环官方仅 2K 源图且无公有领域高清替代，维持 2K 并叠加近观程序化纬向细节增强（±1.5% 幅度，差异已登记）
- 实现法线贴图近观立体细节（P4，需求 4.7）：地球山脉立体起伏（NASA GEBCO 地形数据转换）+ 海洋高光（海面反光、陆地不反光）、月球环形山（NASA LOLA 高程数据转换）、火星陨石坑/峡谷（无轻量 MOLA 合规源，由色彩贴图亮度推导，降级已登记）；法线光照接入现有 terminator/土星环投影 shader，光照方向统一为太阳且不改变昼夜界线位置
- 实现木星云层差速流动动画（P4，需求 4.7）：赤道急流与温带反向急流按真实大气环流带结构以不同角速度漂移（System I/II 速率差 ~0.8%，剖面纯逻辑可测试；漂移相位放大 6 倍便于观察已登记为艺术化处理），流动与自转共用模拟时间轴
- 实现彗星近观与彗尾增强（P4，需求 4.7）：彗核程序化岩石纹理（噪声凹凸 + 陨坑暗斑）+ 顶点噪声不规则花生形外形（哈雷彗核 15×8 km，数据来源 ESA Giotto）+ 缓慢自转；尘埃尾沿轨道后方二次曲线弯曲（曲率随轨道速度/日心距变化，近日点两尾夹角 >15°、远日点 <5°），离子尾保持严格背向太阳；彗尾方向/夹角/活动度公式抽取为 `utils/cometTail.ts` 纯函数并补单元测试
- 实现近观细节分级懒加载与资源门控（P4，需求 4.7 硬性）：4K/法线层仅在相机-天体距离进入近观阈值（滞回防抖）时经 textureManager 优先级队列请求，2K 底图先显示防空窗；仅最近 2 个天体保留细节层（LRU，最坏组合显存增量 ~268 MB ≤ 300 MB 预算）；切换天体/离开 L1 语境自动释放显存；法线光照、木星流动、彗核细节均仅近观激活时渲染/演算
- P4 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L1 地球/月球/火星/木星/彗星近观（4K + 法线 + 流动全开）与 L2 太阳系视角均 60.0–60.4 FPS 满帧无跌破，JS 堆 18–28 MB（与 P3 基线 22–28 MB 持平）；4K 贴图与法线共 12 个新文件，public/textures/ 体积由 ~6 MB 增至 ~43 MB（均为近观懒加载，不影响首屏 <5 秒目标）
- 新增矮行星谷神星（P5，需求 §3.1.1）：IAU 认定的第 5 颗矮行星，位于小行星带内（a≈2.77 AU、e≈0.0785、i≈10.59°、半径 469.7 km、自转 9.07 h，数据来源 NASA JPL SBDB / Dawn 任务）；信息面板附注"小行星带中最大天体，Dawn 探测器 2015 年环绕探测"
- 实现矮行星 L2 太阳系视角可见性增强（P5）：默认模式下矮行星最小显示半径由 0.3 提升至 0.42 场景单位（与水星 L2 视觉尺寸同量级、高于柯伊伯带粒子，夸大策略登记于 `utils/dwarfPlanets.ts`），5 颗矮行星（实体 + 紫色轨道线 + 名称标签）在 L2 下可辨识、不再湮没于柯伊伯带粒子；真实比例模式不夸大（矮行星过小不可见属科学事实，帮助与控制面板已说明）
- 天体切换序列扩展至 16 天体（P5，需求 §3.2.4 扩展）：5 颗矮行星按日心距离插入 `[` / `]` 循环序列（谷神星插于火星与木星之间，冥王星→妊神星→鸟神星→阋神星排于海王星后），HUD 序列计数同步更新（如"冥王星 11/16"）；矮行星聚焦距离按钳制后显示半径适配，近观视野比例与八大行星一致
- 接入冥王星/谷神星真实贴图（P5，需求 §4.7）：冥王星 NASA New Horizons 全球拼接彩色地图（近观可辨识心形汤博区与克苏鲁暗斑；南半球未测绘区如实保留并登记）、谷神星 NASA Dawn FC 全球拼接图（近观可辨识欧卡托撞击坑亮斑），均为公有领域、本地降采样 2K 底图 + 4K 近观层（接入 P4 近观门控与 LRU 显存释放），走现有懒加载优先级队列（接近 L1/聚焦才请求），加载失败静默降级程序化纹理
- 实现冥王星/谷神星法线贴图近观立体细节（P5 §3.4 可选项）：由 USGS 公有领域高程数据本地转换（冥王星 New Horizons 全球 DEM 300m、谷神星 Dawn HAMO DTM 60ppd），近观可见斯普特尼克平原盆地边缘山地与谷神星撞击坑立体起伏；冥王星未测绘半球法线置平（与彩色贴图未测绘区一致）、测绘区边缘立体像对噪声经模糊与梯度钳制抑制（差异登记于 textures.ts/README）
- 实现阋神星/鸟神星/妊神星程序化增强纹理（P5）：阋神星高反照率亮白甲烷冰霜（反照率 0.96，Sicardy et al. 2011）、鸟神星红棕托林色调（光谱观测色指数偏红）、妊神星结晶水冰亮面 + 暗红斑；艺术化推测部分登记于 `proceduralTextures.ts` 文件头，信息面板注明"表面细节为基于观测数据的艺术化呈现"
- 实现妊神星三轴椭球外形（P5）：按真实轴径 2100×1680×1074 km（Ortiz et al. 2017 掩星）缩放球体网格，短轴为自转轴，3.9 小时快速自转下椭球翻滚可见；真实比例/默认模式均生效
- 新增冥卫一卡戎（P5 可选项）：半径 606 km、绕冥王星公转 6.39 天、双向潮汐锁定（公转周期 = 冥王星自转周期），信息面板注明"共同质心位于冥王星体外"（数据来源 NASA New Horizons）
- 矮行星音景映射（P5，需求 §3.4.1）：5 颗矮行星均无有效大气，L1 跟随时复用"水星近真空几乎静音"音景参数（各自登记大气特征科学依据），不新增音景类型
- P5 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L2 太阳系全景 60.1 FPS / L1 冥王星近观（真实贴图 + 4K 细节层 + 法线）60.2 FPS / L1 妊神星近观（椭球 + 程序化增强）59.2 FPS，满帧无跌破；新增贴图 6 个文件约 9.5 MB（均为近观懒加载，不影响首屏 <5 秒目标）
- 新增天宫空间站（P7，需求第三部分 §3.3）：轨道高度约 390 km、倾角 41.5°、周期约 92.2 分钟、三舱组合体约 100 t（CMSA 公开数据 / CelesTrak），信息面板与 ISS 周期/倾角/高度对照可见，适用速率钳制与轨道线机制
- 实现人造卫星 glTF 精细模型近观（P7 §3.1）：ISS / 哈勃 / 静止轨道卫星（TDRS 原型）接入 NASA 3D Resources 公版模型（公有领域，本地 gltf-transform 减面 + meshopt 压缩，合计约 0.66 MB、单模型 ≤3.2 万三角形，均低于单模型 3 MB / 5 万三角形与总量 10 MB 预算）；近观可辨识 ISS 桁架 + 多组帆板 + 舱段、哈勃镜筒 + 双帆板 + 遮光罩、TDRS 抛物面天线 + 帆板；ISS 源模型无颜色差异，按网格形态启发式着色（帆板深蓝/桁架银灰/舱体白色，基于真实外观的艺术化增强已登记）；模型加载失败静默降级为程序化几何组合
- 天宫空间站程序化 T 字构型模型（P7 §3.1）：无 NASA 公版模型且未找到开放许可社区模型，按需求降级为程序化几何组合（天和核心舱 + 问天/梦天实验舱圆柱体 + 柔性太阳翼平板），差异登记于 data/models.ts 与组件文件头
- 人造卫星近观 LOD 与懒加载门控（P7 §3.1 硬性）：远观保留现有轻量盒体（成本不变）；仅 L1 语境且相机-卫星距离进入近观阈值（复用 P4 滞回状态机）时懒加载 glTF 并与盒体交叉淡化切换（0.35 秒无突变）；离开 L1 语境立即释放模型显存，应用卸载时统一释放；首屏无模型网络请求（无头实测确认按需加载）
- 人造卫星姿态系统（P7 §3.1）：ISS/天宫/TDRS 对地定向（模型 −Y 始终指向地心、+X 沿飞行方向，位置/速度构造正交基精确实现，单元测试覆盖）；帆板绕单轴对日跟踪（程序化模型标记组驱动，简化近似已登记；glTF 模型帆板烘焙在整体网格中不单独跟踪，登记）；哈勃保持惯性固定姿态（镜筒指向恒定方向）
- 人造卫星地影变暗（P7 §3.1 可选项）：卫星进入地球本影时模型变暗（圆柱本影近似 + 半影 smoothstep 软化，本影保留 18% 环境底光防全黑，近似已登记于 utils/earthShadow.ts），纯逻辑单元测试覆盖
- 人造卫星材质光照（P7 §3.1）：近观模型为纯反射光照（移除自发光——真实航天器不发光），光照方向与行星 terminator 统一（场景原点太阳点光源）；远观轻量盒体保留微自发光保证可见性（登记）
- 人造卫星差异化视觉尺寸（P7 §3.2）：本体半径由固定 0.06 改为按真实特征尺寸对数分级映射（ISS 109 m > 天宫 55 m > TDRS 翼展 21 m > 哈勃 13.2 m，层次可辨），信息面板新增"真实特征尺寸"对照行；真实比例模式维持真实线性映射（不可见属科学事实，帮助信息已说明）
- 人造卫星轨道分层调整（P7 §3.2，登记）：近地轨道段（200–600 km）由对数压缩改为线性放大——原公式下天宫（390 km）与 ISS（417 km）轨道仅差 ~0.005 场景单位不可分辨，现两者径向间隔 ≥0.05 单位分层可辨；600 km 以上维持对数压缩（静止轨道仍明显低于月球轨道层）
- 人造卫星纳入天体切换序列（P7 §3.4）：`BODY_CYCLE_SEQUENCE` 于月球后插入国际空间站 → 天宫 → 哈勃 → 静止轨道卫星，序列由 16 扩展至 20 天体，`[`/`]` 快捷键与切换控件可直达并自动跟随，HUD 序列位置计数同步更新（如"天宫空间站 6/20"）
- 人造卫星飞往观察距离适配（P7 §3.4）：由"轨道半径×0.8（下限 2.2）"改为固定近观距离 1.6 单位（不低于遨游模式最近距离），配合近观放大系数（相机贴近时模型平滑放大至 3 倍，smoothstep 无跳变，视觉夸大已登记）使模型充满合理视野并触发 glTF 加载
- P7 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L1 ISS 近观（glTF + 地球 4K 纹理同屏最重场景）60.2 FPS / 天宫近观 60.1 FPS / TDRS 近观 60.1 FPS / L2 太阳系全景 60.1 FPS，满帧无跌破；JS 堆 ~25 MB（与 P5 基线持平）；模型按需加载（近观才请求，首屏无新增网络请求）
- 新增银心参考系观察模式（P6，需求第二部分 §3.1.1）：L3 银河系视角下新增"银心固定"参考系——银心居场景原点、太阳系标记沿固定轨道线实际移动（"标记在轨道内跑"直接可见），与现有"跟随太阳系"模式互补；快捷键 `G` 切换（仅 L3 生效）、HUD 快捷按钮，两模式 2 秒 easeInOutCubic 平滑过渡、同一模拟时间轴无跳变；HUD 参考系说明实时更新（"银心系（跟随太阳系）"/"银心系（银心固定）"）；坐标变换纯逻辑抽取 `utils/galacticFrame.ts`（groupOffset=−(1−w)·sunWorld、markerScenePos=w·sunWorld 混合式，不新增任何场景对象，仅位姿计算切换）
- 强化跟随模式运动线索（P6，需求 §3.1.2）：未来预测线由"整个银河年近闭合圆"改为**前方 1/4 银河年非闭合弧段**（随时间滚动刷新，与历史尾迹首尾衔接不重叠）；轨道叠加 48 个圆形软边流动光点（沿运动方向以太阳实际角速度 2π/银河年 流动，使"银河系相对滑动"可感知）；垂直振荡视觉放大 6 倍（默认模式，登记于 `utils/galacticMotionCues.ts`；真实比例模式为 1 不放大）使波浪起伏可辨；"You are here"标记附加银盘面高度指示线 + HUD 实时高度 ly 数值；纯逻辑抽取 `utils/galacticMotionCues.ts`（预测弧段采样/流动刻度相位/垂直增益）并补单元测试
- 银河年与旋转曲线数据自洽修正（P6，需求 §3.1.2）：原"银河年 230 Myr ↔ 26000 ly ↔ 220 km/s"三者角速度存在约 3.3% 偏差；固定 T=230 Myr 与 v=220 km/s（HUD 呈现的圆整值），令 R=v·T/(2π)≈26863 ly≈8.24 kpc（落在 IAU R₀≈8.0–8.3 kpc 观测区间内），使 diskAngularSpeedRadPerMyr(R) 精确等于 2π/T，偏差消除；修正与来源登记于 `utils/galaxy.ts` 文件头，相关单测同步更新
- 全局粒子圆形软边贴图修复（P6，需求 §3.2）：新增共享圆形软边粒子贴图（`createSoftPointCanvas` + `sharedTextures` 进程内缓存），为全部 `PointsMaterial`（蓝巨星/WR 星风、昴星团、M13 球状星团、宇宙网、麦哲伦星流、轨道流动刻度）设置 `map`，消除方形粒子
- L3 恒星表面 shader 升级（P6，需求 §3.2）：红巨星/蓝巨星/造父一/WR 124 由纯色球升级为参数化表面 shader——对流颗粒 fBm（随时间缓慢演化，红巨星大对流胞 / 蓝巨星细颗粒）+ 边缘昏暗（limb darkening 线性定律 N·V）+ 色温梯度（边缘偏暗红）；球体分段由 12–24 提升至 32–48（近观无棱角）；GLSL 与纯函数镜像 `utils/stellarSurface.ts` 一致，按可见性门控（不可见跳过 uniform 更新，L1/L2/L4 零开销）
- L3 白矮星与脉冲星升级（P6，需求 §3.2）：天狼星B 增加程序化衍射芒线贴图（`createDiffractionSpikeCanvas`，致密高亮点星观测质感，保持双星互绕）；蟹状星云脉冲星双极射束由纯色 cone 升级为带径向渐变 + 沿轴湍流噪声 + 流动等离子体团块的 shader 材质（扫过视线的脉冲闪烁保持）
- 程序化星云纹理生成器（P6，需求 §3.2）：新增 `utils/nebulaTexture.ts`（分层 fBm + 域扭曲生成不规则云状/环壳/丝状遗迹壳 RGBA 纹理，参数化颜色梯度/丝状强度/不规则度/双色层，确定性种子，≤512px）+ `nebulaTextures.ts` DataTexture 工厂（按参数缓存、生成一次复用、无 DOM 依赖）；据此重构五大星云：猎户座（多层不规则云 + Trapezium 聚星）、蟹状（红色氢丝网络 shell，与超新星遗迹共用路径）、环状 M57（内 OIII 蓝绿/外 Hα 红色层 ring）、马头（暗云柱剪影 + 背景 IC 434 红光）、昴星团蓝色反射云（丝缕感）
- L3 球状星团 M13 星族色板（P6，需求 §3.2）：由单一颜色改为红黄为主的老年星族 + 约 8% 蓝离散星混合（HST 测光），粒子随机亮度分布
- 银河系整体视觉提升（P6，需求 §3.3）：核球/银晕辉光由纯径向渐变圆斑升级为 fBm 噪声扰动多层辉光（`createBulgeGlowCanvas`）；银盘 shader 增加旋臂 HII 区团块串珠（沿臂脊噪声聚类增亮）与旋臂内侧尘埃带示意（亮度不对称衰减），粒子总量维持 4 万级不扩容
- L4 河外天体细节提升（P6，需求 §3.4）：四类星系程序化纹理升级——旋涡星系加尘埃带暗弧 + HII 区亮点、椭圆星系光度沿 de Vaucouleurs r^(1/4) 轮廓多层衰减、不规则星系维持团块；M31（大核球 + 紧致旋臂 + 显著尘埃环）/M33（松散絮状旋臂 + 大量 HII 区）专属形态与通用旋涡星系区分；类星体/M87 喷流由纯色几何升级为带节点（knots）与沿轴湍流噪声的 shader 材质（M87 HST-1 节点观测特征），流动动画保持
- P6 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L3 跟随模式 60.0 FPS / L3 银心固定模式 60.1 FPS / L4 宇宙视角 60.1 FPS，满帧无跌破；全部升级为程序化生成（canvas/shader），无新增位图网络请求，首屏 < 5 秒不回退；无头 Chrome 目验 L3 银心固定模式下太阳系标记沿轨道可见移动（银河年进度 3.2%→11.6%→23.2% 连续推进、银盘面高度 +183→+206→−299 ly 起伏可辨）、两模式切换平滑无跳变

- 实现太阳光球动态表面（S1，IMPROVEMENT_REQUIREMENTS_SOLAR §4.2）：由静态贴图发光球升级为自定义 shader——米粒组织（球面 3D fBm 对流胞"沸腾"翻滚，随模拟时间演化、暂停冻结；真实寿命 5–10 分钟按速率钳制降速呈现，胞尺度视觉粗化已登记）+ 临边昏暗（V 波段线性系数 u≈0.6，Cox 2000，盘面中心亮边缘暗）+ 边缘色温梯度（边缘偏暗红）；纯逻辑镜像 `utils/sunSurface.ts` 单元测试覆盖 100%
- 实现太阳色球边缘红环（S1）：光球外放大壳层（真实厚度 ~0.3% 半径，视觉放大至 +1.5% 已登记）菲涅尔红色辉光（氢α 656.3 nm），仅近观语境平滑淡入（日全食观测形态参考）
- 实现太阳结构化日冕（S1）：近观淡入日冕广告牌 shader——径向指数衰减 + 赤道方向冕流拉长（极小期典型形态）+ 角向噪声条纹缓慢演化；远观维持原低成本 sprite 光晕观感不变，近观时 sprite 光晕平滑收敛让位结构化日冕（分级呈现无突变）
- 太阳接入近观细节门控与 4K 纹理（S1，§5.2 硬性）：复用 P4 滞回门控状态机（相机-太阳距离进入阈值才请求 4K 层、2K 先显示防空窗、textureManager LRU 保留、离开 L1 语境立即释放显存）；新增 `4k_sun.jpg`（SSS 官方"8K"下载实为 4096×2048，与木星/土星情况一致已核实登记，CC BY 4.0）
- S1 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：太阳近观（米粒 + 色球 + 结构化日冕 + 4K 全开）60.4 FPS / 拉远回 L2 60.2 FPS，满帧无跌破，页面无控制台错误；4K 纹理确认按门控懒加载（近观才发起网络请求）

- 实现太阳黑子系统（S2，IMPROVEMENT_REQUIREMENTS_SOLAR §4.3-1）：光球 shader 叠加本影（~3,500–4,500 °C 低色温深暗核）+ 半影（放射状纤维过渡区）；黑子成对出现（前导/后随，信息面板科普 Hale 极性定律）、分布于 ±35° 中低纬带、生命周期数天至数周（确定性伪随机槽位哈希，可复现、无每帧 Math.random）；黑子角半径视觉放大 2–3 倍以保证近观可辨（登记于 `utils/sunspots.ts`）
- 实现太阳较差自转（S2 §4.2）：角速度剖面 ω(φ)=A+B·sin²φ+C·sin⁴φ（Snodgrass & Ulrich 1990——赤道 25.4 天 / 纬度 60° 30.9 天 / 极区 ~34 天），黑子按所在纬度以正确角速度移动（CPU 侧 float64 绝对相位）；纹理 UV 差速采用"相对赤道剪切 + 32 天窗口回卷"（防 float32 精度失效的统计近似，登记于 `utils/solarRotation.ts`）
- 实现太阳耀斑事件系统（S2 §4.3-2，复用超新星事件范式）：泊松过程自动触发（平均间隔 60 天模拟时间，理想化频率登记）+ 控制面板手动演示按钮；按软 X 射线通量分级 C/M/X；位置锚定活动区（黑子群）附近；阶段动画快速增亮 → 峰值闪光（局部 shader 增亮 + 辉光 sprite，Bloom 自然联动）→ 指数衰减（事件时长 1.6 模拟天，L1 语境下约 10 秒，真实几分钟至几小时的加速处理已登记）；UI 通知含级别与"飞往观看"按钮及"数十亿颗氢弹能量"科普；音效联动短促低频冲击
- 实现日冕物质抛射 CME（S2 §4.3-3）：9,000 粒 GPU 粒子壳层泡状扩张（顶点着色器推进，环形缓冲回收防内存增长），速度按真实量级 250–3,000 km/s 换算场景速度（无钳制，暂停可定格观察壳层形态）；触发方式三种——耀斑联动（关联概率 X 级 90% / M 级 40% / C 级 10%，Yashiro et al. 2005）+ 独立低概率触发（平均 180 天）+ 手动演示按钮；朝向地球的 CME 事件通知附加地磁暴科普（"影响卫星、电网和通信，约 1–3 天后抵达地球"）；音效联动低频涌动
- 实现太阳风常驻粒子流（S2 §4.3-4）：6,000 粒全方向低密度径向外流（400–800 km/s 量级，快慢风混合），亮度克制不喧宾夺主，L2 可感知微弱流动、L1 近观更明显；相位回卷防精度失效；信息面板科普成分（质子/电子）与太阳圈保护作用
- 实现日珥与日冕环（S2 §4.3-6 / §4.2）：3 条边缘环状日珥（红色氢α 色调，缓慢形变演化，数天至数月生命周期）+ 5 条日冕环发光弧（锚定活动区/黑子足点附近，随较差自转移动），池化 TubeGeometry 复用（渲染循环零分配）；日珥高度视觉夸大登记于 `utils/solarActivity.ts`
- 实现太阳内部结构剖面模式（S2 §4.1）：L1 近观语境下控制面板/信息面板开启 1/4 楔形切除视图（开合过渡 ≤1.2 秒平滑），从内到外六层可辨识（核心 0–0.25 R☉ 白热脉动 / 辐射区 0.25–0.7 R☉ 橙黄渐变径向光纹 / 对流区对流胞循环 / 光球 / 色球 / 日冕），切面色带与温度梯度对应（标准太阳模型 Christensen-Dalsgaard et al. 1996）；各层可点选弹出科普卡片（核聚变质子-质子链、光子随机游走数万年、差旋层 tachocline 磁场发电机等）；剖面模式下外部活动特效（耀斑/CME/太阳风/日珥）互斥淡出，避免视觉干扰与性能叠加
- 太阳信息面板与帮助扩展（S2 §4.5）：太阳条目新增结构分层、表面/日冕温度对比（含"日冕加热问题"反直觉科普：百万度远高于表面 5,772 K，机制未解）、较差自转、黑子（Hale 极性）、太阳风行；活跃耀斑/CME 实时状态行（级别/速度/朝向地球提示）；HelpHint 补充太阳观察指引与艺术化说明（活动频率加速、黑子/日珥尺寸夸大、色球厚度放大）；数据来源补充 Snodgrass & Ulrich (1990)、Klimchuk (2006)
- S2 性能与资源登记：太阳风 + CME 峰值 15,000 粒（≤ 20,000 预算）；事件粒子按层级可见性门控（不可见跳过演算与 uniform 更新）；所有活动共享全局模拟时间轴（暂停冻结、时间跳变无残留）；新增对象卸载时正确 dispose
- S2 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L1 太阳近观（黑子 + 太阳风 + 日珥/日冕环全开）60.0 FPS / 耀斑峰值 60.4 FPS / CME 扩张 60.1 FPS / 剖面模式 60.2 FPS / 拉远 L2 60.2 FPS，满帧无跌破；4K 纹理仍按门控懒加载；截图核对黑子本影/半影、耀斑增亮、CME 壳层（暂停定格 263 km/s 泡状扩张）、剖面六层与点选卡片
- 实现太阳 11 年活动周期模型（S3，IMPROVEMENT_REQUIREMENTS_SOLAR §4.4，纯逻辑 `utils/solarCycle.ts`）：理想化 11 年正弦包络（真实 9–14 年且强度不可预测已登记），相位锚定真实第 25 太阳活动周期（2019-12 极小 / 2024-10 极大，SILSO/NOAA SWPC 来源），打开应用即处于正确相位；输出周期序号、相位名（上升期/极大期/下降期/极小期）、黑子相对数包络（0–1，不对称上升快下降慢）、耀斑/CME 频率调制因子、日冕形态各向同性因子、蝴蝶图黑子生成纬度
- 周期联动（S3 §4.4）：黑子激活对数随包络增减（极小期约 1 对 / 极大期满 5 对，`utils/sunspots.ts`）；黑子生成纬度按蝴蝶图相位插值（周期初 ±30° 高纬 → 周期末 ±5° 赤道，Spörer 定律）；耀斑/CME 泊松均值按频率因子缩放（极大期更频繁、极小期更稀疏，`SunActivity.tsx`）；日冕冕流各向异性参数化（极小期赤道集中 → 极大期全纬度铺开，`utils/sunSurface.ts` 冕流因子）
- HUD/太阳信息面板显示活动周期状态行（S3 §4.4）："第 N 周期 · 相位名 · 黑子相对数示意（进度条 + 百分比）"，随快进实时演变；太阳目录新增"日冕洞""活动周期"科普行
- 实现光斑（faculae，S3 §4.2）：黑子群周边亮斑（磁场较弱处对流增强的高温亮区），光球 shader 沿用黑子槽位数据在黑子外环带增亮、临边加权，数量随周期黑子门控自动联动（`sunSurface.faculaeBoost` 镜像）
- 实现超米粒组织（S3 §4.2）：~30,000 km 尺度低频 fBm 亮度调制叠加于米粒之上，仅近观淡入（`sunSurface.supergranulationModulation` 镜像，非速度场真实模拟已登记）
- 实现针状体示意（S3 §4.2）：色球边缘高频噪声锯齿状 alpha 扰动（细小针状喷流示意，未加几何——需求约束，`sunSurface.spiculeRimPerturbation` 镜像）
- 实现日冕洞示意（S3 §4.2）：日冕 shader 极区方向角向暗斑（开放磁力线区域，`sunSurface.coronalHoleDarkening` 镜像）+ 该方向太阳风粒子快风加速（快慢风方向加权，~800 vs ~400 km/s，`solarActivity.windSpeedFactorForDirection`，粒子相位速度因子烘进 attribute 零分配）；信息面板科普日冕洞与高速太阳风来源
- 实现暗条（filament，S3 §4.3-6）：黑子对磁中性线附近日面暗色细线（日珥在日面的投影，冷等离子体吸收），光球 shader 低成本细带暗化（`sunspots.filamentDarkening` 镜像）
- 实现爆发日珥 → CME 前导（S3 §4.3-6）：CME 触发时选取最接近抛射方向的日珥先行拉升脱离作为前导（联动动画，复用现有日珥池，`solarActivity.prominenceEruptionLift`）
- 实现 CME 抵达地球极光联动（S3 §4.3-3）：朝地球 CME 按真实传播时间（1 AU ÷ 事件速度，约 1–3 模拟天，`solarActivity.cmeArrivalDelayDays`）延迟触发，地球极区大气短暂增亮示意（`Planet.tsx` 绿色高层大气辉光层，克制可退化，`solarActivity.auroraEnhancement01`）+ "CME 已抵达地球"事件通知
- 实现日球层顶示意（S3 §4.3-4）：L2 太阳系视角外缘半透明球壳 + 标注（真实约 120 AU，Voyager 1/2 于 2012/2018 穿越，压缩示意半径已登记于 `utils/heliopause.ts`）；可点选查看信息面板科普
- 实现黑子群/日珥单独点选热区（S3 §4.5）：不可见球体热区随特征位置更新，点选弹出科普卡片；黑子卡片含"该黑子约可容纳约 N 个地球"动态换算（按放大前真实角半径 → 日面真实面积 / 地球截面，`sunspots.sunspotEarthCount`，放大倍数 2.5 已登记）
- 实现 L1 太阳近观"沸腾"颗粒噪声音景（S3 §4.6）：`sun-hum` 低频轰鸣之上叠加带通噪声 + 缓慢 LFO 起伏的沸腾层，增益随 L1 近观强度与活动周期相位微调（`audioMixer.sunBoilLayerGain` 纯逻辑 + `audioEngine` 常驻循环噪声层，亮度克制，真空无声艺术化声明）
- S3 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L1 太阳近观（周期黑子 + 光斑 + 超米粒 + 针状体 + 暗条 + 日冕洞全开）60.2 FPS / 耀斑·CME 60.1 FPS / 拉远 L2（日球层顶可见）60.2–60.4 FPS，满帧无跌破；快进 100× 目验周期演变（2026-08 → 2027-02 黑子相对数 72% → 62% 单调下降，第 25 周期下降期，进度条随之收缩）；黑子点选卡片"可容纳 15 个地球"、CME 抵达地球极光通知、日球层顶标注均截图核对；粒子峰值仍 15,000 ≤ 20,000 预算（快风为方向速度加权，未新增粒子）；仅 1 处预存 404（无关纹理），无 S3 相关控制台错误
- 实现黑子群 McIntosh 复杂度分级（S4-A1，IMPROVEMENT_REQUIREMENTS_SOLAR §4.7-A1）：由固定"前导 + 后随"2 颗升级为按确定性哈希赋 McIntosh 简化分级的黑子群——单极群（A，仅 1 前导）/ 双极群（B，前导 + 后随）/ 复杂群（C，前导大黑子 + 后随 + 2–3 颗沿磁中性线散布的卫星黑子，构成不规则多极簇）；shader 渲染上限由 10 颗扩容至 24 颗（`SUNSPOT_MAX_RENDERED`，仍在光球 shader 循环预算内），Sun.tsx uniform 数组长度与循环同步扩容；面板黑子分类以三档复杂度示意（真实三字母 Zpc 分类简化已登记，来源 McIntosh 1990 / Solanki 2003）
- 实现黑子不规则形态与丝状半影（S4-A2，§4.7-A2）：本影边界以低频角向噪声（3、5 次谐波叠加）扰动成不规则多边形（非正圆，每黑子独立方位相位种子避免同形）；半影改为径向拉伸的放射状细丝纹理（penumbral filaments，角向高频条纹 × 径向 sin 包络），纯逻辑镜像 `sunspots.umbraIrregularRadius` / `sunspots.penumbraFibrilOffset`，无新增几何/纹理
- 实现黑子群形态演化（S4-A3，§4.7-A3）：群角半径随生命周期包络生长/衰退（生成期较小、成熟期最大、衰退期缩小解体，`sunspotGrowthScale`）；前导-后随经度间隔随生命周期自行分离加大（真实黑子群东西向拉开，`sunspotProperMotionFactor`）；复杂群衰退时外围卫星黑子先于主偶极消散（按序号错开消散阈值，`sunspotSatelliteStrength`）；零新增渲染成本
- 实现双带耀斑（S4-B1，§4.7-B1）：耀斑增亮由单点圆窗升级为沿磁中性线两侧的两条带状增亮（two-ribbon，符合观测典型形态），带心三角窗 × 沿带 sin 包络两端渐隐；纯逻辑镜像 `solarActivity.flareRibbonBoost`，光球 shader 取与耀斑源方向最接近的活动区群中性线（前导→后随）计算双带几何，峰值超 Bloom 阈值自然泛光（来源 Yashiro et al. 2005）
- 实现活动区磁环族（S4-E2，§4.7-E2）：复杂黑子群上方渲染多重同源日冕环拱（arcade）——按群复杂度（群内黑子颗数）决定环数（单极 1 / 双极 2 / 复杂群最多 4 环，`coronalLoopCountForGroup`），沿磁中性线法向均匀铺开（`coronalLoopArcadeOffset`）、中央环最高两侧略矮的拱形高度包络（`coronalLoopArcadeHeightScale`）；日冕环池上限由 5 扩容至 20（5 群 × 最多 4 环，池化 TubeGeometry 复用，渲染循环零分配）
- S4 单元测试：新增 `sunspotsS4.test.ts`（36 例：McIntosh 分级/群展开/A2 不规则本影与丝状半影/A3 生长衰退与卫星消散/群级中性线填充）+ `solarActivityS4.test.ts`（21 例：双带耀斑对称性与峰值边界/磁环族环数与环拱几何/池预算）；全量 1345 用例 / 87 套件通过，覆盖率 gate ≥90% 保持（语句 97.79% / 分支 97.33% / 函数 98.48% / 行 99.38%）；type-check / lint / build 全绿
- S4 性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L1 太阳近观（复杂黑子群 + 不规则本影/丝状半影 + 双带耀斑 + 磁环族全开）稳态 60.0 FPS / 双带耀斑 X5.1 峰值 60.2 FPS / CME 60.2 FPS，满帧无跌破；截图核对复杂黑子群与不规则本影、双带耀斑沿中性线两条增亮带、复杂群上方多重同源环拱（arcade，0.69 AU 近观清晰可辨）；粒子峰值仍 15,000 ≤ 20,000 预算（本档未新增粒子）；无 S4 相关控制台错误
- 实现 CME 三分量结构（S4-C1，§4.7-C1）：粒子分层着色还原经典 CME 形态——亮前沿壳（最外，被扫积压缩的日冕物质，最亮偏暖白）+ 暗腔（中层磁通量绳低密度空腔，0.35× 显著偏暗）+ 亮核（最内，抛出的日珥物质）；分层概率/径向位置/亮度以确定性随机烘进 attribute（`cmeParticleLayer`/`cmeLayerRadialFactor`/`cmeLayerBrightness`），顶点着色器按层因子径向分层，零新增粒子与运行时分配（来源 Illing & Hundhausen 1985；Vourlidas et al. 2013）
- 实现 CME 加速段运动学（S4-C2，§4.7-C2）：喷发初始匀加速（位移 ∝ t²）后转匀速，替代纯匀速——以"等效已行进时间"换算（`cmeAcceleratedElapsedDays`，加速段占名义抵达时长 12%，段末与匀速段位移/速度连续），生命周期完成判定与渲染共用同一运动学（来源 Zhang et al. 2001 CME 三阶段运动学）
- 实现太阳风帕克螺旋（S4-D1，§4.7-D1）：太阳风流线随太阳自转弯曲成阿基米德螺旋——顶点着色器对粒子方位附加偏转 Δφ = −Ω·t_travel·phase（`parkerSpiralOffsetRad`/`parkerWindingTurns`，赤道自转率 25.4 天/周），日冕洞快风粒子行进时间短、流线更直，慢风更弯（真实快慢风螺旋差异；来源 Parker 1958）
- 实现快慢风交界 CIR 示意（S4-D2，§4.7-D2）：快风追赶慢风在交界处压缩形成共转相互作用区——日冕洞锥缘 ±0.22 rad 交界带内粒子亮度增强（最高 +80%，`cirBrightnessFactor` 烘进 attribute，零分配；来源 Pizzo 1978）
- 实现耀斑多峰光变（S4-B2，§4.7-B2）：耀斑光变由单峰指数升级为多段曲线——脉冲相尖峰（前 10% 快速起落，硬 X 射线爆发示意，峰值 0.7）→ 短暂回落 → 主峰（进度 28% 处软 X 射线主极大 1.0）→ 指数余辉（`flareMultiPeakIntensity01`，光球增亮/辉光 sprite/双带共用；`flareIntensity01` 保留供 CME 联动阈值，来源 Fletcher et al. 2011）
- 实现耀斑后环（S4-B3，§4.7-B3）：耀斑峰后（进度 35% 起）活动区上方拱起 post-flare loop arcade——磁重联后冷却回落的明亮环系（`postFlareLoopStrength01` 快速拱起 → 缓慢消退 → 事件末平滑归零无跳变），锚定耀斑源群中性线（与双带耀斑同锚点），复用日冕环池优先占位（5 环，更高拱顶 + 亮白色调 `#fff3d0` 区分常态环，零新增几何；来源 Švestka 1996）
- 实现日珥纤维结构（S4-E1，§4.7-E1）：日珥由均匀弧面升级为沿磁力线排列的细丝形态——每条日珥独立 ShaderMaterial 沿弧参数条纹调制不透明度（`prominenceFibrilFactor`），按锚点种子确定性区分宁静日珥（细密柔和，幅度 0.25）与活动日珥（粗动荡，幅度 0.5），细丝相位随模拟时间缓慢演化（来源 Mackay et al. 2010）
- 实现盔状冕流与极羽（S4-E3，§4.7-E3）：日冕 shader 叠加赤道盔状冕流尖顶（`helmetStreamerFactor` 高次幂锐化赤道加权，极小期尖锐盔状 → 极大期渐弥散，与周期各向同性因子联动）+ 极区羽状射线（`polarPlumeBrightness`，极轴锥内角向条纹细窄射线；来源 Koutchmy 1988 冕流形态、日全食观测）
- 实现米粒暗巷网络与网络磁场亮点（S4-F1，§4.7-F1）：光球 shader 强化米粒胞间下沉冷物质暗边界（fBm 低值区额外加深 22%，`intergranularLaneDarkening`，仅近观淡入，远观观感不变）+ 超米粒边界磁场聚集的网络亮点（`networkBrightPointBoost`，超米粒 fBm 高值边界带增亮；来源 Nordlund et al. 2009）
- S4 进阶/精修档单元测试：`solarActivityS4.test.ts` 扩至 49 例（+28：C1 分层/C2 加速段连续性与单调性/D1 螺旋偏转与缠绕圈数/D2 CIR 交界带/B2 多峰形态与余辉衰减/B3 后环拱起消退/E1 纤维幅度分档）+ 新增 `sunSurfaceS4.test.ts` 16 例（E3 盔状锐化与极羽锥内包络/F1 暗巷阈值与网络亮点单调性）；全量 1389 用例 / 88 套件通过，覆盖率 gate ≥90% 保持（语句 97.74% / 分支 97.19% / 函数 98.53% / 行 99.40%）；type-check / lint / build 全绿
- S4 进阶/精修档性能实测登记（无头 Chrome，Apple Silicon Metal，1280×800，Bloom 开默认态，垂直同步）：L1 近观（盔状冕流 + 极羽 + 暗巷 + 网络亮点全开）60.2 FPS / 耀斑多峰 + 后环拱 60.2 FPS / CME 三分量 + 加速段 60.2 FPS / L2 帕克螺旋太阳风 60.3 FPS，满帧无跌破；截图核对耀斑后环亮白环拱（M7.5 事件峰后边缘清晰可辨）、CME 分层扩张、周期事件自动触发与联动通知；粒子总量不变（三分量/CIR/螺旋均为既有粒子 attribute 分层与顶点着色器几何，零新增粒子）；无控制台错误
- 实现银河系—仙女座碰撞合并后续演化（R2-11，用户反馈点 7）：合并预览快进到 45 亿年后不再"原地重叠停滞"，完整呈现 穿越 → 回摆 → 潮汐扭曲 → 星暴 → 终态椭圆星系 Milkomeda 序列，且全程为模拟时间的确定性纯函数（时间可逆）。A) 演化状态机（§11.1）——新增纯逻辑 `utils/galaxyMerger.ts`：`mwM31SignedSeparationLy` 签名分离距离替换旧 clamp≥0 语义（合并前委托 `universe.mwM31ApproachSeparationLy` 同源接近曲线；T0 后阻尼振荡 s(τ)=−A·e^(−λτ)·sin(2πτ/P)·(1−椭圆插值)，T0 处连续过零——M31 越过原点到另一侧减速远离，半周期整数倍处回摆过零、每半周期幅度衰减至 45%，约 2 次可辨往返，终态严格归零核心并合）；首摆幅度 ≈1e5 光年取 van der Marel et al. 2012 首次远心点量级，回摆周期 160 Myr/并合完成 T0+400 Myr 为时间压缩登记值（真实约 20–30 亿年；L4 默认压缩比 2000 万年/秒下全程约 20 秒、终态过渡约 10 秒，落在需求 5–10 秒区间）；`mergerStage` 五阶段状态机（approaching/first-passage/oscillation/coalescing/merged，τ 分段）。B) 形态插值与潮汐扭曲——`mergerEllipticalMix01`（smoothstep 200→400 Myr）驱动银盘粒子顶点着色器终态椭球化（盘面按半径比例增厚至轴比≈0.5、旋臂密度波/HII 团块/棒增亮调制抹平、色调偏老年恒星红黄、尘埃带随气体耗尽淡出）；`mergerTidalDistortion01`（接近显现 (1−|s|/5e5 ly) × (1−椭圆插值)）驱动银盘沿 MW–M31 连线拉伸（外盘更强潮汐尾示意 + 外盘朝伴星系侧偏置潮汐桥示意，连线方向经倾斜逆旋转一次性预计算、穿越侧符号每帧乘入）+ M31 贴图平面非均匀缩放拉伸（1+0.55t / 1−0.28t）；全部粒子扰动走顶点着色器 uniforms（零新增粒子、CPU 零逐粒子分配，Galaxy.tsx 文件头预算登记）。C) 星暴示意——`mergerStarburst01`：每次核心穿越（τ=0/80/160 Myr）处高斯脉冲（σ=18 Myr）叠加，权重 1/0.55/0.3 随气体耗尽递减：银盘粒子蓝白增亮（vWave ×(1+1.3b) + 冷色偏移）、合并辉光 sprite 闪亮放大 + 蓝白偏色、M31 贴图蓝白偏色（艺术化登记于文件头）。D) 终态收尾——M31/M32/M110 贴图与标签随椭圆插值淡出并入 Milkomeda（椭球终态由银河系粒子盘承载），合并辉光过渡为核心暖红黄辉光；接近虚线/流动光点仅接近段显示（穿越后语义失效隐藏，时间回退自动恢复）。E) HUD/信息联动（§11.1）——3D 场景标签按阶段更新（首次穿越——两盘交错而过/回摆振荡——动力摩擦下的减幅往返/核心并合中——旋臂瓦解/并合完成：椭圆星系 Milkomeda）；L4 越过合并时刻显示演化科普卡片（阶段 + 合并时刻后经过时间 + 太阳系命运：大概率被潮汐甩入外围、恒星间直接碰撞概率微乎其微 + van der Marel 2012 / NASA GSFC 来源与时间压缩登记）。F) 同源回归——`cameraFocus.galaxyScenePosition` m31/m32/m110 分支改用 `mwM31SignedSeparationSceneUnits`（跟随/飞往解析与渲染位置一致，含穿越后另一侧）。新增 `galaxyMergerR211` 单测 25 例（τ 换算/接近段同源与旧函数合并前一致/T0 连续过零/穿越后另一侧减速远离/回摆过零与减幅/终态严格归零/包络单调与 45% 衰减率/形态插值 smoothstep 边界中点单调/星暴三峰权重与 [0,1] 钳制与前沿对称/潮汐扭曲接近渐强回摆持续终态归零/阶段边界/文案联动与登记文案/场景单位符号透传/确定性与时间回退复原），全量 1766 用例/105 套件通过，覆盖率 gate ≥90% 保持（语句 98.08%/分支 97.83%/函数 98.79%/行 99.44%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）16/16 项通过：预览快进末段潮汐渐显（r211-01）→ T0 星暴闪亮（r211-02）→ 穿越后 M31 另一侧远离（r211-03）→ 回摆振荡（r211-04）→ 核心并合中旋臂瓦解（r211-05）→ 终态 Milkomeda 椭球暖色粒子云（r211-06），"恢复预览前时间"后倒计时标签/旋臂形态/演化卡片全复原（r211-07），重复预览确定性到达同一终态（r211-08），演化期实测 FPS 60.0–60.1 不跌破、零控制台错误
- 实现 L4 轨迹与运动一致性（R2-10，用户反馈点 9）：落地"凡显示轨迹线的天体必沿该轨迹运动（同源公式）、凡运动的天体轨迹线可开关"通用原则。A) 卫星星系轨道线（§10.1）——为 LMC/SMC/人马座矮星系绘制细线轨道（256 段，随"轨道线开关 O"控制），轨道线与运动位置共用 `utils/universe.orbitPointLy` 唯一公式（`satelliteOrbitPointsLy` 与 `satelliteGalaxyPositionLy` 同源，禁止两套参数，单测逐点断言同角度值一致）；B) direction 一致性修复——`satelliteGalaxyPositionLy` 由"x-z 面圆 + phase0 + 绕 x 轴倾斜（忽略数据 direction）"重构为 direction 自洽轨道基（`satelliteOrbitBasis`：u=归一化 direction、v=水平/极向切向按倾角混合，t=0 首帧位置 = direction×distance），消除静态首帧与动态位置的跳变及信息面板天区文案矛盾（`SATELLITE_GALAXY_ORBITS` 移除 phase0Rad；inclinationDeg 重定义为轨道平面姿态参数 0=贴近银道面/90=极轨道，登记）；C) 人马座矮星系运动——从完全静止改为沿极轨道缓慢运动（周期 900 Myr 示意，真实估计约 8.5–10 亿年 Law & Majewski 2010，登记）+ 潮汐流粒子拖尾（`tidalStreamPointsLy` 泛化：前导臂 +260 Myr / 尾随臂 −420 Myr 沿轨道等时距采样 + 离星系越远弥散越大，360 粒 ≤ 需求上限 1,500，老年恒星暖色与麦哲伦星流冷蓝区分；Ibata et al. 2001 来源登记），呼应"正被潮汐撕裂"文案；麦哲伦星流委托同一 `tidalStreamPointsLy`（与 LMC 运动同源，暂停时跳过重采样）；D) 星流澄清——信息面板星系卡片新增"运动（模拟）"行（`GALAXY_MOTION_NOTE_ZH` 逐星系登记：LMC/SMC 沿细线轨道+星流为历史路径剥离的气体非轨道线、M32/M110 随 M31 属预期、M33/M87 静态属预期），HelpHint 补宇宙网除哈勃膨胀外静止属预期登记；E) M31 接近进度感——接近虚线上新增 6 个等相位流动光点（`m31ApproachFlow01`，自 M31 端流向银河系端、周期 7 s 真实秒 UI 节奏，流速非物理量登记——对数距离压缩下 110 km/s 不可感知），复用 Galaxy.tsx 流动刻度模式；F) 相机同源回归——`cameraFocus.galaxyScenePosition` 卫星星系分支改用新公式并纳入人马座矮星系（跟随时相机解析与渲染逐分量一致，单测三时刻断言）。新增 `universeR210` 单测 17 例（轨道基正交归一/退化回退/首帧=direction×distance×3 星系/60 采样时刻严格落在轨道圆上（半径恒定+共面）/轨道线与运动同角度逐点一致/人马座 100 Myr 位移 > 半径 50%/潮汐流前导尾随端点与确定性/流动光点等相位与回绕/非法输入 RangeError）并更新既有受影响用例，全量 1741 用例/104 套件通过，覆盖率 gate ≥90% 保持（语句 98.09%/分支 97.81%/函数 98.76%/行 99.46%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）8/8 项通过：L4 快进 ×10 下 60 秒三帧对比 LMC/SMC 位置始终在轨道线上（r210-02/03/04）、O 键关轨道线即消失（r210-05）、跟随 LMC 10 秒两帧星系压在自身轨道线上无错位（r210-06/07）、跟随人马座矮星系 8 秒两帧可辨识位移 + 潮汐流拖尾（r210-09/10）、M31 流动光点 2.3 秒两帧沿虚线位移（暂停模拟时间下仍流动，像素差分核对 r210-13/14）、全场景实测 FPS 59.9–60.3 不跌破、零控制台错误
- 实现 L4 银河系真实感重构（R2-9，用户反馈点 8）：消除 L4 下银河系"粒子盘 + 两张 billboard 辉光贴纸"的扁平观感，任意角度观察均有立体结构。A) 3D 恒星银晕（§9.1）——新增纯逻辑 `utils/galaxy.generateGalaxyHaloParticles`：3,000 稀疏粒子按数密度 n(r) ∝ r^-3.5 逆变换采样（`haloRadiusFromUniform`，幂律指数来源 Helmi 2008 A&ARv 银晕综述），可视截断 [10,000, 80,000] ly、垂直压扁 q≈0.9（观测 0.6–0.9 区间，登记），老年星族红黄色板 + ~10% 蓝水平支星，叠加于原银晕 sprite 之上（sprite 降为 0.22 弱环境光底，立体包裹感改由粒子承载，叠加方案登记）。B) 球状星团系统——`generateGlobularClusters`：29 个程序化星团（每簇 1 亮核 + 20 成员星 = 609 粒）+ M13（既有 L3 特殊天体条目联动，同一对象不重复渲染——程序化生成在其 t=0 银心系位置周围留 4,000 ly 排除区，`m13GalactocentricT0Ly` 与 data/specialBodies 偏移同源单测断言）共 30 个落在需求 20–40 区间（真实约 150+，Harris 1996 目录，位置为确定性伪随机登记）；分布保留"中心聚集（r = rMin+(rMax−rMin)·u^1.7）+ 高银纬（cosθ 极向偏置，平均 |cosθ|≈0.65 > 各向同性 0.5 单测断言）"真实统计性质。C) 核球/棒结构增强——银河系为棒旋 SBbc：`generateGalaxyDiskParticles` 新增 barFraction 参数（核球占比 0.18 拆分为核球 0.08 + 棒 0.10，盘总数 40,000 不变），棒粒子为沿 x 轴三轴椭球高斯分布（半长 13,000 ly ≈ 4 kpc、轴比 0.32、厚 3,000 ly），以棒图案角速度 Ω_b = 0.04 rad/Myr（≈39 km/s/kpc，Bland-Hawthorn & Gerhard 2016 区间 33–45）刚性旋转（shader mix(ω(r), Ω_b, aBar)，CPU 镜像 `barParticleAngle`），不参与密度波调制、恒定增亮 1.6 + 粒径 +0.4 使俯视棒状核心在核球辉光之外可辨（旋臂对数螺旋起点未与棒端严格对接，艺术化登记）；核球辉光 sprite 纵横比随视角连续变化（`bulgeAxisRatio`：正视圆形 1.0 → 侧视压扁 0.5，billboard 呈现扁椭球轴比）。D) 尘埃带侧视剪影——视角因子 `galaxyFaceOnFactor`（相机方向经银河系倾斜逆旋转取本地 y 占比）驱动 `dustLaneStrength`（faceOn ≤ 0.5 渐入、≤ 0.15 全强度，smoothstep 无跳变）：盘粒子 shader 侧视时盘中平面（|h| < 380 ly）亮度衰减 85% + 片元红棕化，同时叠加盘中平面扁椭球暗带 mesh（厚约 1,200 ly，renderOrder 置后普通透明混合对加性粒子真实"吸光"变暗，正视完全透明；加性混合无法画暗、视角依赖调制为艺术化近似，登记于 utils/galaxy.ts 文件头）+ 核球辉光侧视随强度压低 45% 避免白斑洗掉暗带。E) 风格统一与预算——粒子预算登记于 Galaxy.tsx 文件头：本组件 40,000+3,000+609 = 43,609，L4 峰值 ≈ 43,609 + M13 基础 420 + R2-8 近观层 ≤8,000 ≈ 52,029；银晕/星团粒子复用与盘粒子同一像素换算/软圆点着色管线（颗粒感统一）；较差自转/密度波/时间回卷既有管线零破坏（棒回卷时整体相位一次性跳转，与旋臂图案回卷行为一致登记）。新增 `galaxyR29` 单测 29 例（银晕逆变换采样边界/单调/中心聚集、生成确定性逐字节一致、径向密度统计特征（内 1/4 区间粒子数 > 外 1/2 区间 ×1.5）、立体分布非平面断言、星团数 20–40 区间/M13 排除区/高银纬/中心聚集/点簇紧致 6σ、棒占比与几何有界/俯视轴比 < 0.6/刚性旋转与半径无关/barFraction=0 向后兼容、视角因子三轴基向量与钳制、尘埃带强度单调、非法输入 RangeError），全量 1711 用例/103 套件通过，覆盖率 gate ≥90% 保持（语句 98.07%/分支 97.75%/函数 98.75%/行 99.45%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）：跟随银河系绕行一周多角度截图核对登记（r29-mw-01-default 正视旋臂清晰、r29-sweep-e04 侧视盘厚度 + 中平面暗尘埃带剪影 + 银晕立体包裹、r29-sweep-g05/g06 俯视棒状核心可辨、球状星团点簇可辨，无"二维贴纸"观感）、合并预览银河系与 M31 同屏亮度/色调/颗粒感协调（r29-merge-05-midway）、逐角度实测 FPS 60.0–60.4 不跌破、零控制台错误
- 实现 L4 飞往星系近观细节升级（R2-8，用户反馈点 3 的 L4 部分）：为本星系群 8 个贴图平面星系（M31/M33/LMC/SMC/M32/M110/人马座矮星系/M87）建立"飞往即见 3D 结构"的近观粒子层，消除"飞近后是一张薄片"的观感。A) 近观 3D 粒子层（§8.1）——新建纯逻辑 `utils/galaxyNearView.ts`：旋涡星系（M31 大核球+双主臂 8,000 粒 / M33 弱核球松散臂 6,000 粒）复用 `utils/galaxy.generateGalaxyDiskParticles` 参数化生成核球+盘+旋臂（生成器零重写）；不规则星系（LMC 5,000 / SMC 4,000 粒）为团块状粒子云（恒星形成团块蓝白年轻星 + ~12% 电离氢粉色 + 弥散背景，团块高斯 3σ 截断）；椭圆/矮椭圆星系（M87 n=4 de Vaucouleurs 6,000 / M32 致密椭圆 n=3 3,000 / M110 矮椭圆 n=1.5 3,500 / 人马座矮星系 n=1 3,000 粒）为 Sérsic (1963) 亮度分布近似的椭球粒子云（对 u=(r/Rₑ)^(1/n) 指数逆变换采样，bₙ≈2n−1/3 一阶近似 Ciotti & Bertin 1999，轴比压扁 + 截断 4Rₑ，老年恒星红黄色调中心增亮）；确定性种子 = 星系 id FNV-1a 哈希（两次飞往形态一致，渲染循环零随机），粒子层朝向沿用贴图平面时期 id 哈希朝向公式（单测逐字断言）；M87 近观保留喷流联动（椭球云与相对论喷流同框）。B) 门控与预算——近观激活距离 = 飞往观察距离 × 1.5（与 `resolveFocusTarget` 同源公式单测断言；星系贴图平面尺寸魔法数字 ×2500×2×0.55 收敛至 `utils/universe.galaxyPlaneSizeUnits`，渲染/相机/近观三处同源），退出 ×1.4 滞回复用 `utils/nearView.nearViewGateUpdate`；LRU 容量 1（同 P4 模式，最多 1 个星系持有近观层）——离开跟随后近观层淡出但保留（快速切回免重建），被新激活星系挤出即卸载 dispose（纯函数 `nearViewLruUpdate` + 渲染端持有者注册表，场景卸载重置）；单星系 ≤8,000 粒、同时峰值 +8,000（与太阳活动粒子预算 15,000/20,000 分属不同视角域——R2-4 域隔离下 L4 太阳活动不可见，单测断言容量与预算）。C) 贴图平面过渡——近观激活时贴图 0.5 s 交叉淡出至粒子层、释放时淡入恢复，无突变；远观非跟随保持贴图现状（粒子层不挂载不生成，性能零回退）。D) 薄片修复（§8.1 二选一，取 billboard 方案登记于 `galaxyNearView.ts` 文件头）——贴图平面每帧面向相机，侧向飞入不再呈"纸片"；艺术化差异登记：M31 真实倾角 ~77°（观测特征）在远观 billboard 下不呈现，三维倾斜姿态改由近观粒子层承载。E) 信息面板近观联动——星系卡片新增"结构"行（核球/盘/晕按形态分类四套文案，银河系卡片同步补棒旋结构行），dataSource 追加"Hubble 形态序列（NED）+ Sérsic 亮度分布"来源登记；跟随/飞往星系期间隐藏其自身 L4 标签（R2-7 近观标签治理同款）。实现差异登记：milky-way 近观 = 既有 4 万粒 3D 粒子盘（Galaxy.tsx）、类星体 3C 273 近观 = 既有光变 + 喷流 shader，均不重复建设；结构行常显（不限跟随语境，无害化简）。新增 `galaxyNearViewR28` 单测 29 例（确定性逐字节一致/8 星系形态与数据层分类一致/粒子预算与 LRU 容量断言/Sérsic 单调性与半光半径反解/椭球轴比与盘厚统计特征/团块 3σ 有界/激活距离同源/LRU 释放语义/持有者注册表/非法输入 RangeError），全量 1682 用例/102 套件通过，覆盖率 gate ≥90% 保持（语句 97.99%/分支 97.66%/函数 98.73%/行 99.43%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）17/17 项通过：L4 按 `]` 遍历 8 站一整圈逐站近观截图核对登记（r28-L4-01～08：M31 旋臂/核球立体可辨、M33 倾斜粒子盘、LMC/SMC 团块云、人马座矮星系弥散椭球云、M87 椭球云 + 喷流同框；对照 R2-5 时期 r25-L4-06 确认旧版"整屏薄片贴图"观感消除）、跟随 M31 点选出现结构说明行（核球/旋臂/恒星晕）、绕行拖拽 ~90°/180° 多角度截图无薄片观感、逐站实测 FPS 60.0–60.4 不跌破、再遍历 8 站 GC 后 JS 堆 98.2→98.2 MB（Δ0 MB，LRU 挤出释放无泄漏）、零控制台错误
- 实现 L3 飞往/跟随目标近观细节升级（R2-7，用户反馈点 3 的 L3 部分）：为 L3 域序列全部 15 站建立"飞往即见细节"的近观 LOD 分级。A) 日球层顶近观三层结构（§7.1-A）——跟随/飞往日球层顶时挂载 终端激波（~94 AU 示意内壳，Voyager 1 于 2004 年实测穿越）→ 日鞘（3 层等距渐变壳，琥珀 → 蓝着色渐变）→ 日球层顶（外边界近观增亮）半透明多层壳结构 + 分层标注，层半径按"压缩比例沿用现有登记"换算（层半径 = 示意球壳半径 × 真实 AU/120，纯函数 `utils/heliopause.ts`）；旅行者 1 号（2012 穿越，约 121.6 AU，黄道以北）/ 2 号（2018 穿越，约 119 AU，黄道以南）位置标记点（辉光点 + 点选热区 + 标注，方向按穿越黄纬示意登记）+ 点选科普卡片（catalog 新增 voyager-1/voyager-2 条目，来源 NASA/JPL Voyager Interstellar Mission，含发射/穿越/金唱片信息），`resolveFocusTarget` 支持旅行者标记飞往；迎风/背风不对称水滴形取"不做"选项——维持球壳示意并在信息面板登记"真实为彗尾状不对称"（`HELIOPAUSE_SHAPE_NOTE_ZH` + 卡片"结构分层/形态"行）；B) 近观 LOD 门控（§7.1-B）——新建纯逻辑 `utils/nearView.ts`：为 L3 序列每个成员定义近观激活距离（进入阈值 = 飞往观察距离 ×1.5，与 `resolveFocusTarget` 同源公式单测断言；退出阈值 ×1.4 滞回，复用 P4 `detailGateUpdate` 模式），仅当前跟随/飞往目标激活，离开跟随/超出退出距离即卸载释放（0.5 s 淡入淡出后卸载，几何/材质 dispose，无 LRU 保留）；C) 逐对象近观增量（P6 已交付部分不重复建设，差异登记于 `nearView.ts` 文件头：恒星类/黑洞类近观细节由 P6 表面 shader 与吸积盘 shader 交付、太阳近观 = 既有 L1 渲染）——星云类：猎户座 +18 体积感云团 sprite（确定性 `nebulaPuffLayout` 布局 + 基础平面近观减淡 35% 交叉过渡）、蟹状 +16 丝状遗迹云团（shell 纹理 ×3 种子）+ 射束近观增亮 60%、环状 +200 粒环体环向软边粒子（OIII 蓝绿 → Hα 红径向色层，与环面同姿态同膨胀）+ 外晕壳/中心 OIII 辉光、马头 +2 视差发射层 + 3 前景暗云团（前后景深），近观均无"单张圆形光晕"观感；星团类：M13 +1,200 粒近观分级星场（rand^2.4 分布中心更密、粒径减半，圆形软边贴图无方块粒子）、昴星团 +320 粒疏散星场 + "七姊妹"亮星辉光 ×7；天狼星 A/B：绕共同质心双轨道线（与 `binaryStarPositions` 同源半径）+ 近观 A/B 身份标注（主序星/白矮星）+ 白矮星色调调蓝（~25,000 K，对比清晰）；D) 近观标签治理（R2-5 目验登记的既有"大字号标签"问题）——跟随/飞往目标期间隐藏该天体自身 L3 标签与旅行者标记标注，日球层顶近观语境下隐藏全部 L3 特殊天体标签（相机 ~836 单位时标签放大 ~3 倍互相叠压遮挡三层结构）。粒子预算登记：近观同一时刻至多一个目标激活，单目标最大增量 +1,200（M13），全局峰值 15,000+1,200=16,200 ≤ 20,000（单测断言）。新增 `heliopauseR27`（17 例：层半径比例/壳层递增/色渐变单调/旅行者方向单位向量与位置模长/非法输入）+ `nearViewR27`（43 例：15 站激活距离与观察距离同源/滞回门控四态/云团布局确定性与边界/粒子预算）+ `cameraFocusR27`（6 例：旅行者解析与科普卡片）单测，全量 1653 用例/101 套件通过，覆盖率 gate ≥90% 保持（语句 97.93%/分支 97.69%/函数 98.69%/行 99.44%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）30/30 项通过：L3 按 `]` 遍历 15 站一整圈逐站近观截图核对登记（r27-L3-01～15，近观形态均可辨识）、日球层顶三层结构标注 + 旅行者 1/2 标记可见、点选旅行者标记科普卡片出现（NASA/JPL 来源）并可飞往、天狼星近观 A/B 身份标注可见、逐站实测 FPS 60.0–60.4 不跌破、连续切换 10 个目标后 GC 实测 JS 堆 97.7→98.4 MB（Δ+0.7 MB，无泄漏）、零控制台错误
- 实现太阳系绕银心轨道运动可感知细节增强（R2-6，用户反馈点 2 后半）：默认跟随模式（太阳系钉在场景原点）下 10 秒内肉眼可辨太阳系沿轨道运动——A) 轨道当前位置脉动高亮（§6.1）：You are here 标记新增雷达波纹扩散环（周期 2.4 s、扩散 3 倍后消隐）+ 本体 ±12% 呼吸脉动（真实秒驱动的 UI 高亮节奏，非物理量，登记于 `utils/galacticMotionCues.ts` 文件头），随 You are here 开关联动；已走过尾迹（暖绿实线渐隐，不透明度 0.9→1.0、颜色系数上调）与未来预测弧（冷蓝虚线 0.5→0.72、提亮为 #bfe4ff）对比调亮；B) 参照物滑动（§6.1 手段实现时评估，取"轨道静止刻度"方案登记）：P6 与太阳共转的"流动刻度光点"（跟随模式下相对太阳方位恒定、运动线索弱，为用户"看不出动"根因之一）替换为**银心系静止的轨道银河年刻度**——24 格进度里程碑（每 15° 一格）+ 0%/25%/50%/75% 主刻度（琥珀色大光点 + "银河年 N%" 标注），跟随模式下刻度以太阳真实公转速度整体滑过原点、银心固定模式下脉动标记依次掠过静止刻度（`orbitFlowPhase01`/`orbitFlowTickAngle` 移除，差异登记于文件头；刻度位置固定 → 几何只建一次，渲染循环零位置更新）；C) 刻度角与 HUD"银河年进度（绕行 N°）"严格同源（同一 θ=2π·t/T 公式，单测断言进度 k/24 时进度角恰等于第 k 格刻度角、标记场景位置反解角与 HUD 一致）；D) 垂直振荡视觉增益 ×6→×10（需求建议上限，±150 场景单位 ≈ 轨道半径 11%，波浪起伏默认视角可辨；纯视觉夸大登记更新至文件头 + HelpHint，真实比例模式仍不放大）；E) G 键银心固定模式可发现性：控制面板新增"银河系视角参考系"显式入口（L3 可用、域外置灰附提示"请切换到银河系视角使用"）、HelpHint 文案扩写、首次切入 L3 一次性 toast"按 G 切换银心固定视角观察太阳系公转"（store `galacticFrameTipVisible/Seen` 会话内一次，12 秒自动收起，用户切换过模式即不再打扰）。新增 `galacticMotionCuesR26`（26 例：增益上限/刻度均匀分布与主刻度/HUD 一致性/脉动相位/波纹曲线单调性/非法输入）+ `storeR26`（7 例：tip 一次性语义）单测，全量 1587 用例/98 套件通过，覆盖率 gate ≥90% 保持（语句 97.89%/分支 97.56%/函数 98.66%/行 99.42%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）18/18 项通过：L3 锚点默认速度间隔 10 秒两帧对比——轨道刻度标签屏幕位移 197–200 px（>80 px 验收线，肉眼可辨）、太阳系标记保持原点（位移 0 px）、HUD 绕行角推进 37°（含截图开销，理论 31°）；G 键银心固定模式回归——标记 10 秒沿轨道位移 202 px（绕行 56°→93°）清晰可见；toast 首次出现/G 后收起/再入 L3 不再出现、控制面板入口 L2 置灰/L3 可点且与 HUD 同步，60.3 FPS 满帧零控制台错误（截图 r26-a1～a3/c1～c2/d3 登记）
- 实现通用天体切换序列框架（R2-5，用户反馈点 5 + 点 1 后半）：在 P4 行星序列（L1/L2，20 天体，现状保持）基础上，为 L3 银河系视角与 L4 宇宙视角建立各自的"上一个/下一个"巡游序列——L3 域 15 站（太阳 → 日球层顶 → 银心人马座 A* → 参宿四/参宿七/天狼星/造父一/WR 124/天鹅座 X-1/蟹状星云脉冲星 → 猎户座/环状/马头星云 → 昴星团/武仙座 M13，按"太阳系出发 → 银心 → 恒星类 → 星云类 → 星团类"组织并登记于 `utils/cycleScopes.ts` 文件头）、L4 域 8 站（银河系 → 大/小麦哲伦云 → 人马座矮星系 → M31 → M33 → M87 → 类星体 3C 273）：A) 纯逻辑域框架——新建 `utils/cycleScopes.ts`（`scopeForViewLevel` 域判定：跟随域内天体时以天体归属为准、无跟随按连续层级区间划分；`cycleBodyIdInScope` 域内循环与序列外回落域默认天体 L3=人马座 A*/L4=M31；位置标签 n/15、n/8）；B) UI 与快捷键泛化——`BodyCycleSwitcher` 按当前视角域展示「← 上一个 | 域名+当前天体名+序列位置 | 下一个 →」（L2/L3/L4 未跟随时同样显示，点击即飞往域记忆天体开始游览），`[`/`]` 快捷键全部视角域按域路由，HelpHint 帮助文案更新，信息面板选中天体属于当前域序列时在"飞往/跟随"旁补"←/→"快捷入口与序列位置；C) 每域独立记忆——store 新增 `galaxyAnchorBodyId`/`universeAnchorBodyId`（会话内），切换视角回来后按 `]` 从上次游览位置恢复，`requestFlyTo` 仅在该域语境下记录记忆（防跨域误写：L1/L2 耀斑通知"飞往太阳"不改写银河系域记忆）；D) 飞往联动——`resolveFocusTarget` 新增银河系整体（milky-way）解析分支（目标点=银心、观察距离按银盘显示半径推荐，此前不可飞往），`isGalaxyAnchoredFocusId` 白名单扩展纳入日球层顶（巡游期间银河系上下文不因层级波动淡出；差异登记——序列成员"太阳"不纳入，避免复发"银河粒子贴着太阳"历史 bug）。新增 `cycleScopes`（28 例）/`cameraFocusR25`（29 例）/`storeR25`（17 例）单测套件并扩展 `specialBodies` 白名单断言，全量 1565 用例/96 套件通过，覆盖率 gate ≥90% 保持（语句 97.87%/分支 97.41%/函数 98.64%/行 99.41%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）37/37 项通过：L3 按 `]` 遍历 15 站一整圈（每站 HUD 序列位置正确、跟随模式生效、飞抵后目标完整可见，逐站截图 r25-L3-01～15）、L4 遍历 8 站一整圈（截图 r25-L4-01～08）、`[` 反向切换正确、L3↔L4 往返后各域序列位置独立恢复、L1 行星序列 `[`/`]` 行为不回退（地球 3/20 → 月球 4/20 → 地球 3/20）、零控制台错误

### 安全

- 升级框架链清零生产依赖已知漏洞（npm audit high/critical 门禁通过）：Next.js 14.2 → 16.2.12（22 项 high 级安全通告全部收敛，构建切换为 Turbopack，`next lint` 移除改用 ESLint CLI）、React 18 → 19.2、React Three Fiber 8 → 9.6 / drei 9 → 10.7 / @react-three/postprocessing 2 → 3.0（React 19 配套）；npm overrides 顶升 Next 传递依赖 postcss ≥8.5.23 与 sharp ≥0.35.3（上游未发版收敛的 high 通告，均为构建期/未使用路径，登记于 package.json）；ESLint 迁移至 v9 flat config（eslint.config.mjs，规则语义与原 .eslintrc.json 一致；eslint-plugin-react-hooks v7 新增的 React Compiler 规则与 R3F 渲染循环 mutation 范式冲突属大规模误报，关闭 immutability/refs/set-state-in-effect 三条并登记原因）；升级后全量 1949 用例/112 套件通过、type-check/lint/build 全绿、无头 Chrome 目验场景渲染正常零控制台错误

### 发布流程

- 建立 GitHub 开发与发布流水线：新增 PR 质量与漏洞门禁 workflow（`pr-gate.yml`，针对 main 的 PR 自动执行构建、类型检查、全量单元测试含覆盖率 gate ≥90%，并以 Dependency Review + `npm audit --audit-level=high` 双层拦截生产依赖的 high/critical 已知漏洞，完整 audit 报告上传为 artifact）；新增 GitHub Pages 部署 workflow（`deploy.yml`，仅由 release tag `v*` 或手动触发，流程为单元测试 → 静态导出构建 → 部署，测试不通过不部署）；新增 `.github/CODEOWNERS`（全部变更需 owner 审批，配合分支保护 ruleset 实现"禁止直推 main、合并必须经 PR 且 owner approve"）
- 支持静态导出部署：Next.js 开启 `output: 'export'`（项目为纯前端，无服务端功能），构建产物 `out/` 可托管于 GitHub Pages；新增 `public/CNAME` 绑定自定义域名 stellar.guushu.com（经 Cloudflare 代理提供 CDN 缓存与访问统计）

### 改进

- 项目定名"星海奥德赛 Stellar Odyssey"并补全文档体系：应用内品牌落地——浏览器标签页标题与描述更新（layout.tsx metadata）、控制面板标题"星系运动可视化"改为"星海奥德赛 Stellar Odyssey"、package.json name 同步 stellar-odyssey；README.md 全面重写（项目简介与亮点特性/快速开始/文档索引/技术栈/项目结构/开发命令/科学性承诺，素材许可 Attribution 区完整保留）；新建 docs/ 教程体系 6 篇——getting-started（安装运行 + 第一次遨游推荐路线 + 快捷键速查 + FAQ）、view-guide（L1–L4 四视角导览：看什么/怎么玩/跨视角机制）、controls（鼠标/快捷键/控制面板选项/巡游跟随/HUD 完整参考，含 R3-8 视角作用域说明）、events-guide（耀斑/CME/超新星/合并预览四类动态事件玩法与事件域机制）、science-notes（真实数据依据与艺术化处理登记双向汇总）、development（架构约定/测试要求/无头 Chrome 目验/性能预算/工作流）
- 控制面板功能选项按视角作用域整理（R3-8，**行为变更**，IMPROVEMENT_REQUIREMENTS_3 §R3-8，用户反馈：控制面板选项过多需整理——只有全局选项才跨视角常显，视角专属选项只在对应视角下显示并可用）：A) 作用域注册表——新建纯函数 `utils/panelScopes.ts`（`PANEL_OPTION_SCOPES` 15 选项 × 可见视角集合单一事实来源 + `panelOptionVisible` 判定，未知 id RangeError）；归类：全局恒显 = 轨道线(O)/天体标签(L)/真实比例/泛光/性能监控（视角切换、模拟速度、音效区块不属显示开关保持常显），L1 = 卫星轨道线，L1+L2 = 耀斑/CME 演示，L2 = 太阳内部剖面，L3 = 银心固定参考系(G)整个区块/垂直展开(V)+增益滑块/You are here 标记/超新星演示，L4 = 速度矢量箭头/合并预览+"恢复预览前时间"按钮。B) 域外**隐藏**（非置灰，取代 R2-4"置灰 + tooltip"方案，行为变更登记）——ControlPanel 按 `viewLevel` 条件渲染（滚轮缩放经 syncZoomLevel 自动同步、跟随/飞往期间层级锁定，跟随 L3 天体拉近时选项不闪变）：银心固定区块非 L3 整体不渲染（删除置灰按钮与"银河系视角下可用"文案）、显示区专属项域外不渲染（相对顺序不变、不加新分组标题）、动态事件演示区按视角只渲染域内按钮（域外提示文案分支删除；按钮内部既有 disabled 逻辑——活跃事件进行中/剖面模式/eventDemoEnabled 连续层级域校验——全部保留，可见性与可用性双层，eventScopes 域窗口语义不动；无可用按钮时分区标题一并隐藏，当前四视角各有至少一钮恒显、规则登记备防）；域外面板明显缩短、切换视角选项即时增减。C) V 快捷键补 L3 门控（**行为变更**，与 G 键同模式，修正 R3-6"V 键无层级门控"登记差异）——非 L3 按 V 无任何状态变化；O/L/R 保持全局；HelpHint V 说明补"银河系视角下生效"。D) 零渲染行为变更——store 与全部场景组件零改动，域外已开启状态与场景效果保留（L3 开垂直展开切 L4 复选框消失但银河系仍呈椭球（R3-7 登记语义）、回 L3 复选框仍勾选滑块值不变）。新增单测 `panelScopes`（64 例：15 选项 × L1–L4 可见性矩阵逐格断言/全局项四视角恒真/注册表-判定同源/未知 id RangeError），既有单测零回退，全量 1949 用例/112 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800，截图 r38-01～08 登记，DOM 断言 16/16 通过）：四视角面板逐一核对增减项（L1 = 全局5+卫星轨道线+耀斑/CME、L2 = 全局5+太阳剖面+耀斑/CME、L3 = 全局5+银心区块+垂直展开+You are here+超新星、L4 = 全局5+速度矢量+合并预览）、L3 按 V 开启（勾选+滑块 ×3.0）→ 切 L4 复选框消失银河系仍椭球 → L4 按 V 无效 → 回 L3 勾选与滑块值保留 → L3 再按 V 正常关闭
- 完成 R2 迭代集成回归验收（R2-12，收尾）：R2-1～R2-11 全部交付后的全量端到端复验与文档回写。A) 质量门禁全绿——全量单测 1766 用例/105 套件通过，覆盖率 gate ≥90% 保持（语句 98.08%/分支 97.83%/函数 98.79%/行 99.44%），type-check/lint/build 全绿；B) 无头 Chrome（Metal 1280×800）端到端复验 9 个用户反馈点 66/66 项通过（脚本 A/A2/B/C 四段，截图 r212-* 登记）：① L3 语境点选日球层顶面板打开 → 按 2/4 切视角面板自动关闭无跟随残留 + 底部显示对应域切换控件（行星/银河系/宇宙巡游）；② L3 锚点行星标签全程隐藏（span 精确匹配 10 秒观察无闪现）+ 太阳系沿轨道运动 10 秒可辨（银河年刻度参照物滑动 192–205 px、太阳系标记钉在原点 0 px、HUD 绕行角推进 37°）；③ L3/L4 近观细节抽查——日球层顶三层结构标注（终端激波/日鞘）+ 旅行者 1/2 号标记、天狼星 A/B 身份标注（主序星/白矮星）、猎户座/M13/M31/LMC/M87 近观截图核对；④ 四视角 × 四类事件矩阵——16 格按钮可用域/置灰+tooltip 逐格复验正确，耀斑+CME 通知 L2 卡片 → L3/L4 折叠一行 → 回 L2 恢复、超新星通知 L3 卡片 → L2 折叠 → 回 L3 恢复、域外解除暂停事件状态机照常推进、超新星"飞往观看"跟随后保持银河系尺度、L4 合并预览可启动；⑤ 各域 [/] 遍历整圈——L3 15 站/L4 8 站/L1 20 站全部序列位置+跟随正确、[ 反向正确、跨域往返后域记忆恢复（日球层顶 2/15）；⑥ L2/L3/L4 → 地球视角切换 20 次卫星屏占比逐次 4.2%–5.8% 无一次铺屏，跟随 ISS 稳态放大 ≈2.8× 屏占比恰钳在 10.0%；⑦ 合并预览完整演化序列——接近末段潮汐渐显 → T0 首次穿越+星暴+演化卡片 → 回摆振荡 → 核心并合中 → 终态 Milkomeda 逐阶段 HUD 标签正确，"恢复预览前时间"后倒计时/卡片/阶段标签全复原；⑧ 跟随银河系正视旋臂/侧视盘厚+尘埃带剪影+银晕包裹/俯视棒状核心多角度截图核对，无"二维贴纸"观感；⑨ L4 快进 ×10 三帧对比 LMC/SMC 始终压在轨道线上、O 键轨道线开关生效、人马座矮星系 8 秒两帧可辨识位移+潮汐拖尾；全场景 FPS 实测 60.0–60.4 满帧不跌破、全部脚本零控制台错误；C) 文档回写——需求文档 IMPROVEMENT_REQUIREMENTS_2.md §R2-12 状态回写、文档版本升 1.1，CHANGELOG 查漏补全（R2-1～R2-11 条目此前已逐阶段登记，无遗漏）
- 彗尾细节细腻化：两条彗尾由低多边形半透明圆锥升级为粒子系统（离子尾 2600 + 尘埃尾 2400 粒/彗星，复用 Belt/Galaxy 粒子 shader 范式）——柔和圆点消除锥壳硬边、轴向亮度平滑消隐至尾端（消除截断突兀）、横向轴心亮边缘暗、颜色沿尾轴/径向双向渐变；新增物质外流流动动画（离子尾快、尘埃尾慢）与离子尾太阳风摆动（行波状飘动，示意登记）；尘埃尾二次弯曲公式与两尾夹角验收指标保持不变；彗发多层化（内层亮白心 + 外层彩色晕）；新公式抽入 utils/cometTail.ts 纯函数镜像并补 17 个单元测试
- 更新 REQUIREMENTS.md 需求完成状态：逐项标记 ✅ 已完成 / 🔶 部分完成或待实测 / 🔲 未实现，并登记实现差异（程序化纹理、音景合成、3D 空间音效与性能监控未实现等）
- 提升音效响度：提高各层级合成增益、为低频振荡叠加泛音（小型扬声器上低音可闻）、主链路加入动态压缩器防爆音、默认音量上调至 0.8
- 单元测试扩展至 362 个（21 个套件）：新增卫星缩放、粒子带、尾迹缓冲、银河系/宇宙运动、连续缩放、广义开普勒轨道、数据科学性校验等测试，核心代码覆盖率语句 99.2%、分支 100%、函数 100%
- 单元测试扩展至 483 个（28 个套件）：新增特殊天体动态效果（脉动/闪烁/双星互绕/脉冲/吸积盘/多普勒）、超新星阶段曲线与触发逻辑、飞往/跟随目标解析（银心系坐标变换）、真实比例映射、store P2 状态等测试，核心代码覆盖率语句 98.6%、分支 95.9%、函数 100%、行 99.8%
- 单元测试扩展至 634 个（41 个套件）：新增奥尔特云生成、空间音效门控、性能统计、星风/造父/伽马暴光变、旋臂密度波、合并预览/哈勃膨胀/麦哲伦星流、矮行星与新卫星/新特殊天体/新星系数据校验、质量格式化、store 合并预览状态机、新天体焦点解析等测试，核心代码覆盖率语句 98.8%、分支 96.1%、函数 100%、行 99.8%
- 单元测试扩展至 736 个（49 个套件）：新增纹理清单与许可校验、加载进度聚合、Bloom 层级强度、行星光影计算、恒星闪烁函数、行星音景参数与过渡混合、store P3 状态等测试，核心代码覆盖率语句 98.76%、分支 96.12%、函数 100%、行 99.77%
- 单元测试扩展至 840 个（56 个套件）：新增切换序列循环、近观门控状态机与细节强度、法线立体光照因子与海洋掩码、木星纬向急流剖面、彗尾弯曲公式与彗核外形、纹理 LRU 与显存预算、4K/法线清单校验、store P4 状态等测试，核心代码覆盖率语句 98.85%、分支 96.58%、函数 100%、行 99.79%
- 跟随/飞往外行星（如海王星，30 AU）时同步启用真实位图纹理加载（原按相机-原点距离门控，跟随远距行星时只有程序化纹理）
- 单元测试扩展至 881 个（58 个套件）：新增谷神星数据科学性校验、16 天体切换序列顺序/循环、矮行星最小可见尺寸与真实比例策略、妊神星椭球缩放、冥王星/谷神星纹理/法线清单与许可登记（含文件存在性校验）、矮行星音景映射、冥卫一卡戎数据、矮行星聚焦距离适配等测试，核心代码覆盖率语句 98.87%、分支 96.68%、函数 100%、行 99.80%
- 单元测试扩展至 962 个（66 个套件，P7）：新增天宫数据科学性校验、20 天体切换序列顺序/循环/HUD 位置、差异化尺寸映射与轨道分层间隔断言、近观放大系数、对地定向姿态四元数（含 Shepperd 分支覆盖）与帆板对日角、地影判定与半影过渡、渲染相位注册表与跟随一致性、人造卫星观察距离适配、模型清单/许可/文件与三角形预算（解析 glb 实测断言）等测试，核心代码覆盖率语句 98.93%、分支 97.00%、函数 100%、行 99.82%
- 近观标签避让（P7 §3.5）：相机贴近行星/卫星时隐藏对应天体标签（近距离下 Html distanceFactor 会将标签放大到遮挡画面；卫星近观模型激活时名称已由 HUD 跟随模式与切换控件显示）
- 单元测试扩展至 1048 个（71 个套件，P6）：新增银心参考系位姿计算（tiltAroundX/sunWorldScenePos/computeGalacticFramePose 两模式与过渡混合、advanceFrameTransition 2 秒过渡）、预测弧段采样（1/4 银河年非闭合/滚动/垂直增益）、轨道流动刻度相位（角速度/均匀分布/周期性）、垂直放大系数（真实比例不放大）、银河年自洽参数（ω(R)=2π/T 精确成立）、恒星表面 shader 镜像（limb darkening/色温梯度/对流 fBm/分段提升）、星云纹理生成器（确定性/尺寸约束/三形态包络/参数校验）、store galacticFrameMode 等测试，核心代码覆盖率语句 99.02%、分支 96.84%、函数 100%、行 99.79%
- 单元测试扩展至 1095 个（73 个套件，S1）：新增太阳表面纯逻辑（米粒幅度插值/亮度钳制/演化相位回卷与负时间归一、色球红环菲涅尔单调性与淡入、日冕径向衰减/冕流赤道-极区各向异性/综合亮度线性缩放、光晕分级混合、shader 常量区间）与太阳 4K 纹理清单（含文件存在性）校验，核心代码覆盖率语句 99.07%、分支 96.96%、函数 100%、行 99.80%（`utils/sunSurface.ts` 覆盖率 100%）
- 单元测试扩展至 1185 个（79 个套件，S2）：新增较差自转剖面（赤道 25.4 天/纬度 60° 30.9 天/极区 34 天精确复现、纬度单调性、剪切回卷）、黑子（确定性可复现、±35° 纬度带、成对性、生命周期曲线、零分配填充）、耀斑/CME 阶段曲线（增亮→峰值→指数衰减、壳层扩张半径、km/s 场景换算、朝地球判定、泊松触发与联动概率）、太阳风相位回卷、日珥弧线演化、剖面分层（0.25/0.7 R☉ 边界、开合进度、楔形判定、互斥淡出）、太阳结构数据校验（六层来源/温度/科普文案完整性）、store S2 事件状态机等测试，核心代码覆盖率语句 98.21%、分支 97.42%、函数 100%、行 99.66%
- 单元测试扩展至 1288 个（85 个套件，S3）：新增周期相位/序号/黑子包络（不对称上升下降）/相位名/频率调制/蝴蝶图纬度迁移/状态行（`solarCycleS3`）、黑子周期联动激活门控 + 蝴蝶图纬度 + 暗条暗化 + 可容纳地球数换算（`sunspotsS3`）、周期泊松均值调制 + 日冕洞快风方向加权 + 爆发日珥前导 + CME 抵达延迟 + 极光增强曲线（`solarActivityS3`）、光斑增亮 + 超米粒调制 + 针状体扰动 + 日冕洞暗化 + 冕流各向异性（`sunSurfaceS3`）、日球层顶常量校验（`heliopauseS3`）、太阳沸腾音景增益（`audioMixerS3`）；整体覆盖率保持 gate ≥90%（语句 97.84% / 分支 97.2% / 函数 98.43% / 行 99.34%）
- 太阳近观 sprite 光晕收敛幅度由 45% 提升至 85%（S2）：近观时残留光晕会冲刷光球对比度导致黑子难以辨识，收敛更充分后黑子本影/半影与米粒细节近观清晰可辨（远观 L2 光晕观感不变）

### 修复

- 超新星事件视角域收窄至银河系视角（R3-5，行为变更，IMPROVEMENT_REQUIREMENTS_3 §R3-5，用户反馈：宇宙视角下仍弹出"💥 超新星爆发！"通知）：超新星为单恒星尺度事件，原窗口 [2.5, 4] 含 L4 且宇宙视角高压缩比（2000 万年/秒）下泊松触发约每 3 真实秒一次；现窗口收窄为 [2.5, 3.5]（`SUPERNOVA_EVENT_MAX_LEVEL`，与合并预览下缘 3.6 互补无重叠）——L4 下不再自动触发、不弹通知、演示按钮置灰，活跃超新星缩出 L3 域超 1 秒按 R3-3 硬隔离丢弃；特效淡出窗口同步收窄（snFadeWeight 满值平台终点 3.5 = 域上缘、淡出延伸至 L4 锚点 4.0 归零，避免"特效可见但事件被丢弃"不一致，与太阳事件淡出模式一致）；行为变更登记：永久遗迹与活跃事件共用淡出窗口，超新星内容整体收敛为银河系视角专属（L4 下遗迹不再显示，状态保留、回 L3 完整可见）；L3 下行为零回退；eventScopes/storeR33 断言更新 + L4 域外丢弃 1 例，全量 1833 用例/108 套件通过，覆盖率 gate ≥90% 保持，type-check/lint/build 全绿
- 修复天体说明文字随画面放大而放大、跟随较小天体放大时遮挡画面的问题（R3-4 标签屏幕尺寸统一治理，IMPROVEMENT_REQUIREMENTS_3 §R3-4）：全部世界空间标签均为 drei `Html distanceFactor`（"世界固定大小牌子"语义，屏幕缩放 ≈ distanceFactor/(2·tan(vFOV/2)·相机距离)），相机贴近小天体时标签放大数十倍铺屏；现建立统一近距反向缩放钳制——新增纯逻辑 `utils/labelScale.ts`（`labelCounterScale = min(1, 距离/最小生效距离)`，与外层缩放相乘后近距屏幕尺寸恒定为最小生效距离处的大小，连续无跳变、FOV 无关；最小生效距离默认 distanceFactor×0.5，钳制上限 ≈ 设计尺寸 2 倍以内登记）+ 共用组件 `ClampedHtmlLabel`（锚点 group 每帧取 matrixWorld 计算相机距离，直改内层 div transform 不经 React 重渲染，缩放值量化缓存未变化不写样式），全量接入约 20 处标签（行星/卫星/彗星/L3 特殊天体与天狼星 A/B 近观标注/日球层顶主标注与终端激波·日鞘·旅行者标注/奥尔特云/银河年刻度与"你在这里"/L4 星系名/碰撞倒计时/本动矢量/可观测边界·拉尼亚凯亚·巨引源/河外天体 4 标签），远距（≥最小生效距离）反向系数恒 1 观感零回退；治理缺口补齐——彗星标签补近距隐藏（P7 同款 max(1.2, 彗核半径×6)）+ 跟随/飞往自身隐藏（此前完全无治理，跟随彗星放大标签可放大约 30–60 倍）、河外天体（类星体 3C 273/触须星系/引力透镜弧/GRB 演示）补 R2-8 同款焦点隐藏；日球层顶/奥尔特云跟随期间保持可见（R2-1 语义）但受钳制不再遮挡结构，Galaxy 两标注开关归属维持现状（确认项 2），近观专用标注只钳制不隐藏（确认项 3），既有 P7 近距隐藏/R2-7/R2-8 焦点隐藏/层级门控/淡出零回退；新增 labelScale 单测 9 例（钳制边界连续性/远距恒 1/近距线性收敛/默认比例换算/量化/非法输入 RangeError，覆盖率 100%），全量 1831 用例/108 套件通过，覆盖率 gate ≥90% 保持（语句 98.15%/分支 97.84%/函数 98.97%/行 99.49%），type-check/lint/build 全绿，dev 3100 冒烟 200 零错误
- 动态事件视角域硬隔离（R3-3，行为变更，IMPROVEMENT_REQUIREMENTS_3）：修复域外事件残留提醒在高时间压缩比下频繁闪现的问题——R2-4 软隔离的"域外折叠一行小字提醒"废止（域外零事件 UI），离开事件所属视角域（耀斑/CME/CME 抵达属太阳系 L1/L2、超新星属银河系 L3/L4、合并预览属宇宙 L4）持续超过 1 秒（真实时间宽限，防连续滚轮瞬间穿越域边界误丢弃）后活跃事件被直接丢弃：耀斑/CME 含在途抵达计划与极光整链清空、超新星爆发动画终止不归档遗迹（既有永久遗迹保留，确认项 1）、合并预览等价"恢复预览前时间"自动回跳（确认项 2）；回到域内不恢复被丢弃的事件，等待下一次自然触发（事件计数器不回退，id 单调性保持）；锚点切换（2 秒）/飞往（2.5 秒）运镜期间离域计时豁免（合并预览启动自动切 L4 的运镜途中不被误杀）；丢弃计时用真实时间、不受暂停影响（丢弃语义随视角而非模拟时间，登记设计决策）；泊松自动触发域门控与演示按钮"置灰 + tooltip"维持 R2-4 现状。纯逻辑收敛于 `utils/eventScopes.ts`（宽限/豁免常量 + `outOfScopeElapsedUpdate`/`eventDiscardDue`，域外计时上钳到宽限期防无界增长），丢弃驱动接入 `store.tick`（运镜豁免以 `viewTransitionId`/`flyToRequestId` 代次消费实现）；新增 `storeR33` 单测套件（14 例：四类事件丢弃语义/宽限折返保留/回域不恢复/暂停下丢弃/运镜豁免/自然结束后恢复按钮状态保留）+ `eventScopes` 丢弃纯函数 7 例（折叠提醒文案断言随方案废止移除），全量 1822 用例/107 套件通过，覆盖率 gate ≥90% 保持（语句 98.15%/分支 97.82%/函数 98.97%/行 99.48%），type-check/lint/build 全绿
- 天体简介面板跟随当前巡游天体（R3-2，行为变更）：四个视角域（行星/太阳系/银河系/宇宙）下，凡"切换到某个天体"的操作——底部切换控件"上一个/下一个"、快捷键 `[`/`]`、信息面板 ←/→ 快捷入口（含 L3/L4 未跟随时首次按键的"起始锚定"飞往）、按 1 进入行星视角（飞往锚定天体）、任意"飞往"入口（面板飞往按钮/F 键/耀斑通知"飞往太阳"/CME"飞往地球"等）——右下角"天体简介 + 飞往/跟随"面板自动切换为该天体并显示（同时清空黑子/日珥特征卡片避免叠显）；手动 ✕ 关闭后跟随期间保持关闭，直到下一次天体切换才再次显示；按 2/3/4 回固定锚点（无目标天体）仍自动关闭面板（R2-1 语义保留）；飞往超新星事件（无信息目录条目）维持现状不改写选中
- 重构视角域切换序列（R3 需求 1，行为变更）：修复"行星视角下从地球按下一个切到其他行星/矮行星、太阳系视角序列混入卫星"的序列混杂问题——原 L1/L2 共用的 20 天体扁平序列（行星+月球+人造卫星+矮行星+彗星混排）拆分为四个巡游域：**行星视角（L1）只在当前行星系统内循环**（行星本体 + 其自然/人造卫星，按绕行半长轴升序，如地球系统 6 站：地球→天宫→ISS→哈勃→静止卫星→月球；跟随木星时在伽利略卫星间循环），无卫星的行星（水星/金星/天王星/矮行星/彗星）隐藏"上一个/下一个"按钮；**太阳系视角（L2）只在行星+矮行星+彗星（15 天体）间循环**，统一按轨道半长轴升序混排（恩克 2.22 AU 居火星后、哈雷 17.8 AU 居土星后），不再出现卫星；L3 银河系 15 站 / L4 宇宙 8 站序列不变。当前巡游域由推断制（跟随天体归属+连续层级区间）改为显式状态（store `cycleScope`，锚点切换/自由缩放跨级同步、巡游期间保持不变），`[`/`]`、底部切换控件与信息面板 ←/→ 快捷入口全部按新域路由（`utils/bodyCycle.ts` 序列由真实轨道数据计算生成，`utils/cycleScopes.ts` 四域框架，`cycleAnchorBody` 并入 `cycleScopeBody`）
- 修复跟随/飞往天体期间视角层级随相机距离自动跳级的问题（R3 需求 2，用户反馈：行星视角下切到阋神星/鸟神星自动变成太阳系甚至银河系视角、银河系巡游中飞抵猎户座星云等成员后 HUD 跌回"太阳系视角"）：离散层级 `viewLevel` 原每帧由"相机-场景原点距离"回写（阋神星 67.9 AU ≈ 679 场景单位越过 L2/L3 分界即跳 L3），现**跟随/飞往期间锁定为所属巡游域的主层级**（行星系统=L1 / 太阳系=L2 / 银河系=L3 / 宇宙=L4：显示当前行星时固定行星视角，银河系/宇宙巡游锁定各自视角），HUD 视角标签、控制面板高亮、G 键与事件演示按钮可用域随之稳定；点选"飞往"按目标域归类联动切换（点卫星→行星视角、点日球层顶→银河系视角、点星系→宇宙视角，飞往太阳保持当前域防耀斑通知误改写）；锁定持续到用户按 1-4/层级按钮显式切换或 Esc 取消跟随后恢复距离驱动（确认项 3）；连续层级 `continuousLevel` 仍按相机距离同步，LOD/音景/时间压缩/背景色平滑行为不变
- 修复银河系视角下太阳系外天体被"日球层顶（示意）"球壳盖住无法点击的问题（R3 需求 3）：球壳为 BackSide 大球（半径 380 单位）且点击后 stopPropagation，其 raycast 开启窗口（可见度权重 >0.05，对应连续层级 1.8–3.0）与 L3 区间（2.5–3.5）重叠，L3 下点击球壳投影范围内的特殊天体/奥尔特云等会被球壳吞掉事件并弹出日球层顶面板；现新增 raycast 层级门控（`heliopauseRaycastEnabled`）——连续层级 ≥2.5（银河系视角）禁用球壳点选（球壳仍可见但不拦截点击），L2 太阳系视角内保留点选科普入口，跟随/飞往日球层顶或旅行者标记期间恒可点选（相机按精确相位解析目标而渲染用降速累计相位，近观 glTF 模型下卫星会在数秒内漂出视野；现渲染相位写入注册表，cameraFocus 优先按注册相位解析，跟随与渲染始终一致）
- 修复视角锚点切换时相机过渡目标被连续缩放同步改写导致无法到达目标视角的问题（过渡期间仅同步连续层级，不回写离散层级）
- 修复 Html 天体标签不随父级 visible 隐藏导致跨层级残留显示的问题（按层级门控渲染）
- 修复视角锚点切换竞态：部分帧时序下连续缩放同步先于过渡捕获执行导致切换失效（过渡目标捕获移至帧首执行，无头环境下原可 100% 复现）
- 修复跟随远距天体（如哈雷彗星 ~20 AU）时层级读数已为目标层级导致点击锚点按钮无法取消跟随回到固定锚点的问题
- 修复飞往运镜完成后首帧丢失目标天体位移导致跟随视野永久偏移的问题（高时间压缩下如 L2 跟随冥王星，单帧位移可达数个场景单位，天体会完全偏出近观视野；现于运镜落点当帧播种跟随基准，P5 无头目验发现并修复）
- 修复从银河系视角切回太阳系视角后银河粒子残影贴着太阳显示、被误认为"柯伊伯带位置错误"的问题：银河系层/特殊天体/超新星淡入起点从 2.05/2.1 统一收紧至 L2/L3 离散边界 2.5（HUD 显示"太阳系视角"期间银河系层内容完全不可见），并消除银河系组首帧短暂可见的竞态（初始 visible=false）
- 修复太阳系视角下点击页面会命中隐形银河系/宇宙级对象（如猎户座星云、星团透明点选热区）并弹出信息面板的问题：three.js Raycaster 不检查 visible，新增 raycast 门控工具（utils/raycastGate.ts），特殊天体、超新星遗迹、河外星系/类星体/引力透镜弧/伽马暴、奥尔特云等所有可点选对象在淡出后显式禁用 raycast
- 修复从银河系/宇宙视角切回太阳系视角后小行星带与柯伊伯带粒子坍缩成贴着太阳的错误团块的问题（真正根因）：银河系/宇宙视角的时间压缩使模拟时间推进到 10⁹⁺ 天，粒子带顶点着色器以 float32 计算平近点角 M = M₀ + n·t 时 n·t 达 10⁷ 弧度量级，超出 float32 与 GPU sin/cos 距离归约的可靠范围导致位置坍缩；现将传入 shader 的时间按固定窗口（2¹⁹ 天 ≈ 1435 年）回卷——粒子仍严格沿各自开普勒轨道运动（开普勒剪切保持），仅在窗口跨越时沿轨道相位统计性重排（近似已登记于 utils/belts.ts），无头 Chrome 复现验证 L3/L4 往返后粒子带恢复正常环状
- 排查并修复同类 shader 时间精度隐患（覆盖银河系/宇宙视角切换到太阳系/行星视角的全部路径）：木星云层差速流动相位（float32 uniform，时间推进后累计自转角 ~10¹⁰ 弧度致近观流动错乱）按 1536×2π 窗口回卷——基础自转对齐不受影响、任意模拟时刻云带观感与当前日期基线一致；银盘较差自转时间（宇宙视角长时间驻留后 ω·t 超出可靠范围）按 2048 Myr 窗口回卷（统计近似分别登记于 utils/jupiterFlow.ts / utils/galaxy.ts）；无头 Chrome 端到端验证 L3→L1、L4→L2、L4→L1（含木星近观）、返回 L3 银盘全部正常
- 修复 P6 银心参考系/垂直增益下飞往与跟随 L3 天体位置解析错误（P6 自查）：`cameraFocus.galacticPointToSceneUnits` 原按"跟随模式、无垂直增益"固定公式换算——银心固定模式下飞往特殊天体/超新星/银心会错位整个太阳轨道半径（约 1300 场景单位），跟随模式默认垂直增益 6 下所有银河系组内容的解析 y 与渲染偏差最高 ±72 单位（人马座A* 空间音效定位同受影响）；新增渲染位姿注册表（`galacticFrame.setRenderedGalacticFrame`，镜像 P7 卫星渲染相位注册表模式），Galaxy 每帧写入实际应用的银心固定权重与垂直增益，cameraFocus/SpatialAudio 按注册值解析，飞往/跟随与渲染始终一致（无头 Chrome 目验：银心固定模式下点选银心黑洞并飞往，相机正确收敛）
- 修复 P6 垂直增益不一致导致"随太阳共转"特殊天体相对太阳系垂直振荡漂移的问题（P6 自查）：银河系组偏移的 y 分量按视觉增益放大 6 倍，而特殊天体本地定位用未放大的太阳 y，两者相减不抵消——参宿四等邻近恒星会以 ±(6−1)·300 ly（约 ±72 场景单位）相对太阳系上下漂移（P6 之前相对固定）；现 `useGalacticPlacement` 与 cameraFocus 的 sun-relative 解析统一使用增益后太阳 y，特殊天体恢复相对太阳系固定偏移
- 银心固定模式下任何"飞往"自动切回跟随参考系（需求第二部分 §3.1.1"飞往后自动切回跟随模式"补实现）：银心固定参考系仅用于 L3 尺度观察，位于场景原点的目标（太阳系天体/银心黑洞）与低层级"原点=太阳系"语义矛盾（相机靠近原点后太阳系内容会在银心处淡入）；切回后渲染权重 2 秒过渡、飞往运镜每帧重解析目标位置，全程平滑无跳变（无头 Chrome 目验：银心固定下飞往银心黑洞自动回到跟随模式并正确近观、按 1 飞往地球后返回 L3 参考系为跟随太阳系）
- 太阳系标记（You are here）可点选（需求第二部分 §3.1.1 补实现）：L3 下点击标记选中太阳，经信息面板"飞往"即可导航回太阳系；标记隐藏（标记开关关闭或淡出）时点选热区同步禁用 raycast（无头 Chrome 目验：点击标记弹出太阳信息面板）
- 修复奥尔特云粒子为方形的问题（P6"全场景无方形粒子"验收补遗）：OortCloud 的 PointsMaterial 补设共享圆形软边贴图
- 超新星永久遗迹与遗迹渐显接入丝状星云纹理（需求第二部分 §3.2"与超新星遗迹生成路径共用（新遗迹同样受益）"补实现）：由径向渐变圆斑改为与蟹状星云共用的 shell 丝状壳纹理（缓存共享零额外生成成本），个体差异以事件 id 确定性旋转角表达，活跃动画遗迹渐显与归档后遗迹同一旋转角保证交接无跳变
- 修复 P6 恒星表面 shader 经度接缝（P6 自查）：对流噪声原以 2D 球面参数化（atan 经度展开）采样，在 ±180° 经线处不连续，红巨星/蓝巨星/造父一/WR 124 表面近观有一条垂直接缝；改为 3D 值噪声直接以单位球面坐标采样（无接缝、无极点收缩），纯函数镜像 `stellarSurface.hash3/valueNoise3D/convectionFbm3` 补单元测试（含跨原接缝连续性断言）；单元测试增至 1063 个（71 套件），覆盖率语句 99.04%、分支 96.93%、函数 100%、行 99.79%
- 修复太阳系标记点选热区吞掉附近特殊天体点击的问题（用户反馈：银河系视角下点击参宿四/参宿七等无法显示对应信息面板，弹出的是太阳面板、飞往后去了太阳系）：特殊天体在屏幕上聚集于"你在这里"标记周围，标记热区（半径 42 单位透明球）常被射线先命中并 stopPropagation 拦断天体自身点击；现热区改为让位规则——同一射线还命中其他可交互对象时不选中、不拦截，事件继续传播到天体自身的 onClick（无头 Chrome 目验：点击参宿四弹出参宿四面板、点击标记空白区仍选中太阳）
- 修复飞往/跟随 L3 特殊天体与超新星后目标完全不可见的问题（用户反馈"对应的天体无法正常显示"；回归自"银河粒子残影"修复将银河系内容淡入起点收紧至 2.5）：特殊天体距场景原点仅 150–400 单位，飞抵后相机距原点落入 L2 连续层级区间，目标随银河系内容整体淡出；现跟随/飞往银河系锚定天体（L3 特殊天体/超新星，纯函数 `isGalaxyAnchoredFocusId` 判定）期间对目标天体与银河系组做聚焦权重提升（0.5 秒平滑过渡，取消跟随后恢复层级门控；常规太阳系视角游览无跟随时行为不变，"银河粒子残影"修复不回退）；红巨星气体壳/WR 星云壳分段 16/20→32（近观轮廓无棱角）；单元测试增至 1067 个，覆盖率保持（99.04%/96.93%/100%/99.79%）（无头 Chrome 目验：点击参宿四→飞往→红巨星近观完全可见并跟随）
- 修复太阳黑子群科普卡片与"上一个/下一个"天体切换条底部居中重叠的问题（用户反馈：点选黑子后卡片较高，其顶部顶到切换按钮条上）：黑子群/日珥卡片可见时，`BodyCycleSwitcher` 由 `bottom-28` 上移至 `bottom-64`（卡片上方），卡片关闭后复位，附 300ms 平滑过渡；两者保持底部居中对称，功能不受影响（`selectedSolarFeature` 状态门控）
- 修复日球层顶/奥尔特云"点飞往后无运镜却显示跟随中"的假跟随死锁（R2-1，用户反馈点 1）：两者此前不在 `resolveFocusTarget` 任何解析分支，点"飞往"后 `followBodyId` 已写入但相机无运镜；现新增太阳系外围球壳解析分支（`shellFocusTarget`：目标点=太阳系原点，观察距离=2.2×示意半径，日球层顶 836 单位/奥尔特云 3,520 单位），飞抵后正常进入跟随；并加兜底策略——`requestFlyTo` 在目标解析失败时拒绝进入跟随状态并静默忽略（超新星事件按事件存在性校验），防止未来新增天体重蹈死锁（无头 Chrome 目验：L2 点选日球层顶→飞往→2.5 秒运镜后完整看到半透明球壳与标注、HUD 显示"跟随模式：日球层顶（示意）"、Esc 正常退出，跟随期间 60.0 FPS）
- 修复视角切换后信息面板跨视角残留的问题（R2-1，用户反馈点 1）：显式锚点切换（视角按钮/1-4 快捷键）现同步清空 `selectedBodyId` 与 `selectedSolarFeature`，右下角信息面板（含"飞往/跟随"按钮）自动关闭、无跟随残留；连续滚轮缩放跨越层级边界时不清空选中（遨游模式面板不闪退），L1/L2 现有 `BodyCycleSwitcher` 行为不回退（无头 Chrome 目验：面板打开状态下按 2/4/1 均自动关闭，滚轮从 L2 连续缩放至 L3 面板保持打开）
- 跟随日球层顶/奥尔特云期间聚焦权重提升防淡出（R2-1）：参照 `isGalaxyAnchoredFocusId` 模式，飞往/跟随期间球壳与标注权重提升为满值不受层级门控淡出影响（奥尔特云飞往落点连续层级 ~3.18 已越过其可见窗口 2.1–3.1 上缘，不提升则"飞过去却看不到"）；奥尔特云淡出窗口魔法数字收敛至 `utils/oort.ts` 常量；新增 `cameraFocusR2`（15 例）与 `storeR2`（12 例）单元测试，全量 1416 用例/90 套件通过，覆盖率 gate ≥90% 保持（语句 97.76%/分支 97.23%/函数 98.54%/行 99.41%），type-check/lint/build 全绿
- 修复 L3 视角下太阳系行星高速乱转窗口（R2-3，用户反馈点 2 前半）：行星冻结硬阈值 `continuousLevel > 3.2` 与 L3 锚点（~3.0，时间压缩比已达 ~2×10⁶ 年/秒）之间存在"高压缩比未冻结"窗口，连续层级 2.5–3.2 区间行星每帧数千圈视觉乱跳；现改为 **2.6→3.0 渐变淡出区间**（`utils/freezeGate.ts` 统一收敛 Planet/Comet/SolarSystem/Moon 四处魔法数字，smoothstep 淡出，行星进 L3 锚点前已完成隐藏，与 Belt 带状结构 2.6–3.2 淡出节奏协调）：A) 行星/矮行星/彗星与其轨道线同步淡出（本体以整组缩放收敛呈现——该层级下行星为亚像素点，观感等效透明度淡出且无需给全部材质加透明通道，已登记；轨道线/标签做真正的透明度淡出）；B) 速率钳制兜底——淡出区间部分可见时公转视觉角速度应用与卫星一致的 `rateClampFactor`（0.5 圈/秒阈值），钳制中按降速角速度累计相位（`advanceClampedPhase` 无跳变）并以等效时间调用开普勒求解入口（`equivalentDaysForPhase`，位置/速度/自转全链一致），HUD 提示"行星运动已减速显示"（与卫星文案区分，多天体聚合上报防互写抖动）；C) 时间连续性——淡出/冻结只影响视觉呈现，返回 L2/L1 按共享 `simDays` 重新求值（无头目验：L3 停留 30 秒后返回，模拟时间 76.58→78.11 百万年连续推进无重置）；D) 钳制期间相机跟随一致性——行星/彗星渲染相位写入注册表，`cameraFocus` 优先按注册相位解析目标（P7 卫星范式扩展，含钳制行星的卫星）；太阳本体 L3 保持可见（太阳系标记热区不受影响）。新增 `freezeGate` 单测套件（24 例，覆盖率 100%）+ `cameraFocusR2`/`storeR2` 扩展 5 例，全量 1474 用例/92 套件通过，覆盖率 gate ≥90% 保持（语句 97.84%/分支 97.40%/函数 98.59%/行 99.43%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）17/17 项通过：L2→L3 缓慢缩放海王星标签 opacity 单调平滑下降（1.00→0.98→…→0.00 无阶跃）、淡出区间无乱跳（单步位移 <200px）、L3 锚点观察 30 秒行星始终隐藏无闪烁、L3 60.2 FPS/返回 L2 60.1 FPS，截图核对登记
- 修复运镜路径贴近人造卫星时"卫星巨大化铺满屏幕"的问题（R2-2，用户反馈点 6）：A) 角尺寸钳制——卫星 glTF 模型与远观盒体投影屏占比超过屏幕高度约 10% 时按比例缩小显示尺寸（`satelliteScreenClampFactor` 连续函数无阶跃），相机极近（穿模路径）时叠加平滑淡出（`satelliteProximityFade01`，0.4→0.08 场景单位 smoothstep 至全透明），任意相机距离下不再出现卫星铺屏；B) 飞往运镜（2.5 秒窗口）/视角锚点过渡（2 秒窗口）内近观放大冻结为 1×，到达跟随后 ≤1 秒限速平滑恢复（`approachNearMagnification` 双向限速逼近，无尺寸跳变；差异登记——现有 store 语义下 `flyToBodyId` 飞抵后保留不清空，故以 `flyToRequestId` 变更后的运镜时长窗口等效判定"运镜进行中"）；C) 近观细节视角域门控——行星 4K/法线细节层与人造卫星精细模型仅在焦点目标（飞往/跟随/L1 锚定）属同一行星系统时激活（`focusBodyIdForDetail`/`planetDetailScopeAllowed`/`satelliteDetailScopeAllowed`，`detailGateUpdateScoped` 叠加判定，不一致时立即退出并释放显存），防运镜路径擦过其他天体时误激活，L2-L4 无焦点自由镜头回落距离判据防误杀；新增 `satellitesR2` 单元测试套件（29 例，含极限值距离→0/跨度 0），全量 1445 用例/91 套件通过，覆盖率 gate ≥90% 保持（语句 97.81%/分支 97.35%/函数 98.57%/行 99.42%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）13/13 项通过——从 L2/L3/L4 循环切换到地球视角 20 次（不同模拟时间相位），卫星最大屏占比逐次实测 4.6%–10.0%（钳制上限恰生效，无一次超过约 10%/无一次铺屏）；跟随地球切换至 ISS 后近观放大平滑恢复至 ≈3×、精细模型激活且屏占比仍钳在 10.0%；跟随火星时地球 4 颗人造卫星精细模型/近观放大全部未激活；跟随 ISS/火星 FPS 60.0–60.2 满帧，零控制台错误
- 实现动态事件视角域隔离（R2-4，用户反馈点 4，行为变更）：各视角只呈现本视角域的动态事件——太阳耀斑/CME/CME 抵达属太阳系尺度（连续层级 ≤2.4，与 SunActivity 特效平台一致）、超新星属银河系尺度（≥2.5，与特效淡入起点一致）、银河系—仙女座合并预览属宇宙尺度（≥3.6）。新建 `utils/eventScopes.ts` 纯逻辑建立事件 → 视角域窗口映射与三层门控判定（自动触发域/通知可见域/按钮可用域）：A) 通知按域过滤——域外隐藏完整通知卡片并折叠为一行小字提醒（方案 b，如"太阳耀斑进行中（切回太阳系视角查看）"，保证不错过演示），事件状态照常推进，回到域内且事件仍活跃时通知自动恢复（通知标志位不改动）；"飞往观看"经既有运镜+相机距离层级同步天然落在事件视角域内（验收确认，无"飞过去却看不到"）；B) 演示按钮按域门控——耀斑/CME 按钮 L3/L4 置灰、超新星按钮 L1/L2 置灰、合并预览按钮 L1-L3 置灰，均附 tooltip"请切换到 XX 视角触发"与按钮文案提示（取默认"置灰 + 提示"方案；差异登记——合并预览按钮此前点击会自动切到 L4，现改为域外置灰）；C) 自动触发显式域门控——耀斑/CME 泊松触发显式限定 ≤2.4（此前 L3/L4 停摆仅是高时间压缩比下 Δ>50 天恒触发 `timeJumped` 守卫的副作用，非显式设计；`timeJumped` 时间跳变防护语义保留），超新星泊松触发显式限定 ≥2.5（此前 L1/L2 不触发仅是低压缩比 ΔMyr≈0 的概率副作用，补显式判定与注释）；域外仅抑制新触发，活跃事件的触发/衰减/遗迹归档全链不受视角切换影响。新增 `eventScopes` 单测套件（15 例，窗口映射/闭区间边界/三层一致性/L1-L4 锚点矩阵/非法输入/文案，覆盖率 100%），全量 1489 用例/93 套件通过，覆盖率 gate ≥90% 保持（语句 97.86%/分支 97.44%/函数 98.61%/行 99.44%），type-check/lint/build 全绿；无头 Chrome 目验（Metal 1280×800）33/33 项通过：四视角 × 四类事件按钮可用域矩阵 16 格逐格复验（置灰状态与 tooltip 全部正确）、L3/L4 各观察 10 秒无耀斑/CME 通知弹出、L2 触发耀斑+CME 后切 L3/L4 卡片隐藏且折叠提醒出现、切回 L2 卡片恢复、L3 解除暂停后事件域外快速完成（状态机未被冻结）、L3 触发超新星切 L2 卡片隐藏折叠提醒出现、切回 L3 活跃事件卡片恢复、超新星"飞往观看"进入跟随且保持银河系尺度、L4 合并预览正常启动，零控制台错误，截图核对登记
