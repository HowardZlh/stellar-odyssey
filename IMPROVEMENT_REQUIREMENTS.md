# 改进需求文档

> **文档版本**: 2.0（新增第二部分 P6 迭代：太阳系银心轨道运动可感知化 + 银河系/宇宙天体细节真实感提升）
> **状态标记**: ✅ 已完成 / 🔶 部分完成 / 🔲 未实现
>
> | 部分 | 迭代 | 主题 | 状态 |
> |---|---|---|---|
> | 第一部分 | P5 | 太阳系矮行星展示与近观细节 | 🔲 待实现 |
> | 第二部分 | P6 | 太阳系银心轨道运动可感知化 + L3/L4 天体细节真实感提升 | 🔲 待实现 |

---

# 第一部分（P5 迭代）：太阳系矮行星展示与近观细节

> **参考文档**: REQUIREMENTS.md v2.6（§3.1.1 小天体与外围结构、§3.2.4 行星视角天体切换、§4.7 行星近观表面细节）
> **迭代定位**: P5，依赖 P4 已交付的天体切换序列（`utils/bodyCycle.ts`）与 4K/法线懒加载门控机制（`textureManager` LRU + 近观阈值）。

## 1. 背景与问题

### 1.1 用户反馈

太阳系视角（L2）下矮行星"没有展示出来"；期望：

1. 切换到太阳系视角时能看到矮行星及其细节（位置、轨道、可辨识的存在感）；
2. 能切换（飞往/跟随）到对应矮行星，近距离看到对应的细节纹理。

### 1.2 现状分析（已调研代码确认）

| 能力 | 现状 | 差距 |
|---|---|---|
| 数据 | 已有 4 颗：冥王星、阋神星、鸟神星、妊神星（`src/data/smallBodies.ts`，完整轨道六要素 + JPL SBDB 来源） | **缺谷神星**（IAU 认定的第 5 颗矮行星） |
| 渲染 | 复用 `Planet` 组件（球体 + terminator 光影 + 程序化纹理），轨道线紫色 `#aa99cc` | L2 下位于 30–68 AU 远处，压缩后半径钳制在最小值 0.3 单位，**视觉上淹没在柯伊伯带粒子中，肉眼几乎不可辨识** |
| 点选/信息 | 可点选，catalog 有条目（含"与海王星 2:3 共振"等），可通过信息面板"飞往/跟随" | 用户不知道矮行星在哪，难以点选到 |
| 天体切换 | P4 序列 `BODY_CYCLE_SEQUENCE` 固定 11 个（8 行星 + 月球 + 2 彗星），`[`/`]` 快捷键 | **矮行星不在切换序列中**，无法直达 |
| 纹理 | 冥王星有专属程序化纹理（心形汤博区示意）；阋神星/鸟神星/妊神星走通用岩石纹理 `paintGenericRock` | **无任何真实位图纹理条目**（`textures.ts` 中 `textureUrl('pluto','surface')` 返回 null）；近观细节不足 |
| 形态 | 妊神星按平均半径渲染为正球体 | **椭球外形未表现**（真实为 2100×1680×1074 km 三轴椭球，快速自转导致） |

## 2. 需求范围

- **纳入**：5 颗 IAU 官方矮行星——谷神星（新增）、冥王星、妊神星、鸟神星、阋神星
- **纹理策略（已确认）**：真实优先 + 程序化增强——冥王星/谷神星采用 NASA 任务真实贴图；阋神星/鸟神星/妊神星无真实表面图，采用基于观测特征的高质量程序化纹理并登记艺术化差异
- **排除项**：见 §8

## 3. 功能需求

### 3.1 谷神星数据新增

- 🔲 在 `src/data/smallBodies.ts` 新增谷神星（Ceres）`PlanetData` 条目，纳入 `DWARF_PLANETS` 聚合：
  - 轨道六要素：半长轴 a≈2.77 AU、离心率 e≈0.0785、倾角 i≈10.59°、升交点经度/近日点幅角/历元平近点角按 JPL SBDB J2000 数据
  - 物理参数：半径 469.7 km、自转周期 9.07 h、轴倾角约 4°、质量 9.38×10²⁰ kg、公转周期 4.60 年
  - `classificationZh: '矮行星'`，数据来源标注 NASA JPL SBDB / Dawn 任务
- 🔲 谷神星位于**小行星带内**（2.2–3.2 AU），渲染时与小行星带粒子共存无冲突；轨道线与其他矮行星统一紫色系
- 🔲 catalog 信息面板条目：类型"矮行星"，附注"小行星带中最大天体，Dawn 探测器 2015 年环绕探测"

### 3.2 L2 太阳系视角可见性增强（核心需求 1）

> 原则：默认模式允许视觉夸大以保证可辨识（登记于代码文件头），真实比例模式保持科学事实（不可见即不可见）。

- 🔲 **最小可见尺寸提升**：默认（非真实比例）模式下，矮行星显示半径的最小钳制值提升至可辨识水平（建议与水星在 L2 下的视觉尺寸同量级），确保 L2 视角下 5 颗矮行星均为可辨识的独立天体而非湮没于柯伊伯带粒子；夸大策略在 `utils/scale.ts` 或相关文件头登记
- 🔲 **轨道线默认显示**：矮行星轨道线随"轨道线开关"统一控制（现状保持紫色 `#aa99cc` 区分八大行星），冥王星高倾角（17°）与阋神星高倾角（44°）轨道三维姿态正确（现有 OrbitLine 已支持，验收确认）
- 🔲 **标签显示**：标签开关（L 键）开启时，L2 视角下显示矮行星名称标签（与八大行星标签同机制），避免用户找不到目标
- 🔲 **真实比例模式**：开启时矮行星与八大行星同规则线性映射（现状保持），信息面板/帮助中说明"真实比例下矮行星过小不可见属科学事实"
- 🔲 柯伊伯带粒子与矮行星实体的视觉区分：矮行星亮度/尺寸明显高于带内粒子，不产生"多了几颗大粒子"的混淆

### 3.3 矮行星纳入天体切换序列（核心需求 2，扩展 P4 §3.2.4）

- 🔲 **切换序列扩展**：`BODY_CYCLE_SEQUENCE` 从 11 个扩展为 16 个，按日心距离插入：
  ```
  水星 → 金星 → 地球 → 月球 → 火星 → 谷神星 → 木星 → 土星 → 天王星 → 海王星
  → 冥王星 → 妊神星 → 鸟神星 → 阋神星 → 哈雷彗星 → 恩克彗星 → 回到水星
  ```
  （矮行星按半长轴排序：谷神星 2.77 AU 插于火星与木星之间；柯伊伯带四颗按 39.5 / 43.1 / 45.8 / 67.9 AU 排于海王星后；彗星维持序列末尾）
- 🔲 **切换控件与快捷键**：复用 P4 的「← 上一个 | 当前天体名 | 下一个 →」控件（`BodyCycleSwitcher`）与 `[` / `]` 快捷键，无需新增交互模式
- 🔲 **HUD 序列位置**：显示更新为 16 天体计数（如"冥王星 11/16"）
- 🔲 **飞往/跟随**：复用现有 `requestFlyTo` / `cameraFocus.resolveFocusTarget`（矮行星已支持，验收确认），2.5 秒平滑运镜后自动跟随
- 🔲 **聚焦距离适配**：矮行星半径小（470–1188 km），`viewDistanceUnits` 计算需保证近观时天体充满合理视野比例，与八大行星体验一致

### 3.4 矮行星细节纹理（核心需求 2，真实优先 + 程序化增强）

**真实位图纹理（公有领域来源）**

- 🔲 **冥王星**：NASA New Horizons 全球拼接彩色地图（公有领域），2K 底图登记入 `BODY_TEXTURES`；近观可辨识**汤博区（心形，斯普特尼克平原）**、克苏鲁暗斑等真实地貌；替换现有程序化心形示意
- 🔲 **谷神星**：NASA Dawn 任务全球地图（公有领域），2K 底图；近观可辨识**欧卡托撞击坑亮斑**（碳酸钠沉积）
- 🔲 新增纹理在 `src/data/textures.ts` 登记 URL 与许可，README 补充来源登记；加载失败静默降级为程序化纹理（现有机制）
- 🔲 可选：冥王星/谷神星 4K 近观层 + 法线贴图（冥王星 New Horizons 高程数据、谷神星 Dawn HAMO 高程数据均公有领域），接入 P4 的近观懒加载门控与 LRU 释放机制；若 P4 门控机制未就绪则本项顺延

**程序化增强纹理（无真实表面图的天体，艺术化处理需登记）**

- 🔲 **阋神星**：高反照率（0.96，太阳系反照率最高天体之一）亮白色调 + 甲烷冰霜质感；程序化纹理质量升级（区别于通用岩石）
- 🔲 **鸟神星**：红棕色调（表面甲烷/乙烷冰，观测色指数偏红）+ 低对比度斑块
- 🔲 **妊神星**：高反照率结晶水冰亮面 + "暗红斑"（观测到的表面特征）
- 🔲 三者纹理参数基于真实观测特征（颜色、反照率、光谱），**艺术化推测部分在 `proceduralTextures.ts` 文件头登记**，信息面板注明"表面细节为基于观测数据的艺术化呈现（无探测器实拍图）"

**形态特殊性**

- 🔲 **妊神星三轴椭球**：按真实比例 2100×1680×1074 km 缩放球体网格呈现椭球外形（快速自转 3.9 h 导致，为已知矮行星中自转最快），自转动画下椭球翻滚效果可见；真实比例/默认模式均生效
- 🔲 可选：冥卫一卡戎（半径 606 km，冥王星-卡戎互相潮汐锁定的双矮行星系统），绕冥王星公转 6.39 天，纳入卫星系统渲染；实现时信息面板注明"共同质心位于冥王星体外"

### 3.5 音效与信息联动

- 🔲 L1 跟随矮行星时音景处理：矮行星均无有效大气，复用"水星近真空几乎静音"音景参数（`src/data/sounds.ts` 增加映射），无需新增音景类型
- 🔲 切换/选中矮行星时的 UI 音效复用现有机制（选择双音、飞往运镜），无需新增

## 4. 性能与资源需求（硬性）

- 🔲 新增位图纹理（冥王星/谷神星 2K）纳入现有 `textureManager` 懒加载优先级队列：接近 L1/聚焦时才请求，不影响首屏 < 5 秒
- 🔲 4K/法线近观层（如实现）遵循 P4 门控：仅当前锚定天体保留、LRU 释放、显存增量计入 P4 的 ≤300 MB 总预算
- 🔲 L2 可见性增强不引入新 draw call 峰值问题（5 颗矮行星实体渲染成本可忽略，验收确认 60 FPS 不跌破）
- 🔲 矮行星近观细节仅 L1 渲染（复用现有 `continuousLevel` 门控）

## 5. 数据来源登记（附录 B 补充）

- 谷神星：a≈2.77 AU、e≈0.0785、i≈10.59°、半径 469.7 km、自转 9.07 h、质量 9.38×10²⁰ kg（NASA JPL SBDB / Dawn）
- 冥王星贴图：NASA New Horizons LORRI/MVIC 全球拼接图（公有领域）
- 谷神星贴图：NASA Dawn FC 全球地图（公有领域）
- 阋神星反照率 0.96（Sicardy et al. 2011 掩星测量）；鸟神星色指数偏红（光谱观测）；妊神星三轴 2100×1680×1074 km、自转 3.9 h（Ortiz et al. 2017 掩星）
- 冥卫一卡戎：半径 606 km、公转 6.39 天、双向潮汐锁定（New Horizons）

## 6. 验收标准

### 功能验收
- 🔲 L2 太阳系视角下 5 颗矮行星（含新增谷神星）可辨识：独立天体外观 + 紫色轨道线 + 名称标签（标签开关开启时）
- 🔲 `[` / `]` 与切换控件可循环直达 16 个天体（含 5 颗矮行星），HUD 序列位置正确，运镜平滑并自动跟随
- 🔲 谷神星位于小行星带内、冥王星保持 17° 倾角与海王星 2:3 共振、阋神星 44° 高倾角轨道姿态正确
- 🔲 真实比例模式下矮行星按真实线性比例渲染（不可见为预期行为），关闭后恢复可辨识尺寸

### 视觉验收
- 🔲 L1 近观：冥王星心形汤博区（真实贴图）、谷神星欧卡托亮斑（真实贴图）可辨识
- 🔲 L1 近观：阋神星亮白冰面、鸟神星红棕色调、妊神星椭球外形 + 暗红斑可辨识；程序化增强质感明显优于通用岩石纹理
- 🔲 妊神星自转时椭球翻滚可见
- 🔲 纹理加载失败静默降级为程序化纹理，无报错无空窗
- 🔲 L2 下矮行星与柯伊伯带粒子视觉可区分

### 性能验收
- 🔲 垂直同步 60 FPS 不跌破（L2 全景 / L1 矮行星近观均实测登记）；显存增量符合 §4 预算

### 测试验收
- 🔲 新逻辑纯函数镜像 + 单元测试：谷神星数据校验、切换序列扩展（16 天体顺序/循环）、矮行星最小可见尺寸策略、妊神星椭球缩放、纹理清单校验（冥王星/谷神星 URL 条目）、音景映射
- 🔲 覆盖率 gate ≥90% 保持；现有 dwarfPlanets 相关测试更新（如 `getDwarfPlanetById('ceres')` 断言由 undefined 改为存在）
- 🔲 更新 CHANGELOG.md `[Unreleased]`；README 登记新纹理来源与许可

## 7. 开发优先级建议

1. 🔲 P5-1 谷神星数据 + catalog 条目（§3.1）
2. 🔲 P5-2 L2 可见性增强：最小尺寸 + 标签（§3.2）—— 解决"看不到"的核心痛点
3. 🔲 P5-3 切换序列扩展至 16 天体（§3.3）—— 解决"切不过去"的核心痛点
4. 🔲 P5-4 冥王星/谷神星真实贴图接入（§3.4）
5. 🔲 P5-5 阋神星/鸟神星/妊神星程序化增强 + 妊神星椭球（§3.4）
6. 🔲 P5-6 可选项：4K/法线近观层、冥卫一卡戎（§3.4，依赖 P4 门控机制就绪）
7. 🔲 P5-7 测试补齐 + CHANGELOG/README 登记（§6）

## 8. 本期明确排除

- 其他柯伊伯带候选矮行星（共工星、创神星、赛德娜等）——数据来源不完整，留待后续
- 阋卫一（Dysnomia）、妊神星环与卫星系统——观测数据有限，且妊神星环在当前尺度下不可辨识
- 矮行星专属差异化音景——均无有效大气，复用近真空音景即可
- 柯伊伯带粒子密度/分布调整——本期只解决矮行星实体可见性，不动粒子带

---

# 第二部分（P6 迭代）：太阳系银心轨道运动可感知化 + L3/L4 天体细节真实感提升

> **参考文档**: REQUIREMENTS.md v2.6（§3.1.2 太阳系在银河系中的运动、§3.1.5 特殊天体与动态事件系统、§4.4 银河系视觉、§4.5 宇宙视觉）
> **迭代定位**: P6，不依赖 P5；涉及 `Galaxy.tsx`、`SpecialBodies.tsx`、星系/宇宙网渲染与相机模式扩展。

## 1. 背景与问题

### 1.1 用户反馈

1. 银河系视角（L3）下太阳系"虽然有轨道但并没有在轨道内运行"——看不到太阳系沿轨道运动；
2. 系内标注的天体（红巨星/星云等特殊天体）细节太粗糙，需要更加细节真实。

### 1.2 现状分析（已调研代码确认）

**问题 A：运动逻辑在动，但视觉不可感知（非计算 bug）**

`Galaxy.tsx` 每帧按模拟时间反向平移银河系组（太阳系恒居原点），L3 下约 100–115 真实秒转完一个银河年，`sunGalacticPositionLy` 等纯函数有单测覆盖。但呈现设计抵消了全部运动线索：

| # | 线索抵消原因 | 位置 |
|---|---|---|
| 1 | 太阳系被钉死在屏幕原点（反向平移 + 相机 target=原点），运动只能靠"银河系整体相对滑动"体现 | `Galaxy.tsx` L238-265、`cameraViews.ts` L37 |
| 2 | 未来预测线为一整个银河年的**近闭合圆**，圆绕圆心旋转视觉不可分辨；历史尾迹与预测圆重叠 | `Galaxy.tsx` L220-236 |
| 3 | 平坦旋转曲线下太阳与邻域银盘粒子角速度几乎相同（0.0273 vs 0.0282 rad/Myr），太阳周边星场近似静止（物理正确但强化"不动"感受） | `utils/galaxy.ts` L114-119 |
| 4 | 垂直振荡振幅仅 ±300 ly = 15 场景单位（轨道半径 1300 单位的 1.2%），相机距离约 2900 单位下不可辨 | `utils/galaxy.ts` L76-86 |
| 5 | L3 特殊天体全部"随太阳共转"（位置 = 太阳位置 + 固定偏移），相对太阳静止，进一步消除运动参照物 | `SpecialBodies.tsx` L69-75 |

另发现数据不自洽：银河年 230 Myr 与"26000 ly 处 220 km/s"推出的角速度相差约 3%（0.0273 vs 0.0282 rad/Myr）。

**问题 B：L3/L4 天体渲染细节粗糙**

| 类别 | 现状 | 粗糙点 |
|---|---|---|
| 恒星类（红巨星/蓝巨星/白矮星/造父一/WR 124） | 12–24 段球体 + `meshBasicMaterial` 纯色 + 辉光 sprite | 无表面纹理、无对流颗粒、无边缘昏暗（limb darkening），白矮星就是 12 段小白球 |
| 星风/星团粒子（蓝巨星星风、M13、昴星团） | `PointsMaterial` 未设置 `map` | **粒子渲染为方块**，星团单一颜色 |
| 星云类（猎户座/环状/蟹状/马头/昴星团反射云） | 统一复用 `createGlowSpriteCanvas` 的 **128px 三停径向渐变圆斑**；马头星云为 3 个深色圆 sprite 拼接；环状星云为低分段硬边 torus | 所有星云均呈"圆形光晕"，无丝状/云状/不规则形态，示意性极强 |
| 黑洞（人马座A*） | 吸积盘 + 引力透镜 shader | 相对最精细，本期仅小幅打磨 |
| 银河系整体 | 4 万粒子银盘 + 多层辉光 sprite | 核球/银晕辉光为径向渐变圆斑；星云区为均匀粉色着色，无团块感 |
| L4 河外天体 | 星系四类程序化纹理、类星体/M87 喷流为几何体+sprite | 星系纹理分辨率与形态细节有限；喷流无湍流/节点结构感 |

## 2. 需求范围（已确认）

- **运动可感知化**：双方案并行——新增银心参考系观察模式 + 强化现有跟随模式的运动线索
- **细节提升范围**：L3 特殊天体（主体）+ 银河系整体视觉 + L4 河外天体
- 保持科学准确性优先原则：所有视觉夸大/艺术化处理登记于代码文件头

## 3. 功能需求

### 3.1 太阳系沿银心轨道运动可感知化（核心需求 1）

#### 3.1.1 银心参考系观察模式（新增）

- 🔲 **模式定义**：L3 下新增"银心视角"观察模式——银河系组固定不动（银心居场景原点），**太阳系标记沿固定轨道线实际移动**；与现有"跟随太阳系"模式互补
- 🔲 **切换入口**：控制面板 L3 区域新增"参考系：跟随太阳系 / 银心固定"切换开关（或 HUD 快捷按钮）；快捷键建议 `G`（避开已占用 1-4/空格/M/O/L/F/R/Esc/`[`/`]`）
- 🔲 **银心模式行为**：
  - 相机 target 改为银心（原点），太阳系标记（You are here + 辉光 + 方向箭头）沿轨道线移动，垂直振荡呈现为标记沿波浪轨迹起伏
  - 轨道线固定不动（预测线不再随太阳重算旋转），历史尾迹跟随标记延伸——"标记在轨道内跑"直接可见
  - 银盘粒子较差自转、旋臂密度波、银心辉光维持现有动画
  - 太阳系标记可点选/可"飞往"，飞往后自动切回跟随模式
- 🔲 **模式切换过渡**：两种参考系切换时相机平滑过渡（复用现有 2 秒 easeInOutCubic 运镜），场景内容无跳变（同一模拟时间轴，仅坐标系原点不同）
- 🔲 **HUD 联动**：参考系说明（§3.1.3 参考系 HUD）随模式实时更新（"银心系（跟随太阳系）" / "银心系（银心固定）"）
- 🔲 坐标变换纯逻辑抽取 `src/utils/galacticFrame.ts`（跟随/银心两模式下银河系组与标记的位姿计算），单元测试覆盖

#### 3.1.2 跟随模式运动线索强化

- 🔲 **轨道运动刻度**：轨道线上叠加流动标记（沿运动方向流动的短线/光点动画，速率与实际角速度一致），使"银河系在相对滑动"可感知
- 🔲 **预测线改为非闭合弧段**：未来预测虚线由"一整个银河年闭合圆"改为**前方约 1/4 银河年弧段**（随时间推进滚动刷新），与历史尾迹首尾衔接不重叠，避免"圆转圆看不出动"
- 🔲 **垂直振荡可视化增强**：
  - 尾迹/预测线的波浪形起伏在 L3 默认相机距离下可辨（可对垂直分量做视觉放大，放大倍数登记于文件头；真实比例模式不放大）
  - You are here 标记附加"相对银盘面高度"指示（如标记到银盘投影点的细线 + 当前高度 ly 数值）
- 🔲 **银河年进度联动**：HUD 现有银河年进度（圈数/百分比/角度）在 L3 下更醒目（如轨道线上高亮已走过弧段）
- 🔲 **数据自洽修正**：统一银河年与旋转曲线参数（230 Myr ↔ 26000 ly ↔ 220 km/s 三者取自洽组合，约 3% 偏差消除），修正值与来源登记于 `utils/galaxy.ts` 文件头，相关单测同步更新

### 3.2 L3 特殊天体细节真实感提升（核心需求 2 主体）

> 原则：基于真实观测形态（哈勃/JWST 公开影像为形态参考），程序化生成实现（不引入大位图资产，保持首屏 < 5 秒）；艺术化推测登记于文件头。

**恒星类表面质感**

- 🔲 **恒星表面 shader 升级**（红巨星/蓝巨星/造父一/WR 124 共用参数化 shader）：
  - 表面对流颗粒噪声（fBm 噪声随时间缓慢演化；红巨星对流胞大而明显——参宿四真实存在巨型对流胞，Betelgeuse 2019-20 大变暗事件佐证）
  - 边缘昏暗（limb darkening，N·V 幂次衰减）+ 色温梯度（红巨星边缘偏暗红）
  - 球体分段提升至 32–48（现 12–24），近观无棱角
- 🔲 **白矮星（天狼星B）**：小而致密的白蓝色高亮点 + 衍射芒线 sprite（观测质感），保持双星互绕动画
- 🔲 **脉冲星射束**：锥体射束升级为带径向渐变与噪声扰动的 shader 材质（现为纯色 cone），扫过视线时的脉冲闪烁保持
- 🔲 **粒子贴图修复（全局）**：所有 `PointsMaterial` 统一设置圆形软边贴图（复用/扩展 `createGlowSpriteCanvas`），消除方形粒子——影响蓝巨星/WR 星风、M13、昴星团、及其他 Points 系统

**星云类形态重构（消除"圆形光斑"观感的关键）**

- 🔲 **程序化星云纹理生成器**：新增 `utils/nebulaTexture.ts`（或扩展 proceduralTextures）——基于分层 fBm/域扭曲（domain warping）噪声生成 256–512px 不规则云状 alpha 纹理，参数化（颜色梯度/丝状强度/不规则度），确定性种子保证稳定
- 🔲 **猎户座星云（M42）**：多层不规则云状 sprite（3–4 层不同噪声种子+视差感）替换同心圆光斑；中心四边形聚星（Trapezium）示意 + 局部照亮的云隙结构；形态参考哈勃公开影像
- 🔲 **蟹状星云（M1）**：遗迹云升级为丝状结构纹理（蟹状星云标志性的红色氢丝网络），膨胀动画保持；与超新星遗迹生成路径共用（新遗迹同样受益）
- 🔲 **环状星云（M57）**：硬边 torus 替换为带径向渐变与噪声扰动的环壳 shader/贴图（内缘偏蓝绿 OIII、外缘偏红 Hα 的真实色层），中心白矮星保持
- 🔲 **马头星云**：3 个黑圆斑替换为程序化马头剪影形态纹理（噪声侵蚀边缘的暗云柱形态），背景 IC 434 红光保持
- 🔲 **昴星团反射星云**：蓝色反射云改用不规则云状纹理，包裹亮星的丝缕感
- 🔲 **星团颜色分布**：M13 粒子引入恒星色板（红黄为主的老年星族 + 少量蓝离散星），昴星团保持年轻蓝星为主；粒子大小随亮度分布

**通用**

- 🔲 所有升级后的动态效果保持现有按层级可见性门控（不可见时跳过 uniform 更新）
- 🔲 信息面板补充形态说明（如"丝状结构为超新星抛射物与星际介质相互作用"），数据来源标注

### 3.3 银河系整体视觉提升

- 🔲 **核球/银晕辉光**：径向渐变圆斑升级为带噪声扰动的多层辉光（核球呈略扁椭球光分布），银心方向亮度梯度更真实
- 🔲 **旋臂星云区团块感**：银盘粒子中的星云区（电离氢粉色）由均匀着色改为沿旋臂分布的团块状聚集（噪声聚类），近距观察旋臂时有 HII 区"串珠"感（形态参考真实旋涡星系影像）
- 🔲 **尘埃带示意**：旋臂内侧暗尘埃带（粒子亮度沿旋臂剖面不对称衰减），增强旋臂立体感与真实感
- 🔲 粒子总量维持现有预算（4 万级），以颜色/亮度/分布优化为主，不显著增加粒子数

### 3.4 L4 河外天体细节提升

- 🔲 **星系程序化纹理升级**：四类星系（旋涡/棒旋/椭圆/不规则）纹理生成质量提升——旋涡臂加尘埃带暗纹与 HII 区亮点、椭圆星系光度沿 de Vaucouleurs 轮廓衰减、不规则星系噪声团块；纹理尺寸维持 ≤1024px
- 🔲 **M31/M33 专属形态**：仙女座（倾斜盘面 + 显著尘埃环特征）与三角座（松散旋臂 + 大量 HII 区）差异化呈现，不与通用旋涡星系共用同一纹理
- 🔲 **类星体/M87 喷流质感**：喷流由纯色几何体升级为带节点（knots）与湍流噪声的 shader 材质（M87 喷流 HST-1 节点为真实观测特征），节点流动动画保持
- 🔲 宇宙网背景星系保持点渲染（性能预算），仅前景可辨识星系升级

## 4. 性能与资源需求（硬性）

- 🔲 全部升级采用程序化生成（canvas/shader），**不引入外部位图资产**，首屏加载 < 5 秒不回退
- 🔲 程序化星云纹理 ≤512px、生成一次缓存复用（确定性种子），生成耗时不阻塞首帧（可延迟到 L3 临近时生成）
- 🔲 恒星表面 shader 按可见性门控（L3 特殊天体现有门控保持），L1/L2/L4 下零开销
- 🔲 垂直同步 60 FPS 不跌破；实现后无头 Chrome 复测 L3 视角帧率并登记（现基线参考 P3：Bloom 开 ~394 FPS 非垂直同步）
- 🔲 银心参考系模式不新增场景对象，仅位姿计算切换，无额外渲染成本

## 5. 数据来源登记（附录 B 补充）

- 参宿四巨型对流胞与 2019-20 大变暗：ESO VLT/SPHERE 观测（Montargès et al. 2021）
- 猎户座星云四边形聚星（Trapezium）、马头星云暗云柱形态：NASA/ESA 哈勃公开影像（形态参考）
- 环状星云色层（内 OIII 蓝绿 / 外 Hα 红）：HST/JWST 公开影像
- 蟹状星云氢丝网络：HST 公开影像
- M13 星族颜色（老年红黄 + 蓝离散星）：HST 测光
- M87 喷流 HST-1 节点：HST 观测
- M31 尘埃环：Spitzer/Herschel 红外观测（形态参考）
- 银河年/旋转曲线自洽参数：IAU 推荐值（R₀≈8.2 kpc、Θ₀≈220–233 km/s，取自洽组合并登记）

## 6. 验收标准

### 功能验收
- 🔲 银心参考系模式下：轨道线固定、太阳系标记沿轨道可见移动（L3 默认速度下 2 分钟内可明显看出位置变化）、尾迹跟随标记延伸、垂直振荡起伏可辨
- 🔲 两种参考系模式切换平滑（2 秒过渡）、同一模拟时间轴无跳变，HUD 参考系说明实时更新
- 🔲 跟随模式下：轨道流动刻度动画方向与速率正确、预测弧段随时间滚动、高度指示线数值正确
- 🔲 银河年参数自洽（230 Myr ↔ 220 km/s 偏差消除），单测更新通过

### 视觉验收
- 🔲 红巨星近观可见对流颗粒缓慢演化与边缘昏暗，不再是纯色球
- 🔲 全场景无方形粒子（星风/星团/其他 Points 均为圆形软边）
- 🔲 猎户座/蟹状/环状/马头星云形态不规则、可与"圆形光斑"明确区分；马头星云呈暗云柱剪影
- 🔲 M13 呈红黄为主的颜色层次；昴星团反射云呈丝缕感
- 🔲 旋臂可见 HII 区串珠状团块与内侧尘埃带；核球辉光无明显"贴图圆斑"感
- 🔲 M31/M33 形态可与通用旋涡星系区分；M87/类星体喷流可见节点结构
- 🔲 以上均经无头浏览器截图核对 + 人工目验

### 性能验收
- 🔲 L3 视角垂直同步 60 FPS 不跌破（实测登记）；首屏加载 < 5 秒不回退；无新增位图网络请求

### 测试验收
- 🔲 新逻辑纯函数镜像 + 单元测试：`galacticFrame` 位姿计算、预测弧段采样、轨道流动刻度相位、垂直振荡视觉放大系数、星云纹理生成器参数校验、自洽参数修正
- 🔲 覆盖率 gate ≥90% 保持；更新 CHANGELOG.md `[Unreleased]`

## 7. 开发优先级建议

1. 🔲 P6-1 银心参考系观察模式 + 模式切换过渡（§3.1.1）—— 解决"看不到在轨道内运行"的核心痛点
2. 🔲 P6-2 跟随模式运动线索强化：预测弧段 + 流动刻度 + 垂直振荡增强（§3.1.2）
3. 🔲 P6-3 粒子贴图修复（方形→圆形，全局）+ 恒星表面 shader（§3.2）
4. 🔲 P6-4 程序化星云纹理生成器 + 四大星云形态重构（§3.2）
5. 🔲 P6-5 银河系整体视觉：核球辉光/旋臂团块/尘埃带（§3.3）
6. 🔲 P6-6 L4 河外天体：星系纹理升级 + M31/M33 专属形态 + 喷流质感（§3.4）
7. 🔲 P6-7 数据自洽修正 + 测试补齐 + CHANGELOG 登记（§3.1.2/§6）

## 8. 本期明确排除

- 体积渲染星云（ray marching 体积云）——成本高，程序化多层 sprite 已可达形态目标，留待后续
- L3 特殊天体真实位图纹理（哈勃影像直贴）——引入大位图违背首屏加载目标，仅作形态参考
- 特殊天体相对太阳的真实自行运动（proper motion）——真实量级在 L3 尺度不可辨，维持"随太阳共转"近似（已登记）
- 银盘粒子数量级提升（如 40 万粒子）——本期以颜色/分布优化为主，粒子规模扩容留待性能基准建立后评估
