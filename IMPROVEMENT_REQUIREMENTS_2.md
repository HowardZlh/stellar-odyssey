# 改进需求文档（第二批，R2 迭代）

> **文档版本**: 1.0（需求整理完成，全部待实现）
> **参考文档**: REQUIREMENTS.md v2.7、IMPROVEMENT_REQUIREMENTS.md v3.3（P5/P6/P7）、AGENTS.md
> **状态标记**: ✅ 已完成 / 🔶 部分完成 / 🔲 未实现
> **调研说明**: §0.2 现状分析中的文件与行号已经代码调研核实（2026-07），实现时若行号漂移以符号名为准。

## 优先级与阶段总览

| 阶段 | 优先级 | 主题 | 对应用户反馈点 | 依赖 | 状态 |
|---|---|---|---|---|---|
| R2-1 | P0 | 视角切换自动关闭信息面板 + 日球层顶/奥尔特云飞往解析修复 | 点1 | 无 | ✅ |
| R2-2 | P0 | 人造卫星巨大化修复 + 行星近观细节视角域门控 | 点6 | 无 | ✅ |
| R2-3 | P0 | L3 视角下太阳系内行星运动简化（消除高速乱转窗口） | 点2前半 | 无 | ✅ |
| R2-4 | P0 | 动态事件视角域隔离（通知/演示按钮/自动触发按层级门控） | 点4 | 无 | ✅ |
| R2-5 | P1 | 通用天体切换序列框架（L2/L3/L4 各视角域"上一个/下一个"） | 点5 + 点1后半 | R2-1 | ✅ |
| R2-6 | P1 | 太阳系绕银心轨道运动可感知细节增强 | 点2后半 | R2-3 | ✅ |
| R2-7 | P1 | L3 飞往/跟随目标近观细节升级（日球层顶结构 + 特殊天体近观 LOD） | 点3（L3 部分） | R2-1 | ✅ |
| R2-8 | P2 | L4 飞往星系近观细节升级（星系 3D 粒子近观层） | 点3（L4 部分） | R2-5 | ✅ |
| R2-9 | P2 | L4 银河系真实感重构（3D 银晕/球状星团/尘埃带/风格统一） | 点8 | 无 | 🔲 |
| R2-10 | P2 | L4 轨迹与运动一致性（卫星星系轨道线/方向修复/人马座矮星系运动） | 点9 | 无 | 🔲 |
| R2-11 | P2 | 银河系—仙女座碰撞合并后续演化 | 点7 | R2-9（形态基础） | 🔲 |
| R2-12 | 收尾 | 集成回归验收 + 文档回写 | 全部 | R2-1~R2-11 | 🔲 |

> 优先级依据：P0 = 交互正确性缺陷（用户操作产生错误/失控结果）；P1 = 交互完整性（功能缺口，体验不闭环）；P2 = 视觉真实感增强（不影响操作正确性）。
> 建议实现顺序：R2-1 → R2-2 → R2-3 → R2-4 → R2-5 → R2-6 → R2-7 → R2-9 → R2-10 → R2-8 → R2-11 → R2-12（R2-8 依赖 R2-5 的序列框架与 R2-9 的星系细节风格基准，故后置）。

---

## 0. 背景与现状调研

### 0.1 用户反馈原文归纳（9 点）

1. 银河系视角点击太阳系外围天体（如"日球层顶（示意）Heliopause (schematic)"）显示"飞往/跟随"面板；期望视角切换后该面板自动关闭，并显示"上一个/下一个"按钮。
2. 银河系视角下太阳系内行星运动太快；太阳系也看不出在银河系内对应轨道上运动。期望适当简化 L3 下行星运动，并增加太阳系沿银心轨道运动的细节。
3. 银河系内太阳系天体细节太粗糙；"飞往/跟随"到对应天体时应展示更细节更真实的内容；宇宙视角"飞往/跟随"星系时同理。
4. 银河系视角和宇宙视角下应屏蔽太阳系/行星视角的事件（各视角只显示本视角域的事件）。
5. 参考太阳系视角实现，各视角"飞往/跟随"时都应有"上一个/下一个"天体切换按钮。
6. 切换到地球视角时人造卫星有时变得非常大覆盖整个屏幕；行星近观细节应只在对应行星视角下显示。
7. 宇宙视角下天体碰撞（银河系—仙女座合并）后应有后续演化展示，而不是直接停滞。
8. 宇宙视角下银河系呈现太粗糙（二维粒子旋转圆盘观感），期望更真实。
9. 宇宙视角下部分天体有轨迹线但没有沿轨迹运动。

### 0.2 现状调研结论（关键锚点速查）

| # | 现状机制 | 关键位置 | 核心差距 |
|---|---|---|---|
| 1 | `setViewLevel`（store）切换锚点只清 `followBodyId/flyToBodyId`，**不清 `selectedBodyId`** → 信息面板跨视角残留；`heliopause`/`oort-cloud` 不在 `resolveFocusTarget` 任何分支 → 点"飞往"后进入无运镜的"假跟随"死锁 | `src/store/index.ts` setViewLevel（约 :344-369）、`src/utils/cameraFocus.ts` resolveFocusTarget（约 :228-306）、`src/components/Scene/Heliopause.tsx` :76-79、`src/components/UI/HudInfo.tsx` :369-456（飞往/跟随按钮无条件渲染） | 面板不自动关；飞往死锁；无上一个/下一个 |
| 2 | 行星冻结阈值 `continuousLevel > 3.2`（Planet.tsx `FREEZE_LEVEL_THRESHOLD`），但 L3 标签从 2.5 起、L3 锚点约 3.0 → 存在"高压缩比（~2×10⁶ 年/秒）但未冻结"窗口，行星每帧数千圈视觉乱跳；行星无速率钳制（仅卫星有，Moon.tsx :160-192） | `src/components/CelestialBody/Planet.tsx` :47/:638-643、`src/components/Scene/SolarSystem.tsx` :23-24、`src/utils/time.ts` :13-22/:79-88/:141-154 | L3 锚点（3.0 < 3.2）恰落在乱转窗口内 |
| 3 | L3 聚焦提升白名单仅 `sn-*` + L3 特殊天体（`isGalaxyAnchoredFocusId`）；L4 星系为**固定朝向的 planeGeometry 程序化贴图平面**，飞近后是一张薄片，无 3D 结构/LOD 细节层 | `src/data/specialBodies.ts` :454-460、`src/components/Scene/Galaxy.tsx` :354-408、`src/components/Scene/Universe.tsx` GalaxyObject（:70-196） | 近观无细节升级路径 |
| 4 | 事件通知渲染条件仅 `noticeVisible && activeXxx`，与 viewLevel 无关；控制面板演示按钮不按视角过滤；耀斑/CME 自动触发在 L3/L4 停摆是 `timeJumped`（Δ>50 天）守卫的**副作用**而非显式设计；超新星特效 L1/L2 不可见但通知照发 | `src/components/UI/HudInfo.tsx` :135-297、`src/components/UI/ControlPanel.tsx` :234-304、`src/components/CelestialBody/SunActivity.tsx` :442-501/:557、`src/components/Scene/Supernova.tsx` :54-55/:325-340 | 事件与视角域未隔离 |
| 5 | `BODY_CYCLE_SEQUENCE` 固定 20 个太阳系天体；`cycleControlVisible` = L1 或跟随序列内天体；L3 特殊天体（specialBodies.ts）与 L4 星系（galaxies.ts 8 条目）无序列 | `src/utils/bodyCycle.ts` :19-40/:83-86、`src/components/UI/BodyCycleSwitcher.tsx`、`src/hooks/useKeyboardShortcuts.ts` :71-81 | L2/L3/L4 无切换控件 |
| 6 | `satelliteNearMagnification`：相机距离 ≤2.2 单位时放大 3×，**无角尺寸上限**；飞往地球运镜路径可能贴近/穿过卫星（轨道半径 0.77–1.9 单位）→ 透视下瞬间铺满屏幕 | `src/utils/satellites.ts` :169-196、`src/components/CelestialBody/SatelliteModel.tsx` :127-133、`src/utils/planetDetail.ts` :27-37、`src/utils/cameraFocus.ts` :63 | 缺角尺寸钳制 + 运镜期间缺门控 |
| 7 | 合并预览 12 秒快进到 4500 Myr；`mwM31SeparationLy` clamp ≥0 → 合并后 M31 与银河系**原地重叠**，辉光满值，仅标签换"已合并"，无任何后续演化 | `src/store/index.ts` :309-336/:579-604、`src/utils/universe.ts` :57-64/:139-168、`src/components/Scene/Universe.tsx` :415-447 | 到点即停滞 |
| 8 | L4 银河系 = L3 同一份 4 万粒子盘（权重平台区延伸到 4.5）+ 核球/银晕两张 billboard 辉光 sprite；粒子盘本身有 3D 厚度但银晕仅是贴图；与其他星系（平面贴图）风格不一致 | `src/components/Scene/Galaxy.tsx` :53/:175/:389-407/:563-585、`src/utils/galaxy.ts` :354-390 | 银晕/星团无 3D 结构，观感扁平 |
| 9 | LMC/SMC 沿圆轨道运动但**无轨道线**（麦哲伦星流拖尾易被误读为轨道）；动态位置公式忽略数据 `direction` 字段（与静态首帧位置/天区语义不符）；人马座矮星系完全静止（文案却称"正被潮汐撕裂"）；宇宙网除哈勃缩放外内部静止 | `src/components/Scene/Universe.tsx` :136-164/:319-342、`src/utils/universe.ts` :109-132、`src/data/galaxies.ts` :193-196 | 轨迹与运动不一致 |

---

# 第一部分（P0）：交互正确性修复

## R2-1 视角切换自动关闭信息面板 + 外围结构飞往解析修复（用户反馈点 1）

### 1.1 需求

**A. 视角切换自动关闭选中面板**
- ✅ `setViewLevel`（`src/store/index.ts`）切换视角锚点（L1-L4 任意方向）时，除现有清空 `followBodyId/flyToBodyId` 外，同步清空 `selectedBodyId` 与 `selectedSolarFeature`，使右下角信息面板（含"飞往/跟随"按钮）自动关闭（L1 分支与 L2-L4 分支均清空；已在同层级且无跟随时的早退分支保持原 no-op 行为——"切换"语义不含同层级重按）
- ✅ 连续缩放（滚轮）跨越离散层级边界时**不**自动清空选中（只有显式点击锚点按钮/按 1-4 快捷键才清空），避免遨游模式中面板闪退（`syncZoomLevel`/`syncCameraDistance` 路径不触碰选中状态，单测覆盖）
- ✅ 面板关闭后，若该视角域存在切换序列（R2-5 交付），显示"上一个/下一个"切换控件；R2-5 未交付前本阶段先保证 L1/L2 现有 `BodyCycleSwitcher` 行为不回退（`cycleControlVisible` 逻辑未改动，storeR2 单测断言 L1 分支跟随锚定天体行为不变）

**B. 日球层顶/奥尔特云飞往解析修复（消除假跟随死锁）**
- ✅ `resolveFocusTarget`（`src/utils/cameraFocus.ts`）新增 `heliopause` 分支：目标点取太阳系原点（球壳中心），观察距离按 `HELIOPAUSE_VISUAL_RADIUS_UNITS`（380 单位）给出能看清整个球壳的距离（`shellFocusTarget`：2.2× 半径 = 836 单位，落点连续层级 ~2.65 位于可见窗口内），飞抵后进入跟随
- ✅ `oort-cloud` 采用"补解析分支"方案（与日球层顶同一 `shellFocusTarget` 球壳解析：原点 + 2.2×1600=3,520 单位），死锁路径消除；因飞往落点层级（~3.18）越过其可见窗口上缘，同步给予聚焦权重提升（超出"二选一"最低要求，差异登记：未选"隐藏按钮"方案，飞往体验与日球层顶对齐）
- ✅ 兜底策略：`requestFlyTo` 在 `resolveFocusTarget` 返回 null 时拒绝进入跟随状态（不写入 `followBodyId`）并静默忽略（未采用 HUD 提示，二选一取静默；超新星事件 `sn-*` 由 CameraController 单独解析，兜底按事件存在性校验，活跃/遗迹事件均可飞往）
- ✅ 日球层顶可见窗口（连续层级 1.8–3.0，`utils/heliopause.ts`）与飞往行为联动：飞往/跟随期间聚焦权重提升为满值（`heliopauseVisibilityWeight(level, focused)` 纯函数，奥尔特云同款 `oortVisibilityWeight`），球壳与标注在跟随期间不因层级门控淡出，取消跟随后恢复层级门控

### 1.2 验收标准
- ✅ L3 下点击日球层顶 → 面板弹出 → 按 1/2/4 切换视角 → 面板自动关闭、无跟随残留（无头 Chrome 目验：按 2/4/1 三键均验证，截图 r21-a1-closed-after-key2.png；实测按 1 后按设计跟随锚定天体地球、无日球层顶跟随残留）
- ✅ 点击日球层顶"飞往"→ 2.5 秒运镜后看到完整半透明球壳与标注，HUD 显示跟随模式，Esc 可退出（无头 Chrome 目验：HUD"🔒 跟随模式：日球层顶（示意）"、标注"日球层顶（示意，实际约 120 AU）"与球壳完整可见，截图 r21-a2-following-heliopause.png；跟随期间 60.0 FPS；Esc 退出后跟随状态清除）
- ✅ 连续滚轮从 L2 缩放到 L3 期间选中面板不被强制关闭（无头 Chrome 目验：L2 点选日球层顶后滚轮连续缩放至视角按钮高亮"银河系视角"，面板保持打开，截图 r21-a3-panel-kept-L3.png）
- ✅ 新增纯逻辑（解析分支、null 兜底）有单元测试（`cameraFocusR2.test.ts` 15 例 + `storeR2.test.ts` 12 例）；覆盖率 gate ≥90% 保持（语句 97.76%/分支 97.23%/函数 98.54%/行 99.41%，heliopause.ts/oort.ts 100%）

## R2-2 人造卫星巨大化修复 + 行星近观细节视角域门控（用户反馈点 6）

### 2.1 问题定位（已调研）

飞往地球的运镜路径可能从人造卫星（ISS/天宫/哈勃/GEO，轨道半径 0.77–1.9 单位）旁边或内部穿过：`satelliteNearMagnification` 在相机距离 ≤2.2 单位时恒 3× 放大且**无角尺寸上限**，模型全展跨度 ~0.5 单位，透视投影下瞬间铺满屏幕。"有时发生"取决于卫星当帧相位与运镜进场方向。

### 2.2 需求

**A. 角尺寸钳制（核心修复）**
- ✅ 新增纯函数（建议 `src/utils/satellites.ts` 内 `satelliteScreenClampFactor(distanceUnits, bodySpanUnits, fovRad)` 或等效）：当卫星模型的投影角尺寸超过阈值（建议屏幕高度的 8–12%）时，按比例缩小显示尺寸，保证**任意相机距离下卫星不超过屏幕高度的约 10%**（已实现 `satelliteScreenClampFactor(distanceUnits, spanUnits, fovRad)`，阈值取区间中值 10%（`SATELLITE_MAX_SCREEN_HEIGHT_FRACTION`），钳制边界处系数恰为 1 连续无阶跃；无头 Chrome 20 次切换实测最大屏占比 4.6%–10.0% 恰被钳在上限）
- ✅ 钳制在 `SatelliteModel.tsx` 每帧 scale 计算处生效（`bodyRadius × mag × clamp`），过渡平滑无突变（连续函数，非阶跃）（超出需求补充：远观盒体（`Moon.tsx`）同样接入角尺寸钳制，运镜路径贴近尚未激活精细模型的卫星时盒体亦不超过约 10%）
- ✅ 相机极近（距离 → 0，穿模路径）时卫星平滑淡出（opacity → 0）而非放大，避免"从卫星内部看到模型内壁"（`satelliteProximityFade01`：0.4 单位起 smoothstep 淡出、0.08 单位全透明，与既有 LOD 交叉淡化 opacity 相乘）

**B. 运镜/视角过渡期间冻结近观放大**
- 🔶 飞往运镜进行中（`flyToBodyId` 非空）与视角锚点过渡进行中（`viewTransitionId` 变化后的 2 秒过渡窗口内），`satelliteNearMagnification` 固定为 1×（或直接跳过近观门控），到达并进入跟随后再平滑恢复（差异登记：现有 store 语义下 `flyToBodyId` 飞抵后保留不清空（跟随期间恒非空），按原文判定将永久冻结；改以 `flyToRequestId` 变更后的 2.5 秒运镜窗口（`MAG_FREEZE_FLY_TO_WINDOW_SECONDS`，与 `FLY_TO_SECONDS` 一致）等效判定"运镜进行中"，`viewTransitionId` 2 秒窗口按原文实现（`nearMagnificationFrozen`）；模型挂载常发生在运镜途中，挂载即从 0 计时按冻结处理）
- ✅ 恢复过程 ≤1 秒平滑插值，无尺寸跳变（`approachNearMagnification` 双向限速逼近，1×↔3× 全程恰 1 秒，冻结与恢复同速率；实测跟随 ISS 稳态放大平滑恢复至 ≈3×）

**C. 行星近观细节视角域门控（"单个行星视角的细节只在对应行星视角显示"）**
- ✅ 人造卫星 glTF 精细模型（P7）仅在 **L1 语境且跟随/锚定目标为地球或该卫星本身**时激活近观放大与模型细节层；跟随其他行星、L2 及以上视角时人造卫星保持远观小点/盒体形态（`satelliteDetailScopeAllowed`；实测跟随火星时地球 4 颗人造卫星精细模型/近观放大全部未激活）
- ✅ 复用 P4 的"L1 语境"判定（`viewLevel === 'L1'` 或跟随序列内天体，`utils/bodyCycle.cycleControlVisible` 同款逻辑），扩展为"近观细节激活目标必须与当前跟随/锚定天体属于同一行星系统"（如跟随地球 → 地球/月球/人造卫星细节可激活；跟随火星 → 地球卫星细节不激活）（`focusBodyIdForDetail`（焦点=飞往>跟随>L1 锚定）+ `focusPlanetSystemId`（卫星归其行星、行星归自身）纯函数并单测）
- ✅ 该门控同样适用于既有的行星 4K/法线近观层（现状 `detailGateUpdate` 以相机-天体距离为主判据，正常情况下已隐式满足；本条要求补"目标行星系统一致"显式判定并单测覆盖，防运镜路径擦过其他天体时误激活）（`planetDetailScopeAllowed` + `detailGateUpdateScoped` 接入 Planet/Moon 门控，不一致时立即退出并释放显存；L2-L4 无焦点自由镜头回落距离判据防误杀，登记）

### 2.3 验收标准
- ✅ 反复从 L2/L3/L4 切换到地球视角 ≥20 次（不同模拟时间相位），人造卫星无一次铺满屏幕；卫星最大屏占比 ≤ 屏幕高度 ~10%（无头 Chrome 实测 20 次循环逐次 4.6%–10.0%，两次触顶恰被钳在 10.0%）
- ✅ 跟随地球时缩放到卫星附近，放大效果仍可用（正常近观体验不回退）；跟随火星/木星时地球卫星无近观放大（实测切换至 ISS 后精细模型激活、放大恢复至 ≈3× 且屏占比钳在 10.0%；跟随火星时 iss/tiangong/hubble/geo-satellite 全部未激活）
- ✅ 角尺寸钳制/淡出/门控纯函数单元测试覆盖（含极限值：距离→0、跨度为 0）；60 FPS 不跌破（`satellitesR2` 套件 29 例，全量 1445 用例/91 套件，覆盖率 gate ≥90% 保持；实测跟随 ISS/火星 60.0–60.2 FPS）

## R2-3 L3 视角下太阳系内行星运动简化（用户反馈点 2 前半）

### 3.1 需求

- ✅ **消除"高压缩比未冻结"窗口**：硬阈值 3.2 改为渐变淡出区间 **2.6→3.0**（smoothstep，`utils/freezeGate.planetVisibilityWeight`），行星进 L3 锚点（~3.0）前已完成淡出隐藏；🔶 实现差异登记：行星/矮行星/彗星本体以**整组缩放收敛**呈现淡出（该层级下行星为亚像素点，观感等效透明度淡出，且无需给全部材质增加透明通道），轨道线与标签做真正的透明度淡出（登记于 freezeGate.ts 文件头）
- ✅ **行星速率钳制兜底**：淡出区间部分可见时公转视觉角速度应用与卫星一致的 `rateClampFactor`（0.5 圈/秒阈值），钳制中按降速角速度累计相位（`advanceClampedPhase`，无跳变）并以等效时间调用开普勒求解入口（`equivalentDaysForPhase`，位置/速度/自转全链一致）；HUD 提示"行星运动已减速显示"（`planetRateClampNotice`，与卫星文案区分；多天体钳制阈值随周期不同，经 `reportPlanetRateClamp` 聚合上报防共享布尔互写抖动）；补充：钳制期间行星/彗星渲染相位写入注册表，`cameraFocus` 优先按注册相位解析目标（P7 卫星范式扩展，相机跟随与渲染一致）
- ✅ **时间连续性保持**：淡出/冻结只影响视觉呈现，返回 L2/L1 按共享 `simDays` 重新求值（钳制解除即回精确相位，冻结时钳制累计一并重置）；无头目验：L3 停留 30 秒后返回，模拟时间 76.58→78.11 百万年连续推进无重置
- ✅ 阈值常量统一收敛到 `utils/freezeGate.ts` 纯逻辑模块（Planet.tsx/Comet.tsx/SolarSystem.tsx 三处 + Moon.tsx 提示可见性界限共四处魔法数字消除）；轨道线隐藏与天体淡出同步（`OrbitLine fadeWithPlanets` 按同一权重逐帧调制透明度）
- ✅ 太阳本体在 L3 下保持可见（Sun.tsx 不接入本门控，太阳系标记热区不受影响，无头目验截图核对）

### 3.2 验收标准
- ✅ L3 锚点视角观察 30 秒：行星始终隐藏，无高速乱转/闪烁跳动（60.2 FPS）；缓慢缩放 2.4→3.2 全程平滑淡出——海王星标签 opacity 实测单调下降 1.00→0.98→…→0.00 无阶跃，淡出区间单步位移 <200px 无乱跳，钳制提示正常出现/熄灭（无头 Chrome Metal 1280×800，17/17 项通过）
- ✅ L3 停留后按 2 返回太阳系视角：行星与轨道线完整恢复（标签 opacity=1、运动平滑 maxStep 0.0px）、模拟时间连续、L2 锚点下钳制提示熄灭（恢复精确相位求值），60.1 FPS
- ✅ 淡出曲线/速率钳制/等效时间纯函数单元测试 `freezeGate.test.ts`（24 例，覆盖率 100%）+ 相位连续性回归（钳制逐帧步进 ≤0.5 圈/秒 × 帧时长、解除后与共享时间轴互逆）；🔶 差异登记：需求所述"timeP2 套件"实际不存在（现有为 time.test.ts），相位连续性回归收敛于新建 freezeGate 套件 + `cameraFocusR2` 渲染相位解析扩展（3 例）

## R2-4 动态事件视角域隔离（用户反馈点 4）

### 4.1 需求

**A. 事件视角域定义（数据层）**
- ✅ 建立事件 → 视角域映射（纯逻辑，`src/utils/eventScopes.ts`，`eventScopeWindow` 闭区间窗口，含 `cmeArrival` 独立类别便于逐事件文案）：
  | 事件 | 所属视角域（连续层级窗口） |
  |---|---|
  | 太阳耀斑、CME、CME 抵达地球/极光 | L1/L2（≤2.4，与 SunActivity levelWeight 平台一致） |
  | 超新星爆发 | L3/L4（≥2.5） |
  | 银河系—仙女座合并预览 | L4（≥3.6） |

**B. 通知 UI 按视角过滤**
- ✅ `HudInfo.tsx` 事件通知列渲染条件增加视角域判定：当前连续层级在事件视角域窗口外时**不显示**该事件通知（`eventNoticeVisibleInScope` 布尔选择器，仅域边界跨越时重渲染；事件状态照常推进、通知标志位不改动，回到域内且事件仍活跃时通知恢复——无头目验 B6/D4 复验）
- ✅ 通知上的"飞往观看"按钮点击后自动切换到事件所属视角域（验收确认：通知仅域内可见，`requestFlyTo` 运镜 + `syncCameraDistance` 相机距离层级同步使落点天然处于事件视角域内——超新星飞抵后保持银河系尺度（光年标尺）且聚焦权重提升防淡出，耀斑/CME"飞往太阳/地球"落点为 L1 域内；无新增显式 setViewLevel，行为即满足"不出现飞过去却什么都看不到"）
- ✅ 域外发生的事件提供轻量提醒策略：**采用方案 b**（登记）——通知折叠为一行小字（`eventOutOfScopeSummaryZh` 逐事件文案，如"☀ 太阳耀斑进行中（切回太阳系视角查看）"，与完整通知卡片一一对应，域外每个活跃事件各一行）

**C. 演示按钮按视角门控**
- ✅ 控制面板"触发超新星/耀斑/CME 演示"与"预览碰撞合并"按钮：**采用默认"置灰 + 提示"方案**（登记）——域外禁用 + `title` tooltip"请切换到 XX 视角触发"+ 按钮文案同步提示（`eventDemoEnabled`/`eventDemoDisabledHintZh`）；🔶 差异登记：合并预览按钮此前点击会经 `startMergePreview` 自动切到 L4 再预览，按默认方案统一改为域外置灰（L4 内点击行为不变，store 的 L4 切换逻辑保留）
- ✅ 触发音效联动保持现状（域内触发才有音效；音效路径未改动，域外按钮不可点击天然无触发音效）

**D. 自动触发显式门控**
- ✅ 耀斑/CME 泊松自动触发显式限定 L1/L2（连续层级 ≤2.4，`SunActivity.tsx` 触发分支接入 `eventAutoTriggerAllowed`），移除对 `timeJumped`（Δ>50 天守卫）副作用的隐式依赖（`timeJumped` 本身的时间跳变防护语义保留）；超新星自动触发显式限定 L3/L4（`Supernova.tsx` 补显式判定与注释，登记此前 L1/L2 不触发仅是低压缩比 ΔMyr≈0 的概率副作用）；域外仅抑制新触发，活跃事件衰减/收尾照常推进
- ✅ 门控判定纯函数化并单测（`eventAutoTriggerAllowed`/`eventNoticeVisibleInScope`/`eventDemoEnabled` 三层窗口，当前取值一致、语义独立便于未来分层微调；`eventScopes.test.ts` 15 例覆盖率 100%）

### 4.2 验收标准
- ✅ L3/L4 下无耀斑/CME 通知弹出；触发耀斑演示按钮置灰（无头 Chrome 目验：L3/L4 各观察 10 秒无卡片弹出；L2 触发耀斑+CME 后切 L3/L4 卡片隐藏、折叠一行提醒出现；四视角 × 四按钮 16 格矩阵置灰状态与 tooltip 逐格复验通过，截图 r24-a-matrix-L3/L4、r24-b-L3-collapsed）
- ✅ L1/L2 下无超新星通知弹出（切到 L3 后活跃事件通知恢复——无头目验：L3 触发超新星 → 切 L2 卡片隐藏、折叠提醒出现 → 切回 L3 卡片恢复，截图 r24-d-L2-sn-collapsed；域内 L3 高压缩快进期间超新星自动触发正常发生，见截图 r24-b-L3-collapsed 中自动触发的超新星卡片）
- ✅ 事件状态机（触发/衰减/遗迹归档）不受视角切换影响——域外期间事件正常演化（无头目验 C1：L2 暂停中触发的耀斑/CME 切 L3 后解除暂停，高压缩比下事件快速衰减完成、折叠提醒随之消失，状态机未被视角门控冻结）
- ✅ 视角域窗口纯函数单元测试全覆盖（`eventScopes.ts` 覆盖率 100%，全局 gate ≥90% 保持：语句 97.86%/分支 97.44%/函数 98.61%/行 99.44%）；CHANGELOG 已登记行为变更

---

# 第二部分（P1）：交互完整性

## R2-5 通用天体切换序列框架（用户反馈点 5 + 点 1 后半）

### 5.1 需求

**A. 视角域序列定义（纯逻辑扩展 `utils/bodyCycle.ts` 或新建 `utils/cycleScopes.ts`）**
- ✅ 现有 `BODY_CYCLE_SEQUENCE`（20 个太阳系天体）定义为 **L1/L2 域序列**（现状保持，行为不回退——新建 `utils/cycleScopes.ts`，行星域直接复用 `BODY_CYCLE_SEQUENCE` 同一引用；`cycleScopeBody` 行星域分支与 `cycleAnchorBody` 逐字段一致，bodyCycleP4/storeP4 现有单测全绿）
- ✅ 新增 **L3 域序列**（15 成员，实现差异登记——恒星类按"普通恒星 → 变星 → 致密天体"排序：太阳 → 日球层顶 → 银心人马座 A* → 参宿四 → 参宿七 → 天狼星 A/B → 造父一 → WR 124 → 天鹅座 X-1 → 蟹状星云脉冲星（恒星类 → 星云类过渡站：脉冲星居星云中心）→ 猎户座星云 → 环状星云 M57 → 马头星云 → 昴星团 → 武仙座 M13 →（循环）；"太阳系出发 → 银心 → 恒星类 → 星云类 → 星团类"组织与全部成员登记于 `utils/cycleScopes.ts` 文件头；"太阳系标记"取 `sun`（you-are-here 标记非可飞往实体））
- ✅ 新增 **L4 域序列**（8 成员）：银河系 → 大麦哲伦云 → 小麦哲伦云 → 人马座矮星系 → 仙女座 M31 → 三角座 M33 → 室女座团 M87 → 类星体 3C 273 →（循环）；实现差异登记——可选的 M32/M110 子条目未纳入主序列（保持巡游节奏，仍可点选/飞往）；`milky-way` 此前不可飞往，已在 `resolveFocusTarget` 新增银河系整体解析分支（目标点=银心，观察距离按银盘显示半径推荐）
- ✅ 序列纯函数接口与现有 `cycleBodyId/cycleControlVisible` 同构：`cycleBodyIdInScope(scope, currentId, dir)`、`scopeForViewLevel(continuousLevel, followBodyId)`（跟随域内天体时以天体归属为准，无跟随按连续层级区间 <2.5/<3.5 划分）；不在序列内时回落到该域默认天体（L3 默认人马座 A*，L4 默认 M31）

**B. UI 与快捷键**
- ✅ `BodyCycleSwitcher` 泛化：按当前视角域展示「← 上一个 | 域名 当前天体名 序列位置 | 下一个 →」控件；L3/L4 下跟随任意域内天体时显示，未跟随时点击"下一个"即飞往域记忆天体（初始为域默认）；实现差异登记——控件在全部视角域常驻显示（L2 未跟随时同样显示行星域控件，见验收第 3 条"切换视角即可开始游览"）
- ✅ `[` / `]` 快捷键在所有视角域生效（复用现有键位，`cycleScopeBody` 按域路由）；HelpHint 帮助文案更新（"巡游上一个/下一个天体（按视角域：行星/银河系/宇宙序列）"）
- ✅ 每个视角域独立记忆"上次锚定天体"（store 新增 `galaxyAnchorBodyId`/`universeAnchorBodyId`，会话内），切换视角回来时序列位置恢复；`requestFlyTo` 仅在该域语境下记录记忆（防跨域误写：L1/L2 耀斑通知"飞往太阳"不改写银河系域记忆）
- ✅ 信息面板（HudInfo 选中天体卡片）在天体属于当前域序列时，"飞往/跟随"按钮旁补充"←/→"快捷入口小按钮 + 序列位置标签，行为与底部控件一致（同走 `cycleScopeBody`）

**C. 飞往/跟随联动**
- ✅ 域序列切换复用现有 `requestFlyTo` → 2.5 秒运镜 → 自动跟随管线（含解析失败拒绝跟随的 R2-1 兜底）；`isGalaxyAnchoredFocusId` 白名单扩展纳入 heliopause（L3 特殊天体成员原本已全量在白名单内）；实现差异登记——L3 序列成员"太阳（sun）"未纳入白名单：飞抵太阳后相机落入 L1 太阳系尺度，提升银河系组权重会让太阳邻域银河粒子贴着太阳显示（复发历史 bug"被误认为柯伊伯带跑错位置"），太阳自身可见性不依赖银河系组（登记于 `data/specialBodies.ts`）
- ✅ 观察距离按天体类型适配逐成员核对：星云/星团/黑洞类按 `visualRadiusLy` 推荐距离看整体形态（`cameraFocusR25` 单测断言观察距离 ≥6×显示半径且星云/星团类 > 恒星类）、太阳/日球层顶/银河系各有专属分支；无头 Chrome 逐成员截图核对通过（r25-L3-01～15、r25-L4-01～08）

### 5.2 验收标准
- ✅ L3 下按 `]` 依次遍历全部 15 个序列成员一整圈：每个目标运镜平滑、到达后目标完整可见（无淡出残影）、HUD 序列位置正确（无头 Chrome 37/37 项通过，逐站截图 r25-L3-01～15；备注：飞抵人马座 A*/日球层顶等目标时特殊天体标签因近距呈现为大字号，为 R2-1 时期即存在的既有标签缩放行为（对照截图 r21-a2-following-heliopause），非本阶段引入，近观呈现优化归属 R2-7）
- ✅ L4 下同样遍历一整圈通过（8 站，截图 r25-L4-01～08，`[` 反向切换正确）；L1/L2 现有 20 天体序列行为不回退（现有单测全绿，全量 1565 用例/96 套件；无头目验 L1 地球 3/20 → `]` 月球 4/20 → `[` 地球 3/20）
- ✅ 视角切换后（R2-1 面板已关）底部显示当前域切换控件，点击即可开始游览（L3 显示"银河系巡游 人马座A* 3/15"、L4 显示"宇宙巡游 仙女座星系 5/8"，未跟随时点击即飞往）
- ✅ 序列纯逻辑（域判定/循环/回落/记忆）单元测试全覆盖：`cycleScopes.test.ts` 28 例（`utils/cycleScopes.ts` 覆盖率 100%）+ `storeR25.test.ts` 17 例（记忆/委托/防跨域误写）+ `cameraFocusR25.test.ts` 29 例（全成员可解析/距离适配），全局 gate ≥90% 保持（语句 97.87%/分支 97.41%/函数 98.64%/行 99.41%）

## R2-6 太阳系绕银心轨道运动可感知细节增强（用户反馈点 2 后半）

> P6 已交付基础（历史尾迹 + 虚线预测弧 + 流动刻度光点 + 垂直振荡视觉增益 ×6 + G 键银心固定模式）。本阶段针对用户仍反馈"看不出在轨道上运动"，聚焦**默认视角下的运动可感知性**。

### 6.1 需求

- ✅ **轨道当前位置标记强化**：You are here 标记新增雷达波纹扩散环（周期 2.4 s、扩散 3 倍消隐）+ 本体 ±12% 呼吸脉动（真实秒驱动 UI 节奏，登记于 `utils/galacticMotionCues.ts` 文件头），随 You are here 开关联动；已走过尾迹（不透明度 0.9→1.0、颜色系数上调）与未来虚线弧（0.5→0.72、提亮 #bfe4ff）对比调亮（视觉调优登记于 `Galaxy.tsx`）
- 🔶 **参照物运动线索**：实现差异登记——未取"提高旋臂对比度/邻近特殊天体标记"两个建议手段（特殊天体按已登记近似随太阳共转，无相对滑动可用；旋臂密度波相对漂移仅 ~8°/20 Myr，过慢），改为将 P6"与太阳共转的流动刻度光点"（跟随模式下相对太阳方位恒定，正是"看不出动"根因之一）替换为**银心系静止的轨道银河年刻度**：24 格进度里程碑 + 0%/25%/50%/75% 主刻度（琥珀色 + "银河年 N%" 标注），跟随模式下以太阳真实公转速度整体滑过原点（`orbitFlowPhase01/orbitFlowTickAngle` 移除，差异登记于文件头）；实测 L3 锚点默认速度 10 秒刻度标签屏幕位移 197–200 px，肉眼可辨
- ✅ **银河年进度可视化联动**：刻度角与 HUD 进度严格同源（同一 θ=2π·t/T），单测断言进度 k/24 时进度角恰等于第 k 格刻度角、标记场景位置反解角与 HUD `galacticYearProgress` 一致；实测 HUD 绕行 9°→46° 期间标记/刻度同步移动
- ✅ **垂直振荡波浪轨迹**：视觉增益 ×6→×10（需求建议上限，±150 场景单位 ≈ 轨道半径 11%，保持"准圆轨道"认知），登记更新至 `galacticMotionCues.ts` 文件头 + HelpHint；真实比例模式仍为 1（不放大）
- ✅ **银心固定模式引导**：控制面板新增"银河系视角参考系（G 切换）"显式入口（L3 可用，域外置灰附"请切换到银河系视角使用（快捷键 3）"提示）+ HelpHint 文案扩写；可选项一并实现——首次切入 L3 一次性 toast"按 G 切换银心固定视角观察太阳系公转"（含"立即切换"按钮，12 秒自动收起，会话内仅一次，用户已切换过模式不再打扰）
- ✅ 所有视觉增益/艺术化调整登记：增益 ×10/静止刻度替换/脉动节奏登记于 `galacticMotionCues.ts` 文件头，尾迹/预测弧调亮登记于 `Galaxy.tsx`，HelpHint 补 ×10 垂直放大说明

### 6.2 验收标准
- ✅ L3 锚点默认视角、默认速度下，10 秒内可肉眼辨识太阳系沿轨道运动（无头 Chrome 实测：刻度参照物滑动 197–200 px、太阳系标记钉在原点位移 0 px、HUD 绕行角推进 37°；截图 r26-a2/a3 两帧对比登记）
- ✅ G 键银心固定模式下太阳系沿波浪轨迹绕银心运动清晰可见（回归实测：标记 10 秒沿轨道位移 202 px，绕行 56°→93°；截图 r26-c1/c2 登记）
- ✅ 新增纯逻辑单测 `galacticMotionCuesR26`（26 例：增益上限/刻度分布/HUD 一致性/脉动相位与波纹曲线）+ `storeR26`（7 例：toast 一次性）；覆盖率 gate ≥90% 保持（语句 97.89%/分支 97.56%）；无头 Chrome 18/18 项通过（60.3 FPS，零控制台错误）

## R2-7 L3 飞往/跟随目标近观细节升级（用户反馈点 3，L3 部分）

> P6 已升级恒星表面 shader（3D 噪声对流/临边昏暗）与部分星云。本阶段补齐剩余粗糙对象，并建立"飞往即见细节"的近观 LOD 分级。

### 7.1 需求

**A. 日球层顶近观细节（联动 R2-1 飞往能力）**
- ✅ 飞往日球层顶后可辨识结构分层：终端激波（Termination Shock，~94 AU 示意内壳）→ 日鞘（Heliosheath，壳层间渐变区）→ 日球层顶（外边界）；以半透明多层壳 + 着色渐变呈现，压缩比例沿用现有登记（层半径 = 示意球壳半径 × 真实 AU/120，日鞘取 3 层等距渐变壳，琥珀 → 蓝着色渐变 + 分层标注；纯函数 `utils/heliopause.ts`，组件 `Scene/Heliopause.tsx` 近观门控挂载，L2 常态零新增开销）
- ✅ 旅行者 1 号（2012 年穿越，朝向鼻部方向）/ 旅行者 2 号（2018 年）位置标记点 + 点选科普卡片（数据来源 NASA/JPL Voyager Interstellar Mission，catalog 新增 voyager-1/voyager-2 条目；标记为辉光点 + 点选热区 + 标注，V1 约 121.6 AU 黄道以北/V2 约 119 AU 黄道以南，方向按穿越黄纬示意登记；`resolveFocusTarget` 支持标记飞往，跟随标记期间三层结构保持）
- ✅ 迎风面/背风面不对称形态示意——取"维持球形"选项：信息面板"形态"行登记"真实为彗尾状不对称"（`HELIOPAUSE_SHAPE_NOTE_ZH`，卡片另补"结构分层"行），水滴形建模未做（可选项取舍登记于 `utils/heliopause.ts` 文件头）

**B. 特殊天体近观 LOD 分级**
- ✅ 为 L3 序列（R2-5）每个成员定义"近观激活距离"（复用 P4 `detailGateUpdate` 滞回门控模式）：跟随时激活近观细节层，离开跟随/超出距离即释放（新建纯逻辑 `utils/nearView.ts`：进入阈值 = 飞往观察距离 ×1.5、退出 ×1.4 滞回，与 `resolveFocusTarget` 同源公式单测断言 15 站逐一覆盖；组件 `useNearViewGate` 挂载/卸载近观子树，0.5 s 淡出后 dispose，无 LRU 保留）
- ✅ 逐对象近观细节要求（在 P6 基础上的增量，逐项核对现状后实施，差异登记于 `nearView.ts` 文件头：恒星类参宿四/参宿七/造父一/WR 124 与黑洞类人马座 A*/天鹅座 X-1 的近观细节已由 P6 表面 shader/吸积盘+引力透镜 shader 交付且常开零边际开销，不重复建设仅登记激活距离；太阳近观 = 既有 L1 太阳渲染）：
  - ✅ 星云类（猎户座/环状/蟹状/马头）：猎户座 +18 体积感云团 sprite（确定性 `nebulaPuffLayout` + 基础平面近观减淡 35% 交叉过渡）、蟹状 +16 丝状遗迹云团（shell 纹理 ×3 种子）、环状 +200 粒环体环向软边粒子（OIII→Hα 径向色层，与环面同姿态同膨胀）+ 外晕壳、马头 +2 视差发射层 + 3 前景暗云团（前后景深）——逐站截图核对，近观不呈单张圆形光晕
  - ✅ 星团类（M13/昴星团）：M13 +1,200 粒近观分级星场（rand^2.4 分布中心更密、粒径减半）、昴星团 +320 粒 + "七姊妹"亮星辉光 ×7；全部粒子为圆形软边贴图（getSoftPointTexture，无方块粒子）
  - ✅ 双星（天狼星 A/B）：互绕运动既有（24 s 可视化周期）+ 近观新增绕共同质心双轨道线（与 `binaryStarPositions` 同源半径）+ A/B 身份标注（主序星/白矮星）；白矮星色调调蓝（~25,000 K），大小对比 4:1 既有
  - ✅ 脉冲星：射束扫描/脉冲闪烁节奏既有（P6 降频登记）+ 近观射束增亮 60%，单张光晕 sprite 近观减淡 45% 交叉过渡到丝状体积云团
- ✅ 近观细节层遵循资源门控：仅当前跟随目标激活（门控以跟随/飞往目标为判据），粒子增量逐成员登记（`NEAR_VIEW_PARTICLE_INCREMENTS`，单目标最大 +1,200，全局峰值 15,000+1,200=16,200 ≤ 20,000 单测断言），逐站实测 FPS 60.0–60.4 不跌破
- 🔶 补充（R2-5 目验登记问题治理）：跟随/飞往目标期间隐藏该天体自身 L3 大字号标签；日球层顶近观语境下隐藏全部 L3 特殊天体标签（相机 ~836 单位时标签放大 ~3 倍叠压遮挡三层结构；实现差异登记——非需求原文条目，属点 3"近观呈现"的配套治理）

### 7.2 验收标准
- ✅ 通过 R2-5 序列遍历 L3 全部成员，每个目标近观形态可辨识、无"圆形光晕糊一团"观感（无头 Chrome 30/30 项通过，逐站截图 r27-L3-01～15 核对登记）
- ✅ 日球层顶近观三层结构 + 旅行者标记可点选（三层标注可见；点选标记 → 科普卡片出现（NASA/JPL 来源）→ 可飞往，截图 r27-C2/C3 登记）
- ✅ 近观层激活/释放无内存泄漏（连续切换 10 个目标后 GC 实测 JS 堆 97.7→98.4 MB，Δ+0.7 MB 稳定）；FPS 60.0–60.4 保持；新增 `heliopauseR27`（17 例）/`nearViewR27`（43 例）/`cameraFocusR27`（6 例）单测，全量 1653 用例/101 套件通过，覆盖率 gate ≥90% 保持（语句 97.93%/分支 97.69%）

---

# 第三部分（P2）：视觉真实感增强

## R2-8 L4 飞往星系近观细节升级（用户反馈点 3，L4 部分）

### 8.1 需求

- ✅ **星系近观 3D 粒子层**：飞往/跟随 M31、M33、LMC、SMC 时，在贴图平面之上叠加/替换为轻量 3D 粒子结构（旋涡星系：核球 + 盘 + 旋臂粒子，复用 `utils/galaxy.ts` 生成器参数化；不规则星系：团块状粒子云），粒子数按预算分配（单星系 ≤8,000，激活门控同 P4 LRU：最多同时 1 个星系持有近观层）（新建纯逻辑 `utils/galaxyNearView.ts` + 组件 `Scene/GalaxyNearView.tsx`：M31 大核球+双主臂 8,000 粒 / M33 弱核球松散臂 6,000 粒，`generateGalaxyDiskParticles` 参数化零重写；LMC 5,000 / SMC 4,000 粒团块云（蓝白年轻星 + ~12% 电离氢粉 + 弥散背景）；近观激活距离 = 飞往观察距离 ×1.5 与 `resolveFocusTarget` 同源单测断言（贴图平面尺寸公式收敛至 `utils/universe.galaxyPlaneSizeUnits` 三处同源），退出 ×1.4 滞回复用 `nearViewGateUpdate`；LRU 容量 1——离开跟随后淡出保留、被新激活星系挤出即卸载 dispose，`nearViewLruUpdate` 纯函数 + 持有者注册表）
- ✅ **贴图平面过渡**：近观层激活时贴图平面交叉淡出、释放时淡入，无突变；远观（非跟随）保持现状贴图平面，性能零回退（0.5 s 交叉淡入淡出 `NEAR_VIEW_TRANSITION_SECONDS`；非跟随时粒子层不挂载不生成）
- ✅ **薄片问题修复**：取 **billboard 面向相机**方案（二选一登记于 `galaxyNearView.ts` 文件头）——贴图平面每帧面向相机，侧向飞入不再呈"纸片"；艺术化差异登记：M31 真实倾角 ~77°（观测特征）在远观 billboard 下不呈现，三维倾斜姿态改由近观粒子层承载（粒子层朝向沿用贴图平面时期 id 哈希朝向公式，单测逐字断言）
- ✅ **矮椭圆/椭圆星系**（M32/M110/M87）：近观为 3D 椭球粒子云（Sérsic 亮度分布近似：u=(r/Rₑ)^(1/n) 指数逆变换采样，bₙ≈2n−1/3（Ciotti & Bertin 1999），截断 4Rₑ，轴比压扁，老年恒星红黄色调；M87 n=4 de Vaucouleurs 6,000 粒 / M32 n=3 3,000 粒 / M110 n=1.5 3,500 粒；超出需求范围补人马座矮星系 n=1 3,000 粒——数据层为 elliptical 形态且在 L4 序列内），M87 近观保留喷流联动（目验椭球云与喷流同框，截图 r28-L4-07）
- ✅ 近观层生成为确定性算法（种子 = 星系 id 的 FNV-1a 哈希），两次飞往形态一致（单测逐字节断言；渲染循环零随机）
- ✅ 信息面板近观联动：星系卡片新增"结构"行（核球/盘/晕按形态分类四套文案，银河系卡片同步补棒旋结构行），dataSource 追加"Hubble 形态序列（NED）+ Sérsic 亮度分布"来源（差异登记：结构行常显而非仅跟随时显示，无害化简；另跟随/飞往星系期间隐藏其自身 L4 标签——R2-7 近观标签治理同款）

### 8.2 验收标准
- ✅ 飞往 M31：从贴图平面平滑过渡到 3D 粒子结构，旋臂/核球立体可辨，绕行观察无"薄片"观感（无头 Chrome 目验：截图 r28-L4-05-m31-near 旋臂/核球立体清晰，绕行拖拽 ~90°/180° 截图 r28-B3 无薄片；对照 R2-5 时期 r25-L4-06 确认旧版"整屏薄片贴图"观感消除）
- ✅ 遍历 L4 序列（R2-5）全部成员，每个星系近观形态与其类型（旋涡/棒旋/椭圆/不规则）一致（目验 17/17：8 站一整圈逐站截图 r28-L4-01～08——M31/M33 旋涡粒子盘、LMC/SMC 团块云、人马座矮星系/M87 椭球云；差异登记：milky-way 近观 = 既有 4 万粒 3D 粒子盘（棒旋，Galaxy.tsx）、类星体 3C 273 非星系贴图平面近观 = 既有光变+喷流 shader，均不重复建设；单测断言 8 个配置形态与数据层 morphology 一致）
- ✅ 粒子预算与 LRU 释放单测；60 FPS 不跌破（`galaxyNearViewR28` 29 例：单星系 ≤8,000/LRU 容量 1/挤出释放/保留语义；实测逐站 FPS 60.0–60.4，再遍历 8 站 GC 后 JS 堆 Δ0 MB 无泄漏，零控制台错误；全量 1682 用例/102 套件、覆盖率语句 97.99%/分支 97.66%/函数 98.73%/行 99.43% ≥90% gate 保持）

## R2-9 L4 银河系真实感重构（用户反馈点 8）

### 9.1 需求

- 🔲 **3D 恒星银晕**：以稀疏粒子球壳（建议 2,000–4,000 粒，径向密度 ∝ r⁻³·⁵ 近似）替代/叠加现有银晕 billboard sprite，L4 下从任意角度观察银河系均有立体包裹感
- 🔲 **球状星团系统**：银晕中散布 ~20–40 个球状星团标记点（亮度聚集小点簇，位置确定性随机、参考真实球状星团系统分布——中心聚集 + 高银纬），M13 与现有 L3 特殊天体条目联动（同一对象不重复渲染）
- 🔲 **核球 3D 增强**：核球粒子分布现状为压扁球（0.6），L4 距离下补充核球辉光的椭球感（sprite 替换为按视角轴比变化的着色，或小规模粒子增补）；棒旋结构（银河系为棒旋星系 SBbc）在俯视角度可辨——现有旋臂生成若无棒结构则补棒状核心粒子分布
- 🔲 **尘埃带侧视剪影**：侧视（银盘水平方向）观察时盘中平面有暗尘埃带剪影（现有 shader 若已含尘埃带验收确认侧视效果；不足则补盘中平面吸光暗带）
- 🔲 **与 L4 其他星系风格统一**：银河系（粒子）与 M31（R2-8 近观粒子/远观贴图）在同屏（合并预览场景）观感一致——亮度、色调、粒子颗粒感协调；验收以 L4 合并预览同屏截图为准
- 🔲 粒子增量计入预算：L4 场景总粒子峰值登记，帧率不跌破 60；新增分布纯函数（银晕密度/星团位置/棒结构）单测

### 9.2 验收标准
- 🔲 L4 下绕银河系一周观察：正视旋臂清晰、侧视有盘厚度 + 尘埃带 + 银晕立体包裹、无"二维贴纸"观感
- 🔲 球状星团点簇可辨；棒旋结构俯视可辨
- 🔲 无头 Chrome 多角度截图核对登记；60 FPS 保持

## R2-10 L4 轨迹与运动一致性（用户反馈点 9）

### 10.1 需求

- 🔲 **卫星星系轨道线**：为 LMC、SMC 绘制轨道线（与 `satelliteGalaxyPositionLy` 同一公式生成的椭圆/圆，含倾角姿态），随"轨道线开关"控制；轨道线与实际运动位置严格一致（同源公式，禁止两套参数）
- 🔲 **direction 一致性修复**：`satelliteGalaxyPositionLy` 当前忽略 `galaxy.direction`（数据登记的真实天区方位）——修复为轨道平面/初始相位与 direction 自洽（首帧位置 = direction × distance，轨道从该点起步），消除静态首帧与动态位置的跳变及信息面板文案矛盾
- 🔲 **人马座矮星系运动**：从完全静止改为沿极轨道缓慢运动（周期示意登记）+ 潮汐流粒子拖尾示意（沿轨道前后延伸的稀疏星流，呼应"正被撕裂"文案；粒子 ≤1,500）；若数据不足做完整轨道，最低要求为"有可辨识的缓慢位移 + 拖尾"并登记近似
- 🔲 **麦哲伦星流澄清**：星流拖尾（回溯轨迹）与新增轨道线视觉区分（星流为弥散粒子带、轨道线为细线），信息面板注明"星流为历史路径上剥离的气体"
- 🔲 **M31 接近进度感**：接近虚线上增加等时间距刻度或流动光点（复用 Galaxy.tsx 流动刻度模式），使对数压缩下的"正在接近"可感知
- 🔲 **通用原则落地**：凡显示轨迹线的天体必须沿该轨迹运动（同源公式）；凡运动的天体轨迹线可开关——本阶段对 L4 全部运动对象逐一核对并登记结论（M32/M110 随 M31、宇宙网静止属预期，信息面板说明）

### 10.2 验收标准
- 🔲 开启轨道线：LMC/SMC 沿各自轨道线运动，位置时刻在线上（快进 10× 观察 60 秒无偏离）
- 🔲 人马座矮星系有可辨识运动与潮汐拖尾；文案与视觉一致
- 🔲 轨道公式/direction 修复/刻度相位纯函数单测；跟随 LMC/SMC 时相机解析与渲染无错位（现有 cameraFocus 同源公式回归）

## R2-11 银河系—仙女座碰撞合并后续演化（用户反馈点 7）

### 11.1 需求

> 现状：合并预览快进到 4500 Myr 后两星系原地重叠、辉光满值、标签"已合并"，无后续。目标：合并时刻后继续呈现科学上公认的合并演化序列（参考 NASA/ESA 模拟结果，Milkomeda 情景）。

- 🔲 **演化阶段状态机**（纯逻辑 `utils/galaxyMerger.ts`，时间轴基于合并时刻 T0 之后的模拟时间）：
  1. **首次穿越**（T0 前后）：两盘交错穿过而非重叠停滞——M31 越过原点继续沿运动方向减速远离
  2. **回摆振荡**（T0 + ~10 亿年内，压缩呈现）：两核心 1–2 次减幅往返（阻尼振荡近似动力摩擦）
  3. **潮汐扭曲**：穿越期间双方盘面粒子/贴图呈现潮汐拉伸（旋臂拉出潮汐尾示意；R2-9 的银河系粒子盘做顶点扰动，M31 侧贴图扭曲或粒子层扰动）
  4. **星暴示意**：穿越时刻短暂蓝白色增亮（气体压缩触发恒星形成爆发，艺术化登记）
  5. **终态椭圆星系**：核心并合后 5–10 秒（真实 ~20 亿年，压缩登记）过渡为单一椭球形粒子云（Milkomeda），旋臂结构消失，色调偏老年恒星红黄
- 🔲 **时间可逆**：演化由模拟时间驱动（非一次性动画）——"恢复预览前时间"按钮回退后场景恢复合并前状态；再次快进重现同样演化（确定性）
- 🔲 **HUD/信息联动**：合并各阶段 HUD 标签更新（"首次穿越/回摆/并合完成 Milkomeda"）；信息面板科普卡片（太阳系命运：大概率被抛入外围，恒星间碰撞概率极低）
- 🔲 **科学性登记**：时间压缩、振荡次数简化、星暴亮度等艺术化处理登记文件头 + 面板说明；参考来源标注（van der Marel et al. 2012 / NASA GSFC 模拟）
- 🔲 性能：演化期粒子扰动在现有粒子预算内（顶点着色器实现，CPU 零逐粒子分配）；60 FPS 保持

### 11.2 验收标准
- 🔲 点击"预览碰撞合并"：快进到 T0 后不再停滞——可见穿越 → 回摆 → 扭曲 → 星暴 → 终态椭圆星系完整序列
- 🔲 "恢复预览前时间"回退后一切复原；重复预览结果一致（确定性）
- 🔲 演化阶段函数（分离距离曲线、振荡包络、形态插值、星暴亮度曲线）单测全覆盖；无头 Chrome 分阶段截图登记

---

## R2-12 集成回归验收 + 文档回写（收尾）

- 🔲 全量单测/type-check/lint/build 绿；覆盖率 gate ≥90% 保持
- 🔲 无头 Chrome 端到端目验清单：9 个用户反馈点逐点复验（每点给出验证操作路径与截图/FPS 登记）
- 🔲 四视角切换 × 各域序列遍历 × 事件触发矩阵回归（L1-L4 × 耀斑/CME/超新星/合并）
- 🔲 CHANGELOG.md `[Unreleased]` 按类别（新增/修复/改进）补全全部 R2 条目
- 🔲 本文档各阶段状态 🔲 回写 ✅/🔶（差异登记）；REQUIREMENTS.md 相关小节如受影响同步更新
- 🔲 README 如有新增资源/许可登记同步

---

## 附录 A：全局硬性约束（各阶段一体遵循，违反即返工）

1. **测试**：纯逻辑抽 `src/utils/` 镜像 + `__tests__` 单测；覆盖率 gate ≥90%（jest.config.mjs 全局阈值）保持；组件内 GLSL 不直接测
2. **性能**：60 FPS 垂直同步不跌破；粒子峰值预算登记（现状太阳活动 15,000/上限 20,000，L3/L4 新增粒子单独登记预算）；纹理 ≤4096；内存 <1GB
3. **渲染纪律**：渲染循环零分配；确定性伪随机（禁每帧 Math.random）；不可见时跳过演算与 uniform 更新；卸载 dispose
4. **科学性**：艺术化/夸大处理一律登记代码文件头 + HelpHint/信息面板说明；科学参数标注来源；真实比例模式行为不回退
5. **时间轴**：共享全局模拟时间轴 `simDays`；暂停冻结/快进/时间跳变无残留；跨层级返回时按共享时间轴重新求值
6. **Git 流程**（AGENTS.md）：开工前询问用户是否新建分支；完成后本地 commit，询问是否创建 PR；禁止直接提交 master
7. **CHANGELOG**：每阶段完成即补 `[Unreleased]` 条目

## 附录 B：本批明确排除（不做）

- 星系合并的真实 N 体/流体动力学模拟（演化用参数化曲线近似）
- L4 星系的真实观测贴图替换（维持程序化，近观靠 3D 粒子层）
- 宇宙网内部纤维流动/星系团内星系运动（除哈勃缩放外维持静止，面板说明）
- 人造卫星轨道避让运镜路径规划（用角尺寸钳制 + 过渡冻结解决，不做路径规划）
- L3 行星在银河系尺度下的"真实可见"呈现（尺度差 10 个数量级，淡出隐藏是正确策略）
- VR/AR、多语言、移动端专项适配
