# 改进需求文档（第五批，R5 迭代：宇宙视角天体深度优化）

> **文档版本**: 1.7（R5-4 交付回写 ✅——M87 纵深与星系团环境（喷流节点 sprite + 球状星团幂律外包络 2,000 点 + M87* EHT 参数档推近联动 + 室女座 2MRS 子集成员/ICM sprite），实现差异逐条登记于 §4.1/§4.2；1.6：R5-3 交付回写 ✅——真实巡天目录背景（2MRS 全量快照烘焙 + 两级 Points 点云 + 宇宙网降氛围底层 + 拉尼亚凯亚边界置真实超星系平面），实现差异逐条登记于 §3.1/§3.2，人工检查点待用户目检；1.5：R5-2 交付回写 ✅——星系体积尘埃盘视线消光（M31 打样 + M33/LMC 套用，方案 a + dust² 对比度整形 + 各向异性光程修正），实现差异与极侧视限制逐条登记于 §2.1/§2.2；1.4：R5-1 交付回写 ✅——影像驱动星系升级（烘焙管线 + 四星系近观粒子 + 远景贴图），实现差异逐条登记于 §1.1/§1.2；1.3：R5-8 交付回写 ✅——事件门控判定基准改离散 viewLevel 视角集合，实现差异逐条登记于 §8.2/§8.3；1.2 追加 R5-8 事件门控层级与视角域锁定对齐——P0 缺陷修复，独立于 R5 主线可先行；其余粒度沿 1.1：12 阶段合并为 7 阶段，按"单 Agent 保质量的最大内聚粒度"组织）
> **参考文档**: REQUIREMENTS.md、IMPROVEMENT_REQUIREMENTS_4.md（R4 基础设施与 L4 近观基线）、IMPROVEMENT_REQUIREMENTS_2.md（R2-8/R2-9/R2-10）、AGENTS.md
> **状态标记**: ✅ 已完成 / 🔶 部分完成 / 🔲 未实现
> **前置基线（强制）**: 本批次以 **R4 交付后的状态为基线**——各阶段"依赖"列注明所需 R4 阶段，未交付前对应 R5 阶段不得启动；启动任何 R5 阶段前先核对 `IMPROVEMENT_REQUIREMENTS_4.md` 总览表对应条目已 ✅。
> **任务粒度原则**: 每个 R5-x 阶段 = 单个 Agent 单个任务；合并依据为"同一技术路线 / 同一数据管线 / 同一代码区域"内聚，且 R5 大量复用 R4 已验证的框架（体积/detailLayer/烘焙/透镜），单任务风险低于 R4 同体量任务。含多个子目标的阶段在提示词中规定**内部先打样后批量**的执行顺序，替代跨任务人工检查点。
> **定位**: R4 的 L4 部分走"参数化程序生成"路线；R5 升级为"**真实观测数据驱动**"路线（公版影像烘焙密度图、真实巡天目录点云），并补齐 R4 有意排除的 L4 对象（GRB、远景贴图、星系团环境）。

## 优先级与阶段总览

| 阶段 | 优先级 | 主题 | 复杂度 | 依赖（R4/R5） | 状态 |
|---|---|---|---|---|---|
| R5-1 | P0 | 影像驱动星系升级（烘焙管线 + M31/M33/LMC/SMC 近观粒子 + 远景贴图） | L | R4-5, R4-10 | ✅ |
| R5-2 | P0 | 星系体积尘埃盘（视线消光，M31 打样 + 旋涡星系套用） | L | R4-2, R4-3, R4-4, R5-1 | ✅ |
| R5-3 | P1 | 真实巡天目录背景（2MRS 烘焙 + 大尺度结构点云渲染） | L | R4-5 | ✅ |
| R5-4 | P1 | M87 纵深与星系团环境（喷流节点/球状星团/EHT 联动/室女座团成员+ICM） | M | R4-13（R5-3 可选） | ✅ |
| R5-5 | P1 | 本星系群与河外长尾细节（LMC 30 Doradus/棒分层 + GRB 近观） | M | R5-1, R4-8 | ✅ |
| R5-6 | P1 | 银河系 L4 增补（费米气泡 + HI 翘曲盘） | M | R4-3 | 🔲 |
| R5-7 | 收尾 | 集成回归验收 + 文档回写 | M | R5-1~R5-6 | 🔲 |
| R5-8 | P0 | 事件门控层级与视角域锁定对齐（离散视角判定，修复银河系视角下太阳事件仍触发） | M | 无（独立缺陷修复，可先行或与主线并行） | ✅ |

> 优先级依据：P0 = 观感跃升最大的"真实数据驱动"主线 + 交互正确性缺陷修复（R5-8）；P1 = 标志性结构与真实宇宙背景；收尾 = 集成验收。
> 建议实施顺序：**R5-8（缺陷修复，建议最先）** → R5-1 → R5-2 → R5-3 → R5-4 → R5-5 → R5-6 → R5-7。
> 可并行组：{R5-3, R5-6} 与影像主线（R5-1/R5-2）互不依赖可并行；R5-4 仅依赖 R4-13（室女座成员子集在 R5-3 未交付时用内嵌 VCC 精简表替代，登记）；R5-8 与全部主线阶段互不依赖。
> **人工检查点（强制，仅 2 处）**：R5-1（M31 影像驱动打样，任务内先行完成并自检后继续批量——交付后用户目检确认，方可启动 R5-2）；R5-3（真实点云背景，交付后用户目检）。其余阶段复用 R4 已验证框架，风险低，不设跨任务检查点。

---

## 0. 背景与现状调研

### 0.1 需求来源

用户在 R4 立项后追问"宇宙视角下的天体是否可以在细节上进一步优化"，经分析确认存在两档明确空间：① R4 参数化路线之上的"真实观测数据驱动"跃升（旋臂位置、恒星形成区与真实照片对应；真实巡天目录替代示意性宇宙网）；② R4 有意排除的 L4 对象补齐（GRB 近观、远景贴图、星系团环境）。收益递减边界已向用户说明：本批次交付后 L4 细节优化进入边际收益低区间，不再规划 R6 同主题批次。

### 0.2 基线与差距（R4 交付后视角）

| # | 对象 | R4 交付后基线 | 核心差距（R5 目标） |
|---|---|---|---|
| 1 | 旋涡/不规则星系近观（M31/M33/LMC/SMC） | R4-9/10：参数化多分量粒子（对数螺旋臂 + 尘埃暗粒子 + HII/年轻星团按泊松盘随机布点） | 旋臂形状/恒星形成区位置是"数学上合理"而非"与真实影像对应"——对照真实照片无法认出具体结构（如 M31 的 NGC 206 恒星云、M33 的 NGC 604） |
| 2 | 旋涡星系尘埃 | R4-10：dust 暗粒子 normal 混合近似 | 无视线消光——侧视/斜视时近侧尘埃带应真实遮挡后方星光（体积消光），暗粒子只能"画暗点"不能"挡光" |
| 3 | 星系远景贴图 | 程序化 Canvas 贴图（`proceduralTextures.ts` `createGalaxySpriteCanvas`） | 旋臂纹理/尘埃带/颜色分布均为程序近似，与真实影像差距明显；且与 R5 近观影像粒子层过渡时结构不对应 |
| 4 | LMC | R5-1 后有真实影像分布 | 缺 30 Doradus（本星系群最亮恒星形成区，LMC 观感地标）与中央棒的显式色彩分层 |
| 5 | M87 | R4-10 Sérsic 椭球云 + 既有喷流 + R4-13 黑洞透镜层（M87 核心未接） | 喷流无 HST-1 类节点；缺球状星团系统（真实 ~12,000 个）；飞近核心无 EHT 光子环联动；孤立呈现无星系团环境（真实为 ~1,300 成员的室女座团中心） |
| 6 | 银河系 L4 | R2-9：3D 银晕/球状星团/棒/尘埃带 | 缺费米气泡（银心上下 ~25,000 ly 伽马射线泡，标志性真实结构）与 HI 外盘 S 形翘曲 |
| 7 | 宇宙网/大尺度背景 | 程序化确定性节点—纤维—空洞（`utils/universe.ts` `generateCosmicWeb`）+ 拉尼亚凯亚示意边界 | 纯示意非真实宇宙——室女座团/长城等真实结构位置不真实；公开巡天目录（2MRS）可提供数万个真实星系 3D 位置 |
| 8 | GRB | 静态 sprite 演示物（`ExtragalacticObjects.tsx` `GammaRayBurst`） | 无近观细节：缺相对论双喷流与余辉膨胀壳 |

### 0.3 总体技术方案

- **方案 E（影像驱动粒子 + 远景贴图）**：离线脚本将公版天文影像处理为小尺寸权重图（亮度→密度、色彩→颜色、暗带→尘埃遮罩，256×256）与远景 sprite 贴图（512px），存 `public/data/`；运行时粒子按密度图逆变换采样布点 + 颜色图查色，远景 billboard 换影像贴图——远近景同源影像，过渡天然一致。R4-9 参数化生成器保留为降级路径与 z 向厚度模型（影像只提供盘面 2D 分布，垂直分布仍用参数模型，登记）。确定性种子沿用。
- **方案 F（体积消光尘埃盘）**：复用 R4-3 体积框架——旋涡星系近观叠加薄盘包围盒体积层，密度取影像尘埃通道，**纯吸收**（无发射）对后方粒子星光产生真实视线消光；与 R4-10 dust 暗粒子互斥启用（体积激活时暗粒子淡出，登记）。
- **方案 G（真实目录点云）**：离线烘焙公开红移巡天目录（2MRS ~43,000 星系）→ 超星系笛卡尔坐标 + 形态/亮度档，运行时两级 Points 呈现；与程序化宇宙网的关系推荐"真实目录为主、程序化纤维降为氛围底层"（实现登记）。红移距离本动速度误差、指状效应、银道遮挡带等失真登记说明。
- **方案 H（既有基元复用组装）**：GRB 双喷流复用 `RelativisticJet` + 余辉壳复用体积基元；费米气泡为双极椭球体积；30 Doradus 为小型体积发射区；M87 球状星团/室女座成员为稀疏 Points；EHT 联动为 R4 透镜层换参数档——全部为 R4 已验证基元的参数化组装，不引入新渲染路线。

### 0.4 公开数据源登记（实现时在代码/面板 dataSource 登记；影像授权逐一核实登记 license）

| 数据 | 来源 | 授权要点 | 用途 |
|---|---|---|---|
| M31/M33/LMC/SMC 影像 | NASA/ESA Hubble Heritage、GALEX、DSS2 彩色合成（NASA 公有领域；ESA/Hubble 为 CC BY 4.0 须署名登记） | 烘焙为权重图/贴图（非原图分发，仍登记出处与授权） | R5-1/R5-2 |
| 2MASS Redshift Survey (2MRS) | Huchra et al. 2012，公开目录 | 公开科学数据，引用登记 | R5-3/R5-4 |
| 30 Doradus | NASA/ESA Hubble 公版影像 + SIMBAD 位置 | 同影像行 | R5-5 |
| M87 喷流 HST-1 / 球状星团系统 | HST 观测（Biretta et al.）；球状星团数 ~12,000（Tamura et al. 2006 量级） | 文献参数 | R5-4 |
| 室女座团成员 | VCC/EVCC 目录（公开）或 2MRS 子集 | 公开目录 | R5-4 |
| 费米气泡形态 | Su, Slatyer & Finkbeiner 2010（Fermi-LAT） | 文献形态参数 | R5-6 |
| 银河系 HI 翘曲 | Levine, Blitz & Heiles 2006 | 文献 warp 振幅/相位参数 | R5-6 |
| GRB 喷流/余辉 | 相对论火球模型图景（Piran 2004 综述，近似登记） | 文献图景 | R5-5 |

---

# 第一部分（P0）：真实影像驱动主线

## R5-1 影像驱动星系升级（烘焙管线 + 近观粒子 + 远景贴图）

> 内部执行顺序（强制）：A 管线 → B 采样纯逻辑 → C **M31 打样并自检**（并排对照公版影像 ≥3 处结构可对应）→ D 批量 M33/LMC/SMC → E 远景贴图接入。M31 打样不达标时停止批量、向用户报告。

### 1.1 需求

**A. 烘焙管线扩展（`scripts/bake-data/` 增量）**
- ✅ 影像处理脚本（`galaxyMaps.ts` 配置驱动 + 纯逻辑核 `galaxyMapsCore.ts` + 最小 PNG 编解码器 `pngCodec.ts`——零新依赖，node:zlib，省 token 约定落实）：输入 DSS2 彩色合成公版影像（hips2fits 切取 TAN 1024²，源图入 `scripts/bake-data/assets/galaxy-images/` **不进 public/**，下载 URL/授权登记于同目录 README）→ 输出 `public/data/galaxy-maps/`：
  - ✅ 密度通道（亮度提取 + 前景星去除（局部中值对比钳制，NGC 604/30 Dor 类延展亮结经局部中值抬升保留）+ 噪声地板 + gamma 归一化，256×256）、颜色通道（亮度归一色调 + 饱和度档，256×256）、尘埃通道（暗带遮罩 = 相对局部平滑亮度的暗缺损，供 R5-2 用，256×256）；实现差异登记：增加污染源遮罩步骤——M31 画面的伴系 M32/M110 与亮星 ν And、SMC 画面的前景球状星团 47 Tuc/NGC 362 按天球坐标圆形遮罩 + 归一化卷积 inpainting 填充（伴系为应用内独立天体、前景球状星团非星系成员）
  - ✅ 远景贴图（512px sprite，alpha 羽化边缘 + 亮度噪声地板 + 4bit 量化压缩档登记）
  - ✅ meta 登记 `{ source, license, credit, pixelScaleLyPerPx, inclinationDeg, positionAngleDeg }` + 反投影方法/残差 + 失真登记（附录 A §3）
- ✅ M31 77° 倾角反投影到盘面坐标（薄盘假设短轴 1/cos i 拉伸 + 核球径向缓和（r01 0.1→0.3）防球状核球被拉成雪茄，方法登记于 meta 与 galaxyMapsCore 文件头；残差 = 盘环带（r01 0.45–0.9）流量加权二阶矩轴比 **0.836** 登记）；M33/LMC/SMC 各一套（未反投影——影像已含天空投影；不规则星系密度图直接驱动团块分布，无旋臂模型，登记）；单星系全部产物 213–298 KB ≤600 KB，public/data/ 总量 1.85 MB ≤15 MB（附录 A §1 修订上限同步 index.ts 5→15 MB）

**B. 运行时采样（纯逻辑扩展 `utils/galaxyNearView.ts`）**
- ✅ `sampleParticlesFromMap(densityMap, colorMap, count, seed, opts)`：密度图 2D 逆变换采样盘面位置（CDF 前缀和 + 二分 + 像素内抖动，确定性——每粒子固定 7 随机数消耗）+ z 向厚度沿用 R4-9 参数模型（旋涡 = cfg.thicknessLy 高斯薄层；不规则 = 半径×flattenY×0.55；"盘面分布真实、垂直分布参数化"口径登记）+ 颜色图查色（亮度按密度调制防加性混合过曝，目验调参登记）；HII/年轻星团分量改为密度图高亮蓝区加权布点（权重 = 密度²×蓝色超出，替代泊松盘随机；权重全零返回空分量）；实现差异登记：尘埃分量同步改为尘埃遮罩图加权布点（真实暗带走向，权重×密度约束防盘缘无背景漂浮暗粒；M31 参数化 M31_DUST_RING/applyBulgeTint 在影像路径不套用——10 kpc 尘埃环与偏黄核球由影像自带）
- ✅ 产物缺失/加载失败降级 R4-9 参数化生成（统一入口 `generateGalaxyNearViewCompositeAuto`，逐星系降级输出与参数化逐字节一致单测）；两次进入形态逐字节一致（单测；种子 `:map` 后缀与参数化域分离）

**C/D. 近观接入（M31 打样 → 三星系批量）**
- ✅ `GalaxyNearView.tsx` 四星系分支切换影像驱动（maps 可选 prop + 新 hook `useGalaxyImageMaps`：meta 经 `bakedData.loadGalaxyMapMeta` 校验、像素经浏览器 Image/canvas 解码，模块级缓存，近观激活才懒加载）；椭圆类（M87/M32/M110/人马座矮）**不套用**（Sérsic 解析分布已足够，登记；M87 近观回归截图零回退）；粒子预算沿用 ≤12,000（分量配额与参数化同源 galaxyComponentQuota，M31 9,850 峰值/GPU 估算零变化）；实现差异登记：M33/LMC/SMC 近观姿态由 id 哈希改为盘面正对视线（影像已含天空投影，哈希姿态会双重投影致照片结构不可对应；M31 = 反投影 + 真实倾角 77° 再倾转；其余星系哈希零变化，单测等价迁移）
- ✅ 预览页 `?body=m31|m33|lmc|smc`（新增 m33/smc 条目）增加"参数化/影像驱动"对比切换滑杆（目检对照用；非反投影星系预览默认倾角 0——影像已含投影登记）

**E. 远景贴图接入**
- ✅ `Universe.tsx` `GalaxyObject` 贴图源：影像贴图优先（`galaxySpriteImageUrl` 覆盖清单纯函数：m31/m33/lmc/smc）、程序化 canvas 降级（未覆盖星系沿用现状，覆盖清单登记）；billboard/尺寸/淡入淡出逻辑零改动；经既有 `useBitmapTexture`/`textureManager` 懒加载计入纹理预算与加载进度体系（L4 淡入域内才请求，接入登记）
- ✅ 远近景一致性：远景影像贴图 ↔ 近观影像粒子层交叉过渡时结构对应（同源影像；M31 近观 → 拉远序列截图 r51/scene-m31-seq0–3 核对连续）；授权署名（附录 A §2）呈现于星系信息面板数据来源行（GALAXY_STRUCTURE_SOURCE_ZH，位置登记）

### 1.2 验收标准

- ✅ 预览页对比切换 + 与公版影像并排截图（r51 系列登记）：四星系逐一 ≥3 处真实结构可对应（M31 尘埃带走向/暖核球-蓝盘色分层/蓝区星团串；M33 絮结旋臂与 NGC 604 类蓝亮结；LMC 棒与 30 Dor 亮区；SMC 延展形状与蓝色棒），参数化对照档切换直观可见
- ✅ L4 巡游回归（无头 Chrome Metal 1280×800）：影像驱动 4 星系近观与远景观感提升（对照截图登记）、M31 远近景过渡结构连续（序列截图）、其余星系不回退（M87 站截图确认）；逐站 60 FPS、m31↔m33 LRU 进出 ×3 强制 GC 堆 48→48 MB 无泄漏、控制台零错误
- ✅ 采样/降级/反投影/贴图源选择纯函数单测（galaxyNearViewR51 31 例 + bakedDataR51 18 例 + scripts galaxyMapsCore 26 例；scripts 不计入 src 覆盖率 gate 登记）；`npm run bake:data` 幂等（两次运行 shasum 逐字节一致实测）；产物总量登记（public/data/ 1.85 MB）；覆盖率 gate ≥90% 保持（语句 98.53%，全量 2,735 用例/142 套件）
- ✅ **交付后用户目检确认（人工检查点），通过后方可启动 R5-2**——用户已目检确认（R5-2 启动依据）

## R5-2 星系体积尘埃盘（视线消光）

> 内部执行顺序（强制）：M31 打样并自检（斜视消光效果成立）→ 套用 M33/LMC。

### 2.1 需求

- ✅ 旋涡星系薄盘体积层（复用 R4-3 `VolumeMaterial`，扁盒包围盒）：密度 = R5-1 尘埃通道 2D 图 × z 向指数薄层（伪 3D 登记）；**纯吸收模式**（发射为零，仅衰减后方颜色）——纯逻辑 `utils/galaxyDustVolume.ts` + 组件 `Scene/GalaxyDustVolumeLayer.tsx`；实现差异登记：① 尘埃图经对比度整形 dust²（与 R4-10 尘埃粒子权重同口径）——不整形时弥散暗缺损沿斜视长光程积累为全域压暗、暗带图案被淹没（M31 打样目验修正）；② `VolumeMaterial` 增 uWorldStepScale 各向异性光程修正（薄盒非均匀缩放下局部步长不反映世界光程，若不修正正视与侧视光学深度相同；最长轴归一化，默认 (1,1,1) 既有星云体积层零行为变化）；③ 密度纹理非立方 128×32×128 R8 512 KB（各维 ≤128 附录 A；可分离乘积同步构建 ≪100ms 免分帧登记）；④ 渲染路径沿用 R4-4/R4-8 半分辨率 RT + 合成（发射为零 → 合成即 C_out = T·C_bg 纯衰减）
- ✅ 消光实现方案二选一登记：**取 a**（体积层置于星光粒子之后按透过率调制，合成 renderOrder 晚于粒子层）；与 b（粒子 shader 逐粒子采样尘埃图）差异登记于 galaxyDustVolume.ts 文件头——a 对盒足印内"尘埃层前方"的粒子有过度压暗（近观仅跟随本星系时激活、与星云层"无深度合成"差异登记同源）；b 深度精确但需改造全部星光粒子 shader 且无法作用于粒子以外的辉光/贴图层
- ✅ M31 打样验收特征：77° 斜视近侧尘埃暗带（10 kpc 环，影像自带）真实遮挡后方星光与核球辉光（打样自检达标后批量，截图 r52 系列）；R4-10 dust 暗粒子在体积激活时按体积淡入权重互补淡出（互斥登记，体积卸载/降级即恢复零回退）；套用 M33/LMC（LMC 尘埃弱：σ=10 = M31 的 1/3 弱档登记；M33 σ=22；M33/LMC 尘埃图为天空投影直贴盘面近似登记）；**极侧视（~90°）限制登记**：暗带屏幕宽度为真实尺度（标高 ~500 ly ≈ 数像素）且星光加性 HDR 堆积 + Bloom 泛光冲淡 → 呈细弱暗线（与 R4-10 暗粒子"画粗暗线"的非物理观感不同，属正确物理呈现；主验收姿态 = M31 真实 77° 斜视）
- ✅ volume 池预算核对：与星云体积层同池（容量 1），主场景 m31 ↔ orion-nebula 巡游切换逐出实测单持有者无双挂；自适应质量（R4-4）接入（状态机逐帧写步数/RT 比例，实测全程 high 未降档）；预览页滑杆：消光强度 σ（0 = R4-10 暗粒子对照档）/盘厚两件（m31/m33/lmc 各 6 件 ≤8）+ HUD 体积质量档行接入 + `&spin=0` 关自转（dev 专用，A/B 截图固定姿态）

### 2.2 验收标准

- ✅ M31 近观斜视：77° 近侧暗带遮挡核球辉光立体真实（与 σ=0 = R4-10 暗粒子版并排截图登记 r52/m31-oblique-volume vs m31-oblique-r410）；正视暗纹与影像对应（r52/m31-faceon-volume）；M33/LMC 套用同验（r52/m33-*、lmc-*）；侧视 90° 对比登记：暗线细弱属真实尺度 + HDR/Bloom 限制（见 2.1 登记），无头放大截图可辨（r52/crop-vol40）
- ✅ 星系↔星云连续巡游 volume 池 LRU 正常无双挂（m31→orion-nebula→m31 holders 逐一核对）；全程 60 FPS ≥55（自适应 high 档未降档）；m31↔m33 进出 ×5 两轮强制 GC 堆 48→49→49 MB 平台无泄漏；控制台零错误
- ✅ 尘埃密度扩展/透过率纯函数单测（galaxyDustVolumeR52 41 例，galaxyDustVolume.ts 覆盖率 100%；透过率与 R4-3 integrateEmissionAbsorption 离散格式交叉校验）；覆盖率 gate ≥90% 保持（语句 98.54%，全量 2,776 用例/143 套件）

---

# 第二部分（P1）：真实宇宙背景与标志性结构

## R5-3 真实巡天目录背景（烘焙 + 渲染全链）

### 3.1 需求

**A. 目录烘焙（`scripts/bake-data/` 增量）**
- ✅ 2MRS（Huchra et al. 2012，~43,000 星系）为主源（获取方式沿用 R4-5 二选一策略：公开接口或内嵌精简表——网络受限时按亮度截断至 ~2 万仍足够呈现大尺度结构，截断判据登记）；红移 → 距离（哈勃流近似，H₀ 取值登记）+ 天球坐标 → 超星系笛卡尔坐标；输出 `public/data/galaxy-catalog.bin`（Float32：位置 + 亮度档 + 形态档，~16 B/星系，≤1 MB）
  - 实现登记：取"公开接口 + 提交快照"路径——VizieR TAP（J/ApJS/199/26/table3）全量快照 gzip 入库 `scripts/bake-data/snapshots/2mrs-vizier.csv.gz`（`--fetch-2mrs` 重拉）；**按亮度截断降级路径未启用**（快照即全量 43,507 行，登记）；H₀ = 70 km/s/Mpc、cz < 100 km/s 近距剔除 9 条（meta 登记）；产物 43,488 星系 679.5 KB ≤ 1 MB，w 通道 = 形态档·1000 + 亮度档·999 整数打包（Float32 精确 → 幂等）；坐标链（赤道→银道→超星系）与运行时同源 `src/utils/galaxyCatalogCore.ts` 纯函数
- ✅ 失真登记三项（meta + 信息面板文案常量）：本动速度污染/指状效应、近距距离误差、银道遮挡带（Zone of Avoidance 为观测限制非真实空洞）
  - 实现登记：meta `distortions` 三条 + `GALAXY_CATALOG_DISTORTIONS_ZH` 常量呈现于 ControlPanel 显示开关下方说明文案（信息面板承载体为显示选项区说明而非天体卡片——目录为背景层无可选天体卡片，登记）
- ✅ 已有 L4 实体星系（9 个 + M87）目录内剔除去重（匹配半径登记）；脚本自校验含"**室女座团方向计数显著高于全天平均**"断言（真实结构在数据中可验证）；`bakedData.ts` 扩展二进制解析 + 校验 + 降级 null（fixture 单测）
  - 实现登记：匹配半径 0.5°（天球角距 + 距离窗），实际命中 10 条（LMC×4/M33×1/M87×5，其余实体在 2MRS 无对应条目属预期）；自校验：室女座 6° 锥（5–30 Mpc 壳）超密度比 **32.6× ≥ 3** + 银道带 |b|<5° 占比 <2% + M87 方向近距清零 + 产物 ≤1 MB，失败退出非零；`validateGalaxyCatalog`（魔数 21175/版本/字节长度精确匹配/坐标 ≤800 Mpc/w 整数域）+ fixture 全分支单测

**B. 渲染接入**
- ✅ 新建 `src/components/Scene/GalaxyCatalog.tsx`：两级 Points——远景单像素（亮度档→size/alpha、形态档→色调：椭圆偏黄/旋涡偏蓝白）+ 拉尼亚凯亚近域适度增大；对数距离压缩与现有 L4 尺度同源（`utils/scale.ts` 换算登记）
  - 实现登记：近域 ≤80 Mpc 11,110 粒软圆点 + 远景 32,378 粒单像素，各一次 draw call（WebGL 层核对）；alpha 经加性混合下颜色强度等效实现；透明度档目验调参登记（远景 0.3 实测仅 371 可辨像素 → 0.75、近域 0.95）；距离经 `cosmicDistanceToSceneUnits` 对数压缩同源；**场景对齐两级锚定登记**（`utils/galaxyCatalog.ts` 文件头）：主锚银道面↔场景银盘面（银道空带精确落在渲染银盘平面内）+ 次锚绕 y 方位对齐 M87（残余仰角偏差 ≈15.2°，各实体偏差 `catalogAnchorDeviationDeg` 单测断言；det=+1 真旋转手性登记）
- ✅ 与程序化宇宙网关系（推荐方案登记）：真实点云为主层、`generateCosmicWeb` 纤维降为低透明度氛围底层（或显示开关互斥，二选一登记）；拉尼亚凯亚示意边界与真实室女座团位置对齐核对（偏差登记）
  - 实现登记：**取推荐方案**——目录激活时宇宙网 opacity 0.75→0.2 氛围底层，开关关闭/加载失败即恢复主层；拉尼亚凯亚边界环由场景 XZ 平面改置**真实超星系平面**（SGZ=0，室女座团 SGB≈−2.3° 落在面内 → 环穿过目录室女座超密度处，对齐核对通过；环相对银盘面倾角 ≈84.5° 为真实几何登记），边界标签随环迁移
- ✅ LOD：L4 窗口（3.05–3.6）淡入沿用；哈勃膨胀联动保持；加载失败降级现状宇宙网；显示开关接入显示选项区；HelpHint/信息面板补"真实巡天背景"说明（2MRS 来源 + 三项失真）
  - 实现登记：淡入公式收敛至 `utils/universe.universeFadeWeight`/`UNIVERSE_FADE`（Universe.tsx 与 GalaxyCatalog.tsx 同源，禁止两套参数）；哈勃膨胀经 `hubbleScaleFactor` 组缩放同式；开关"真实巡天背景（2MRS）"L4 域（panelScopes）+ 说明文案（来源 + 三项失真）；HelpHint 补段落；**P0 竞态修复登记（无头目验发现）**：`useGalaxyCatalog` L4 飞入途中 enabled 抖动 true→false→true 时 fetch 结果被 alive 守卫丢弃且模块缓存已写入 → 目录永不挂载；修复为重新 enabled 时同步模块缓存 + renderHook 竞态回归测试
- ✅ 性能实测登记：~43,000 单像素 Points（一次 draw call）前后 FPS 对比
  - 实现登记：目录开 60 / 关 60 / 绕行 60 / 跟随 M87 60 / 降级 60 FPS（无头 Chrome Metal 1280×800）；两级各一次 draw call 经 WebGL drawArrays 钩子核对（11,110 + 32,378 随开关挂载/卸载）

### 3.2 验收标准

- ✅ 烘焙幂等；产物 ≤1 MB；自校验通过（含室女座超密度断言）
  - 登记：两次 `npm run bake:data` 产物 shasum 逐字节一致；679.5 KB；室女座 6° 锥超密度比 32.6×
- ✅ L4 拉远：室女座团聚集、银道空带、纤维走向可辨（标注截图，与 2MRS 已发表切片图对照登记）；与 9 个实体星系无重影（去重目验）；开关/降级正常；60 FPS 不变（对比登记）
  - 登记：截图 r53 系列（41–49：L4 锚点/开关对照/拉远/偏航×2/俯视/侧视/M87/降级）；定量复核（最大拉远 ON/OFF 像素差分）：目录独占像素 71,469、**室女座团方向局部密度 5.19× 全场平均**（反方向 3.26×，方向聚集成立）；银道空带经主锚设计落在渲染银盘平面内 + 烘焙 |b|<5° 占比 <2% 断言（与 2MRS 已发表全天分布图的空带/超星系平面聚集特征对应，逐一标注对照未做——以数据自检 + 方向定量替代，登记）；M87 方向 0.3° 锥近距条目清零单测 + 跟随 M87 目验无重影；开关/CDP 拦截降级正常；FPS 前后对比全 60
- ✅ 加载/解析/去重/坐标换算/两级 LOD 单测；覆盖率 gate ≥90% 保持
  - 登记：`bakedDataGalaxyCatalog`（22 例，含实际产物集成断言）+ `galaxyCatalogCore` + `galaxyCatalogR53` + `storeR53` + `useGalaxyCatalog` 竞态回归 4 例；全量 2,836 用例/148 套件，语句覆盖率 98.55%
- 🔶 **交付后用户目检确认（人工检查点）**——已交付待用户目检

## R5-4 M87 纵深与星系团环境

### 4.1 需求

- ✅ 喷流节点：既有 M87 喷流轴向叠加 3–5 个亮 knot（HST-1 类，sprite/小体积二选一登记），亮度沿轴衰减、uTime 缓慢外移（视觉化登记，非真实速度）。实现登记：**取 sprite 方案**（节点为点状极亮源，辉光 sprite 即达观感且零额外 raymarch 成本）；4 个 knot（`M87_JET_KNOTS`，首节点 = HST-1 类最亮），外移速率 0.006 轴长/秒（全轴 ≈2.8 分钟，循环端点淡入淡出防回绕闪现）；`ExtragalacticObjects.M87JetKnots` 主场景与预览页共用
- ✅ 球状星团系统：M87 近观叠加 ~2,000 稀疏 Points（真实 ~12,000 按预算缩减登记；Sérsic 外包络径向分布、红黄老年色），点比椭球云更小更锐以区分。实现差异登记：径向取 **Sérsic 外包络的幂律近似档** n(r) ∝ r^−2.5（McLaughlin 1999 外区斜率量级；Sérsic n=4 精确逆变换过度向心集中不呈"外包络"观感）——半数以内半径 20,000 ly > 恒星 Rₑ 12,000、截断 4×Rₑ 与椭球云同口径；轴比沿用近观椭球（0.86/0.92）；点样式复用 GalaxyNearView 软圆点 shader（导出复用，maxSizePx 3 + 高 alphaBase 与基础云 6px 软点区分）；确定性种子两次采样逐字节一致
- ✅ 核心 EHT 联动：跟随 M87 推近至核心距离阈值 → 激活 R4-13 `BlackHoleLensed`（M87* 参数档：盘更暗、环更大，登记），"飞进星系看见黑洞"跨尺度过渡；退出释放；与 Sgr A*/Cyg X-1 共用 lensing 池。实现登记：阈值 900 units（< 飞抵观察距离 9,900，滞回 ×1.4 与 R2-7 同源；包围球 126 < 阈值恒在球外）；M87* 参数档 `M87_CORE_LENSED_CONFIG`——盘亮度 0.35（< Sgr A* 0.55，ADAF 低吸积率）、r_s 世界长度 9 units（≈ Sgr A* 4.8 的 1.9×，真实质量比 ~1,500× 可视化压缩登记）、盘倾角 17°（EHT 2019 近正视环形态，与两黑洞侧视 69.23° 区分）、独立星场种子 4133；跟随判据仅 followBodyId（R4-13 Esc 释放修复同款）；release-on-exit 退出淡出即卸载 dispose
- ✅ 室女座团环境：成员点缀 + ICM 弥散辉光。实现登记：**取 R5-3 目录室女座方向子集**（R5-3 已交付，内嵌 VCC 精简表替代路径不启用）——6° 锥（与 R5-3 自校验同源）+ 径向壳 [11,25] Mpc 团本体窗口（全壳 [5,30] 含前后景污染，收窄登记）按亮度取前 100；椭圆偏黄/旋涡蓝白（R5-3 形态档同源色板）；相对 M87 位移方向真实（超星系→场景与目录同一旋转链 `supergalacticToScene`）、幅度**线性压缩 1,200 units/Mpc 艺术化登记**（对数全域压缩在团尺度局部线性化）；目录经 `useGalaxyCatalog` 懒加载，未就绪/失败仅无成员点缀其余照常（降级登记）；ICM **取径向渐变 sprite 方案**（≈2 Mpc 半径，X 射线热气体可见光淡蓝紫艺术化登记，不占 volume 池）；**成员不可点选**（环境层全部子层禁用 raycast，信息面板"室女座团"行列名说明——二选一登记）
- ✅ 信息面板 M87 卡片补 M87*/球状星团/室女座团行；预览页 `?body=m87` 增加核心推近预设视角按钮。实现登记：三行 + dataSource 来源（`M87_EXTRA_INFO_LINES_ZH`/`M87_ENVIRONMENT_SOURCE_ZH`）；预览页新增 `PreviewEntry.viewPresets`/`minCameraDistance` 基建（harness 预设按钮 + 相机距离应用器）——「全景语境」/「核心推近（EHT 光子环）」按钮，透镜门控在预览中以相机距离对主场景阈值（× 预览缩放）同式滞回判定 + 0.5s 淡入淡出（无 store 跟随语义的预览适配登记）；滑杆 3 件（球状星团数/成员点缀开关/ICM 强度）

### 4.2 验收标准

- ✅ M87 近观呈"星系团中心"完整语境：椭球星光 + 锐利星团点 + 节点喷流 + 成员点缀 + 弥散辉光（对照 R4 交付纯椭球云，截图 r54-01~03/20 登记）；推近核心平滑过渡到 EHT 光子环（序列截图 r54-04~08/21 登记：近正视大环 + 暗弱盘与 Sgr A* 侧视暖环参数档区分明显）
- ✅ lensing 池与两黑洞巡游交叉切换 LRU 正常（Sgr A* → M87 核心 → Sgr A* 截图 r54-13~15）；M87 点选/飞往交互回归不受干扰（selectBody/requestFlyTo 全程正常、环境层 raycast 全禁用登记）；全程 60 FPS ≥55；进出 ×3 强制 GC 堆 52→52→53 MB 平台无泄漏、控制台零错误
- ✅ 星团分布/成员子集筛选/阈值纯函数单测（`m87EnvironmentR54` 32 例，含真实产物集成断言：满配 100 成员、位移有界落于近观语境、无 M87 重影点）；覆盖率 98.49%（gate ≥90% 保持）

## R5-5 本星系群与河外长尾细节（LMC 标志结构 + GRB 近观）

### 5.1 需求

**A. LMC 标志性特征**
- ✅ 30 Doradus：LMC 近观激活时在真实相对位置（SIMBAD NGC 2070 → gnomonic 切平面镜像烘焙侧 skyToPixel 换算至影像盘面，`utils/lmcStructures.ts` 登记；落点在产物密度图为 255 饱和亮区——位置正确性以产物 PNG 解码单测实证）叠加小型体积发射区（48³ R8 直绘 raymarch，复用 `makeSphericalFbmCloudSampler` 球壳+fBm 基元；Hα 粉红 + 中心 R136 星团蓝白亮核 sprite，`Scene/LmcTarantula.tsx`）；实现差异登记：① 放大系数取 **5×**（需求示例 3.5× 档无头目验粉红区仅数像素被加性星光淹没，上调登记；视觉直径 ~3,000 ly ≈ 盘 10%）；② 挂载随 lmc particles 池细节层（GPU/sprite 预算并入 `galaxyDetailLayerSpec('lmc')` 登记）不占 volume 池——与 R5-2 体积尘埃盘共存无互逐；③ 影像降级参数化时不挂载零回退（参数化团块分布无亮区几何对应，登记）
- ✅ 中央棒色彩分层：棒区老年偏黄 vs 盘面蓝白（R5-1 密度图已含棒亮区，只做颜色通道加权——`applyLmcBarTint` 副本语义，棒外逐字节零改动单测锚定）；棒椭圆几何登记：产物密度图高亮区流量加权二阶矩拟合档（中心 (−266,−421) ly/主轴 −0.503 rad/半轴 4,300/2,000 ly，van der Marel 2001 图景近似）
- ✅ 信息面板 LMC 卡片补"标志结构"行 + dataSource（LMC_LANDMARK_NOTE/SOURCE_ZH）；预览页 `?body=lmc` 滑杆：30 Dor 亮度（0=关闭对照）/尺度放大 1–8（合计 8 恰达上限）

**B. GRB 近观**
- ✅ GRB 近观细节层（`GammaRayBurst` 增量，`useDetailLayer({kind:'particles'}, 'lru-retain')` 与星系近观/类星体共池、阈值与类星体同式同源）：相对论双喷流（复用 `RelativisticJet` 参数化——新增可选 radiusFactor prop = tan(全开角/2)、缺省 0.035 现状锚定零回退；GRB 档全开角 5°、蓝白更亮、轴与静态双锥同姿态单点同源）+ 余辉膨胀壳（**取壳层 mesh 渐变**（二选一登记）：临边增亮 shader + 周期时钟驱动 R ∝ t^(1/4) 膨胀与幂律 α=1.2 减暗、颜色随龄蓝白→暗橙；火球模型图景 Piran 2004 登记 `utils/grbNearView.ts` 文件头；真实数天–数月压缩至 45s 演示周期登记）
- ✅ 与 GRB 现有呈现节奏兼容：现状核对结论登记——GRB 为**常驻演示物**（Universe 无条件挂载 + grbFlashState 45s 周期重放 FRED 闪光，非事件触发物），近观层随周期时钟出现/衰减（喷流爆发同步增亮→指数衰减至 0.15 地板档（演示可见性登记）→周期末淡出归零无重放跳变；静态双锥按 GRB_STATIC_NEAR_DIM 减淡让位、退出恢复）；预览页注册 `?body=grb`（滑杆 4 件：时间流速/喷流开角/喷流亮度/余辉强度）

### 5.2 验收标准

- ✅ LMC 近观：30 Dor 粉红发射区 + 蓝白亮核位置正确可辨（截图 r55-p10 vs 关闭对照 r55-p11，像素探针 53 粉红 px @(583,323) vs 0；落点密度图 255 亮区单测实证）、棒色彩分层可辨（中心偏黄 vs 外围蓝白）；与体积尘埃盘共存正常（主场景 r55-m01～03，LMC σ=10 尘埃盘同挂）
- ✅ GRB 近观：双喷流 + 膨胀余辉壳可辨且随时间演化（两帧对比登记：预览 r55-p06 8.7s 蓝白致密壳 → r55-p07 27.9s 膨胀减暗橙壳；主场景 r55-m04/m05 间隔 15s 同验；开角 12° 对照 r55-p08/余辉关闭对照 r55-p09）
- ✅ L4 巡游回归（8 站 m31/m33/lmc/触须/类星体/透镜团/grb/m87 逐站 60 FPS、堆 46→51 MB 平稳、控制台零错误）；60 FPS；位置换算/放大系数/膨胀曲线纯函数单测（lmcStructuresR55 22 例 + grbNearViewR55 16 例，两模块覆盖 100%）；覆盖率 gate ≥90% 保持（语句 98.44%，全量 2,906 用例/151 套件）

## R5-6 银河系 L4 增补（费米气泡 + HI 翘曲盘）

### 6.1 需求

- 🔲 费米气泡：银心上下双极椭球体积（复用 R4-3 基元，64³ 单纹理双泡；尺度各 ~25,000 ly，Su et al. 2010 登记）——极低密度弥散辉光（伽马射线的可见光艺术化呈现登记：淡紫/品红低透明度），默认可见度低、L4 近观银河系时可辨；显示开关接入现有显示选项区（实现登记）
- 🔲 HI 翘曲盘：`utils/galaxy.ts` 盘粒子生成器外盘（r > ~35,000 ly）叠加 m=1 S 形垂直位移（振幅/相位 Levine et al. 2006 近似参数登记）——CPU 生成期一次性位移，shader 零改动；纯函数 `warpYLy(rLy, phi)` 单测（振幅径向增长/方位角相位/内盘零位移）
- 🔲 组合核对：warp（生成期基线位移）与 R3-7 垂直展开 uExpand、R2-11 合并 uEll（shader morph）组合无形变异常（单测断言组合顺序 + 展开态/合并态目验截图）；R2-9 银晕/星团/棒/尘埃带零回退；粒子总量不变（warp 只改位置）

### 6.2 验收标准

- 🔲 L4 侧视银河系：外盘 S 形翘曲可辨（有/无 warp 对照截图登记）；银心上下费米气泡弥散双泡可辨（正侧两视角截图）
- 🔲 R3-7 垂直展开与合并预览回归无形变异常（各一张截图）；60 FPS；覆盖率 gate ≥90% 保持

---

# 收尾

## R5-7 集成回归验收 + 文档回写

### 7.1 任务清单

- 🔲 全量 `npm test` / `npm run type-check` / `npm run lint` / `npm run build` 全绿；覆盖率 gate ≥90% 确认；`npm run bake:data` 幂等确认（影像/目录全部产物）
- 🔲 无头 Chrome 端到端：L4 8 站巡游整圈逐站截图 + FPS + JS 堆（对照 R4-24 基线登记提升与开销）；真实点云背景多角度；M31 影像驱动/体积尘埃/远近景过渡完整序列；M87 推近至 EHT 联动序列；连续切换 10 个细节层目标堆稳定
- 🔲 R4 关键点回归抽查：L3 体积星云站 1 项、黑洞透镜站 1 项、detailLayer LRU 交叉 1 项（确认 R5 未破坏 R4 交付）
- 🔲 资产与性能总账：`public/data/` 总量对照上调后预算（≤15 MB）、体积/纹理显存、L4 粒子峰值逐项登记；影像/目录授权与署名信息汇总登记（呈现位置登记）
- 🔲 `CHANGELOG.md` 查漏补全 R5 条目（含实测数据）；本文档全部 🔲 回写 ✅/🔶（差异登记），版本升 1.x；REQUIREMENTS.md 受影响小节同步

### 7.2 验收标准

- 🔲 上述全部完成并登记；R4/R2/R3 抽查项无回归

---

# 第三部分（P0 缺陷修复，独立于 R5 主线）

## R5-8 事件门控层级与视角域锁定对齐（离散视角判定）

### 8.1 背景与根因（已代码调研核实，2026-07）

**用户反馈**（附截图）：银河系视角下依然可以看到太阳的事件和地球的事件——跟随造父一（L3 巡游）时，"☀ 太阳耀斑爆发（X6.1 级）""🌊 日冕物质抛射（CME）""📡 CME 已抵达地球"三张通知卡片同时弹出，HUD 却显示"银河系视角"。

**根因**：事件门控（自动触发/通知可见/演示按钮/R3-3 离域丢弃）四处消费方全部读取 **`continuousLevel`**（相机-原点距离换算的连续层级），而 R3 层级锁定使**跟随/飞往巡游天体期间离散 `viewLevel` 锁定为域主层级**（银河系巡游 → L3），`continuousLevel` 仍随相机距离同步。造父一等 L3 特殊天体距场景原点仅 150–400 单位，跟随时相机距离 ≈190 单位 → `continuousLevel` ≈ 2.2，落入太阳事件窗口 [1, 2.4]——用户看着"银河系视角"，事件系统却按"太阳系尺度"运行。**镜像缺陷**：同场景下超新星（窗口 [2.5, 3.5]）被判定域外——活跃超新星被 R3-3 误丢弃、超新星演示按钮被误置灰。

| 门控消费方 | 位置（层级来源均为 continuousLevel） |
|---|---|
| 通知可见 | `src/components/UI/HudInfo.tsx` :79/:82 |
| 自动触发 | `src/components/CelestialBody/SunActivity.tsx` :554、`src/components/Scene/Supernova.tsx` :335 |
| 演示按钮 | `src/components/UI/ControlPanel.tsx` :80-86 |
| R3-3 丢弃计时 | `src/store/index.ts` `eventScopeDiscardUpdates` :400-403 |

**用户确认方案**（2026-07 需求澄清）：事件触发条件按**域主层级（离散 `viewLevel`）**判断——当前为太阳系视角只触发太阳系事件、银河系视角只触发银河系事件，**不判断相机距离**；两处边界微调（见 8.2-B）已确认可接受。

### 8.2 需求

**A. eventScopes 判定基准改为离散视角集合（`src/utils/eventScopes.ts` 重构）**
- ✅ 事件 → 视角集合映射替代连续层级窗口：耀斑/CME/CME 抵达 → **{L1, L2}**；超新星 → **{L3}**（R3-5 收窄语义保持）；合并预览 → **{L4}**；`eventInScope(kind, viewLevel)` 及三层门控（`eventAutoTriggerAllowed`/`eventNoticeVisibleInScope`/`eventDemoEnabled`）签名改收 `ViewLevel`（实现说明：`eventScopeWindow`（连续闭区间）替换为 `eventScopeLevels`（只读视角集合）；ViewLevel 为字符串联合类型，原"非有限数抛 RangeError"校验随数值参数一并移除——类型系统静态兜底）
- ✅ 原连续窗口常量（`SOLAR_EVENT_MAX_LEVEL`/`SUPERNOVA_EVENT_MIN_LEVEL`/`SUPERNOVA_EVENT_MAX_LEVEL`/`MERGER_EVENT_MIN_LEVEL`）废弃删除（文件头登记语义变迁：R2-4 连续窗口 → R5-8 离散视角集合）；R3-3 丢弃纯函数（宽限/运镜豁免/`outOfScopeElapsedUpdate`/`eventDiscardDue`）语义不变（零改动，单测原样通过）
- ✅ 特效 LOD 淡入淡出**仍由连续层级驱动**（`SunActivity` trapezoid(0.5, 0.9, 2.4, 3.0)、`Supernova` snFadeWeight(2.5, 2.9, 3.5, 4.0) 均不动——保持缩放平滑，不随离散层级跳变；已知边界登记：跟随 L3 天体但相机贴近时超新星通知可见而爆发特效可能已随连续层级淡出，"飞往观看"按钮可直达，可接受差异——登记于 eventScopes.ts 文件头与 Supernova.tsx snFadeWeight 注释）

**B. 边界微调登记（自由缩放场景的行为变更，用户已确认）**
- ✅ 自由缩放（无跟随）时 `viewLevel` 随距离同步离散层级（`utils/scale.ts` `discreteLevelFromContinuous`：L1 <1.5 / L2 [1.5, 2.5) / L3 [2.5, 3.5) / L4 ≥3.5），等效边界较原连续窗口两处微调：太阳事件上界 2.4 → **2.5**（宽 0.1，落在特效淡出带 2.4–3.0 内，消除原 2.4–2.5"事件已域外但特效仍满值"空窗）；合并预览下界 3.6 → **3.5**（宽 0.1，无实质影响）；CHANGELOG [Unreleased] 已登记

**C. 四处消费方统一接入**
- ✅ HudInfo 通知选择器（:79/:82）、ControlPanel 演示按钮（:80-84）、SunActivity（:557）/Supernova（:385）自动触发、store `eventScopeDiscardUpdates` 丢弃计时（:400-405）——层级来源全部改为 `viewLevel`（单一语义，杜绝再次分叉）；R3-3 运镜豁免（`viewTransitionId`/`flyToRequestId` 代次消费）保持零改动——按 1-4 显式切换时 `viewLevel` 即时变更而相机仍在运镜，豁免窗口继续防误丢弃

**D. 测试与回归**
- ✅ eventScopes 单测重写为 ViewLevel 矩阵（四视角 × 五事件类别逐格 + 三层门控一致性 + 视角互斥覆盖断言）；storeR33 丢弃套件层级设置等价迁移为 `viewLevel` + `continuousLevel` 双写（迁移逐条登记于该测试文件头；原"合并预览启动运镜途中 continuousLevel <3.6 不误杀"例迁移登记：R5-8 后 startMergePreview 即时置 viewLevel L4 且运镜期间不回写离散层级——恒域内，运镜豁免语义由独立套件继续覆盖）
- ✅ **用户场景回归单测**：eventScopes 层（`viewLevel 'L3'`：太阳三层门控全域外/超新星三层门控全域内）+ storeR33 层（跟随造父一态 `viewLevel: 'L3'` 锁定 + `continuousLevel: 2.2` + followBodyId——活跃耀斑/CME/抵达链 1 秒宽限后整链丢弃、离域计时持续累加、活跃超新星保留不误杀）
- ✅ 覆盖率 gate ≥90% 保持（语句 98.55%，eventScopes.ts 100%）；CHANGELOG [Unreleased] 登记（行为变更 + 两处边界微调）；REQUIREMENTS.md §3.1.5 同步；IMPROVEMENT_REQUIREMENTS_2.md §R2-4 / IMPROVEMENT_REQUIREMENTS_3.md §R3-3/§R3-5 登记"判定基准已被 R5-8 修订为离散视角集合"

### 8.3 验收标准

- ✅ 复现用户截图场景（无头 Chrome，截图 r58-01/02 登记）：银河系巡游跟随造父一（viewLevel L3 锁定、continuousLevel 2.25——正落用户缺陷区间）——无耀斑/CME/CME 抵达通知弹出、不再自动触发；种入的活跃太阳事件整链（耀斑/CME/在途抵达/极光/通知）1.8 秒采样点确认全链丢弃；同场景超新星可触发、通知正常、演示按钮域内可用（活跃期间按既有语义禁用显示"进行中"）
- ✅ 四视角锚点 × 四类事件矩阵按 `viewLevel` 逐格复验（截图 r58-03-matrix-L1~L4）：L1/L2 仅太阳事件（L2 耀斑短时长自然走完生命周期、通知按最短展示时长驻留=域内语义）、L3 仅超新星、L4 仅合并预览；触发/通知/按钮三层一致（按钮层由单测矩阵覆盖）
- ✅ 自由缩放跨边界宽限（<1 秒折返保留）与按 1-4 锚点切换/飞往/合并预览启动运镜豁免——storeR33 全套件回归通过（R3-3 语义零回退）；合并预览启动无头复验（viewLevel 即时 L4 恒域内，mergePreviewActive 保持）
- ✅ 特效渲染回归：LOD 权重函数零改动（trapezoid/snFadeWeight 未触碰），跟随造父一 + 活跃超新星特效场景 60 FPS
- ✅ 单测全通过（2,659 用例/139 套件）、覆盖率 gate ≥90% 保持；60 FPS 不跌破、控制台零错误

---

## 附录 A：全局硬性约束（在 R4 附录 A 基础上的增补与修订；未列项沿用 R4 附录 A 全部条款）

1. **资产预算修订**：`public/data/` 总量（gzip 前）由 ≤5 MB 上调至 **≤15 MB**（影像权重图/贴图 + 目录；上调理由登记）；远景贴图若入 `public/textures/` 则计入既有纹理预算体系。
2. **影像授权（新增，强制）**：所有影像来源逐一核实授权（NASA 公有领域 / ESA CC BY 4.0 须署名）；**源图不进 public/ 不随构建分发**，仅烘焙产物（权重图/贴图）分发且 meta 登记 source/license/credit；CC BY 素材的署名在应用内可见位置呈现（位置登记）。
3. **真实数据失真登记（新增）**：红移距离近似、指状效应、银道遮挡带、影像反投影残差等观测性失真一律在 meta 与信息面板登记，不得默称"完全真实"。
4. **降级路径（强化）**：一切数据驱动特性（影像粒子/体积尘埃/目录点云/影像贴图）在产物缺失或加载失败时回落 R4 交付形态，回落路径逐一单测——R5 不得让 R4 基线变脆弱。
5. **性能**：60 FPS 目标与 R4 各池语义（volume/lensing 容量 1、GPU 预算 64 MB）不变；目录点云 draw call 与显存增量实测登记。
6. **内部打样纪律（替代跨任务检查点）**：含"打样 → 批量"结构的阶段（R5-1 的 M31、R5-2 的 M31）必须先完成打样并对照验收标准自检达标后再批量；打样不达标即停止并向用户报告，禁止带病批量。
7. **人工检查点（仅 2 处）**：R5-1、R5-3 交付后须等用户目检确认（R5-1 通过后方可启动 R5-2）。
8. **验证闭环与 Git 流程**：同 R4 附录 A（五步收尾、3100 端口、纯函数先行、确定性种子、覆盖率 gate ≥90%、CHANGELOG、分支询问流程）。
